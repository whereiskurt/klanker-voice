---
gsd_state_version: 1.0
milestone: v1.1
milestone_name: Telephony
status: executing
stopped_at: Completed 12-02-PLAN.md
last_updated: "2026-07-12T19:23:35.612Z"
last_activity: 2026-07-12
last_activity_desc: "Phase 12 Plan 02 (§23 caller-ID mint path: normalizeE164, AccessCode phone/byPhone GSI/resolvePhoneToCode, private /tel route) complete"
progress:
  total_phases: 6
  completed_phases: 3
  total_plans: 18
  completed_plans: 12
  percent: 50
current_phase: 5
current_phase_name: Browser Client & Conference Readiness
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-07-04)

**Core value:** The conversation must feel slick — ≤1.2s voice-to-voice latency with natural barge-in and ElevenLabs-quality speech.
**Current focus:** Phase 12 — voip-ms-telephony-inbound-did

## Current Position

Phase 1 (Local Pipeline & Latency Harness): ✅ COMPLETE — 5/5 plans, verification PASSED 5/5 (amended latency criterion; 01-VERIFICATION.md)
Phase 2 (Infra Skeleton): ✅ COMPLETE — 7/7 plans merged, verification PASSED 5/5 (02-VERIFICATION.md; TLS handshake deferred to Phase 4 by design)
Phase 3 (Auth Service & Access Codes): ✅ COMPLETE — 4/4 plans merged, verification 4/5 (03-VERIFICATION.md). run.auth ported to apps/auth/webapp; access-code→tier + login→token bridge; OIDC RS256 JWT tokens; kv CLI. JWKS signing key live in SSM; kmv-auth-electro seeded (demo/kphdemo123 + tiers). Criterion-3 no-access GUIDANCE deferred to Phase 5 (logic done); deployed E2E is a Phase-4 verification item.
Phase 4 (Voice Service Deployed & Quota Enforcement): IN PROGRESS — Plan 04/6 done. 04-02 (deploy infra): voice ECS task/service enabled + wired, webrtc_udp SG narrowed to 20000-20100/udp, kmv-voice-usage DynamoDB table, least-privilege task-role IAM, session-count autoscale min1/max4. 04-03 (deployed ICE smoke, INFR-03/KV-05 VERIFIED LIVE): `kv smoke` against `https://voice.klankermaker.ai` reports PASS — ICE connected, host+srflx candidates, 244 RTP packets — real UDP media flowing on the deployed public-IP Fargate task. 04-04 (race-safe quota enforcement, QUOT-01/QUOT-02/QUOT-04/INFR-06 done, see 04-04-SUMMARY.md): `usage.ts` (4 ElectroDB entities) + `quota.py` (typed 5-way start-gate reject, atomic-enough heartbeat lease, 15s tick with rollup+auto-trip) + `session.py` (`SessionLifecycle`: service timer hard-stop, ActiveSessions CloudWatch metric, ECS scale-in protection) — 32 new tests, 134/134 total pass. Known Gap: the deployed voice task role's IAM does not yet grant cross-table read on `kmv-auth-electro` (the tiers table) — real `/api/offer` calls would fail closed at `read_tier()` until that IAM statement is added; must fix before live-traffic verification. Plans 05-06 (idle teardown + spoken wind-down, kv operator loop) remain.
Phase 5 (Browser Client & Conference Readiness): CODE-COMPLETE — 7/7 plans done (05-01..05-07-SUMMARY.md); all seven checkpoints deferred to one consolidated post-deploy validation pass (see below). 05-01: server-side RTVI wiring (RTVIProcessor after transport.input(), RTVIObserver w/ audio-level flags; LatencyReportObserver emits one kmv-latency RTVIServerMessageFrame/turn for the HUD; server.py StaticFiles mount for client/dist w/ 404 deep-link fallback). 05-02 (CLNT-04, 05-02-SUMMARY.md): bespoke Vite+TS+React SPA at apps/voice/client/ — UI-SPEC design system encoded as CSS tokens (src/styles/tokens.css); hero orb <OrbCanvas state amplitude /> = WebGL2 shader plasma + orbiting particle ring (sketch 001 winner A) with mandatory 2D/reduced-motion OrbFallback; orbState.ts (ORB_STATE_VISUALS + smoothAmplitude EMA) is the single state->color/motion source 05-04 live wiring reads; D-07 attract landing (Attract.tsx, onTapToTalk seam for 05-03); D-03 multi-stage Dockerfile (node:22-slim client-build -> COPY dist into python:3.12-slim /app/client/dist). Two @pipecat-ai transport deps exact-pinned (1.12.0/1.10.5) + committed lockfile. npm build green, tsc clean, orbState.test.ts 9/9. Both plan checkpoints orchestrator-cleared (npm legitimacy precleared; attract "whoa" approved — authoritative visual sign-off deferred to the 05-04 deployed-AWS checkpoint). Local build needs node>=22.12 (vite8/rolldown floor; used node v23.6.0; image node:22-slim is above it). 05-03 (CLNT-08, 05-03-SUMMARY.md): PKCE (RFC 7636, Web-Crypto-only) + public OIDC client (buildAuthorizeUrl/exchangeCode, verified against the RFC's own known-answer vector); in-memory-only tokenStore (no localStorage/cookie, grep-gated) is the Bearer source 05-04 reads; Callback.tsx (state-validated code exchange) + NoAccessGate.tsx (D-13 verbatim exclusive/invite-only copy). Found + fixed two real deploy blockers: (1) the 'voice' OIDC client registration (apps/auth/webapp/src/config/oidc.ts) was still the pre-D-01/D-02 confidential-client shape (client_secret_post + Auth.js callback URIs) — corrected to a public PKCE client (token_endpoint_auth_method: none) with the SPA's own /callback redirect_uri; full auth webapp suite still 33/33; (2) build-voice.yml passes no --build-arg, so Dockerfile now bakes public VITE_OIDC_* as ARG defaults (no secret exists) so the deployed bundle isn't built with them undefined. Checkpoint (live sign-in round-trip against deployed auth+client) deferred to the orchestrator's post-05-04 deployed-AWS validation, not self-approved. 05-04 (CLNT-01/02/03/04, 05-04-SUMMARY.md): gesture-gated getMic.ts (denied/no-device/unsupported, verbatim MicError.tsx copy); voiceSession.ts (PipecatClient + SmallWebRTCTransport -> POST /api/offer, Bearer as an Authorization header — NOT request_data, since small-webrtc-transport 1.10.5 nests it back as camelCase requestData which server.py's pre-gate check doesn't read); connectionState.ts pure reducer (idle/requesting-mic/connecting/connected/rejected/failed, rejected distinct from connected+failed); found + fixed a real vendor-library gap (Rule 2): SmallWebRTCTransport's negotiate() swallows the offer POST's HTTP error and never rejects connect() on a 401/403/429 — added a window.fetch interceptor scoped to one connect() call to detect + surface it (residual known limitation: a possible one-shot background retry before disconnect() lands, flagged for 05-06). useOrbBinding.ts derives live OrbState + EMA-smoothed uAmplitude from RTVI events into the existing OrbCanvas; captionReducer.ts + Captions.tsx render last-exchange-only subtitle captions; Live.tsx + App.tsx route to the live stage only once connected. 40/40 tests pass, tsc/build clean. Checkpoint (live end-to-end conversation) deferred to the orchestrator's post-05-04 deployed-AWS validation — also blocked on the still-open Phase-4 IAM gap (voice task role lacks cross-table read on kmv-auth-electro), not self-approved here. 05-05 (CLNT-05/06, 05-05-SUMMARY.md): useCountdown.ts (remaining seconds + normal/warning<=30s/critical<10s escalation, synced to the server's spoken -30s winddown_warning_seconds) + Countdown.tsx (persistent corner pill, amber/red-pulse escalation, aria-live, orb never recolored); useLatencyMetrics.ts (reduces live kmv-latency RTVI serverMessages, null stage stays null/dash) + LatencyHud.tsx (off-by-default, 'H'-key/affordance-toggled translucent panel, monospace per-stage rows) + Live.tsx mounts both. Found + fixed a real plan gap (Rule 2): sessionMaxSeconds has no source in the JWT (tier_id only) or anywhere in the existing /api/offer response — extended server.py's answer with gate_result.session_max_seconds (additive field) and voiceSession.ts's existing 05-04 fetch interceptor to peek it non-destructively on the success path, threaded through useVoiceSession -> App.tsx -> Live.tsx. 25 new tests (65/65 total client-side), 166/166 Python tests still pass, tsc/build clean. Checkpoint (live countdown escalation + HUD toggle with real per-turn numbers) deferred to the orchestrator's post-deploy validation — also blocked on the still-open Phase-4 IAM gap, not self-approved here. 05-06 (CLNT-02/07, 05-06-SUMMARY.md): retryPolicy.ts (createRetryController — pure, timer-injectable bounded N=3 exponential-backoff schedule, 500ms/1s/2s, exhausted terminal verdict, success resets the counter) wired into useVoiceSession.ts so a genuine pre-connect transport/ICE failure auto-retries with visible "Reconnecting… (attempt n of N)" (ConnectingRetry.tsx) then, once exhausted, renders the verbatim UdpBlockedWall.tsx with a manual "Try again" — no infinite spinner. gateMapping.ts maps every typed quota.GateResult error_type (daily-limit/concurrency-limit/site-paused/no-access/at-capacity) to verbatim UI-SPEC gate copy + a retryable flag (at-capacity/unknown share one documented Claude's-discretion generic-provider-error copy, since the contract has no assigned string for that one transient case); GateCard.tsx renders it in the same translucent-card treatment as NoAccessGate/UdpBlockedWall. SessionEnd.tsx + a reworked useVoiceSession.ts (new wasConnectedRef/connectedAtRef pair, no change to the shared connectionState.ts reducer) distinguish a clean server-driven end ("Nice talking with you." + {m:ss} spoken) from a post-connect provider-error drop (generic-provider-error-end copy); Reconnect and the gate card's "retry" both funnel through the same start() — a fresh /api/offer that re-executes the server's quota start_gate, never a silent transport reopen. 14 new tests (79/79 total client-side), tsc/build clean. Checkpoint (real hostile/UDP-blocked network, kill-switch/concurrency/daily-exhausted gate copies, a real session ending via timer/goodbye then Reconnect re-checking quota) deferred to the orchestrator's post-deploy validation — also blocked on the still-open Phase-4 IAM gap, not self-approved here. 05-07 (mobile/iOS layout + a11y baseline hardening, CLNT-01..08 cross-cutting, 05-07-SUMMARY.md): audited every existing screen against the UI-SPEC mobile/a11y baseline and found most touch-target/safe-area/focus-ring work already done in 05-02..05-06; scoped real work to the remaining gaps. New a11y/liveRegions.ts (announcePolite/announceAssertive shared live regions + a REACTIVE useReducedMotion hook, subscribing to matchMedia's change event instead of a one-shot mount check) + a11y.test.ts (6 tests). Found + fixed two real bugs (Rule 1/2): Countdown.tsx's sr-only span was continuously aria-live=polite, re-announcing every second (screen-reader spam) — now announces once at the warning/critical escalation boundaries only; Callback.tsx had NO aria-live/role at all — added role/aria-live matching every other screen's pattern. OrbCanvas.tsx now reads useReducedMotion reactively so toggling iOS Reduce Motion mid-session swaps the orb immediately (the checkpoint's own step 6 needs this). New responsive.css (100dvh fallback, mobile-portrait/short-landscape safe-area tuning, global :focus-visible fallback); tokens.css --sz-display now clamp(24px,7vw,32px). Esc now dismisses GateCard/inline MicError via a new useVoiceSession.dismissMicError (not UdpBlockedWall/SessionEnd, which need an explicit choice). Contrast floors hand-verified (~15:1 caption, ~12.3:1 CTA, both clear the >=7:1/>=4.5:1 floors) — no token change needed. 85/85 total client-side tests pass, tsc/build clean. Checkpoint (real iPhone + restricted/UDP-blocked network, VoiceOver, iOS Reduce Motion) — the phase's headline "verified on real phones and hostile networks" requirement (success criteria 1-5) — deferred to the orchestrator's post-deploy validation pass, not self-approved here; also blocked on the still-open Phase-4 IAM gap. **Phase 5 is now code-complete, 7/7 plans, pending that one consolidated live-verification pass.**
Phase 05.2 (Slick Start — single-tap silent SSO + instant pre-rendered greeting): CODE-COMPLETE — 4/4 plans done. 05.2-01 (Workstream A, 05.2-01-SUMMARY.md): single-tap silent SSO — breadcrumb store, prompt=none attemptSilentSso guard, Callback login_required branch; 4/4 tasks, client tests + tsc build green; live verification deferred to the phase's consolidated pass. 05.2-04 (Workstream B, 05.2-04-SUMMARY.md): PersonaConfig.greet_first (default True, back-compat) read from `[persona]` TOML; pipeline.toml ships `greet_first = false`; server.py's `register_greet_first(...)` now guarded behind `cfg.persona.greet_first` so KPH doesn't greet twice on WebRTC now that the client plays a pre-rendered opener on tap; concierge.md "Opening move" rewritten so KPH doesn't re-introduce itself. 2 new config tests, full server suite 168/168 pass. 05.2-02 (Workstream B assets, 05.2-02-SUMMARY.md): `greetings.source.json` (3 approved KPH texts) + `render_greetings.py` (reads `[tts] voice_id` from pipeline.toml, calls ElevenLabs eleven_flash_v2_5 @ speed 1.1, emits mp3_44100_128) + `make -C apps/voice greetings` target; LIVE billed render executed (checkpoint pre-authorized) — 3 committed `greeting-N.mp3` + `greetings.manifest.json` confirmed `voiceId: UgBBYS2sOqTuMpoF3BR0`; new `test_greeting_voice_drift.py` CI guard (2 tests, proved it bites by a temporary manifest.voiceId mutation then revert) fails the build if the clips ever drift from the configured voice. Full Python suite 170/170 pass. 05.2-03 (Workstream B client player + handoff, 05.2-03-SUMMARY.md): `greetingPlayer.ts` — `playRandomGreeting()` loads the 05.2-02 manifest (module-cache), plays a random clip on a fresh Audio element, resolves an `ended` promise on finish/error/blocked-play (never rejects/hangs), returns null on an empty manifest; wired into `useVoiceSession.ts` — plays the clip on the tap gesture (right after mic grant, before beginConnect — the iOS audio unlock), defers the CONNECTED dispatch behind `greetingEndedRef` so Live mounts only once BOTH the clip ended AND the transport connected (self-loop guard: greeting speaker vs live-STT mic never overlap), and releases the clip on clip-end, on stop(), AND on the pre-connect TRANSPORT_ERROR branch (so a failed connect never plays the clip over the retry UI / UdpBlockedWall). 2 tasks (5a9a9c0 + 8d5b30e), 2 test-harness-only auto-fixes (arrow→`function` Audio mock + per-test vi.resetModules; `act` imported from @testing-library/react), full client suite 100/100 + tsc/vite build clean, server suite 170/170. Client tests run under Node ≥22.12 (nvm use 23) due to the ambient Node 22.1.0 jsdom/html-encoding-sniffer bug. **Gap-closure (05.2-VERIFICATION.md, commit cfc7013):** phase verification found one reproducible, non-live bug — App.tsx's `handleAuthenticated` unconditionally re-called `markReturningUser()`, undoing Callback.tsx's `clearReturningUser()` on the `login_required` safe-degrade path (criterion 2), so a signed-out device kept re-arming the breadcrumb and would re-loop the silent SSO bounce forever. Fixed via TDD: removed the redundant call (+ its now-unused import) from App.tsx; added `App.breadcrumb.test.tsx` exercising the real App+Callback wiring (not a stub), proven RED against the unfixed code then GREEN after the fix, plus a guard test confirming the successful `?code=` path still marks the breadcrumb. Full client suite 102/102 (100 prior + 2 new), tsc/build clean. **Phase 05.2 is now code-complete, 4/4 plans + gap closed (6/6 success criteria verified at code level)**, pending the same consolidated live iPhone/Safari verification pass every 05-0x checkpoint defers to (and the still-open Phase-4 IAM gap).
Phase 6 (Latency v2): scoped, deferred.
Phase 7 (KPH Knowledge Base): CODE-COMPLETE — 5/5 plans done (see 07-01-SUMMARY.md, 07-02-SUMMARY.md, 07-03-SUMMARY.md, 07-04-SUMMARY.md, 07-05-SUMMARY.md). 07-05 (production polish -- steering/pacing/honest-unknowns/do-not-say + benchmark eval set, PIPE-10/PIPE-06, 07-05-SUMMARY.md): `prompts/concierge.md` persona v4 -- adaptive steering (directed answer-then-hook vs. tour-mode offer walking `tour_priority`'s order, D-04/D-05), the D-12 honest-unknowns rule (never a bluff, redirect to something known), a spoken do-not-say boundary (account IDs/internal hostnames/unreleased roadmap), and the PG-13/match-and-escalate guardrail (already in the style layer) restated in the persona itself. `prompt_assembly.build_system_blocks`'s long-present `remaining_seconds` seam now actually renders (`render_pacing_note`, `PACING_TIGHT_THRESHOLD_SECONDS=90s`) a tight-highlights-or-go-deeper note prepended to block1 ONLY -- block0 byte-identical, `None` reproduces the Plan-01/02 shape unchanged. `router.KnowledgeRouterProcessor` gained `remaining_seconds_fn` (a zero-arg callable invoked synchronously on the same deep-turn block1 rebuild that fires the ack -- no second timer); `session.SessionLifecycle` gained `remaining_seconds()` (a pure read of `session_max_seconds - elapsed`, `None` for a bypass session) as the pre-existing-state source the router's seam reads from -- added even though `session.py` wasn't in this plan's own declared file scope, since its own read_first note required sourcing from there and no public accessor existed (Rule 2 auto-add). 8 new tests (`test_knowledge_pacing.py`). Five new benchmark scenarios complete ROADMAP criterion 4: `kph_unknowns.yaml` (D-12 honest+redirect, judged against invented facts), `kph_tour_mode.yaml` (D-04/D-05 tour offer + stop-by-stop itinerary), `kph_crude_humor_guard.yaml` (T-07-04 persona-guardrail proof -- neutral opener stays PG-13, no unprompted crude/edgy material; a second observational turn shows match-and-escalate is permitted but not required), `kph_retrieval_depth.yaml` (a second, distinct long-tail km depth proof -- `spec.agent:` per-tool/per-CLI autoApprove/deny gating, deliberately opening on a different topic first since the router only fires retrieval on a genuine switch and km is already the initial topic), `kph_router_accuracy.yaml` (3-topic directed battery + the Pitfall-1 ambiguous "toolkit" utterance, judged for correct content + safe deferral). Full suite 255/255 pass (247 prior + 8 new, zero flake). Not yet exercised (same documented `pipecat-ai[evals,local]` blocker as every prior 07-0x plan): the full live-audio benchmark run (all `kph_*.yaml` + `memory.yaml`/`kph_cache_verify.yaml` via `pipecat eval run` against a real `bot.py -t eval`) that would produce the pass-rate + router-accuracy numbers this plan's phase-gate output asks for -- flagged for a human `uv sync --group dev` then a live run, not self-approved. Also flagged (not blocking): `remaining_seconds_fn`/`SessionLifecycle.remaining_seconds()` are real and tested but NOT yet wired into production (`pipeline.py`/`server.py` were out of this plan's declared scope) -- a small follow-up would thread `lifecycle.remaining_seconds` through `build_pipeline()` into the router constructor if live pacing behavior is desired. **Phase 7 is now CODE-COMPLETE, 5/5 plans**, pending that one consolidated live-audio benchmark pass every 07-0x plan's own checkpoint has been deferring to (same pattern as Phase 5's consolidated live-verification deferral). 07-04 (offline knowledge refresh, PIPE-10/PIPE-07, 07-04-SUMMARY.md): `apps/voice/scripts/refresh_knowledge.py` -- the D-07 manual refresh, offline only (Amendment 3.G). `read_manifest()`/`parse_manifest()` enforce D-01 (manifest is the only source list) and D-02 (every source now carries an explicit `public: true` flag added to `manifest.yaml`; missing/false is refused, recorded not crashed). `resolve_topic_sources()` skips a missing local checkout with a warning. `build_topic_chunks()`/`write_chunk_file()` reuse Plan-02's `chunk_text()` verbatim and write `knowledge/index/{topic}/docs.jsonl`, proven to load through a real `RetrievalIndex`. `flag_landmines()`/`write_pack()` reuse Plan-01's `advisory_lint()` verbatim -- FLAGS findings, never blocks the write (Amendment 3.E). The doc-generation seam (`generate_docs()`) defaults to a NO-OP per DESIGN-NOTES Amendment 5 (grill-with-docs DROPPED, code indexed directly) -- this reverses the plan's own stale Task 2 text/`user_setup` block, confirmed against Amendment 5 + 07-RESEARCH's own superseding note + the already-committed manifest's "no doc-gen step" source notes; the seam itself stays swappable. `distill_topic`/`survey_repo`/`style_pass` (curated-pack distillation map-reduce) are real, LLM-call-behind-a-seam functions, never exercised live in this session. Exposed via `make -C apps/voice knowledge` (Makefile) and `kv knowledge refresh` (`kv/internal/app/cmd/knowledge.go`, thin `exec.Command` dispatcher, registered in root.go). 20 new keyless tests. Found + fixed two real bugs via manual dry-run verification against this machine's actual external checkouts (NOT covered by the tmp_path RED tests): `iter_source_files` used an unbounded `Path.rglob` that hung on a real 443,982-file `node_modules` tree under `defcon.run.34/apps` (switched to `os.walk` + directory pruning); manifest relative paths (`apps/voice/...`, `.planning/...`) resolved against the wrong base directory (cwd, not repo root) -- switched to a `REPO_ROOT`-anchored resolution. Full suite 247/247 pass (227 prior + 20 new, zero flake); `kv` builds clean, its existing Go test suite still passes. Not yet exercised: a real, live `make -C apps/voice knowledge` run with a billed Anthropic call for the distillation pass + full local checkouts, reviewed as a D-09 git diff -- deliberately not run during automated execution (would spend real money and/or risk clobbering real content); flagged as a human-judgment item. 07-03 (full primary topic set, PIPE-10, 07-03-SUMMARY.md): extends the km walking slice to all three headline systems with ZERO changes to the Plan-01/02 loader/router/retrieval code (N-topic-ready design confirmed). Authored `knowledge/topics/defcon-run-34.md` + `knowledge/topics/meshtk.md` (curated, advisory-lint-clean deep packs, each folding in verbatim Kurt-voiced transcript quotes; deliberately NO Landmines/do-not-say section per this plan's own explicit prohibition, unlike 07-01's km precedent). Promoted both topics in `knowledge/router/topic-map.yaml` (hook + weighted keywords -- meshtk deliberately omits a bare "toolkit" keyword, Pitfall 1's own documented overlap example) and `knowledge/manifest.yaml` (pack refs + "doc-gen then index" retrieval-source notes for Plan 04). Added 3 new keyless router tests: three-topic discrimination without collision, the toolkit-overlap-guard fallback (resolves to None, never a confident wrong meshtk pick), and a real km-\>defcon topic switch proving the ack fires once and a same-topic follow-up does not re-ack. Authored `scenarios/kph_knowledge_defconrun.yaml` + `kph_knowledge_meshtk.yaml` (same judge_factory shape as the km scenario). Found + fixed zero code bugs; one documentation-only plan/reality reconciliation (Rule 1 class, same as 07-02's): followed the REAL shipped architecture (manifest.yaml owns `pack:` paths, topic-map.yaml is classification-only) over this plan's own stale acceptance-criteria snippets (which asked for a non-existent `pack:` field inside topic-map.yaml and a `klanker_voice.knowledge.pack.load_topic_pack` function that has never existed). Full suite 227/227 pass (no flake this run). Not yet exercised (same documented `pipecat-ai[evals,local]` blocker as 07-01/07-02): the two new scenarios' full live-audio eval run, and the cross-topic (km-\>defcon) cache-warmth live proof (`cache_read_input_tokens > 0` on the switched turn) -- mechanism-level proof for the same-topic case already exists from 07-01; block0 is architecturally never touched on any switch (verified by the new topic-switch test), but the live cross-topic reading itself remains an open item. Plans 07-04..07-05 (refresh workflow, pacing/evals) remain. 07-01 (the km walking slice, PIPE-10/PIPE-06/PIPE-07): `[knowledge]` config seam (`KnowledgeConfig`/`load_knowledge_config`, independent of `PipelineConfig`, mirroring the `load_quota_config` precedent); N-topic-ready `knowledge/manifest.yaml` + `knowledge/router/topic-map.yaml` (km populated only, 07-03 only appends); `knowledge/topics/klanker-maker.md` (km deep pack, promoted from `corpus/km-digest.md` + the sandbox diagram legend + verbatim transcript quotes); `knowledge/style/kurt-voice.md` (Kurt STYLE layer — distilled transcript cadence + the humor/personality profile incorporated in full + verbatim exemplars + the PG-13 match-and-escalate guardrail — lives in the cached stable prefix); `klanker_voice.knowledge` package (`prompt_assembly.build_system_blocks`/`apply_system_blocks`/`count_tokens`, `router.classify`/`KnowledgeRouterProcessor`, `lint.advisory_lint`); `pipeline.py` rewired to the two-block cached system array + the router between stt and user_aggregator. Found + fixed two real bugs (Rule 1/Rule 3): the plan's own `cache_min_tokens` field name collided with `config.py`'s credential-field-rejection regex (renamed to `cache_floor`); pipecat 1.5.0's `AnthropicLLMAdapter` flattens a list-content system message into a joined string before the API call, silently discarding `cache_control` — `apply_system_blocks()` now sets the two-block array directly on the LLM service's `Settings.system_instruction` instead (the one path that survives verbatim), documented at length in code. **Live-proven** (ROADMAP success criterion 1): a direct two-call Anthropic API test against `build_system_blocks()`'s real output shows `cache_creation_input_tokens=5438` on turn 1 and `cache_read_input_tokens=5438` on turn 2 (block0 measured 5444 tokens, clearing the 4096 floor). 205/206 Python tests pass (1 pre-existing, unrelated `test_session.py` timing flake, confirmed pre-existing and passing 3/3 in isolation). Not yet exercised: the full live-audio eval run (`kph_knowledge_km.yaml`/`kph_cache_verify.yaml` via `pipecat eval run` against a real `bot.py -t eval`) — this venv's `kokoro`/`moonshine` local eval deps aren't installed; flagged as a blocker for a human to `uv sync` and run, not self-approved here. 07-02 (local BM25/FTS5 retrieval wired into the deep turn, PIPE-10/PIPE-07, 07-02-SUMMARY.md): `klanker_voice.knowledge.retrieval` — keyless, in-process stdlib `sqlite3` FTS5 + its own BM25 ranking (`Chunk`, heading-aware `chunk_text`, `fts5_available`, `build_topic_index`, `RetrievalIndex` with lazy per-topic index build + sanitized-query + top-k/budget-capped `query()`, graceful `[]` degrade for missing topics). `knowledge/index/klanker-maker/docs.jsonl`: 2601 real chunks from km's actual `docs/` tree (32 files) + the diagram legend — a real walking-slice corpus, advisory-linted (58 findings, all generic AWS-placeholder patterns, none real). `[knowledge]` config gains `index_dir`/`retrieval_enabled`/`retrieval_top_k`/`retrieval_budget` (named `retrieval_budget`, not `retrieval_max_tokens` — same credential-regex collision class as `cache_floor`). `prompt_assembly.build_system_blocks` now accepts `retrieved_chunks: list[Chunk]` and appends a THIRD uncached, post-breakpoint block when non-empty — `system[0]`/`system[1]` stay byte-identical either way. `router.KnowledgeRouterProcessor` gained a `retrieval_index` param and queries the classified topic on the exact same genuine deep-turn switch that fires the ack (never on a same-topic follow-up/shallow one-liner) — ack-masked, no new network hop. `pipeline.build_pipeline` constructs one `RetrievalIndex` per session, reused across turns. **Real-corpus proof**: a BM25 query for km's Phase 121 "action quota freeze quarantine" mechanism returns real chunks (`action_frozen`/`onBreach`), and that exact detail is asserted absent from the curated `klanker-maker.md` pack — retrieval genuinely adds depth. Query timing: ~30ms cold (index build), <1ms warm — well under the 100ms budget. `scenarios/kph_retrieval_km.yaml` authored (live run deferred, same `pipecat-ai[evals,local]` blocker as 07-01). Full suite 224/224 pass. One process incident: an accidental (forbidden) `git stash --include-untracked` mid-session reverted uncommitted Task 2 work; recovered fully by re-applying the known edits directly (no stash pop/apply/drop used) — a stale, inert `stash@{0}` entry remains in the repo's shared stash stack, flagged for optional manual cleanup (`git stash list` / `git stash drop`). Plans 07-03..07-05 (more topics, refresh workflow, pacing/evals) remain.
Phase 11 (VoIP.ms Telephony — Local Asterisk Edge): CODE-COMPLETE — 7/7 plans done, pending ONE outstanding human-verify item (see below). 11-06 (D-05, 11-06-SUMMARY.md): the §24 silent answer-gate. `telephony/gate.py` — `GateProcessor` (FrameProcessor, inline in the persistent pipeline, same slot as `KnowledgeRouterProcessor`) + `match_passphrase` (order-independent set match) + `accumulate_dtmf` (pure early-exit buffer compare) + a self-contained fail-closed `asyncio` timer; while locked, `process_frame` never `push_frame`s `TranscriptionFrame`/`InterimTranscriptionFrame`/`UserStartedSpeakingFrame`/`UserStoppedSpeakingFrame` — the structural redaction boundary (D-05e). `pipeline.build_pipeline` gains an additive `gate_processor` param (after `stt`, before duplex/router; `None` = byte-identical WebRTC order). `call_runtime.create_call_session` threads it through, skips `register_greet_first` when gated, and `CallSession` gains a `context` field. `session.SessionLifecycle` gains `upgrade_from_bypass()` (Rule 2 auto-add) promoting an already-`start()`-ed `bypass_accounting=True` placeholder lifecycle into real accounting in place, at unlock — no second lifecycle ever built. `telephony/controller.py`: `on_stasis_start` branches on `telephony_cfg.require_gate` — gated (production default) builds the persistent pipeline immediately with a zeroed bypass `GateResult` (no real accounting while locked, D-05d), runs it as a background task, and defers the REAL `quota.start_gate` call + `greet_now` to unlock (`_gate_unlock`, either the passphrase matched inside the pipeline or DTMF matched entirely at the controller/ARI layer via a new `on_channel_dtmf_received` handler + `accumulate_dtmf` — the PIN never touches the pipeline/LLM); both fail-closed triggers (gate-window expiry, quota-denied-after-unlock) route through `_gate_fail_closed` → the existing single idempotent `_close_active_call` teardown. Ungated (`require_gate=False`) preserves Plan 05's interim immediate-grant behavior byte-for-byte — `test_telephony_lifecycle.py`'s `_telephony_cfg()` helper now defaults to it so every pre-existing plumbing test stays green unchanged; a new dedicated test section (12 tests, `require_gate=True`) covers the gated flow. Found + fixed one real bug (Rule 1): `_gate_fail_closed` was double-hanging-up the SIP channel (`_close_active_call`'s own `on_released` cascade already hangs it up, mirroring the R6 hard-timeout pattern) — removed the redundant explicit hangup call. 32 new tests (20 gate-isolated + 12 controller-integration), full project suite 399 passed/53 skipped/0 failed. Plan 07 (standalone telephony entrypoint + live SIP integration + the §19-C exit-criterion proof) remains. 11-01 (11-01-SUMMARY.md): `TelephonyConfig`/`load_telephony_config()` (frozen dataclass, media + §24 gate knobs only) plus a widened `_CREDENTIAL_FIELD_RE` that refuses pin/passphrase/pass_word-shaped TOML fields before parse (with a real false-positive collision found and fixed against the existing `[duplex]` `backchannel_words` fields). 11-02 (11-02-SUMMARY.md): five hand-authored Asterisk configs (inbound-only Stasis dialplan, private authenticated ARI, ulaw-only PJSIP endpoint) plus a pinned-image, macOS-safe docker-compose dev harness and a 9-test structural invariant lint proving the D-01/§25 security posture mechanically; ARI/HTTP bindaddr flagged 127.0.0.1-only (a host-run controller can't yet reach it through the published port — noted for a later plan, not required by 11-02's own verification). 11-03 (D-03, 11-03-SUMMARY.md): `SocketRtpMediaSession` + `_AsteriskRtpProtocol` (`telephony/rtp_socket.py`) — a socket-backed `types.RtpMediaSession` over real `asyncio` UDP loopback, binding BEFORE the controller creates Asterisk's `externalMedia` channel (R2, `connection_type=client` means Asterisk always dials out) and learning the peer `(ip,port)` via symmetric-RTP first-packet source learning, updated every packet; hostile-input safe (`datagram_received`/`error_received` never raise, T-11-03-01). `media.py`/`transport.py` verified byte-unchanged (`git diff --stat` empty) — the Phase 10 seam is reused verbatim. 5 new tests, 45/45 telephony suite green, full project suite 353 passed/53 skipped/0 failed. Not yet wired into a live controller/ARI client (D-08, a later plan). 11-04 (D-06/D-01, 11-04-SUMMARY.md): `AriClient` (`telephony/ari.py`) — raw-`aiohttp` ARI REST client (no new dependency, D-06) covering the six ARI REST calls (answer, create externalMedia channel, create mixing bridge, addChannel, hangup, destroy bridge) funneled through one `_request()` helper, plus the events-WebSocket dispatch loop (`on()`/`run()`) routing `StasisStart`/`ChannelDtmfReceived`/`ChannelDestroyed` to registered handlers — unregistered event types and handler exceptions are both caught/logged, never fatal (T-11-04-02); a WS-close frame ends `run()` cleanly. `AriError(status, path)` never leaks the password (verified by test + `grep`). Confirmed against the installed aiohttp 3.14.1 source that session-level `BasicAuth` (set once in `connect()`) covers both REST and `ws_connect` automatically. DTMF accumulation deliberately deferred to the Plan-05 controller (Landmine 5). Explicit `aiohttp>=3.14,<4` pyproject pin added (documents the already-transitive dependency); `uv lock` relocked clean. 14 new tests, full project suite 367 passed/53 skipped/0 failed. Not yet wired into a controller/entrypoint (Plan 05). 11-05 (D-02/D-04, 11-05-SUMMARY.md): `AsteriskCallController` + `ActiveCall` registry (`telephony/controller.py`, §13 field shape) — `on_stasis_start` (context/app gate, R2 bind-first socket media session, externalMedia channel + mixing bridge, an interim-placement `quota.start_gate` call, `CallSession` construction via `create_call_session(channel="pstn")`, tracked background worker task) and the single idempotent `_close_active_call` (guarded by `ActiveCall.lock`+`closed`, wraps every ARI teardown call in a new `_safe_ari` helper so an already-gone Asterisk-side resource never aborts the rest of a teardown — Rule 2 auto-add for T-11-05-01). Hard-timeout `lifecycle.on_released` composed with `ari.hangup(sip_channel_id)` (R6, never a silent open PSTN call); quota-denied leaves no bridge (`_teardown_gate_resources`, no `ActiveCall` ever registered). `test_telephony_lifecycle.py`: `FakeAriClient`/`FakeRtpMediaSession` + a `stub_call_session_run` fixture (brackets `lifecycle.start()` only, never `runner.run()`, avoiding a live Deepgram/Anthropic/ElevenLabs round trip — Rule 3 auto-add for test hermeticity) — 6 tests covering the §16/§17 matrix (allocation, unexpected-context no-alloc, exactly-once ChannelDestroyed close, simultaneous hangup+timeout release-once, hard-timeout SIP hangup, quota-denied no-bridge). Full project suite 373 passed/53 skipped/0 failed. `webrtc.py`/`server.py` verified byte-unchanged (D-08). Plan 11-07 (standalone telephony entrypoint + live SIP integration + live verification) remains. 11-07 IN PROGRESS (2/4 tasks committed, paused at Task 3 checkpoint): Task 1 (D-08) `telephony/__main__.py` — `main()` loads config+knowledge+quota+telephony config, guards on `[telephony].enabled`, reads `ASTERISK_ARI_*` from env (raises `ConfigError` if user/pass unset, never logs them), constructs `AriClient`+`AsteriskCallController`, registers handlers, connects+runs the ARI events WebSocket; `controller.py` gained an `if __name__ == "__main__"` path importing this module's `main()` so both `python -m klanker_voice.telephony` and the D-08-literal `python -m klanker_voice.telephony.controller` work. `configs/telephony.toml` (harness config, `[telephony] enabled = true`, all other tables cloned from `pipeline.toml`, relative paths resolved against `configs/`). Task 2 (D-07) `tests/test_telephony_integration.py` — the CI-required deterministic fake-media artifact: reuses `test_telephony_lifecycle.py`'s `FakeAriClient`/`FakeRtpMediaSession`/`_build_controller` to drive one coherent end-to-end scenario per test (StasisStart → passphrase or DTMF gate-unlock → real `quota.start_gate` grant + `greet_now` → `ChannelDestroyed` → the single idempotent teardown) plus a fail-closed scenario; proven green with a fully clean env (`env -i`, no leaked provider keys). `asterisk/sipp/gate-pass.xml` (SIPp UAC scenario) + `asterisk/sipp/fixtures/README.md` (pcap recording recipe, fixtures deliberately not committed — D-09) + `docker-compose.yml` gains a `sipp` service under `profiles: [integration]` (never runs on plain `up`), built from the official SIPp source (11-RESEARCH.md R4) with an explicitly flagged unverified-in-sandbox version pin. Found + fixed one real gap (Rule 1): the happy-path/DTMF integration tests are the first to combine a REAL gate unlock (`bypass_accounting=False`) with a teardown in the same test — `SessionLifecycle.release()` then calls the real `quota.release_heartbeat()`, uncovered by the existing `fake_aws` fixture (boto3 CloudWatch/ECS only); added a `_stub_release_heartbeat` monkeypatch. Full project suite 403 passed/53 skipped/0 failed (399 prior + 4 new). Task 4 (README manual §19-C proof recipe, 11-07-SUMMARY.md commit 0f54206) is now done: `apps/voice/asterisk/README.md`'s "Manual §19-C softphone proof" section documents the full 8-step recipe (fill secrets incl. the `${VAR}` substitution workaround → `docker compose up` → resolve the 11-02-flagged ARI-loopback-vs-published-port prerequisite via one of two documented options → run `python -m klanker_voice.telephony.controller` with real ARI/PIN/passphrase env → register a SIP softphone → confirm silent-answer/unlock/greeting-not-clipped/converse/barge-in/clean-hangup → confirm fail-closed → confirm no leaks) plus an explicit "CI-vs-manual boundary" section. **Task 3's checkpoint itself (the actual live human-run softphone call) was explicitly DEFERRED by human decision, not fabricated** — this sandbox's Docker daemon is not running (`docker info` fails; the `docker` CLI + compose plugin ARE present and were used for config-validation-only checks in Plans 02/07). All 4 tasks now committed (`2dd1efa`, `057b4a3`, `cc0ed2b`, `0f54206`); 11-07-SUMMARY.md written (coverage D3 explicitly `human_judgment: true`, not auto-passed). **Phase 11 is now CODE-COMPLETE (7/7 plans)**, blocked from a genuine "fully verified" claim only by this one outstanding §19-C live-softphone item — see Blockers/Concerns below.
Phase 12 (VoIP.ms Telephony — Inbound DID): IN PROGRESS — Plan 02/8 done (D-03/D-04/SC-1/D-02/SC-2, 12-01-SUMMARY.md, 12-02-SUMMARY.md). `kv voipms` command family (`balance`/`route-did <did>`/`set-caps`/`create-subaccount`) automates the API-drivable VoIP.ms provisioning steps over stdlib `net/http` (no new Go dependency); all VoIP.ms REST base URL + six method-name constants centralized in one block in `kv/internal/app/cmd/voipms.go`, credentials read only from `VOIPMS_API_USERNAME`/`VOIPMS_API_PASSWORD` env (SSM-sourced per D-04, never a flag, never printed). Every method name is marked `UNVERIFIED` in code — this executor session had no outbound web-fetch/browsing tool to confirm them against the live VoIP.ms API docs as the plan's VERIFY-BEFORE-HARDCODE step required; flagged as an explicit human follow-up (coverage D3, `human_judgment: true`) before running `kv voipms` against a real account. 17 new offline tests (`httptest.Server`-backed, no live network call) prove request shape for every sub-command + the centralization/no-credential-literal invariants mechanically. `docs/operators/voipms-provisioning-runbook.md`: the full §25.F blank-account order (2FA → international lock → balance/alerts → API+IP whitelist → `kv voipms create-subaccount` → order ONE DID (Toronto POP, per-minute, CNAM off) → `kv voipms route-did` → re-lock whitelist), a Secrets→SSM table naming all six `VOIPMS_*`/telephony SecureString parameters under `/kmv/secrets/use1/...`, the 8-IP Toronto POP table with a 6-month re-verification note, and an explicit registration-POP-must-equal-DID-POP statement — no outbound-dialing step anywhere (§25.A). `cd kv && go build ./... && go test ./...` green. 12-02 (the §23 caller-ID → code → tier mint path, D-02/SC-2, 12-02-SUMMARY.md): `apps/auth/webapp/src/lib/phone-normalization.ts` — `normalizeE164()`, a pure single-source E.164 canonicalizer (spaced/dashed/parenthesized/bare-10-digit North American numbers → `+1<10digits>`, idempotent, `""` for blank), TDD-proven RED before GREEN. `AccessCode` entity (`access-code.ts`) gains a non-required `phone` attribute (canonicalized via a `set` transform) + `phoneEnabled` boolean (default false, mirrors `bypassEnabled`) + a sparse `byPhone` GSI reusing the table's existing `gsi3pk-gsi3sk-index` (no new index — electro table already declares gsi1..gsi3), and `resolvePhoneToCode()` mirroring `resolveBypassToken`'s null-on-any-miss no-oracle contract exactly. `GET /tel/<e164>/route.ts` — a private, internal-only route (mirrors `/join/[token]/route.ts` byte-for-byte) that normalizes → resolves → mints via the UNCHANGED `mintAnonToken` (same iss/aud/jwks/kid as bypass /join), with an optional `TELEPHONY_ENDPOINT_AUTH_TOKEN` shared-bearer defense-in-depth check whose mismatch returns the identical 404 as every other failure mode (empty/unmapped/disabled/expired number, mint error). Only one log line in the whole module (tier-only, success path) — no raw caller ID, no mapped/not-mapped distinction anywhere. 16 new tests (5 normalization + 7 phone-resolution against real dynamodb-local + 4 tel-route, incl. identical-404 proofs across every failure mode); full auth suite 59/59 green. Plans 03-08 (SSM wiring, Asterisk VoIP.ms trunk, controller wiring, tier composition + seed data, telephony-edge deploy, manual cellular proof) remain.
Status: Executing Phase 12
Last activity: 2026-07-12 — Phase 12 Plan 02 (§23 caller-ID mint path: normalizeE164, AccessCode phone/byPhone GSI/resolvePhoneToCode, private /tel route) complete

### Phase 4 handoff (the auth contract Phase 4 consumes)

- JWT ACCESS token contract (pinned in 03-03-SUMMARY): issuer https://auth.klankermaker.ai/use1/api/oidc, jwks .../use1/api/oidc/jwks, aud https://voice.klankermaker.ai, RS256, scope voice, TTL 3600s, claims https://klankermaker.ai/tier_id (string, "no-access" default) + https://klankermaker.ai/group (string|null). Voice service uses PyJWT+PyJWKClient to validate offline — implemented in 04-01 (apps/voice/src/klanker_voice/auth.py).
- Live: DynamoDB kmv-auth-authjs + kmv-auth-electro (ACTIVE, seeded); SSM /kmv/secrets/use1/oidc/jwks (RS256 JWK Set, kid kmv-oidc-m-zCTIi5).
- Phase 4 will also: re-measure deployed voice-to-voice p50/p95 vs the ~1402ms local baseline (us-east-1 proximity expected to improve it), and build the usage table + quota enforcement (QUOT-01..05) against the tiers this phase defined.
- 04-01 done: production entrypoint (server.py), offline JWT validation (auth.py), public-IP+STUN ICE gathering (webrtc.py), Dockerfile — all unit-tested (30 new tests, 90/90 total) and the Docker image live-verified (build + running container). Known gap: real ECS task-metadata shape and live ICE/SDP-munging interop are unverified until 04-03's deployed smoke test.

Progress: [██████████] 97%

## Performance Metrics

**Velocity:**

- Total plans completed: 3
- Average duration: -
- Total execution time: 0 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 09 | 1 | - | - |
| 10 | 2 | - | - |

**Recent Trend:**

- Last 5 plans: -
- Trend: -

*Updated after each plan completion*
| Phase 02 P01 | 5 min | 3 tasks | 5 files |
| Phase 04 P01 | 10min | 3 tasks | 9 files |
| Phase 04 P02 | 35min | 3 tasks | 8 files |
| Phase 04 P04 | ~55min | 3 tasks | 10 files |
| Phase 05 P01 | 25min | 3 tasks | 6 files |
| Phase 05 P02 | 35min | 3 tasks | 23 files |
| Phase 05 P03 | 45min | 5 tasks | 17 files |
| Phase 05 P04 | 45min | 3 tasks | 16 files |
| Phase 05 P05 | 35min | 2 tasks | 15 files |
| Phase 05 P06 | 55min | 3 tasks | 10 files |
| Phase 05 P07 | 40min | 1 tasks | 10 files |
| Phase 05.2 P01 | 12min | 4 tasks | 12 files |
| Phase 05.2 P04 | 15min | 1 tasks | 5 files |
| Phase 05.2 P02 | 20min | 3 tasks | 8 files |
| Phase 05.2 P03 | 20min | 2 tasks | 4 files |
| Phase 07 P01 | 65min | 3 tasks | 17 files |
| Phase 07-kph-knowledge-base P02 | 40min | 2 tasks | 10 files |
| Phase 07-kph-knowledge-base P03 | 30min | 2 tasks | 7 files |
| Phase 07-kph-knowledge-base P04 | 70min | 3 tasks | 6 files |
| Phase 07-kph-knowledge-base P05 | 25min | 2 tasks | 9 files |
| Phase 11 P01 | 20min | 3 tasks | 4 files |
| Phase 11 P02 | 20min | 3 tasks | 9 files |
| Phase 11 P03 | 15min | 2 tasks | 2 files |
| Phase 11 P04 | 20min | 2 tasks | 4 files |
| Phase 11 P05 | 25min | 3 tasks | 2 files |
| Phase 11 P06 | 30min | 3 tasks | 7 files |
| Phase 11 P07 | 35min | 4 tasks | 8 files |
| Phase 12 P01 | 25min | 2 tasks | 4 files |
| Phase 12 P02 | 30min | 3 tasks | 6 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Roadmap: 5-phase structure per research — local pipeline first (de-risks core value, zero infra dependency), infra parallel (SES clock), auth before voice (JWT/tier-claims contract is voice's hard dependency), client/hardening last
- Roadmap: `kv` CLI has no standalone phase — grows incrementally in Phase 3 (code/tier CRUD) and Phase 4 (usage, kill-switch, smoke test)
- Roadmap: INFR-03 (deployed ICE smoke test) and INFR-06 (autoscaling) live in Phase 4, not Phase 2 — only verifiable once the voice service is deployed
- [Phase 02]: SGUID locked at 6e913c73 — state bucket/lock table tf-kmv-use1-6e913c73 live in 052251888500 — bootstrap-state.sh is the single source (Pitfall 3); same value must land in site.hcl random_suffix default (Plan 02) and gh repo var SGUID (Plan 06)
- [Phase 02]: Apex DMARC via route 2: standalone _dmarc inline unit, make_site_domain=false — zone audit found zero apex mail records and no auth./voice. NS delegation collisions; route 1 would hijack apex inbound MX (Pitfall 6)
- [Phase 02]: kmv-github-delegate CORRECTION — the 02-06 "user-created" role never existed; 02-07 executor created it via sudo-management (which HAS admin in 481723467561, contra 02-06's assumption) + added route53:ListTagsForResource (5th action, needed by aws_route53_zone data source). INFR-07 proof run green. 02-06-SUMMARY/02-USER-SETUP reconciled
- [Phase 01]: Endpointing A/B FINAL — winner Nova-3 + SmartTurn v3 + persona v2, v2v p50 ~1402ms / p95 ~2080ms ACCEPTED (user decision after two measured rounds); Flux LOSES on pipecat 1.5.0 (hard-coded 0.5s ExternalUserTurnStopStrategy hold); eager EOT rejected; prompt caching ruled out at this prompt size (Haiku 4096-token cache minimum vs ~600-token persona); ≤1.2s committed to Phase 6 (ack-masking headline, lighter LLM A/B, optional Flux double-endpointing)
- [Phase 04]: [Phase 04-01]: ENI public-IP lookup keys off the ECS task-metadata MAC address (ec2:DescribeNetworkInterfaces mac-address filter), not an ENI-id field — task metadata v4 doesn't expose the ENI id directly
- [Phase 04]: [Phase 04-01]: Dockerfile resolves the libvpx package name dynamically via apt-cache (libvpx9 on the current Debian trixie base, not the libvpx7 CLAUDE.md documented against an older base); CMD invokes uvicorn directly instead of uv run uvicorn (uv run re-syncs the dev group's pyaudio and breaks the container)
- [Phase 04-02]: Extended the ecs-task module (dedicated per-task IAM role + container systemControls) beyond this plan's declared file scope - no existing mechanism in the codebase could express least-privilege task-role IAM or kernel sysctls; the ecs-cluster module's shared task role is wide-open (dynamodb:*/cloudwatch:*/ssm:*/s3:*/secretsmanager:* on Resource=*)
- [Phase 04-04]: Concurrency-limit enforcement is a consistent Query + conditional write, not a cross-item DynamoDB transaction (the deployed task role's IAM grants no TransactWriteItems) — an atomic per-user counter alternative was rejected because it can't self-heal on a crashed task without a reaper, which would violate D-01's "no reaper" requirement. `release_heartbeat` never calls DeleteItem (not IAM-granted) — it sets `expiresAt` into the past; TTL is the real backstop either way.
- [Phase 04-04]: KNOWN GAP — the deployed voice task role's IAM (`voice/service.hcl`'s `UsageTableCrud` statement) only grants access to `kmv-voice-usage`; `quota.read_tier()` needs `dynamodb:GetItem` on `kmv-auth-electro` (the Phase-3 tiers table) too. A real deployed `/api/offer` call will get `AccessDeniedException` on every non-bypass session today. Add a cross-table read statement to `voice/service.hcl` and re-apply before any live-traffic verification of the quota gate.
- [Phase 05-01]: kmv-latency emission is pushed from the last-observed downstream FrameProcessor (cached in on_push_frame), not a constructor-injected RTVIProcessor reference — keeps the change within observers.py + tests only (no server.py change), since any live processor's push notifies the same pipeline-wide observer set
- [Phase 05-01]: SPA deep-link fallback (client-side callback route) implemented via a global @app.exception_handler(404), not a second catch-all route — a root-mounted StaticFiles("/") prefix-matches every path in Starlette's router, so a later catch-all route would never be reached
- [Phase 05-02]: Inter self-hosted by vendoring the Google-Fonts latin-subset variable woff2 as a static asset (not @fontsource/inter) — UI-SPEC mandates self-hosting, not a package; keeps the npm supply-chain surface minimal (threat T-05-02-SC)
- [Phase 05-02]: Client hero orb is a faithful React port of sketch 001 Variant A (WebGL2 plasma shader + orbiting particle ring); orbState.ts ORB_STATE_VISUALS + smoothAmplitude() EMA is the single state->color/motion source 05-04 live RTVI wiring reads; feature-detection (webgl2 + prefers-reduced-motion) swaps to the calm 2D OrbFallback
- [Phase 05-02]: D-03 multi-stage Dockerfile: node:22-slim client-build stage runs npm ci && npm run build, COPYs dist into /app/client/dist (the 05-01 StaticFiles mount path); client artifacts gitignored+dockerignored (no committed build output). Local build needs node>=22.12 (vite8/rolldown floor) — used node v23.6.0 locally; image node:22-slim is above the floor
- [Phase 05]: [Phase 05-03]: Corrected the 'voice' OIDC client from a stale confidential-client shape (client_secret_post + Auth.js callback URIs) to a public PKCE client (token_endpoint_auth_method: none) with the SPA's own /callback redirect_uri — D-01/D-02 pivoted the voice app away from the Next.js/next-auth assumption this client registration predated
- [Phase 05]: [Phase 05-03]: Baked public VITE_OIDC_* config into the Dockerfile client-build stage as ARG defaults (no secret exists for this PKCE public client) since build-voice.yml passes no --build-arg values
- [Phase Phase 05-04]: Bearer token sent as an Authorization header only (not request_data.access_token) -- small-webrtc-transport 1.10.5 nests requestData back camelCase, not the snake_case request_data server.py's pre-gate token check reads
- [Phase Phase 05-04]: Added a window.fetch interceptor around SmallWebRTCTransport connect() to detect and surface 401/403/429 offer rejections -- the vendor client silently swallows the offer POST's HTTP error and never rejects connect() otherwise
- [Phase ?]: [Phase 05-05]: Extended session_max_seconds through the /api/offer connect flow (server.py answer + voiceSession.ts fetch-interceptor peek) since the JWT only carries tier_id, not the numeric cap -- the client countdown's only viable source per the plan's own key_link
- [Phase 05-06]: at-capacity/unknown quota error_types share one Claude's-discretion "generic provider-error" gate copy -- the UI-SPEC contract has no assigned string for this one transient reject (distinct from SessionEnd's own verbatim "Generic provider-error end" row, a different screen for a session that already connected)
- [Phase 05-06]: A post-connect transport drop routes to a sessionSummary (reason: provider-error), not Task 1's pre-connect retry/wall flow -- wasConnectedRef/connectedAtRef live entirely in useVoiceSession.ts's own state, no change to the shared connectionState.ts reducer
- [Phase ?]: [Phase 05-07]: Countdown announces at escalation boundaries (warning/critical), not every tick -- fixed a real screen-reader-spam bug where the sr-only span was continuously aria-live=polite
- [Phase ?]: [Phase 05-07]: prefers-reduced-motion is now a reactive matchMedia subscription (useReducedMotion), not a one-shot mount check -- OrbCanvas swaps to the calm fallback immediately on a mid-session OS toggle
- [Phase 05.2]: 05.2-01: navigate.ts wraps window.location.assign for spy-friendliness (this vitest/jsdom disallows redefining window.location.assign directly), per the source plan's own documented fallback
- [Phase 05.2]: 05.2-01: installed @testing-library/react + @testing-library/dom as client devDependencies (were entirely absent; required by the plan's own verbatim renderHook/render test code)
- [Phase 05.2]: Guard uses cfg.persona.greet_first (in-scope load_config() result), not built.config.persona.greet_first -- BuiltPipeline has no .config field
- [Phase 05.2-02]: Combined Task 1 (source/script/Makefile) and Task 2 render artifacts into ONE commit per the source plan's own Task 5 Step 5 boundary -- no separate unrendered-files commit exists
- [Phase 05.2-03]: Deferred only the CONNECTED *dispatch* behind greetingEndedRef, NOT the connected bookkeeping (wasConnectedRef/connectedAtRef/reportSuccess/retry-reset stay synchronous) -- preserves 05-06's post-connect-drop-vs-pre-connect-failure classification while still holding the visible Live handoff until the greeting clip ends
- [Phase 05.2-03]: Clip released on THREE paths per the revised (checker-passed) plan -- clip-end, stop(), and the pre-connect TRANSPORT_ERROR branch (~useVoiceSession.ts:139, dispatch+reportFailure) -- explicitly NOT the post-connect wasConnectedRef teardown path above it (out of scope; already runs after clip-end)
- [Phase 05.2 gap-closure]: Fixed the reproduced login_required breadcrumb re-arm bug (criterion 2) by removing App.tsx's unconditional markReturningUser() from handleAuthenticated -- Callback.tsx already marks the breadcrumb itself on the successful code-exchange path, so App no longer needs to (and must not, on the login_required safe-degrade path). TDD-driven: added apps/voice/client/src/App.breadcrumb.test.tsx (real App+Callback wiring, not a stub), proven RED against unfixed code then GREEN after the fix; full client suite 102/102, tsc/build clean. Commit cfc7013.
- [Phase 07]: [Phase 07-01] apply_system_blocks() sets the two-block cached system array directly on the LLM service's Settings.system_instruction, bypassing LLMContext's system-message convention -- pipecat 1.5.0's AnthropicLLMAdapter flattens list-content system messages into a joined string, discarding cache_control markers; Settings.system_instruction is the one path that survives verbatim
- [Phase 07]: [Phase 07-01] Renamed the planned cache_min_tokens config field to cache_floor -- collided with config.py's existing credential-field rejection regex (any _token/_tokens-suffixed field is flagged as credential material)
- [Phase 07]: [Phase 07-01] Live-proven: build_system_blocks()'s block0 (persona + Kurt STYLE layer + topic hooks) measures 5444 tokens and genuinely engages Anthropic prompt caching (cache_creation_input_tokens=5438 turn1, cache_read_input_tokens=5438 turn2) -- ROADMAP success criterion 1 verified against the real API
- [Phase ?]: [Phase 07-02]: retrieval_budget (not retrieval_max_tokens) -- _CREDENTIAL_FIELD_RE rejects any _token(s)-suffixed field name (same class as 07-01's cache_min_tokens -> cache_floor)
- [Phase ?]: [Phase 07-02]: RetrievalIndex constructed from KnowledgeConfig (needs .index_dir), not PipelineConfig -- the plan's literal acceptance-criteria snippets call load_config() into RetrievalIndex(), which would AttributeError
- [Phase 07-03]: Followed 07-01/07-02's real shipped architecture (manifest.yaml owns pack paths, topic-map.yaml is classification-only) over this plan's own stale acceptance-criteria snippets; no Landmines/do-not-say section in either new pack per this plan's explicit prohibition; meshtk's keyword list omits a bare 'toolkit' term (Pitfall 1 guard)
- [Phase 07-04]: generate_docs() defaults to a no-op per DESIGN-NOTES Amendment 5 (grill-with-docs dropped, direct code indexing) -- reverses this plan's own stale Task 2 text
- [Phase 07-04]: iter_source_files uses os.walk + directory pruning (node_modules/vendor/dist/build/target/coverage/dotdirs), not Path.rglob -- an unbounded rglob hung on a real 443,982-file node_modules tree under defcon.run.34/apps
- [Phase 07-04]: Relative manifest source paths resolve against REPO_ROOT (APP_ROOT.parent.parent), not cwd -- manifest.yaml's paths (apps/voice/..., .planning/...) are repo-root relative, confirmed live when the wrong base silently reported every real in-repo source as not-found
- [Phase 07-05]: SessionLifecycle.remaining_seconds() added to session.py (Rule 2 auto-add) -- a pure __post_init__-stamped elapsed-time read, no new timer -- since the plan's own read_first note required sourcing remaining_seconds from session.py's existing state but no public accessor existed
- [Phase 07-05]: remaining_seconds_fn/SessionLifecycle.remaining_seconds() are not wired into pipeline.py/server.py production code -- both files were outside this plan's declared scope; the seam defaults to None (Plan 01/02 behavior unchanged) pending a follow-up wiring task
- [Phase 11]: Phase 11-01: widened _CREDENTIAL_FIELD_RE credential-stem "words" to an exact-match alternative (not the shared compound-boundary group) after it false-positive-rejected the existing [duplex] backchannel_words/max_backchannel_words fields; passphrase_words is still independently caught via the passphrase stem
- [Phase 11]: Phase 11-02: ARI/HTTP bindaddr kept at literal 127.0.0.1 (loopback) matching D-01's private-only truth, not the compose-network 0.0.0.0 the research skeleton also offered -- a host-run controller can't yet reach ARI through the published 8088:8088 port (Docker port-publish forwards to a container's routable interface, not its own loopback); flagged in README for a later plan (D-08's controller) to resolve, not required by this plan's own verification
- [Phase 11]: Phase 11-02: ari.conf/pjsip.conf ${VAR} placeholders are documentation-only (Asterisk's .conf parser doesn't shell-substitute) -- README documents a manual local-substitution workaround rather than building an envsubst/entrypoint rendering pipeline, since no acceptance criterion required a live authenticated ARI session in this plan
- [Phase 11]: Phase 11-03: read_packet() after close() returns None via two complementary mechanisms (explicit _closed flag + connection_lost's queued None sentinel) -- the flag handles post-close calls, the sentinel unblocks a reader already awaiting the queue concurrently
- [Phase 11]: Phase 11-04: session-level aiohttp.ClientSession(auth=BasicAuth(...)) covers both REST and events-WebSocket auth automatically (ws_connect falls back to ClientSession._default_auth) -- no second explicit auth= kwarg on ws_connect, which is now deprecated in aiohttp 3.14.1
- [Phase 11]: Phase 11-04: DTMF accumulation deliberately left out of AriClient (Landmine 5) -- run() dispatches one ChannelDtmfReceived event per digit; the Plan-05 controller owns accumulating digits across its gate_window_seconds window
- [Phase ?]: Phase 11-05: _close_active_call/_teardown_gate_resources wrap every ARI teardown call in _safe_ari (swallow AriError) so an already-gone Asterisk-side resource never aborts the rest of a teardown sequence (T-11-05-01)
- [Phase ?]: Phase 11-05: quota.start_gate is called directly inside on_stasis_start at an interim placement (granting telephony_cfg.unlock_tier_id) -- Plan 06 moves the real grant to the §24 unlock boundary without changing the teardown contract
- [Phase ?]: Phase 11-05: rtp_bind_host/rtp_advertise_host/app_name/expected_context are AsteriskCallController constructor kwargs, not TelephonyConfig fields -- network/process plumbing stays config-driven at the Plan 07 entrypoint layer, not the [telephony] behavior-only TOML table
- [Phase ?]: Phase 11-06: bypass-placeholder GateResult + SessionLifecycle.upgrade_from_bypass() (Rule 2 auto-add to session.py) lets create_call_session build the persistent gated pipeline once, up front, with zero real accounting until quota.start_gate grants a tier at the real §24 unlock moment
- [Phase ?]: Phase 11-06: telephony_cfg.require_gate branches on_stasis_start into gated (production default, new §24 flow) vs ungated (Plan 05's interim immediate-grant, test/dev-only escape hatch)
- [Phase 12]: Phase 12: all six VoIP.ms method-name constants marked UNVERIFIED in voipms.go -- no web-fetch tool was available to confirm them against live VoIP.ms API docs; flagged as required human follow-up before running kv voipms against a real account
- [Phase 12-02]: byPhone GSI reuses gsi3pk-gsi3sk-index (already defined in the electro dynamodb table); phoneEnabled boolean gates resolvePhoneToCode, mirroring bypassEnabled; the /tel route's shared-bearer mismatch returns the identical notFound() as every other failure, never a distinct 401/403

### Pending Todos

- [captured 2026-07-05] Post-conference: swap KPH to an ElevenLabs voice clone trained on Kurt (IVC now / PVC better; Pro plan covers both). Architecture-ready: one-line voice_id change in pipeline.toml [tts]; verify eleven_flash_v2_5 is supported by the clone; re-run the 01-05 audition renderer to A/B clone vs incumbent by ear before switching.
- [captured 2026-07-05, user at Phase-1 sign-off] KPH knowledge base: "massive RAG or something really smart that can steer... all of the knowledge of my repos, and some scripts and stuff I'd train it on." Recommended shape for the voice-latency constraint (two-tier): (1) curated repo/project digests as a large cached system-prompt knowledge pack — once ≥4096 tokens, Haiku prompt caching ENGAGES (0.1× cost, fast cached prefill), turning the 01-04 caching dead-end into a win at knowledge-pack scale; pre-warm at session start; (2) a retrieval tool for depth questions, latency masked by Phase-6 ack-masking (natural synergy). Fine-tuning is the wrong tool; curated context + retrieval is. Candidate Phase 7 or fold into Phase 6 planning discussion.

### Blockers/Concerns

- [Phase 2]: SES production access is a multi-day manual review — request must go out at Phase 2 start (week one)
- [Phase 3]: oidc-provider v9 JWT access tokens with tier claims (Resource Indicators) not yet prototyped — spike early; it is the contract Phase 4 blocks on
- [Phase 4]: STUN srflx behind Fargate 1:1 NAT is source-verified but not live-tested — deployed ICE smoke test is the first Phase 4 deliverable
- [Phase 4]: Confirm the ElevenLabs API key SOPS entry is populated before the voice deploy (flagged by 02-07)
- [Phase 4]: Re-measure deployed voice-to-voice p50/p95 against the 1402ms local baseline as part of the ICE smoke test — expectation (user + analysis 2026-07-05): us-east-1 proximity to Deepgram/Anthropic/ElevenLabs endpoints + fresh-session context (~600 vs ~3000 tokens) should improve on local numbers; new browser↔task WebRTC leg adds ~20-50ms each way
- [Phase 4]: REQUIREMENTS.md's INFR-03 checkbox was auto-marked `[x]` after 04-01 (per the plan's own `requirements:` frontmatter and the standard per-plan mark-complete step) — but INFR-03's text explicitly requires "verified by a deployed ICE smoke test," which is 04-03's job. 04-01 only delivers the code half (auth + candidate gathering + entrypoint, all unit-tested against synthetic fixtures, no live Fargate task yet). Treat INFR-03 as genuinely done only once 04-03's deployed smoke test passes, not from this checkbox alone.
- [Phase 4-02]: AWS SSO session (Developer, profiles klanker-terraform/klanker-application/klanker-management) expired (InvalidGrantException on refresh) - could not run terragrunt run-all validate/plan against live state for the voice deploy infra. Module-level terraform validate -backend=false passed for all touched modules as a substitute. Before 04-03 applies: run 'aws sso login --profile klanker-terraform' interactively, then re-run terragrunt run --all validate + a targeted terragrunt plan on the ecs-task/ecs-service units.
- [Phase 4-04]: BLOCKER for live quota enforcement — the deployed voice task role's IAM does not grant `dynamodb:GetItem` on `kmv-auth-electro` (Phase-3 tiers table); `quota.read_tier()` will get `AccessDeniedException` in production until `voice/service.hcl`'s task role gets a cross-table read statement added and re-applied. All 04-04 tests pass locally against dynamodb-local, where this constraint doesn't exist — this is a real-deployment-only gap. Fix before relying on the quota gate against live traffic.
- [Phase 5-03]: REQUIREMENTS.md's CLNT-08 checkbox is auto-marked [x] per the standard per-plan step, but the plan's own checkpoint (live PKCE sign-in round-trip against deployed auth.klankermaker.ai + a deployed client, no-access gate, refresh re-auth) has NOT been exercised — deferred by explicit orchestrator instruction to the post-05-04 deployed-AWS validation pass. Treat CLNT-08 as genuinely done only once that live check passes, not from this checkbox alone (same pattern as Phase 4's INFR-03). Also: apps/auth must be redeployed alongside voice for the corrected 'voice' OIDC client (public PKCE, /callback redirect_uri) to take effect against the live auth.klankermaker.ai.
- [Phase 5-04]: REQUIREMENTS.md's CLNT-01/CLNT-03/CLNT-04 checkboxes are auto-marked [x] per the standard per-plan step, but the plan's own checkpoint (live end-to-end conversation: reactive orb, dual captions, distinct mic errors against the deployed stack) has NOT been exercised -- deferred by explicit orchestrator instruction to the post-05-04 deployed-AWS validation pass, which is also blocked on the still-open Phase-4 IAM gap (voice task role lacks cross-table read on kmv-auth-electro). Treat these as genuinely done only once that live check passes (same pattern as Phase 4's INFR-03 and Phase 5-03's CLNT-08). CLNT-02 intentionally left Pending -- this plan only delivers its happy path; the requirement's own text bundles auto-retry/UDP-blocked messaging, explicitly 05-06's job.
- [Phase 5-06]: REQUIREMENTS.md's CLNT-02/CLNT-07 checkboxes are auto-marked [x] per the standard per-plan step, but the plan's own checkpoint (a real hostile/UDP-blocked network showing the retry->wall sequence; kv killswitch + two concurrent sessions + exhausted daily minutes showing the three typed gate copies against real quota state; a real session ending via the server's timer/goodbye then Reconnect re-checking quota) has NOT been exercised -- deferred by explicit orchestrator instruction to the post-deploy validation pass, which is also blocked on the still-open Phase-4 IAM gap (voice task role lacks cross-table read on kmv-auth-electro). Treat these as genuinely done only once that live check passes (same pattern as Phase 4's INFR-03 and every prior 05-0x deferred checkpoint).
- [Phase 5-07]: PHASE 5 IS NOW CODE-COMPLETE (7/7 plans) but its final plan's checkpoint -- a LIVE real iPhone (Safari, normal Wi-Fi/cellular AND a restricted/UDP-blocked conference network) exercising the full attract->sign-in->mic->live-conversation->countdown->session-end flow, distinct mic-error states, the honest UDP-blocked wall + hotspot recovery, no-access/killswitch gates, and iOS Reduce Motion + VoiceOver -- is the phase's headline "verified on real phones and hostile networks" requirement (success criteria 1-5) and has NOT been exercised. This is gate="blocking" and was intentionally NOT self-approved. It is the single consolidated live-verification pass every 05-0x plan's own checkpoint has been deferring to (05-03's PKCE round-trip, 05-04's live conversation, 05-05's countdown/HUD, 05-06's hostile-network/gate-copy/reconnect checks all fold into this one pass) and remains additionally blocked on the still-open Phase-4 IAM gap (voice task role lacks cross-table read on kmv-auth-electro). Treat Phase 5 as genuinely DONE only once this pass completes, not from any plan's auto-marked REQUIREMENTS.md checkbox alone.
- [Phase 07-01] kph_knowledge_km.yaml/kph_cache_verify.yaml full live-audio eval run (pipecat eval run against bot.py -t eval, real router+STT+TTS frame flow) NOT exercised -- this venv's kokoro/moonshine local eval deps (pipecat-ai[evals,local]) are not installed. Package installs are excluded from auto-fix (Rule 3); requires a human-run 'uv sync --group dev' (or equivalent) then a live run before treating 07-01's km-eval + cache-verify scenarios as genuinely verified end-to-end (mechanism-level cache proof already done directly against build_system_blocks() with real API calls, see 07-01-SUMMARY.md).
- [Phase 07-05] The full live-audio benchmark run (all kph_*.yaml scenarios + memory.yaml/kph_cache_verify.yaml via pipecat eval run against a real bot.py -t eval) has NOT been exercised -- same pipecat-ai[evals,local] (kokoro/moonshine) blocker as every prior 07-0x plan. Needed to record the pass-rate + router-accuracy numbers this plan's phase-gate output requires; requires a human uv sync --group dev, not self-approved.
- [Phase 11] HUMAN-NEEDED, outstanding: §19-C real-softphone exit-criterion proof (Phase 11's ONLY remaining item -- all 7/7 plans otherwise code-complete). Requires a human with a working Docker daemon to run `docker compose up` (this execution sandbox's Docker daemon is not running -- `docker info` fails), start the standalone telephony controller with real ARI/PIN/passphrase env values, register a real SIP softphone, and confirm: silent answer -> passphrase/PIN unlock -> greeting-not-clipped -> converse -> interrupt(barge-in) -> clean hangup, plus the fail-closed goodbye path and no resource leaks. The human explicitly decided to DEFER this live run rather than have it fabricated. Full 8-step recipe (incl. the two documented workarounds for the 11-02-flagged ARI-loopback-vs-published-port prerequisite) is in `apps/voice/asterisk/README.md` -> "Manual §19-C softphone proof". Treat Phase 11 as genuinely, perceptually verified end-to-end only once this item is run (same deferred-live-verification pattern as Phase 5's consolidated iPhone pass, Phase 05.2's, and Phase 7's live-audio benchmark run). 11-07-SUMMARY.md's coverage entry D3 marks this `human_judgment: true` with rationale, not auto-passed.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260706-pfo | Fix kmv-github-deploy role IAM permissions in site.hcl so CI deploy can terragrunt apply + tag ECS resources | 2026-07-06 | b78074f | [260706-pfo-fix-kmv-github-deploy-role-iam-permissio](./quick/260706-pfo-fix-kmv-github-deploy-role-iam-permissio/) |
| 260706-q42 | Fix reusable-workflow concurrency deadlock in deploy.yml so build-*.yml can chain to the deploy job | 2026-07-06 | 47400f6 | [260706-q42-fix-reusable-workflow-concurrency-deadlo](./quick/260706-q42-fix-reusable-workflow-concurrency-deadlo/) |
| 260709-aah | Voice client UX hardening: robust gesture-primed greeting playback (root-caused invisible first-load autoplay silence) + move End chat button off the latency toggle | 2026-07-09 | bb5eb20 | [260709-aah-voice-client-ux-hardening-robust-gesture](./quick/260709-aah-voice-client-ux-hardening-robust-gesture/) |
| 260710-ixf | Per-variant label (KPH v1/v2) from TOML shown subtly in UI + fix first-turn instruction leak (drop NO_REGREET context seed, harden persona) + make voice2 the default variant | 2026-07-10 | 3863552 | [260710-ixf-voice-variant-label-in-ui-kph-v1-v2-from](./quick/260710-ixf-voice-variant-label-in-ui-kph-v1-v2-from/) |
| 260712-ckd | Make the Phase 11 §19-C local softphone proof runnable as a one-command harness (shared-netns klanker-telephony controller service + gitignored conf-secret render; removed dead 8088 host publish — tightens §18/§25 private-only ARI) | 2026-07-12 | a1c8750 | [260712-ckd-make-the-phase-11-19-c-local-softphone-p](./quick/260712-ckd-make-the-phase-11-19-c-local-softphone-p/) |

### Roadmap Evolution

- Phase 05.1 inserted after Phase 5: Operator Admin Panel — gated /admin in auth app (operational-only; transcripts deferred to Phase 7) (URGENT)
- Phase 8 added: Documentation & Architecture — polished system diagram + README/architecture/runbook (starter Excalidraw diagram exported 2026-07-06)
- Phase 05.2 inserted after Phase 5: Slick Start — single-tap silent SSO + instant pre-rendered greeting (conference-readiness UX polish on Phase-5 client) (URGENT)

## Deferred Items

Items acknowledged and carried forward from previous milestone close:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| *(none)* | | | |

## Session Continuity

**Resume file:** .planning/phases/12-voip-ms-telephony-inbound-did/12-CONTEXT.md

Last session: 2026-07-12T19:22:20.536Z
Stopped at: Phase 12 context gathered
Resume next: Run the §19-C live-softphone proof (`apps/voice/asterisk/README.md` -> "Manual §19-C softphone proof", 8-step recipe) against a real Docker daemon + SIP softphone to close out Phase 11's final verification item, then advance to whichever phase follows in ROADMAP.md (Phase 12+ per the VoIP.ms telephony spec's PSTN/public-DID phases, per the voipms-telephony-integration memory note). Also still outstanding, unrelated to Phase 11: Phase 7 (KPH Knowledge Base) is CODE-COMPLETE, 5/5 plans (07-01 km walking-slice curated pack + router + caching; 07-02 local BM25/FTS5 retrieval wired into the deep turn, real km depth proven; 07-03 full primary topic set — defcon.run.34 + meshtk curated packs authored, topic-map/manifest promoted, three-way router discrimination + Pitfall-1 toolkit-overlap guard proven; 07-04 offline knowledge refresh — `refresh_knowledge.py` regenerates BOTH curated packs and per-topic FTS5 chunk/index files from the manifest, `public:true` D-02 gate added to manifest.yaml, doc-gen seam defaults to Amendment 5's direct-code-indexing no-op (grill-with-docs dropped), advisory-lint flags-never-blocks, exposed via `make -C apps/voice knowledge` + `kv knowledge refresh`; 07-05 production polish — concierge.md persona v4 (adaptive steering/tour-mode, D-12 honest-unknowns, spoken do-not-say boundary, restated PG-13 guardrail), `build_system_blocks`'s `remaining_seconds` seam now actually renders a tight-vs-depth pacing note into block1 only (block0 byte-identical), `KnowledgeRouterProcessor.remaining_seconds_fn` + new `SessionLifecycle.remaining_seconds()` thread it through with no second timer, and 5 new benchmark scenarios — `kph_unknowns`/`kph_tour_mode`/`kph_crude_humor_guard`/`kph_retrieval_depth`/`kph_router_accuracy` — complete ROADMAP criterion 4's eval set). Full suite 255/255 pass. Next step is the phase's own consolidated live-audio verification pass (`/gsd-verify-work` or a manual `pipecat eval run` against `bot.py -t eval` with `pipecat-ai[evals,local]` installed), then advancing to the next phase per ROADMAP (Phase 6 Latency v2 is scoped-but-deferred; Phase 8 Documentation & Architecture is queued). Deferred, not blocking: the live-audio eval runs for ALL of 07-01..05's scenarios still need `pipecat-ai[evals,local]` (kokoro/moonshine) installed in this venv — flagged for a human `uv sync --group dev`, not self-approved; 07-03 additionally has a deferred cross-topic (km→defcon) live cache-warmth proof (mechanism-level same-topic proof already exists from 07-01); 07-04 additionally has a deferred real, billed `make -C apps/voice knowledge` run (full local checkouts + a live Anthropic distillation call, reviewed as a D-09 git diff) — deliberately not run during automated execution; 07-05 additionally leaves `remaining_seconds_fn`/`SessionLifecycle.remaining_seconds()` unwired in production (pipeline.py/server.py were out of its declared scope) as a small, non-blocking follow-up. Also unrelated/still open: Phase 05.2's consolidated LIVE iPhone/Safari verification pass (single-tap+instant-greeting+no-double-greet; signed-out two-step; barge-in + no greeting/STT overlap) remains blocked on the still-open Phase-4 IAM gap (voice task role lacks cross-table read on kmv-auth-electro) and has not been executed.

---
Prior resume note (Phase 05): Phase 05 is now CODE-COMPLETE (7/7 plans). The only remaining phase-5 work is the single consolidated LIVE verification pass every 05-0x plan's checkpoint has deferred to: a real iPhone (Safari, normal Wi-Fi/cellular AND a restricted/UDP-blocked conference network) exercising the full attract->sign-in->mic->live-conversation->countdown->session-end flow, mic-error states, the UDP-blocked wall + hotspot recovery, no-access/killswitch gates, and iOS Reduce Motion + VoiceOver -- gate="blocking", NOT self-approved. It remains additionally blocked on the still-open Phase-4 IAM gap (voice task role lacks cross-table read on kmv-auth-electro). Next step is the orchestrator's phase-level verification/sign-off, not another plan.
Also this session: 05-07 executed (a11y/liveRegions.ts shared announcer + reactive useReducedMotion hook + a11y.test.ts 6 tests; new responsive.css; tokens.css --sz-display mobile clamp; fixed two real bugs -- Countdown's per-second aria-live spam and Callback's total lack of any live region; Esc-dismiss on GateCard/MicError via new useVoiceSession.dismissMicError -- 1 task, 10 files, 85/85 total client-side tests pass, tsc/build clean). Checkpoint (real iPhone + restricted network, VoiceOver, Reduce Motion) deferred to post-deploy validation per orchestrator guidance, not self-approved -- this IS the consolidated pass every prior 05-0x checkpoint (05-03 PKCE round-trip, 05-04 live conversation, 05-05 countdown/HUD, 05-06 hostile-network/gate-copy/reconnect) has been folding into, additionally blocked on the still-open Phase-4 IAM gap (voice task role lacks cross-table read on kmv-auth-electro).
Note: git guard is a harmless `rm -f` wrapper in ~/.zshrc (footgun-prevention) — avoid `rm -f`/`-r` in non-interactive shells (use plain `rm`), git itself is fine.
