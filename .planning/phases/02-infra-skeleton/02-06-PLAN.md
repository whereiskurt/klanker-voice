---
phase: 02-infra-skeleton
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - infra/.envrc
  - infra/terraform/live/site/global/github-oidc/ (state)
  - .planning/phases/02-infra-skeleton/02-DELEGATE-TRUST.json
autonomous: false
requirements: [INFR-07]
user_setup:
  - service: aws-management-account (481723467561)
    why: "Create the kmv-github-delegate IAM role — no in-scope profile has IAM rights in the management account (klanker-management is HostedZoneAdmin only)"
    dashboard_config:
      - task: "Create IAM role kmv-github-delegate with the trust policy JSON from 02-DELEGATE-TRUST.json (external_id kmv) and Route53 change/list permissions scoped to zone Z036807010CWM2JH60RKQ"
        location: "AWS Console -> 481723467561 -> IAM -> Roles -> Create role (custom trust policy)"
must_haves:
  truths:
    - "IAM OIDC provider + roles kmv-github-{terragrunt,readonly,deploy,release} exist in 052251888500, scoped to whereiskurt/klanker-voice, with environment/branch restrictions intact (D-09, INFR-07)"
    - "github-oidc applied AFTER the SOPS KMS key existed — the kms-sops-decrypt policies carry the real key ARN, not the placeholder (research Pitfall 5 ordering honored)"
    - "GitHub repo variables (SITE_LABEL, SGUID, AWS_ACCOUNT_ID, TF_VAR_MANAGEMENT_ACCOUNT_ID, TF_VAR_SOPS_KMS_KEY_ID, TF_VAR_SSM_KMS_KEY_ARNS) and environments terraform-plan / terraform-apply (with required reviewers) are configured (D-08 gate)"
    - "The kmv-github-delegate trust policy is delivered to the user and the manual mgmt-account creation is checkpointed (research Open Question 2)"
  artifacts:
    - path: "terraform state: global/github-oidc"
      provides: "OIDC provider + four kmv-github-* roles + managed policies"
    - path: ".planning/phases/02-infra-skeleton/02-DELEGATE-TRUST.json"
      provides: "management_account_trust_policy module output for the manual delegate-role creation"
    - path: "infra/.envrc (TF_VAR_SSM_KMS_KEY_ARNS filled)"
      provides: "CMK ARN list so CI readonly plans can kms:Decrypt (research SOPS step 6)"
  key_links:
    - from: "kmv-github-* role trust policies"
      to: "github.com/whereiskurt/klanker-voice"
      via: "token.actions.githubusercontent.com sub claims + environment/branch restrictions"
      pattern: "token.actions.githubusercontent.com"
    - from: "kmv-github-{terragrunt,readonly} roles"
      to: "arn:aws:iam::481723467561:role/kmv-github-delegate"
      via: "cross_account_arns assume-role with external_id kmv (role created manually by user)"
      pattern: "kmv-github-delegate"
---

## Phase Goal

**As a** klanker-voice developer, **I want to** provision the complete kmv AWS foundation (DNS/TLS, data stores, secrets flow, container plumbing, and an OIDC-gated CI deploy path) as applied, verifiable cloud state, **so that** the auth and voice services in Phases 3–5 deploy onto proven infrastructure with no plaintext secrets and no long-lived keys.

<objective>
Apply the github-oidc unit (D-09) — deliberately last in the apply order so the SOPS KMS key ARN (Plan 03) and the module CMKs (Plan 05) are real before the role policies bake them in (research Pitfall 5). Configure the GitHub side (repo variables, gated environments per D-08) and hand the user the exact trust policy for the one action Claude cannot perform: creating kmv-github-delegate in the management account.

Purpose: INFR-07 — CI deploys via OIDC roles, zero long-lived AWS keys.
Output: Applied OIDC provider + roles, configured repo, delegate trust policy delivered, manual role creation checkpointed.
</objective>

## Artifacts this plan produces

- AWS (052251888500): IAM OIDC provider + roles `kmv-github-terragrunt`, `kmv-github-readonly`, `kmv-github-deploy`, `kmv-github-release` (+ policies)
- `.planning/phases/02-infra-skeleton/02-DELEGATE-TRUST.json` — trust policy for the manual role
- GitHub: repo variables SITE_LABEL / SGUID / AWS_ACCOUNT_ID / TF_VAR_MANAGEMENT_ACCOUNT_ID / TF_VAR_SOPS_KMS_KEY_ID / TF_VAR_SSM_KMS_KEY_ARNS; environments `terraform-plan`, `terraform-apply` (required reviewers)
- Config key: `TF_VAR_SSM_KMS_KEY_ARNS` filled in infra/.envrc
- AWS (481723467561, BY USER): IAM role `kmv-github-delegate`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-infra-skeleton/02-CONTEXT.md
@.planning/phases/02-infra-skeleton/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply github-oidc (post-SOPS-key), persist CMK ARNs, configure repo variables + gated environments (D-08, D-09)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "GitHub OIDC (INFR-07)" section (role list, restrictions, kmv edits, GitHub-side list) and "SOPS Setup" step 6 (TF_VAR_SSM_KMS_KEY_ARNS discovery rationale)
    - .planning/phases/02-infra-skeleton/02-03-SUMMARY.md — the SOPS KMS key id (precondition evidence)
  </read_first>
  <files>infra/.envrc, .planning/phases/02-infra-skeleton/02-DELEGATE-TRUST.json</files>
  <action>
    Precondition gate: assert TF_VAR_SOPS_KMS_KEY_ID is exported and matches a real key (`aws kms describe-key --key-id "$TF_VAR_SOPS_KMS_KEY_ID"` succeeds) — hard stop otherwise (Pitfall 5). Load infra/.envrc; SSO preflight with dynamic auth checkpoint.

    First, CMK discovery (research SOPS step 6): list KMS aliases in us-east-1 via the application profile, collect the ARNs for the module-created CMKs (aliases created by the secrets/dynamodb/email modules in Plan 05 — kmv-prefixed alias names), and set TF_VAR_SSM_KMS_KEY_ARNS in infra/.envrc to the comma-joined list. Without this, CI readonly plans die on kms:Decrypt.

    From infra/terraform/live/site/global/github-oidc: terragrunt plan, review (expect the OIDC provider, four roles named kmv-github-terragrunt/readonly/deploy/release per the Plan 02 site.hcl pruning, managed policies including kms-sops-decrypt referencing the REAL key ARN — verify in the plan output, environment restriction terraform-apply on the terragrunt role, branch restriction main on deploy), then apply.

    Capture the module's management_account_trust_policy output (`terragrunt output -json`) and write it to .planning/phases/02-infra-skeleton/02-DELEGATE-TRUST.json — this is the artifact Task 2 hands the user.

    GitHub side (gh CLI): `gh variable set` for SITE_LABEL=kmv, SGUID=<Plan 01 value>, AWS_ACCOUNT_ID=052251888500, TF_VAR_MANAGEMENT_ACCOUNT_ID=481723467561, TF_VAR_SOPS_KMS_KEY_ID, TF_VAR_SSM_KMS_KEY_ARNS. Create environments via gh api: terraform-plan (no gate) and terraform-apply with required reviewer whereiskurt (this environment gate IS the human apply approval D-08 demands). Commit the .envrc change and the trust-policy artifact.
  </action>
  <verify>
    <automated>[ "$(aws iam list-roles --profile klanker-application --query "Roles[?starts_with(RoleName,'kmv-github-')].RoleName" --output text | wc -w)" -ge 4 ] && test -s .planning/phases/02-infra-skeleton/02-DELEGATE-TRUST.json && python3 -c "import json;json.load(open('.planning/phases/02-infra-skeleton/02-DELEGATE-TRUST.json'))" && gh variable list --repo whereiskurt/klanker-voice | grep -q SGUID && gh api repos/whereiskurt/klanker-voice/environments/terraform-apply --jq '.protection_rules | length' | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Four kmv-github-* roles exist; kms-sops-decrypt policies reference the real SOPS key ARN (checked in plan output — no placeholder mrk-000 pattern)
    - 02-DELEGATE-TRUST.json is valid JSON containing the trust policy with external_id condition
    - All six repo variables set; terraform-apply environment has a required-reviewer protection rule (D-08 human gate)
    - TF_VAR_SSM_KMS_KEY_ARNS non-empty in .envrc and mirrored as a repo variable
  </acceptance_criteria>
  <done>CI identity exists with least-privilege role split and gated apply; only the cross-account delegate remains, which is structurally a user action.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: USER — create kmv-github-delegate in the management account (research Open Question 2)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "Cross-Account DNS & Certs" CI cross-account paragraph and Pitfall 8
  </read_first>
  <what-built>Plan 06 Task 1 applied the app-account OIDC roles and wrote the exact trust policy the delegate role needs to .planning/phases/02-infra-skeleton/02-DELEGATE-TRUST.json. Claude cannot create this role: no in-scope profile has IAM permissions in 481723467561 (klanker-management is HostedZoneAdmin only). Until it exists, CI plans that touch management-provider units (site, certs, email, dmarc) fail with AccessDenied — local applies are unaffected and all Phase 2 applies are already done.</what-built>
  <action>Present the user with the exact console steps and the trust-policy JSON, then pause. This is a genuine human-only action (management-account admin credentials).</action>
  <how-to-verify>
    1. Sign in to AWS account 481723467561 with admin access (console or an admin CLI profile Claude does not have)
    2. IAM -> Roles -> Create role -> Custom trust policy -> paste the contents of .planning/phases/02-infra-skeleton/02-DELEGATE-TRUST.json (it requires external_id "kmv" from the app-account roles)
    3. Role name: kmv-github-delegate (exact — the generated CI provider assumes this ARN)
    4. Attach an inline policy allowing route53:ChangeResourceRecordSets, route53:ListResourceRecordSets, route53:GetHostedZone on zone Z036807010CWM2JH60RKQ plus route53:ListHostedZones on *
    5. Create the role and confirm the ARN reads arn:aws:iam::481723467561:role/kmv-github-delegate
  </how-to-verify>
  <acceptance_criteria>
    - Role kmv-github-delegate exists in 481723467561 with the module-output trust policy (external_id kmv) and Route53-scoped permissions only
    - User confirms the ARN, or explicitly defers (deferral is acceptable: CI mgmt-provider plans stay red per Pitfall 8; note it in the SUMMARY as an open follow-up — Plan 07 tolerates this state)
  </acceptance_criteria>
  <resume-signal>Type "created" (optionally paste the role ARN), or "defer" to continue with CI DNS plans expected red</resume-signal>
  <done>The cross-account CI path is either live or consciously deferred with its blast radius documented (local-only infra applies, which D-08 gates anyway).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions (public repo) → AWS IAM | OIDC federation; fork PRs and unprotected branches must not reach write roles |
| app-account CI roles → mgmt-account delegate | cross-account assume-role for Route53 only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-17 | Elevation of Privilege | fork PR assuming write-capable OIDC role | high | mitigate | readonly role is the only PR-reachable role; terragrunt role restricted to environment terraform-apply with required reviewers; deploy/release branch-restricted to main (module restrictions verified in plan output) |
| T-2-18 | Spoofing | confused-deputy on the cross-account delegate | high | mitigate | Trust policy requires external_id kmv (module output; providers send it in assume_role) — user instructed to paste the exact JSON |
| T-2-19 | Elevation of Privilege | overbroad terragrunt CI role (inherited kms:*/iam:* breadth) | medium | accept | Known dc34-inherited tradeoff per research Security Domain; only reachable through the human-gated terraform-apply environment; D-02 says prune later, not during import — documented |
| T-2-20 | Tampering | github-oidc applied with placeholder KMS id causing IAM policy drift | medium | mitigate | Task 1 hard precondition gate on TF_VAR_SOPS_KMS_KEY_ID resolving via kms describe-key |
</threat_model>

<verification>
- INFR-07 structural half: roles + provider + gated environments live; no long-lived AWS keys anywhere (grep CI vars: only role ARNs and non-secret ids)
- Ordering constraint honored (SOPS key before oidc apply)
- Schema push gate: not applicable — terraform-only phase, no ORM/schema files
</verification>

<success_criteria>
- github-oidc applied clean; repo variables + environments configured; delegate role created or consciously deferred with documented consequence
</success_criteria>

<output>
Create `.planning/phases/02-infra-skeleton/02-06-SUMMARY.md` when done. MUST record: role ARNs, environment gate configuration, delegate-role status (created/deferred), TF_VAR_SSM_KMS_KEY_ARNS value.
</output>
