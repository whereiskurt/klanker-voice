---
phase: 02-infra-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - infra/terraform/providers/global.hcl
  - infra/terraform/providers/regional.hcl
  - infra/terraform/modules/ (11 modules, config.hcl + v1.0.0 each)
  - infra/terraform/live/site/terragrunt.hcl
  - infra/terraform/live/site/site.hcl
  - infra/terraform/live/site/SECRETS.md
  - infra/terraform/live/site/.secrets.sops.json.template
  - infra/terraform/live/site/global/github-oidc/terragrunt.hcl
  - infra/terraform/live/site/global/waf/waf.hcl
  - infra/terraform/live/site/services/auth/service.hcl
  - infra/terraform/live/site/services/voice/service.hcl
  - infra/terraform/live/site/region/skip.hcl
  - infra/terraform/live/site/region/us-east-1/ (all unit terragrunt.hcl + *.hcl wrappers)
  - infra/terraform/live/site/region/us-east-1/dmarc/main.tf
  - infra/terraform/live/site/region/us-east-1/dmarc/terragrunt.hcl
autonomous: true
requirements: [INFR-01]
must_haves:
  truths:
    - "The full terragrunt tree exists under infra/terraform/ mirroring the source layout: providers, 11 verbatim modules (D-02, D-04), live/site with kmv-rewritten site.hcl (D-01, D-03)"
    - "terragrunt run --all validate and run --all plan succeed from live/site with the excluded units (ecs-task/ecs-service, skipped regions) cleanly excluded"
    - "Forbidden-string greps return zero across infra/: no kmk, no voiceai, no source-site labels in .hcl files (D-01, project naming constraint)"
    - "global/waf/waf.hcl exists as data (research trap 3) and the plaintext secrets fallback is gitignored before it exists"
  artifacts:
    - path: "infra/terraform/live/site/site.hcl"
      provides: "kmv site definition — label kmv, zone klankermaker.ai, subdomains [auth, voice], secrets definitions for deepgram/anthropic/elevenlabs/jwt/oidc/altcha, ecs_tasks+ecs_services disabled"
    - path: "infra/terraform/modules/"
      provides: "site, certs, network (+ new webrtc_udp SG), ecs-cluster, ecr, dynamodb, secrets, email, github-oidc, ecs-task, ecs-service — config.hcl + v1.0.0 pattern preserved"
    - path: "infra/terraform/live/site/services/voice/service.hcl"
      provides: "voice service data stub: voice-app ECR repo, assign_public_ip=true groundwork, no file() reads (research Pitfall 7)"
    - path: "infra/terraform/live/site/region/us-east-1/dmarc/main.tf"
      provides: "apex _dmarc.klankermaker.ai TXT p=quarantine inline unit (D-11, research SES route 2)"
  key_links:
    - from: "infra/terraform/live/site/terragrunt.hcl"
      to: "AGENTS.md"
      via: "find_in_parent_folders repo-root anchor (created in Plan 01)"
      pattern: "AGENTS.md"
    - from: "infra/terraform/live/site/site.hcl"
      to: "infra/terraform/live/site/services/*/service.hcl"
      via: "read_terragrunt_config aggregation into dynamodb/ecr/ecs_tasks/ecs_services blocks"
      pattern: "read_terragrunt_config"
    - from: "infra/terraform/live/site/site.hcl"
      to: "infra/terraform/live/site/.secrets.sops.json"
      via: "run_cmd sops --decrypt at parse time (plaintext .secrets.json fallback until Plan 03)"
      pattern: "sops"
---

<!-- planner-discipline-allow: dc34 -->
<!-- planner-discipline-allow: defcon -->
<!-- planner-discipline-allow: file( -->

## Phase Goal

**As a** klanker-voice developer, **I want to** provision the complete kmv AWS foundation (DNS/TLS, data stores, secrets flow, container plumbing, and an OIDC-gated CI deploy path) as applied, verifiable cloud state, **so that** the auth and voice services in Phases 3–5 deploy onto proven infrastructure with no plaintext secrets and no long-lived keys.

<objective>
Clone the proven source tree at /Users/khundeck/working/defcon.run.34/infra/terraform/ into infra/terraform/ exactly as the research Clone Inventory prescribes: providers and 11 modules verbatim (D-02, D-04, multi-region machinery kept per D-03), the live tree with the site.hcl delta table applied (D-01 naming), new auth/voice service.hcl stubs, the apex-DMARC inline unit (D-11), and the WebRTC UDP security group groundwork. Ends with the whole tree validating and planning clean against the Plan 01 backend — a verifiable slice even before any apply.

Purpose: INFR-01's substrate. Every apply plan (04–06) runs against this tree.
Output: Complete infra/terraform/ tree, validated (run --all validate + plan), forbidden-string-clean, committed.
</objective>

## Artifacts this plan produces

- `infra/terraform/providers/{global,regional}.hcl` — backend + provider generation (verbatim)
- `infra/terraform/modules/{site,certs,network,ecs-cluster,ecr,dynamodb,secrets,email,github-oidc,ecs-task,ecs-service}/` — config.hcl + v1.0.0 each; network module gains one additive `webrtc_udp` security group + output entry
- `infra/terraform/live/site/` — terragrunt.hcl, site.hcl (kmv), SECRETS.md (corrected filenames), .secrets.sops.json.template (kmv shape), global/{github-oidc,waf}, region/skip.hcl, region/us-east-1/* units, services/{auth,voice}/service.hcl, region/us-east-1/dmarc/ inline unit
- `infra/terraform/live/site/.secrets.json` — TEMPORARY gitignored plaintext fallback (placeholder values) so parse-time decrypt works until Plan 03 replaces it
- Module addition: SG name pattern `webrtc-udp` in network module outputs (consumed Phase 4)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-infra-skeleton/02-CONTEXT.md
@.planning/phases/02-infra-skeleton/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verbatim copies — providers, 11 modules, live-tree fixed files (D-02, D-04)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "Clone Inventory (exact file list)" — this is the authoritative file-by-file disposition table (verbatim / rewrite / DO NOT COPY); follow it exactly
    - /Users/khundeck/working/defcon.run.34/infra/terraform/ — the source tree itself (spot-read files as you copy; do not modify the source)
  </read_first>
  <files>infra/terraform/providers/, infra/terraform/modules/, infra/terraform/live/site/ (verbatim subset)</files>
  <action>
    Copy from the source root /Users/khundeck/working/defcon.run.34/infra/terraform/ into infra/terraform/, following the research Clone Inventory tables verbatim-column exactly:

    1. providers/global.hcl and providers/regional.hcl — zero edits (D-03 keeps the multi-region aliases and CI provider switching).
    2. All 11 in-scope modules (site, certs, network, ecs-cluster, ecr, dynamodb, secrets, email, github-oidc, ecs-task, ecs-service): each module's config.hcl + entire v1.0.0/ directory, zero edits in this task (D-02). Include email module's lambdas/email-forwarder/index.py source (research: forwarder_lambda_source_path needs it). Copy NONE of the excluded modules listed in D-04 / the inventory's not-copied row.
    3. Live tree verbatim files: live/site/terragrunt.hcl (root), global/waf/waf.hcl (MUST exist even with WAF disabled — the root terragrunt.hcl reads it unconditionally, research trap 3), global/github-oidc/terragrunt.hcl, region/skip.hcl, region/us-east-1/region.hcl, and the nine unit terragrunt.hcl files for certs/network/ecs-cluster/ecr/dynamodb/secrets/email/ecs-task/ecs-service, plus the thin locals wrappers dynamodb.hcl, ecr.hcl, ecs-cluster.hcl, ecs-task.hcl, secrets.hcl.
    4. Exclusions during every copy: never copy .terragrunt-cache/, .terraform.lock.hcl (regenerated), .DS_Store, or any region folder other than us-east-1 (skip_regions machinery stays inert per D-03).

    Use rsync or cp -R per directory followed by explicit rm of excluded artifacts, or copy file-by-file — whichever, the result must be byte-identical for the verbatim set (verified by diff -r below). Do not start renaming in this task: research anti-pattern "Renaming while copying" — pure copy first, single rewrite pass in Task 2.
  </action>
  <verify>
    <automated>for m in site certs network ecs-cluster ecr dynamodb secrets email github-oidc ecs-task ecs-service; do diff -r "/Users/khundeck/working/defcon.run.34/infra/terraform/modules/$m" "infra/terraform/modules/$m" -x '.terragrunt-cache' -x '.terraform.lock.hcl' -x '.DS_Store' || exit 1; done && test -f infra/terraform/providers/global.hcl && test -f infra/terraform/live/site/global/waf/waf.hcl && test ! -d infra/terraform/live/site/region/us-east-1/mqtt && test ! -d infra/terraform/modules/cloudtrail && find infra/terraform -name '.terragrunt-cache' -o -name '.terraform.lock.hcl' | grep -c . | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - All 11 modules byte-identical to source (diff -r clean); no excluded module or unit directory present (D-04)
    - global/waf/waf.hcl present (trap 3), providers pair present, skip.hcl + region.hcl present
    - No cache/lock/DS_Store artifacts copied
  </acceptance_criteria>
  <done>The verbatim skeleton is in place; only designated rewrite files remain to be authored.</done>
</task>

<task type="auto">
  <name>Task 2: kmv rewrites — site.hcl delta, service stubs, email/network hcl, DMARC unit, WebRTC SG, secrets template (D-01, D-03, D-10, D-11)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "site.hcl Delta Table (dc34 → kmv)" (apply every row), "Code Examples" (voice service.hcl stub + apex DMARC inline unit — use these skeletons), "SES / Email" (route-2 decision, make_site_domain=false), "WebRTC Groundwork" (recommended SG placement), "SOPS Setup" step 4 (template shape), Pitfalls 6, 7, 9
    - .planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md — confirm the DMARC route decision recorded in Plan 01 before authoring the dmarc unit
    - /Users/khundeck/working/defcon.run.34/infra/terraform/live/site/site.hcl and services/run.auth/service.hcl — the rewrite sources
  </read_first>
  <files>infra/terraform/live/site/site.hcl, infra/terraform/live/site/services/auth/service.hcl, infra/terraform/live/site/services/voice/service.hcl, infra/terraform/live/site/region/us-east-1/email/email.hcl, infra/terraform/live/site/region/us-east-1/network/network.hcl, infra/terraform/live/site/SECRETS.md, infra/terraform/live/site/.secrets.sops.json.template, infra/terraform/live/site/.secrets.json, infra/terraform/live/site/region/us-east-1/dmarc/main.tf, infra/terraform/live/site/region/us-east-1/dmarc/terragrunt.hcl, infra/terraform/modules/network/v1.0.0/securitygroups.tf, infra/terraform/modules/network/v1.0.0/outputs.tf</files>
  <action>
    Author the rewrite set, starting from the source file and applying the research delta table row by row:

    1. site.hcl — apply EVERY row of the research delta table: label kmv, github_repo_name klanker-voice (repo name only; org comes from TF_VAR_GITHUB_ORG), tf_state_prefix tf-kmv, random_suffix default = the Plan 01 SGUID (Pitfall 3 — read it from 02-01-SUMMARY.md or infra/.envrc), skip_regions unchanged, zonename klankermaker.ai, subdomains [auth, voice], urls/local_ports (auth 3002, voice 7860), service_conf reading the two new service.hcl files, email block per the delta row with make_site_domain = false (Pitfall 6 — the apex-MX hijack trap; route 2 was confirmed by the zone audit), waf enabled=false, cloudfront enabled=false with the minimal block kept present (root merges it), ec2spots enabled=false with empty instances, ecs_clusters us-east-1 only, dynamodb/ecr concat of the two services, ecs_tasks enabled=false tasks=[], ecs_services enabled=false services=[], secrets.definitions exactly the six from the delta row (deepgram/anthropic/elevenlabs api_key; jwt secret+internal_secret; oidc cookie_keys; altcha secret — creating Phase 3 paths now avoids a secrets re-apply), secrets.replica_regions empty, github_oidc block kept with: role list pruned to terragrunt/readonly/deploy/release, ec2_runner_instance_profile disabled, the three kms-sops-decrypt policy ARN lists reduced to the single us-east-1 entry, and the release role's task/execution PassRole patterns adjusted to the kmv naming the ecs-task module produces (confirm exact patterns by reading modules/ecs-task/v1.0.0/main.tf). Delete the user_uploads/upload_processors/waffaw/cloudtrail blocks (assumption A3 — if the site unit later errors on a missing variable at plan time in this task's validation, fall back to enabled=false stub blocks instead).
    2. services/auth/service.hcl + services/voice/service.hcl — data-only stubs modeled on the source run.auth file and the research voice-stub example: ecr_repositories (auth-app, voice-app; us-east-1; IMMUTABLE; modest lifecycle), dynamodb.tables = [] for both (Phase 3 adds auth tables), task = minimal placeholder map, service = placeholder with voice's assign_public_ip = true (WebRTC groundwork). CRITICAL (Pitfall 7): no file() calls anywhere in either stub — not even inside comments, since the verify gate greps the stubs for that token — hardcode any image-tag string; site.hcl parses every service.hcl unconditionally and one missing file() target breaks all plans.
    3. email.hcl rewrite — drop the source's receive_rules, minimal fwd_rules, keep forwarder_lambda_source_path pointing at the copied lambda source.
    4. network.hcl adapt — keep source defaults (10.0.0.0/16, 2 AZ, ALB on), set nlb.enabled = false (no MQTT). Check whether modules/network/v1.0.0/alb.tf exposes an idle-timeout input (research Open Question 4): if yes, set it to 2400 here; if not, note "defer to Phase 4" in the SUMMARY — do not modify alb.tf.
    5. dmarc inline unit (research Pattern 3, bib-secrets precedent): region/us-east-1/dmarc/{terragrunt.hcl,main.tf} exactly per the research Code Example — one aws_route53_record TXT _dmarc.klankermaker.ai with p=quarantine and rua pointing at dmarc-reports@auth.klankermaker.ai, written via the global-management provider into the mgmt zone; terragrunt.hcl includes providers/regional.hcl + skip.hcl with source "." (satisfies D-11 without an apex SES identity).
    6. WebRTC SG groundwork (CONTEXT discretion, research recommendation): add one additive aws_security_group resource named for webrtc-udp to modules/network/v1.0.0/securitygroups.tf — ingress UDP ports 1024-65535 from 0.0.0.0/0, egress all — and append its id to the module's security_group_ids output list in outputs.tf. This is the single sanctioned functional delta to a copied module (the design spec's "only new infra"); ecs_services is disabled so nothing attaches to it until Phase 4 tightens the range. Keep the diff to exactly these two files.
    7. SECRETS.md rewrite — document the ACTUAL mechanism (Pitfall 9): the encrypted file is .secrets.sops.json decrypted by site.hcl at parse time, plaintext fallback .secrets.json is gitignored, keys managed via sops edit. Fix the stale filename conventions from the source doc.
    8. .secrets.sops.json.template — exactly the kmv shape from research SOPS Setup step 4 (six top-level objects matching secrets.definitions).
    9. TEMPORARY plaintext fallback: copy the template to infra/terraform/live/site/.secrets.json with its placeholder values intact so parse-time decrypt has a target before the SOPS key exists (Plan 03 replaces it with the encrypted file and deletes this one). FIRST verify `git check-ignore` passes for this path (ignore rule landed in Plan 01 — hard stop if not).
    10. mock_outputs scrub: anywhere a copied unit terragrunt.hcl carries source-site names inside mock_outputs, rewrite the values to kmv-flavored equivalents so the Task 3 grep gates hold unconditionally.
  </action>
  <verify>
    <automated>git check-ignore -q infra/terraform/live/site/.secrets.json && grep -q 'label.*=.*"kmv"' infra/terraform/live/site/site.hcl && grep -q 'klankermaker.ai' infra/terraform/live/site/site.hcl && grep -q 'make_site_domain[[:space:]]*=[[:space:]]*false' infra/terraform/live/site/site.hcl && test -f infra/terraform/live/site/region/us-east-1/dmarc/main.tf && grep -q 'p=quarantine' infra/terraform/live/site/region/us-east-1/dmarc/main.tf && grep -q 'webrtc' infra/terraform/modules/network/v1.0.0/securitygroups.tf && [ "$(grep -rc 'file(' infra/terraform/live/site/services/ | awk -F: '{s+=$2} END {print s}')" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - site.hcl carries every delta-table row; make_site_domain is false (apex-MX trap avoided); secrets.definitions has the six kmv entries; ecs_tasks/ecs_services disabled
    - Both service.hcl stubs contain zero file() calls; voice stub sets assign_public_ip = true
    - dmarc unit exists with p=quarantine TXT via the management provider (D-11)
    - network module diff vs source is exactly the webrtc SG + output append (two files)
    - .secrets.json fallback exists, is gitignored, and .secrets.sops.json.template matches the six-secret shape
  </acceptance_criteria>
  <done>The tree is fully kmv-shaped: every rewrite from the research inventory is authored, the three clone-breaking traps are all handled, and Phase 3/4 flip only enable-flags.</done>
</task>

<task type="auto">
  <name>Task 3: Gates — forbidden strings, hcl fmt, run --all validate, run --all plan</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "Grep gates after rewrite" line under the delta table, "Assumptions Log" A2/A3, Validation Architecture section
  </read_first>
  <files>infra/terraform/ (validation only — fixes as needed)</files>
  <action>
    Run the gate battery from infra/terraform with the Plan 01 .envrc loaded (source infra/.envrc if direnv is not hooked). Requires a live SSO session (dynamic auth checkpoint on failure, as in Plan 01).

    1. Forbidden strings (D-01 + project naming constraint): the two banned name strings (the three-letter label typo from D-01, and the copyright-banned product word from REQUIREMENTS Out of Scope) must appear nowhere under infra/; the source-site labels must appear in no .hcl file. If a verbatim module comment carries a source-site string, scrub that comment only (comment-only edits don't violate D-02's mechanism-preservation intent; the research gate demands zero).
    2. `terragrunt hcl fmt --check` across the live tree; fix formatting drift.
    3. `terragrunt run --all validate` from infra/terraform/live/site. This is the first backend contact — assumption A2 says the pre-created bucket/table suffice without ambient terraform-profile creds; if init fails on backend auth, replicate the source env.sh export-credentials approach in a small scripts/tg-local.sh helper and document it (A2 fallback).
    4. `terragrunt run --all plan` — the deep gate. Excluded units (ecs-task, ecs-service via enabled=false; skipped regions) must exclude cleanly; every other unit must produce a plan without errors. This exercises the sops fallback decrypt, the mgmt-zone data lookup (A1, probed in Plan 01), and validates A3 (deleted site.hcl blocks). Expected: plans show resources to CREATE — do NOT apply anything in this plan.
    5. Commit the tree.
  </action>
  <verify>
    <automated>! grep -riq "kmk" infra/ && ! grep -riq "voiceai" infra/ && ! grep -riq "dc34\|defcon" infra/ --include='*.hcl' && (cd infra/terraform/live/site && terragrunt hcl fmt --check >/dev/null)</automated>
  </verify>
  <acceptance_criteria>
    - All three forbidden-string greps return zero matches (the automated command)
    - `terragrunt run --all validate` exits 0 from live/site
    - `terragrunt run --all plan` completes with no errors; ecs-task/ecs-service units excluded; site unit plans the two hosted zones + NS delegation records
    - Tree committed; git status clean of untracked hcl
  </acceptance_criteria>
  <done>INFR-01's static half is proven: the whole kmv tree parses, validates, and plans against the real backend — applies (Plans 04-06) are now mechanical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (public) → plan-time sops decrypt | site.hcl runs sops at parse time; plaintext fallback file must never be tracked |
| local plans → mgmt-account Route53 (data lookup) | read-only at plan time via klanker-management profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-04 | Information Disclosure | temporary plaintext .secrets.json in public repo | high | mitigate | git check-ignore hard-stop before creating the file (Task 2 step 9); file holds only placeholder values until Plan 03 replaces it; deleted in Plan 03 |
| T-2-05 | Tampering | wide-UDP webrtc SG (1024-65535 from anywhere) | medium | accept | Structure-only this phase: ecs_services.enabled=false so no ENI ever attaches it; Phase 4 tightens via sysctl+narrow range per research; documented in SUMMARY |
| T-2-06 | Spoofing | email sender domain (missing org DMARC would allow apex spoofing) | medium | mitigate | dmarc inline unit writes apex p=quarantine TXT (applied Plan 05); make_site_domain=false avoids the receive-MX takeover |
</threat_model>

<verification>
- diff -r proves module verbatim-ness (D-02); excluded modules absent (D-04)
- Delta table fully applied; three traps handled (profile prefix handled in Plan 01 .envrc; AGENTS.md anchor Plan 01; waf.hcl kept here)
- run --all validate + plan green; forbidden-string greps zero
- Schema push gate: not applicable — terraform-only phase, no ORM/schema files
</verification>

<success_criteria>
- Full tree committed, validated, and planning clean with excluded units skipping — ready for sequential applies
</success_criteria>

<output>
Create `.planning/phases/02-infra-skeleton/02-02-SUMMARY.md` when done. MUST record: whether the ALB idle-timeout input existed (Open Question 4 disposition), the A2/A3 outcomes, and the exact network-module diff (two files).
</output>
