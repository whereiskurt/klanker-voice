---
phase: 02-infra-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - .gitignore
  - scripts/bootstrap-state.sh
  - infra/.envrc
  - .planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md
autonomous: true
requirements: [INFR-01]
must_haves:
  truths:
    - "Local toolchain matches the CI pins: terraform 1.14.3 (upgraded from 1.8.2), terragrunt >= 0.96, sops 3.11.0, direnv installed"
    - "Versioned + encrypted S3 state bucket and DynamoDB lock table tf-kmv-use1-<SGUID> exist in 052251888500 with all public access blocked, created idempotently by scripts/bootstrap-state.sh (D-05)"
    - "AGENTS.md exists at repo root so the copied root terragrunt.hcl can anchor repo_root (research trap 2)"
    - "The klankermaker.ai zone audit and SES account probe are recorded, resolving the apex-mail question that gates the DMARC route (research Open Question 1) and confirming D-12 (out of sandbox)"
    - "Repo is public at github.com/whereiskurt/klanker-voice with zero secret material tracked (D-07)"
  artifacts:
    - path: "scripts/bootstrap-state.sh"
      provides: "Idempotent state-backend bootstrap (klanker-terraform profile, tf-kmv prefix) printing TG_BUCKET_USE1/TG_TABLE_USE1/SGUID exports (D-05)"
    - path: "infra/.envrc"
      provides: "Checked-in non-secret env for direnv: TG_* names, account IDs, TF_VAR_profile_prefix=klanker with NO trailing dash (D-06 as corrected by research)"
    - path: "AGENTS.md"
      provides: "Repo-root anchor file required by the cloned root terragrunt.hcl"
    - path: ".planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md"
      provides: "Read-only audit: mgmt zone records (apex MX?), SES account status, profile identity checks"
  key_links:
    - from: "infra/.envrc"
      to: "infra/terraform/providers/*.hcl"
      via: "TG_BUCKET_USE1/TG_TABLE_USE1 env vars name the bucket/table the bootstrap script created"
      pattern: "TG_BUCKET_USE1"
    - from: "scripts/bootstrap-state.sh"
      to: "tf-kmv-use1-<SGUID> bucket + lock table"
      via: "aws s3api/dynamodb calls under the klanker-terraform profile"
      pattern: "tf-kmv-use1"
---

## Phase Goal

**As a** klanker-voice developer, **I want to** provision the complete kmv AWS foundation (DNS/TLS, data stores, secrets flow, container plumbing, and an OIDC-gated CI deploy path) as applied, verifiable cloud state, **so that** the auth and voice services in Phases 3–5 deploy onto proven infrastructure with no plaintext secrets and no long-lived keys.

<objective>
Stand up everything the terragrunt clone depends on: the pinned toolchain (research Pitfall 4: local terraform 1.8.2 cannot share state with the 1.14.3 CI pin), the AGENTS.md repo-root anchor (research trap 2), read-only AWS probes that resolve research Open Question 1 and assumptions A1/D-12, the idempotent state backend per D-05, the checked-in infra/.envrc per D-06 (with the research-corrected profile prefix), and the public GitHub repo per D-07.

Purpose: Every later plan needs this substrate — the backend must exist before any terragrunt init, the SGUID chosen here threads into site.hcl and CI vars (research Pitfall 3), and the zone audit decides the apex-DMARC route before anything touches DNS.
Output: Working toolchain, live state backend, infra/.envrc, AGENTS.md, zone/SES audit record, public repo with clean history.
</objective>

## Artifacts this plan produces

- `AGENTS.md` (repo root) — terragrunt repo_root anchor
- `scripts/bootstrap-state.sh` — idempotent state backend creator
- `infra/.envrc` — direnv non-secret env contract (config keys: SITE_LABEL, SGUID, TG_BUCKET_USE1, TG_TABLE_USE1, AWS_REGION, TF_VAR_APPLICATION_ACCOUNT_ID, TF_VAR_MANAGEMENT_ACCOUNT_ID, TF_VAR_profile_prefix, TF_VAR_GITHUB_ORG, TF_VAR_SOPS_KMS_KEY_ID placeholder, TF_VAR_SSM_KMS_KEY_ARNS placeholder)
- `.planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md` — zone/SES/identity probe record
- AWS: S3 bucket + DynamoDB lock table `tf-kmv-use1-<SGUID>` in 052251888500
- GitHub: public repo `whereiskurt/klanker-voice` (pushed)
- `.gitignore` additions: plaintext secrets fallback, terragrunt caches, tfstate files

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-infra-skeleton/02-CONTEXT.md
@.planning/phases/02-infra-skeleton/02-RESEARCH.md
@docs/superpowers/specs/2026-07-04-klanker-voice-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolchain pins, AGENTS.md anchor, and read-only AWS probes</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "Environment Availability" table (what is installed vs missing), "Common Pitfalls" #2 and #4, "Runtime State Inventory" (zone audit rationale), "Assumptions Log" A1
    - .planning/phases/02-infra-skeleton/02-CONTEXT.md — D-12 (SES already out of sandbox)
  </read_first>
  <files>AGENTS.md, .planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md</files>
  <action>
    Install/upgrade the toolchain to match the CI pins (research Pitfall 4): `brew install direnv tfenv` (skip whichever already exists), then `tfenv install 1.14.3 && tfenv use 1.14.3`. If tfenv conflicts with an existing brew terraform install, `brew unlink terraform` first. Add the direnv shell hook to ~/.zshrc only if not already present (idempotent append of the standard `eval "$(direnv hook zsh)"` line); if modifying the user's shell rc is undesirable at execution time, fall back to documenting `source infra/.envrc` in the audit file — direnv is a convenience, not a blocker. Confirm terragrunt (0.99.1, >= 0.96 required for exclude blocks) and sops 3.11.0 are already present per research.

    Create AGENTS.md at the repo root — the cloned root terragrunt.hcl computes repo_root via find_in_parent_folders on this exact filename (research trap 2 / Pitfall 2). Content: a short note that this file anchors the terragrunt repo-root lookup and points readers to ./.claude/CLAUDE.md for project instructions. Do NOT rename the anchor inside the copied terragrunt.hcl — keeping the marker file is the smaller diff per research.

    Run read-only AWS probes (these need a live SSO session; first run `aws sts get-caller-identity --profile klanker-application` — if it fails with an SSO/auth error, create a dynamic auth checkpoint asking the user to run `aws sso login --sso-session=Developer`, then retry; never work around with pasted credentials):
    1. `aws sts get-caller-identity` for each of the three profiles klanker-application, klanker-management, klanker-terraform — confirm accounts 052251888500 / 481723467561 / (state account) respectively.
    2. `aws route53 list-hosted-zones --profile klanker-management` — validates assumption A1 (HostedZoneAdmin can list) before any apply depends on it.
    3. `aws route53 list-resource-record-sets --hosted-zone-id Z036807010CWM2JH60RKQ --profile klanker-management` — the zone audit for klankermaker.ai. Specifically record: does the apex have an MX record or other mail-related records (decides research Open Question 1 — expected answer: no apex mail, so the standalone _dmarc inline unit route is safe); do NS records for auth. or voice. subdomains already exist (delegation collision check).
    4. `aws sesv2 get-account --profile klanker-application --region us-east-1` — confirm D-12: ProductionAccessEnabled true; record the send quota.

    Write the findings to .planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md: a short markdown doc with sections for profile identities, zone record summary (record names/types only — full values not needed), apex-mail verdict + chosen DMARC route (route 2, standalone _dmarc unit, per research recommendation unless the audit surprises), and SES account status/quota. This file is the evidence Plans 04/05 read before touching DNS.
  </action>
  <verify>
    <automated>terraform version | grep -q 'v1.14.3' && terragrunt --version >/dev/null && sops --version | grep -q '3.11.0' && command -v direnv >/dev/null && test -f AGENTS.md && test -f .planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md && grep -qi 'ProductionAccessEnabled' .planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `terraform version` reports 1.14.3 (CI pin match); terragrunt and sops report working versions; direnv is installed
    - AGENTS.md exists at repo root
    - 02-ZONE-AUDIT.md records: three profile identities confirmed, HostedZoneAdmin list succeeded (A1), apex-mail verdict with an explicit DMARC-route decision, no pre-existing auth./voice. NS delegation collisions (or collisions documented), SES production access + quota (D-12 confirmed)
  </acceptance_criteria>
  <done>Toolchain matches CI pins, the terragrunt anchor exists, and every risky assumption about the two AWS accounts has been probed read-only and recorded before any write happens.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/bootstrap-state.sh (D-05) and infra/.envrc (D-06, corrected)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "State Bootstrap" section (naming contract tf-kmv-use1-<SGUID>, script checklist, exact .envrc contents) and "Code Examples" (bootstrap idempotence core), "Common Pitfalls" #1 and #3
  </read_first>
  <files>scripts/bootstrap-state.sh, infra/.envrc</files>
  <action>
    Write scripts/bootstrap-state.sh implementing D-05 and the research checklist exactly: bash with set -euo pipefail; uses `--profile klanker-terraform --region us-east-1` on every AWS call. Accept SGUID as $1 or generate one (first 8 hex of `uuidgen`, lowercased) and echo it prominently — it is the single source of truth (research Pitfall 3: SGUID drift between bucket, site.hcl default, and CI vars). Bucket and table share the name tf-kmv-use1-$SGUID. Idempotence per the research code example: head-bucket-or-create-bucket (us-east-1 needs no LocationConstraint), describe-table-or-create-table (attribute LockID type S, hash key, PAY_PER_REQUEST). Always (re)apply: put-bucket-versioning Enabled, put-bucket-encryption (SSE-S3 AES256), put-public-access-block with all four flags true (threat T-2-02: terraform state will contain secret values — public exposure is critical). Finish by printing `export TG_BUCKET_USE1=... TG_TABLE_USE1=... SGUID=...` plus the matching `gh variable set SITE_LABEL/SGUID/AWS_ACCOUNT_ID` commands for later CI setup. chmod +x.

    Run the script once (SSO session required — same dynamic auth-gate protocol as Task 1). Capture the SGUID it prints.

    Write infra/.envrc with exactly the research "State Bootstrap" section's contents, substituting the real SGUID: SITE_LABEL=kmv, SGUID, TG_BUCKET_USE1 and TG_TABLE_USE1 derived as tf-${SITE_LABEL}-use1-${SGUID}, AWS_REGION=us-east-1, TF_VAR_APPLICATION_ACCOUNT_ID=052251888500, TF_VAR_MANAGEMENT_ACCOUNT_ID=481723467561, TF_VAR_profile_prefix=klanker (CRITICAL — no trailing dash; D-06 and the design spec carry a trailing-dash typo, and the providers layer appends the dash itself: research Pitfall 1, trap 1 — research wins over the D-06 literal), TF_VAR_GITHUB_ORG=whereiskurt, and empty-for-now TF_VAR_SOPS_KMS_KEY_ID / TF_VAR_SSM_KMS_KEY_ARNS lines (Plan 03 and Plan 06 fill them). Include a one-line comment warning about the prefix (phrase it as "providers append the dash — do not add one here"). Do NOT put any aws sso login or credential-export lines in .envrc (direnv runs on every cd; login is interactive per research). Run `direnv allow infra` if direnv is hooked.

    Re-run the bootstrap script a second time with the same SGUID to prove idempotence (must exit 0 with no changes/errors).
  </action>
  <verify>
    <automated>bash -n scripts/bootstrap-state.sh && test -x scripts/bootstrap-state.sh && source infra/.envrc && aws s3api get-bucket-versioning --bucket "$TG_BUCKET_USE1" --profile klanker-terraform --query Status --output text | grep -q Enabled && ! aws s3api get-public-access-block --bucket "$TG_BUCKET_USE1" --profile klanker-terraform --output text | grep -q False && aws dynamodb describe-table --table-name "$TG_TABLE_USE1" --profile klanker-terraform --query Table.TableStatus --output text | grep -q ACTIVE && [ "$(grep -c 'TF_VAR_profile_prefix=klanker$' infra/.envrc)" -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/bootstrap-state.sh <SGUID>` run twice: second run is a clean no-op (idempotent, D-05)
    - Bucket tf-kmv-use1-<SGUID>: versioning Enabled, default encryption set, public-access-block all four true; lock table ACTIVE with LockID hash key
    - infra/.envrc line `TF_VAR_profile_prefix=klanker` ends without a dash (exactly one match for the anchored grep); .envrc contains no secrets and no sso-login lines
    - Script printed TG_BUCKET_USE1/TG_TABLE_USE1/SGUID exports and the gh variable commands
  </acceptance_criteria>
  <done>The terragrunt backend exists, is locked down, and is reproducible; the SGUID single source is threaded into .envrc and recorded for site.hcl (Plan 02) and CI vars (Plan 06).</done>
</task>

<task type="auto">
  <name>Task 3: Public GitHub repo whereiskurt/klanker-voice — hygiene scan then push (D-07)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "Security Domain" table (plaintext-secret-in-public-repo threat, gitignore-before-first-commit requirement)
  </read_first>
  <files>.gitignore</files>
  <action>
    Before anything becomes public, extend the root .gitignore (created in Phase 1) with the infra-phase entries so no future plaintext secret or state file can ever be tracked: the plaintext secrets fallback at infra/terraform/live/site/.secrets.json, a general env.local.sh pattern, .terragrunt-cache/ directories, .terraform/ directories, and any *.tfstate / *.tfstate.backup files. (Plan 02 creates the fallback file — the ignore rule must land in git history first, per the research Security Domain requirement.)

    Run a secret-hygiene scan over everything currently tracked: `git ls-files -z | xargs -0 grep -lE 'AKIA[0-9A-Z]{16}|BEGIN (RSA |OPENSSH )?PRIVATE KEY'` must return nothing, and confirm apps/voice/.env is not tracked (`git check-ignore -q apps/voice/.env`). Commit the .gitignore change.

    Create the public repo per D-07 and push: if `git remote get-url origin` already succeeds, just `git push -u origin main`; otherwise `gh repo create whereiskurt/klanker-voice --public --source=. --remote=origin --push`. The repo must be PUBLIC — SOPS encryption (Plan 03) is the control that makes this safe, and the quota design assumes a public URL.
  </action>
  <verify>
    <automated>git check-ignore -q infra/terraform/live/site/.secrets.json && git check-ignore -q apps/voice/.env && [ "$(gh repo view whereiskurt/klanker-voice --json visibility -q .visibility)" = "PUBLIC" ] && git ls-remote --heads origin main | grep -q main</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore covers the plaintext secrets fallback path, terragrunt caches, .terraform dirs, and tfstate patterns BEFORE any such file exists
    - Tracked-file secret scan (AKIA keys, private-key blocks) returns zero hits
    - github.com/whereiskurt/klanker-voice exists, is public, and main is pushed
  </acceptance_criteria>
  <done>The public repo exists with defense-in-depth ignore rules in history before any secret-adjacent file is created; CI plans (Plan 07) and gh repo variables (Plan 06) now have a home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine → AWS (3 SSO profiles) | all writes gated on interactive SSO session; no long-lived keys |
| repo → public internet | everything committed becomes world-readable (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Information Disclosure | plaintext secret committed to public repo | high | mitigate | Task 3: gitignore rules land before any secret-adjacent file exists; tracked-file scan for key patterns before push; SOPS-only workflow from Plan 03 on |
| T-2-02 | Information Disclosure | tf state bucket (state embeds SSM SecureString values) | critical | mitigate | Task 2: put-public-access-block all-true + default encryption + versioning, verified by automated command |
| T-2-03 | Tampering | bootstrap script re-run clobbering backend | low | mitigate | idempotent head-bucket/describe-table guards; second-run no-op is an acceptance criterion |
| T-2-SC | Tampering | package installs (npm/pip/cargo) | low | accept | Not applicable this phase — research Package Legitimacy Audit: zero language-registry installs; tooling is Homebrew/GitHub-release binaries pinned by the cloned dc34 workflows |
</threat_model>

<verification>
- Toolchain versions match CI pins (terraform 1.14.3 / terragrunt >= 0.96 / sops 3.11.0)
- State backend exists, encrypted, versioned, public-blocked; second bootstrap run is a no-op
- 02-ZONE-AUDIT.md records the apex-mail verdict (DMARC route decision) and SES production-access confirmation
- Repo public with clean secret scan
- Schema push gate: not applicable — terraform-only phase, no ORM/schema files
</verification>

<success_criteria>
- All Task acceptance criteria green
- SGUID recorded and identical in: bucket name, .envrc, bootstrap output (site.hcl default follows in Plan 02)
</success_criteria>

<output>
Create `.planning/phases/02-infra-skeleton/02-01-SUMMARY.md` when done. MUST record: the SGUID value, the DMARC route decision from the zone audit, SES quota, and any deviation in direnv hook setup.
</output>
