---
phase: 02-infra-skeleton
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - infra/terraform/live/site/region/us-east-1/ (state only — applies)
autonomous: true
requirements: [INFR-01, INFR-04, INFR-05]
must_haves:
  truths:
    - "ECS cluster 'app', ECR repos for auth-app and voice-app, and the dynamodb module are applied (container plumbing exists before Phase 3 CI pushes images)"
    - "Provider API keys live at /kmv/secrets/use1/<name>/api_key as SecureStrings, decryptable, and the /kmv/bootstrap/* params are deleted ONLY for keys whose SSM round-trip verified (INFR-05 complete)"
    - "SES domain identity auth.klankermaker.ai is verified with Easy-DKIM CNAMEs, MAIL FROM SPF, per-identity DMARC, and the apex _dmarc.klankermaker.ai TXT carries p=quarantine — sender sign-in@auth.klankermaker.ai is production-ready (D-10, D-11, D-12 → INFR-04)"
    - "No MX record was added at the klankermaker.ai apex (make_site_domain stayed false — hijack trap avoided)"
  artifacts:
    - path: "terraform state: ecs-cluster / ecr / dynamodb / secrets / email / dmarc units"
      provides: "cluster, repos, table plumbing, SSM SecureStrings, SES identity + DNS records, apex DMARC"
    - path: "SSM /kmv/secrets/use1/{deepgram,anthropic,elevenlabs,jwt,oidc,altcha}/*"
      provides: "SecureString paths Phases 1 (bootstrap replacement), 3 and 4 consume via valueFrom"
  key_links:
    - from: "infra/terraform/live/site/.secrets.sops.json"
      to: "SSM /kmv/secrets/use1/*"
      via: "site.hcl parse-time decrypt → secrets module apply (INFR-05 second hop)"
      pattern: "/kmv/secrets/use1"
    - from: "email module ses-domain"
      to: "auth.klankermaker.ai zone (app acct) records"
      via: "identity TXT + 3 DKIM CNAMEs + MAIL FROM MX/SPF"
      pattern: "_domainkey"
    - from: "dmarc inline unit"
      to: "mgmt zone Z036807010CWM2JH60RKQ"
      via: "TXT _dmarc.klankermaker.ai p=quarantine via global-management provider"
      pattern: "p=quarantine"
---

## Phase Goal

**As a** klanker-voice developer, **I want to** provision the complete kmv AWS foundation (DNS/TLS, data stores, secrets flow, container plumbing, and an OIDC-gated CI deploy path) as applied, verifiable cloud state, **so that** the auth and voice services in Phases 3–5 deploy onto proven infrastructure with no plaintext secrets and no long-lived keys.

<objective>
Apply the parallel regional band from the research DAG: ecs-cluster, ecr, dynamodb, secrets, email, and the dmarc inline unit. This completes INFR-05 (keys land in SSM SecureString; bootstrap params retired after verified round-trip) and INFR-04 (SES identity + DKIM + SPF + DMARC for the sign-in@auth.klankermaker.ai sender — D-12 confirmed no sandbox review is needed).

Purpose: The data/secrets/email slices Phases 3 and 4 build directly on.
Output: Applied cluster/repos/tables, live SecureStrings, verified SES identity, apex DMARC.
</objective>

## Artifacts this plan produces

- AWS: ECS cluster `app` + Cloud Map namespace; ECR repos `auth-app`, `voice-app`
- AWS: dynamodb module state (tables empty this phase), secrets module KMS CMK + SecureStrings under /kmv/secrets/use1/
- AWS: SES identity auth.klankermaker.ai (verification TXT, 3 DKIM CNAMEs, MAIL FROM s.auth.* MX+SPF, per-identity DMARC), receipt rule set, SMTP IAM user, SES SSM params
- AWS: TXT `_dmarc.klankermaker.ai` p=quarantine in mgmt zone
- Deleted: /kmv/bootstrap/* SSM params (verified-migrated keys only)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-infra-skeleton/02-CONTEXT.md
@.planning/phases/02-infra-skeleton/02-RESEARCH.md
@.planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply ecs-cluster, ecr, dynamodb — container and data plumbing</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — Architecture DAG (this band is parallel after network) and Clone Inventory module rows for ecs-cluster/ecr/dynamodb
  </read_first>
  <files>infra/terraform/live/site/region/us-east-1/{ecs-cluster,ecr,dynamodb}/ (state)</files>
  <action>
    Preflight SSO check (dynamic auth checkpoint on failure), load infra/.envrc. From each of region/us-east-1/ecs-cluster, ecr, and dynamodb: terragrunt plan, review, apply. Expected: Fargate cluster "app" with the Cloud Map namespace, ECR repositories auth-app and voice-app (from the two service.hcl stubs — existing before Phase 3 CI pushes, per research Pattern 2 rationale), and the dynamodb module applying cleanly with zero tables (both stubs expose empty table lists; Phase 3 adds auth tables by editing its service.hcl only).
  </action>
  <verify>
    <automated>[ "$(aws ecs describe-clusters --clusters app --profile klanker-application --region us-east-1 --query 'clusters[0].status' --output text)" = "ACTIVE" ] && aws ecr describe-repositories --profile klanker-application --region us-east-1 --query 'repositories[].repositoryName' --output text | grep -q 'voice-app' && aws ecr describe-repositories --profile klanker-application --region us-east-1 --query 'repositories[].repositoryName' --output text | grep -q 'auth-app'</automated>
  </verify>
  <acceptance_criteria>
    - Cluster app ACTIVE; both ECR repos exist with IMMUTABLE tag mutability
    - dynamodb unit applied clean with no tables (empty concat)
  </acceptance_criteria>
  <done>Phase 3/4 can push images and enable services by flipping site.hcl flags only.</done>
</task>

<task type="auto">
  <name>Task 2: Apply secrets — SSM SecureStrings live, then retire /kmv/bootstrap/* (INFR-05)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "SOPS Setup" step 5 (the ONLY-THEN deletion rule), secrets module row (ssm_prefix → /kmv/secrets/use1), "Runtime State Inventory" stored-data row
    - .planning/phases/02-infra-skeleton/02-03-SUMMARY.md — elevenlabs migration status (pending keys must NOT have their bootstrap param deleted)
  </read_first>
  <files>infra/terraform/live/site/region/us-east-1/secrets/ (state)</files>
  <action>
    From region/us-east-1/secrets: terragrunt plan, review (expect the module's regional KMS CMK plus SecureStrings for all six definitions), apply.

    Round-trip verification per migrated key: `aws ssm get-parameter --name /kmv/secrets/use1/<name>/api_key --with-decryption --profile klanker-application --region us-east-1 --query Parameter.Value --output text | wc -c` must exceed 10 for deepgram and anthropic (length check only — never print values). Check jwt/oidc/altcha paths exist (get-parameter without decryption is fine for existence).

    ONLY for keys whose round-trip verified AND whose value was actually migrated from a bootstrap param (per the 02-03-SUMMARY): delete the corresponding /kmv/bootstrap/<name>_api_key parameter. If elevenlabs was pending in Plan 03, leave any elevenlabs bootstrap param untouched and record the outstanding follow-up (user fills via sops edit, re-applies this unit, then deletes the param). Deleting a bootstrap param whose value never reached SSM would destroy the only copy — hard rule.
  </action>
  <verify>
    <automated>[ "$(aws ssm get-parameter --name /kmv/secrets/use1/deepgram/api_key --with-decryption --profile klanker-application --region us-east-1 --query Parameter.Value --output text | wc -c)" -gt 10 ] && [ "$(aws ssm get-parameter --name /kmv/secrets/use1/anthropic/api_key --with-decryption --profile klanker-application --region us-east-1 --query Parameter.Value --output text | wc -c)" -gt 10 ] && ! aws ssm get-parameter --name /kmv/bootstrap/deepgram_api_key --profile klanker-application --region us-east-1 >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - All six secret paths exist under /kmv/secrets/use1/; deepgram + anthropic decrypt to non-trivial values (length checks)
    - Bootstrap params deleted for verified-migrated keys only; pending keys' params preserved and follow-up documented
    - SecureStrings use the secrets module's own CMK (not alias/aws/ssm default)
  </acceptance_criteria>
  <done>INFR-05 is end-to-end real: SOPS file → terraform → SSM SecureString, with the interim bootstrap namespace retired safely.</done>
</task>

<task type="auto">
  <name>Task 3: Apply email + dmarc — SES identity, DKIM/SPF, apex p=quarantine (INFR-04, D-10, D-11)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "SES / Email (INFR-04)" full section (what ses-domain creates, route-2 decision, Pitfall 6)
    - .planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md — apex-mail verdict (route confirmation)
  </read_first>
  <files>infra/terraform/live/site/region/us-east-1/{email,dmarc}/ (state)</files>
  <action>
    From region/us-east-1/email: terragrunt plan and REVIEW BEFORE APPLY — the plan must NOT contain any record at the bare apex klankermaker.ai other than what the zone audit sanctioned (specifically: no apex MX, no apex SES identity — make_site_domain is false per Plan 02; Pitfall 6). Then apply. Expected: SES domain identity auth.klankermaker.ai, verification TXT + 3 DKIM CNAMEs + MAIL FROM (s.auth.klankermaker.ai MX + SPF TXT) + per-identity DMARC — all landing in the app-account auth zone — plus receipt rule set, received-mail S3 bucket, forwarding lambda, and the SMTP IAM user per the module.

    Then from region/us-east-1/dmarc: plan/apply the inline unit — one TXT _dmarc.klankermaker.ai with p=quarantine and rua to dmarc-reports@auth.klankermaker.ai in the mgmt zone (D-11; relaxed alignment means DKIM d=auth.klankermaker.ai passes org DMARC per research).

    SES identity verification is DNS-propagation-bound: poll `aws ses get-identity-verification-attributes --identities auth.klankermaker.ai --profile klanker-application --region us-east-1` in a bounded loop (up to ~20 minutes) until VerificationStatus is Success; same for DKIM via get-identity-dkim-attributes. Record final SES send quota in the SUMMARY (D-12 evidence chain: production access + verified identity = magic-link sender ready).
  </action>
  <verify>
    <automated>[ "$(aws ses get-identity-verification-attributes --identities auth.klankermaker.ai --profile klanker-application --region us-east-1 --query 'VerificationAttributes."auth.klankermaker.ai".VerificationStatus' --output text)" = "Success" ] && dig +short TXT _dmarc.klankermaker.ai | grep -q 'p=quarantine' && [ -z "$(aws route53 list-resource-record-sets --hosted-zone-id Z036807010CWM2JH60RKQ --profile klanker-management --query "ResourceRecordSets[?Type=='MX' && Name=='klankermaker.ai.']" --output text)" ]</automated>
  </verify>
  <acceptance_criteria>
    - SES identity auth.klankermaker.ai VerificationStatus Success; DKIM verified (3 CNAMEs resolving)
    - MAIL FROM s.auth.klankermaker.ai MX + SPF present in the auth zone
    - dig on _dmarc.klankermaker.ai returns p=quarantine (D-11 satisfied at the org domain)
    - The apex MX query returns empty — no inbound-mail takeover occurred (Pitfall 6 gate)
    - SES send quota recorded (D-12: already out of sandbox, no review clock)
  </acceptance_criteria>
  <done>INFR-04 complete: sign-in@auth.klankermaker.ai can send production email with SPF+DKIM alignment and org-level p=quarantine DMARC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SOPS ciphertext → terraform → SSM SecureString | secret values transit terraform state (bucket locked down in Plan 01) |
| internet email receivers → klankermaker.ai sender reputation | SPF/DKIM/DMARC are the anti-spoofing controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-13 | Spoofing | mail claiming to be klankermaker.ai / auth subdomain | high | mitigate | Task 3: DKIM + MAIL FROM SPF + per-identity DMARC + apex p=quarantine TXT with relaxed alignment (D-11) |
| T-2-14 | Denial of Service | apex receive-MX hijack rerouting existing mail | high | mitigate | make_site_domain=false locked in Plan 02; Task 3 plan-review gate + automated empty-apex-MX check |
| T-2-15 | Information Disclosure | secret values in terraform state (SSM params in state) | high | mitigate | State bucket: SSE + versioning + public-access-block (Plan 01, verified); access only via SSO profiles / gated CI roles |
| T-2-16 | Repudiation/Data loss | deleting bootstrap params before SSM copy verified | medium | mitigate | Task 2 ONLY-THEN rule: length-checked decrypt round-trip precedes each deletion; pending keys' params preserved |
</threat_model>

<verification>
- INFR-04: identity Success + DKIM + SPF + org DMARC quarantine, zero apex MX
- INFR-05: SOPS → SSM chain live; bootstrap namespace retired for migrated keys
- INFR-01: all in-scope regional units now applied except github-oidc (Plan 06)
- Schema push gate: not applicable — terraform-only phase, no ORM/schema files
</verification>

<success_criteria>
- All six units applied; every automated verify green; outstanding elevenlabs follow-up (if any) documented in SUMMARY
</success_criteria>

<output>
Create `.planning/phases/02-infra-skeleton/02-05-SUMMARY.md` when done. MUST record: SSM paths created, which bootstrap params were deleted vs preserved, SES quota, DKIM token count, dmarc record confirmation.
</output>
