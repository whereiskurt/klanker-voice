---
phase: 02-infra-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - scripts/setup-sops.sh
  - .sops.yaml
  - infra/.envrc
  - infra/terraform/live/site/.secrets.sops.json
autonomous: true
requirements: [INFR-05]
must_haves:
  truths:
    - "A single-region SOPS KMS key with alias/sops exists in 052251888500 us-east-1 (D-03 simplification — no multi-region CMK, no replicate loop)"
    - "infra/terraform/live/site/.secrets.sops.json is committed, encrypted, and round-trips via sops decrypt showing exactly the six secret objects — real provider keys migrated from /kmv/bootstrap/*, tolerating a missing elevenlabs param"
    - "No plaintext secret value ever touches the repo or the terminal output; the temporary Plan 02 fallback file is deleted"
    - "TF_VAR_SOPS_KMS_KEY_ID is persisted in infra/.envrc and as a GitHub repo variable BEFORE github-oidc applies (research Pitfall 5 ordering)"
  artifacts:
    - path: "scripts/setup-sops.sh"
      provides: "Single-region adaptation of the source env.sops.sh: key+alias idempotent create, .sops.yaml writer, TF_VAR persistence, gh-variable printout"
    - path: ".sops.yaml"
      provides: "creation_rules pinning .secrets*.json to the us-east-1 alias/sops KMS ARN"
    - path: "infra/terraform/live/site/.secrets.sops.json"
      provides: "Encrypted secret source consumed by site.hcl parse-time decrypt → secrets module → SSM (INFR-05 first hop)"
  key_links:
    - from: ".sops.yaml"
      to: "KMS alias/sops (052251888500 us-east-1)"
      via: "creation_rules kms ARN"
      pattern: "arn:aws:kms:us-east-1:052251888500"
    - from: "infra/terraform/live/site/site.hcl"
      to: "infra/terraform/live/site/.secrets.sops.json"
      via: "run_cmd sops --decrypt at parse time"
      pattern: "secrets.sops.json"
    - from: "infra/.envrc"
      to: "site.hcl github_oidc kms-sops-decrypt policies"
      via: "TF_VAR_SOPS_KMS_KEY_ID interpolation (must be set before Plan 06 applies github-oidc)"
      pattern: "TF_VAR_SOPS_KMS_KEY_ID"
---

## Phase Goal

**As a** klanker-voice developer, **I want to** provision the complete kmv AWS foundation (DNS/TLS, data stores, secrets flow, container plumbing, and an OIDC-gated CI deploy path) as applied, verifiable cloud state, **so that** the auth and voice services in Phases 3–5 deploy onto proven infrastructure with no plaintext secrets and no long-lived keys.

<objective>
Create the SOPS encryption backbone (single-region KMS key per D-03) and migrate the user's /kmv/bootstrap/* SSM provider keys into the encrypted .secrets.sops.json (D-01 namespace, INFR-05's first hop). Generate real random values for the Phase 3 secret paths (jwt/oidc/altcha) so no placeholder ever lands in SSM. This plan MUST complete before Plan 06 applies github-oidc — the kms-sops-decrypt IAM policies interpolate the key ID (research Pitfall 5's drift loop).

Purpose: INFR-05 requires SOPS → SSM SecureString → container secrets with zero plaintext in the repo. This is the SOPS half; Plan 05 applies the SSM half.
Output: KMS key + alias, .sops.yaml, setup-sops.sh, committed encrypted secrets file, TF_VAR persistence.
</objective>

## Artifacts this plan produces

- `scripts/setup-sops.sh` — idempotent SOPS bootstrap (single-region)
- `.sops.yaml` (repo root) — creation rules → alias/sops ARN
- `infra/terraform/live/site/.secrets.sops.json` — encrypted, committed
- AWS: KMS key (plain UUID id, MultiRegion=false) + alias/sops in 052251888500 us-east-1
- Config keys: `TF_VAR_SOPS_KMS_KEY_ID` filled in infra/.envrc + GitHub repo variable
- Deletion: temporary plaintext fallback `infra/terraform/live/site/.secrets.json` removed

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-infra-skeleton/02-CONTEXT.md
@.planning/phases/02-infra-skeleton/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/setup-sops.sh — single-region KMS key, alias, .sops.yaml, TF_VAR persistence (D-03)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "SOPS Setup (single-region variant)" steps 2, 3, 6 (exact commands, .sops.yaml contents, persistence targets) and "Common Pitfalls" #5
    - /Users/khundeck/working/defcon.run.34/env.sops.sh — the source script being adapted (keep: alias existence check, .sops.yaml writer, TF_VAR persistence, gh-variable printout; DROP: the replica-regions loop and the multi-region key flag per D-03)
  </read_first>
  <files>scripts/setup-sops.sh, .sops.yaml, infra/.envrc</files>
  <action>
    Write scripts/setup-sops.sh as the single-region adaptation of the source env.sops.sh: set -euo pipefail; profile klanker-terraform, region us-east-1 throughout. Behavior: (1) if alias/sops already resolves via kms describe-key, reuse that key id (idempotent); otherwise create a key with a kmv SOPS description — WITHOUT any multi-region flag — and create alias/sops pointing at it. (2) Write .sops.yaml at the repo root with one creation_rules entry: path_regex matching .secrets and .secrets.sops JSON files, kms set to the single ARN arn:aws:kms:us-east-1:052251888500:alias/sops (research step 3 — the source lists three regional ARNs; kmv lists one). (3) Upsert TF_VAR_SOPS_KMS_KEY_ID=<KeyId> into infra/.envrc, replacing the empty placeholder line from Plan 01. (4) Print the matching `gh variable set TF_VAR_SOPS_KMS_KEY_ID` command AND run it (repo exists since Plan 01). chmod +x.

    Run the script (SSO session required; dynamic auth checkpoint on failure). Then run it a second time to prove idempotence (no duplicate keys/aliases; same key id).

    Ordering note for the SUMMARY: record the key id prominently — Plan 06's github-oidc apply is blocked on this value being present in the environment (Pitfall 5: applying github-oidc with the placeholder default bakes a bogus ARN into the kms-sops-decrypt policies and every later apply flip-flops IAM).
  </action>
  <verify>
    <automated>bash -n scripts/setup-sops.sh && test -x scripts/setup-sops.sh && [ "$(aws kms describe-key --key-id alias/sops --profile klanker-terraform --region us-east-1 --query KeyMetadata.MultiRegion --output text)" = "False" ] && grep -q 'arn:aws:kms:us-east-1:052251888500' .sops.yaml && grep -Eq '^export TF_VAR_SOPS_KMS_KEY_ID=[0-9a-f-]{36}$' infra/.envrc && gh variable list --repo whereiskurt/klanker-voice | grep -q TF_VAR_SOPS_KMS_KEY_ID</automated>
  </verify>
  <acceptance_criteria>
    - alias/sops resolves to a key with MultiRegion=False (plain UUID id, not an mrk- id) — D-03's single-region simplification
    - .sops.yaml has exactly one kms ARN (us-east-1); no other regional ARNs
    - TF_VAR_SOPS_KMS_KEY_ID present in infra/.envrc and as a repo variable
    - Second script run is a no-op reusing the same key id
  </acceptance_criteria>
  <done>SOPS can encrypt against a real key; the key id is persisted everywhere the github-oidc policies will read it, unblocking Plan 06's ordering constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate /kmv/bootstrap/* into .secrets.sops.json; generate Phase 3 secrets; verified round-trip (INFR-05)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "SOPS Setup" step 5 (exact migration flow, elevenlabs tolerance) and "Runtime State Inventory" stored-data row
    - .planning/phases/02-infra-skeleton/02-CONTEXT.md — Specific Ideas (the three bootstrap params; ElevenLabs "in progress")
  </read_first>
  <files>infra/terraform/live/site/.secrets.sops.json</files>
  <action>
    Build the real encrypted secrets file. Secret-handling discipline throughout: umask 077 before creating any working file; the working plaintext lives ONLY in the session scratchpad directory (never under the repo); no secret value is ever echoed, logged, or included in a commit message; delete working files with rm -P (or shred equivalent) when done.

    1. Start from the template shape (six objects). For each of the three provider params — the deepgram, anthropic, and elevenlabs api-key entries under the /kmv/bootstrap/ SSM namespace — fetch with `aws ssm get-parameter --with-decryption --profile klanker-application --region us-east-1 --query Parameter.Value --output text` and place the value into the working JSON with jq (values passed via --arg from command substitution, not shell history). If the elevenlabs parameter does not exist yet (ParameterNotFound — CONTEXT says it is in progress), leave that entry's placeholder value and continue; record in the SUMMARY that the user fills it later via `sops edit infra/terraform/live/site/.secrets.sops.json` followed by re-applying the secrets unit. Any OTHER fetch failure is a hard stop.
    2. Generate real values for the Phase 3 paths so no placeholder string ever reaches SSM: jwt.secret and jwt.internal_secret each `openssl rand -hex 32`; oidc.cookie_keys `openssl rand -hex 32`; altcha.secret `openssl rand -hex 32`.
    3. Encrypt: `sops encrypt <workfile> > infra/terraform/live/site/.secrets.sops.json` (creation rule from Task 1 selects the key). Delete the working plaintext AND the temporary Plan 02 fallback file infra/terraform/live/site/.secrets.json (its placeholder job is done; the encrypted file now feeds site.hcl's parse-time decrypt).
    4. Round-trip verification without exposing values: `sops --decrypt infra/terraform/live/site/.secrets.sops.json | jq -S 'keys'` must list exactly altcha, anthropic, deepgram, elevenlabs, jwt, oidc; additionally verify each provider api_key decrypts non-empty via a jq length check (numbers only, never the value).
    5. Confirm site.hcl still parses: from infra/terraform/live/site run `terragrunt run --all validate` (now exercising the real sops decrypt path instead of the fallback).
    6. Commit the encrypted file. Do NOT delete the /kmv/bootstrap/* SSM params — deletion happens in Plan 05 only after the secrets module apply proves the SSM round-trip (research step 5's "ONLY THEN" rule).
  </action>
  <verify>
    <automated>test ! -f infra/terraform/live/site/.secrets.json && git ls-files --error-unmatch infra/terraform/live/site/.secrets.sops.json >/dev/null && grep -q '"sops"' infra/terraform/live/site/.secrets.sops.json && [ "$(sops --decrypt infra/terraform/live/site/.secrets.sops.json | jq -r 'keys | join(",")')" = "altcha,anthropic,deepgram,elevenlabs,jwt,oidc" ] && [ "$(sops --decrypt infra/terraform/live/site/.secrets.sops.json | jq '.deepgram.api_key | length')" -gt 10 ]</automated>
  </verify>
  <acceptance_criteria>
    - Encrypted file committed; contains sops metadata + the us-east-1 KMS ARN; plaintext fallback file deleted
    - Decrypt round-trip lists exactly the six objects; deepgram + anthropic api_key values are non-trivially long (length check only — values never printed)
    - elevenlabs entry either migrated or documented-as-pending in the SUMMARY (missing param tolerated)
    - jwt/oidc/altcha values are freshly generated random hex, not template placeholders
    - /kmv/bootstrap/* params still exist (deletion deferred to Plan 05)
  </acceptance_criteria>
  <done>INFR-05's SOPS hop is real: provider keys are encrypted-at-rest in the public repo, decryptable only via the KMS key, and the tree plans against them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSM bootstrap params → local plaintext → SOPS file | secret values transit the local machine during migration |
| public repo → encrypted secrets file | world-readable ciphertext; KMS key policy is the only gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-07 | Information Disclosure | plaintext working file during migration | high | mitigate | umask 077, scratchpad-only location outside repo, rm -P after encrypt, values moved via jq --arg (no shell history echo), no value ever printed |
| T-2-08 | Information Disclosure | committed ciphertext in public repo | medium | mitigate | KMS-backed SOPS (never hand-rolled crypto, ASVS V6); decrypt requires kms:Decrypt on the 052251888500 key; round-trip verification uses jq keys/length only |
| T-2-09 | Tampering | premature bootstrap-param deletion losing the only key copy | medium | mitigate | Deletion explicitly deferred to Plan 05 after the SSM apply round-trip; elevenlabs absence tolerated without failing |
</threat_model>

<verification>
- KMS key single-region, alias/sops, id persisted to .envrc + repo variable (Pitfall 5 ordering satisfied for Plan 06)
- Encrypted file round-trips; six objects; no plaintext artifacts remain
- Schema push gate: not applicable — terraform-only phase, no ORM/schema files
</verification>

<success_criteria>
- `terragrunt run --all validate` green against the real encrypted file
- Everything committed except secrets values (which exist only as ciphertext)
</success_criteria>

<output>
Create `.planning/phases/02-infra-skeleton/02-03-SUMMARY.md` when done. MUST record: KMS key id, elevenlabs migration status (done or pending + the sops edit follow-up), confirmation that bootstrap params remain until Plan 05.
</output>
