---
phase: 02-infra-skeleton
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - infra/terraform/live/site/ (state only — applies; hcl fixes only if plans surface drift)
autonomous: true
requirements: [INFR-01, INFR-02]
must_haves:
  truths:
    - "Hosted zones auth.klankermaker.ai and voice.klankermaker.ai exist in 052251888500 with NS delegation records in the management zone Z036807010CWM2JH60RKQ (cross-account DNS, INFR-02)"
    - "ACM certificates for auth./voice. (app-account validated) and the site cert (mgmt-zone validated) reach ISSUED status — the Phase 2 TLS gate per research (no service answers yet)"
    - "VPC, subnets, ALB, and security groups — including the additive webrtc-udp SG — are applied in us-east-1"
  artifacts:
    - path: "terraform state: site unit (use1 global key)"
      provides: "zone_map output consumed by certs/email; NS delegation live"
    - path: "terraform state: region/us-east-1/certs"
      provides: "cert_map consumed by network ALB listener"
    - path: "terraform state: region/us-east-1/network"
      provides: "vpc/subnets/SG list (with webrtc-udp) + ALB consumed by ecs-cluster/ecs-service"
  key_links:
    - from: "site module (app acct zones)"
      to: "mgmt zone Z036807010CWM2JH60RKQ"
      via: "NS delegation records written through the klanker-management profile (HostedZoneAdmin, probed in Plan 01)"
      pattern: "NS"
    - from: "certs module"
      to: "app-account subdomain zones + mgmt zone"
      via: "ACM DNS validation records (site cert validates in mgmt zone)"
      pattern: "acm"
---

## Phase Goal

**As a** klanker-voice developer, **I want to** provision the complete kmv AWS foundation (DNS/TLS, data stores, secrets flow, container plumbing, and an OIDC-gated CI deploy path) as applied, verifiable cloud state, **so that** the auth and voice services in Phases 3–5 deploy onto proven infrastructure with no plaintext secrets and no long-lived keys.

<objective>
Apply the first three units in the research dependency order: site (cross-account zones + NS delegation) → certs (ACM ISSUED) → network (VPC/ALB/SGs). After this plan, voice.klankermaker.ai and auth.klankermaker.ai resolve via delegated NS and hold ISSUED certificates — INFR-02's Phase 2 gate (research validation map: cert ISSUED is the TLS proof until a service answers in Phase 3/4).

Purpose: The applied DNS/TLS/network slice everything regional (Plan 05) depends on.
Output: Live zones, delegation, ISSUED certs, VPC/ALB/SG stack.
</objective>

## Artifacts this plan produces

- AWS: hosted zones auth./voice.klankermaker.ai (052251888500) + NS delegation records (481723467561 zone)
- AWS: ACM certs — auth. (+SAN *.auth.), voice. (+SAN *.voice.), site cert (apex + wildcard + use1 SANs) — all ISSUED
- AWS: VPC 10.0.0.0/16, 2-AZ subnets, ALB, SG set including `webrtc-udp`
- Terraform state objects under tf-kmv-use1-<SGUID> for site/certs/network units

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-infra-skeleton/02-CONTEXT.md
@.planning/phases/02-infra-skeleton/02-RESEARCH.md
@.planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the site unit — cross-account zones + NS delegation (INFR-02)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "Cross-Account DNS & Certs" (mechanism + management-permission audit) and "Assumptions Log" A1
    - .planning/phases/02-infra-skeleton/02-ZONE-AUDIT.md — collision check results from Plan 01 (no pre-existing auth./voice. NS records expected)
  </read_first>
  <files>infra/terraform/live/site/ (site unit state)</files>
  <action>
    Preflight: `aws sts get-caller-identity` on all three profiles; on SSO failure create a dynamic auth checkpoint (`aws sso login --sso-session=Developer`) and retry. Load infra/.envrc.

    From infra/terraform/live/site, plan then apply the SITE root unit only (`terragrunt plan` / `terragrunt apply` in the root unit directory — not run --all). Review the plan first: expected resources are exactly two hosted zones in the app account, NS delegation records into the mgmt zone, and any site-module baseline resources; if the plan wants to MODIFY or DELETE any pre-existing mgmt-zone record (zone audit said none expected), STOP and surface to the user rather than applying.

    Post-apply verification with propagation tolerance: `aws route53 list-hosted-zones --profile klanker-application` shows both subdomain zones; `aws route53 list-resource-record-sets --hosted-zone-id Z036807010CWM2JH60RKQ --profile klanker-management` contains NS record sets for auth.klankermaker.ai and voice.klankermaker.ai; then `dig +short NS auth.klankermaker.ai` in a bounded retry loop (up to ~5 minutes) until non-empty — if public resolution lags beyond the loop, verify authoritatively by querying one of the mgmt zone's own NS servers directly and note propagation in the SUMMARY.
  </action>
  <verify>
    <automated>aws route53 list-hosted-zones --profile klanker-application --query 'HostedZones[].Name' --output text | grep -q 'auth.klankermaker.ai' && aws route53 list-resource-record-sets --hosted-zone-id Z036807010CWM2JH60RKQ --profile klanker-management --query "ResourceRecordSets[?Type=='NS'].Name" --output text | grep -q 'voice.klankermaker.ai'</automated>
  </verify>
  <acceptance_criteria>
    - Both subdomain zones exist in 052251888500; NS delegation for both lives in the mgmt zone (cross-account write succeeded — A1 resolved affirmatively)
    - dig resolves NS for at least one subdomain (or authoritative-NS query documented if public propagation lagged)
    - No unrelated mgmt-zone records were modified
  </acceptance_criteria>
  <done>Cross-account DNS is live: the app account owns auth./voice. zones and the world can find them via delegation.</done>
</task>

<task type="auto">
  <name>Task 2: Apply certs — ACM ISSUED for auth./voice./site certificates (INFR-02)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "Cross-Account DNS & Certs" certs-module paragraph (which cert validates in which zone)
  </read_first>
  <files>infra/terraform/live/site/region/us-east-1/certs/ (state)</files>
  <action>
    From region/us-east-1/certs, terragrunt plan then apply. The module requests: auth.klankermaker.ai + SAN wildcard, voice.klankermaker.ai + SAN wildcard (both DNS-validated in the app-account zones created in Task 1), and the site cert for klankermaker.ai + SANs (validated via records in the mgmt zone — the second cross-account write path).

    ACM DNS validation is asynchronous: after apply, poll `aws acm list-certificates --profile klanker-application --region us-east-1 --query "CertificateSummaryList[?Status=='ISSUED'].DomainName"` in a bounded loop (up to ~15 minutes; validation typically completes in 2-5 once delegation resolves). If a certificate sticks in PENDING_VALIDATION beyond the window, check that its validation CNAME landed in the right zone (describe-certificate → resource record → list-resource-record-sets) before retrying — do not delete/recreate blindly.
  </action>
  <verify>
    <automated>aws acm list-certificates --profile klanker-application --region us-east-1 --query "CertificateSummaryList[?Status=='ISSUED'].DomainName" --output text | grep -q 'auth.klankermaker.ai' && aws acm list-certificates --profile klanker-application --region us-east-1 --query "CertificateSummaryList[?Status=='ISSUED'].DomainName" --output text | grep -q 'voice.klankermaker.ai'</automated>
  </verify>
  <acceptance_criteria>
    - auth. and voice. certificates ISSUED (INFR-02's valid-TLS gate for Phase 2 — research validation map notes no service answers yet, so ISSUED status is the proof)
    - Site cert (klankermaker.ai) ISSUED via mgmt-zone validation records
  </acceptance_criteria>
  <done>Valid TLS material exists for both service hostnames; the ALB listener (network) and Phase 3/4 services consume cert_map directly.</done>
</task>

<task type="auto">
  <name>Task 3: Apply network — VPC/ALB/SGs including the webrtc-udp groundwork SG</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "WebRTC Groundwork" (SG rationale; services disabled so nothing attaches yet)
    - .planning/phases/02-infra-skeleton/02-02-SUMMARY.md — the exact network-module diff (webrtc SG + output) and ALB idle-timeout disposition
  </read_first>
  <files>infra/terraform/live/site/region/us-east-1/network/ (state)</files>
  <action>
    From region/us-east-1/network, terragrunt plan then apply. Expected: VPC 10.0.0.0/16, two AZs of public/private subnets, NAT, ALB (attached to the Task 2 certs via cert_map dependency), flow logs, VPC endpoints, and the SG set — now including the additive webrtc-udp SG from Plan 02 (wide UDP ingress; attached to nothing while ecs_services.enabled=false, so zero live exposure this phase).

    Post-apply: confirm the webrtc SG exists and carries the UDP rule; confirm the ALB is provisioned and its HTTPS listener uses an ISSUED cert.
  </action>
  <verify>
    <automated>aws ec2 describe-security-groups --profile klanker-application --region us-east-1 --filters Name=group-name,Values='*webrtc*' --query 'SecurityGroups[0].IpPermissions[0].IpProtocol' --output text | grep -q udp && aws elbv2 describe-load-balancers --profile klanker-application --region us-east-1 --query 'LoadBalancers[0].State.Code' --output text | grep -q active</automated>
  </verify>
  <acceptance_criteria>
    - VPC/subnets/ALB active; ALB HTTPS listener bound to an ISSUED certificate
    - webrtc-udp SG exists with a UDP ingress permission (structure shipped; INFR-03 verification stays Phase 4 per roadmap)
    - No ECS service/task resources created (flags still false)
  </acceptance_criteria>
  <done>The network slice is applied and verifiable; Plan 05's regional units have every dependency output they need.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app account → mgmt account Route53 | writes limited to NS delegation + cert-validation + (later) SES records via HostedZoneAdmin |
| internet → ALB / (future) task ENIs | ALB TLS-only; webrtc SG attached to nothing this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-10 | Elevation of Privilege | cross-account writes beyond Route53 | medium | mitigate | Research management-permission audit verified only Route53 data/record resources use mgmt providers; plan review stops on any unexpected mgmt-account resource; HostedZoneAdmin cannot exceed Route53 anyway |
| T-2-11 | Tampering | NS delegation clobbering existing mgmt-zone records | high | mitigate | Plan-before-apply review with explicit STOP on modify/delete of pre-existing records; zone audit (Plan 01) established the baseline |
| T-2-12 | Denial of Service | wide-UDP SG exposure | medium | accept | SG attached to zero ENIs this phase (ecs_services disabled); Phase 4 attaches + tightens (sysctl/narrow range path documented in research) |
</threat_model>

<verification>
- INFR-02 gate: NS delegation resolves + certs ISSUED for both hostnames
- Applied state matches the plan-time expectations from Plan 02's run --all plan
- Schema push gate: not applicable — terraform-only phase, no ORM/schema files
</verification>

<success_criteria>
- site, certs, network units applied clean; all automated verifies green; no manual mgmt-console action was needed (HostedZoneAdmin sufficed, A1 closed)
</success_criteria>

<output>
Create `.planning/phases/02-infra-skeleton/02-04-SUMMARY.md` when done. MUST record: zone IDs, cert ARNs, ALB DNS name, webrtc SG id, propagation observations.
</output>
