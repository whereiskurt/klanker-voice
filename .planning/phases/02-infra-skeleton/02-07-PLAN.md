---
phase: 02-infra-skeleton
plan: 07
type: execute
wave: 7
depends_on: ["02-06"]
files_modified:
  - .github/workflows/terragrunt-plan.yml
  - .github/workflows/terragrunt-apply.yml
  - .github/workflows/build-voice.yml
  - .github/workflows/build-auth.yml
  - .github/workflows/deploy.yml
  - .github/workflows/gitleaks-scan.yml
autonomous: true
requirements: [INFR-07]
must_haves:
  truths:
    - "Push-to-main CI is path-filtered per D-08: infra/** triggers plan-only (kmv-github-readonly, environment terraform-plan); applies run only via the terraform-apply environment gate (kmv-github-terragrunt); apps/voice/** and apps/auth/** trigger their build workflows"
    - "A real workflow run assumed an OIDC role from GitHub Actions with zero long-lived AWS keys and completed a terragrunt plan (INFR-07 end-to-end proof)"
    - "gitleaks scanning runs in CI (public-repo hygiene, research Security Domain)"
  artifacts:
    - path: ".github/workflows/terragrunt-plan.yml"
      provides: "OIDC readonly plan on PRs + infra/** pushes; tool pins tg 0.97.1 / tf 1.14.3 / sops 3.11.0"
    - path: ".github/workflows/terragrunt-apply.yml"
      provides: "Human-gated apply (environment terraform-apply, kmv-github-terragrunt)"
    - path: ".github/workflows/build-voice.yml"
      provides: "Path-filtered voice image build/push via kmv-github-release (inert until Phase 4 image exists)"
    - path: ".github/workflows/build-auth.yml"
      provides: "Path-filtered auth image build/push via kmv-github-release (inert until Phase 3)"
    - path: ".github/workflows/deploy.yml"
      provides: "ECS update workflow via kmv-github-deploy (inert until services enabled — Phase 3 flips flags only)"
    - path: ".github/workflows/gitleaks-scan.yml"
      provides: "Secret-leak scanning on push/PR"
  key_links:
    - from: ".github/workflows/*.yml"
      to: "kmv-github-{readonly,terragrunt,release,deploy} roles"
      via: "aws-actions/configure-aws-credentials with role-to-assume + id-token: write (no static keys)"
      pattern: "role-to-assume"
    - from: "workflow env blocks"
      to: "infra/.envrc values"
      via: "repo variables (SITE_LABEL, SGUID, AWS_ACCOUNT_ID, TF_VAR_*) reconstructing TG_BUCKET_USE1/TG_TABLE_USE1 (D-06 CI mirror)"
      pattern: "TG_BUCKET_USE1"
---

<!-- planner-discipline-allow: dc34 -->
<!-- planner-discipline-allow: defcon -->

## Phase Goal

**As a** klanker-voice developer, **I want to** provision the complete kmv AWS foundation (DNS/TLS, data stores, secrets flow, container plumbing, and an OIDC-gated CI deploy path) as applied, verifiable cloud state, **so that** the auth and voice services in Phases 3–5 deploy onto proven infrastructure with no plaintext secrets and no long-lived keys.

<objective>
Deliver the CI half of INFR-07 per D-08: clone/adapt the two terragrunt workflows from the source repo, write lean build/deploy workflows (Claude's-discretion structure per CONTEXT, as long as path filtering + the infra plan-gate hold), add gitleaks for the public repo, then prove the whole chain by triggering a real run that assumes kmv-github-readonly via OIDC and plans.

Purpose: INFR-07's behavioral proof — GitHub Actions deploys with no long-lived AWS keys.
Output: Six workflows on main, one green (or documented-partially-red per Pitfall 8) plan run.
</objective>

## Artifacts this plan produces

- `.github/workflows/terragrunt-plan.yml`, `terragrunt-apply.yml`, `build-voice.yml`, `build-auth.yml`, `deploy.yml`, `gitleaks-scan.yml`
- One executed workflow run proving OIDC role assumption from Actions

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-infra-skeleton/02-CONTEXT.md
@.planning/phases/02-infra-skeleton/02-RESEARCH.md
@.planning/phases/02-infra-skeleton/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Terragrunt plan + apply workflows (clone/adapt per research table)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — "GitHub Actions (workflows to clone/adapt)" table (per-file adaptations) and the common-workflow-env paragraph beneath it
    - /Users/khundeck/working/defcon.run.34/.github/workflows/terragrunt-plan.yml and terragrunt-apply.yml — the sources being adapted
  </read_first>
  <files>.github/workflows/terragrunt-plan.yml, .github/workflows/terragrunt-apply.yml</files>
  <action>
    Adapt the two source terragrunt workflows exactly per the research table:

    terragrunt-plan.yml — role becomes kmv-github-readonly (constructed from vars.AWS_ACCOUNT_ID + role name, matching the source's construction pattern); environment terraform-plan; triggers become pull_request plus push-to-main with paths infra/** (D-08: any infra change plans — widen the source's VERSION-file filter); keep the tool-cache pins terragrunt 0.97.1 / terraform 1.14.3 / sops 3.11.0; reduce the env block to single-region (TG_BUCKET_USE1/TG_TABLE_USE1 reconstructed from vars.SITE_LABEL + vars.SGUID; drop the other-region exports); env carries TF_VAR_APPLICATION_ACCOUNT_ID from vars.AWS_ACCOUNT_ID, TF_VAR_MANAGEMENT_ACCOUNT_ID, TF_VAR_GITHUB_ORG from github.repository_owner, TF_VAR_SOPS_KMS_KEY_ID and TF_VAR_SSM_KMS_KEY_ARNS from vars; permissions id-token write, contents read.

    terragrunt-apply.yml — role kmv-github-terragrunt; environment terraform-apply (the required-reviewer gate from Plan 06 makes every CI apply human-approved — this workflow IS D-08's manual apply approval); keep workflow_dispatch + workflow_call triggers; same single-region env reduction and tool pins.

    Neither workflow may contain any AWS access key material or the source site's role/label names — scrub every occurrence while adapting.
  </action>
  <verify>
    <automated>python3 -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('.github/workflows/terragrunt-*.yml')]" && grep -q 'kmv-github-readonly' .github/workflows/terragrunt-plan.yml && grep -q 'terraform-apply' .github/workflows/terragrunt-apply.yml && grep -q 'id-token' .github/workflows/terragrunt-plan.yml && ! grep -riq 'AWS_SECRET_ACCESS_KEY\|AKIA' .github/workflows/ && ! grep -riq 'dc34\|defcon' .github/workflows/</automated>
  </verify>
  <acceptance_criteria>
    - Both workflows parse as valid YAML; plan uses readonly role + terraform-plan env; apply uses terragrunt role + terraform-apply env (gated)
    - infra/** path trigger on plan; workflow_dispatch on apply; tool pins match research (tg 0.97.1 / tf 1.14.3 / sops 3.11.0)
    - Zero long-lived-credential references; zero source-site strings
  </acceptance_criteria>
  <done>The plan/apply split D-08 demands exists as code, wired to the Plan 06 roles and environments.</done>
</task>

<task type="auto">
  <name>Task 2: Lean build-voice / build-auth, trimmed deploy, gitleaks (D-08 path filters)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — workflows table rows for buildpub.yml (recommendation: do NOT clone wholesale — write lean builds), deploy.yml, gitleaks-scan.yml
    - /Users/khundeck/working/defcon.run.34/.github/workflows/deploy.yml and gitleaks-scan.yml — sources for the trim/clone
  </read_first>
  <files>.github/workflows/build-voice.yml, .github/workflows/build-auth.yml, .github/workflows/deploy.yml, .github/workflows/gitleaks-scan.yml</files>
  <action>
    build-voice.yml and build-auth.yml — written fresh (research recommends against cloning the 879-line source build machinery; structure is Claude's discretion per CONTEXT as long as D-08 holds): trigger on push to main with paths apps/voice/** (respectively apps/auth/**); permissions id-token write; OIDC-assume kmv-github-release; docker build from the app directory and push to the matching ECR repo (voice-app / auth-app) tagged with the git SHA; then invoke deploy.yml via workflow_call. Both are expected to be inert no-ops today (no Dockerfiles under apps/*/ yet — Phase 1 is local-only Python; Phases 3/4 add the Dockerfiles and these workflows light up unchanged). Guard gracefully: skip the build job if the app directory lacks a Dockerfile.

    deploy.yml — clone the source and trim to single region: OIDC-assume kmv-github-deploy, terragrunt apply limited to the ecs-task/ecs-service units. Inert until Phase 3 flips ecs_tasks/ecs_services to enabled (the exclude blocks make it a clean no-op — ship it now so Phase 3 changes site.hcl flags only).

    gitleaks-scan.yml — clone from source (cheap, and D-07's public repo makes secret scanning non-optional per research Security Domain).

    Commit and push all workflows from Tasks 1+2 to main.
  </action>
  <verify>
    <automated>python3 -c "import yaml,glob; fs=glob.glob('.github/workflows/*.yml'); assert len(fs)>=6, fs; [yaml.safe_load(open(f)) for f in fs]" && grep -q 'apps/voice/\*\*' .github/workflows/build-voice.yml && grep -q 'apps/auth/\*\*' .github/workflows/build-auth.yml && grep -q 'kmv-github-release' .github/workflows/build-voice.yml && grep -q 'kmv-github-deploy' .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - Six valid workflows on main; path filters exactly per D-08 (apps/voice/**, apps/auth/**, infra/**)
    - Build workflows assume kmv-github-release, deploy assumes kmv-github-deploy; Dockerfile-absence guard present
    - gitleaks workflow runs on push/PR
  </acceptance_criteria>
  <done>The full CI surface is committed; Phases 3/4 activate builds/deploys by adding Dockerfiles and flipping site.hcl flags — no workflow edits needed.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end OIDC proof — trigger terragrunt-plan and verify role assumption (INFR-07)</name>
  <read_first>
    - .planning/phases/02-infra-skeleton/02-RESEARCH.md — Pitfall 8 (expected red on mgmt-provider units if the delegate role is deferred) and the Validation Architecture INFR-07 row
    - .planning/phases/02-infra-skeleton/02-06-SUMMARY.md — delegate-role status (created vs deferred) — determines the expected outcome
  </read_first>
  <files>(no file changes — verification run)</files>
  <action>
    Trigger the plan workflow: `gh workflow run terragrunt-plan.yml --repo whereiskurt/klanker-voice` (or push a trivial infra/** doc change if the workflow lacks workflow_dispatch — add the dispatch trigger in that case). Watch with `gh run watch` / `gh run view --log`.

    Success evaluation depends on the Plan 06 checkpoint outcome:
    - Delegate role CREATED: the run must be fully green — OIDC assumption of kmv-github-readonly, backend init against tf-kmv state, all units plan (mgmt-provider data lookups succeed through the delegate).
    - Delegate role DEFERRED: the run must show successful OIDC role assumption and clean plans on app-account-only units (ecr, dynamodb, secrets, ecs-cluster, network), with AccessDenied confined to mgmt-provider units (site, certs, email, dmarc) — exactly the Pitfall 8 signature. Capture the log lines proving role assumption (the configure-aws-credentials step and a successful sts identity) and record the partial-red state as the documented expected outcome with the delegate follow-up.

    Either way, verify from the log that credentials came from OIDC (assumed-role ARN contains kmv-github-readonly) and no step referenced a static key. Record the run URL and conclusion in the SUMMARY.
  </action>
  <verify>
    <automated>gh run list --repo whereiskurt/klanker-voice --workflow terragrunt-plan.yml --limit 1 --json databaseId,status --jq '.[0].status' | grep -q completed && gh run view --repo whereiskurt/klanker-voice "$(gh run list --repo whereiskurt/klanker-voice --workflow terragrunt-plan.yml --limit 1 --json databaseId --jq '.[0].databaseId')" --log | grep -q 'kmv-github-readonly'</automated>
  </verify>
  <acceptance_criteria>
    - A completed terragrunt-plan run exists whose log shows assumption of kmv-github-readonly via OIDC (INFR-07's no-long-lived-keys proof)
    - Outcome matches the delegate-role status: fully green, or app-account-green with mgmt-unit AccessDenied documented as the expected Pitfall 8 state plus follow-up note
  </acceptance_criteria>
  <done>INFR-07 verified end-to-end: GitHub Actions plans kmv infrastructure using federated, short-lived credentials, with applies gated behind a human-approved environment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public-repo PRs / forks → CI workflows | untrusted code can open PRs; must never reach write roles or secrets |
| CI runners → AWS | short-lived OIDC creds only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-21 | Elevation of Privilege | PR-triggered workflow escalating to apply | high | mitigate | plan workflow uses readonly role only; apply requires workflow_dispatch + terraform-apply environment with required reviewers (Plan 06); deploy/release roles branch-restricted to main |
| T-2-22 | Information Disclosure | secret leaked via workflow logs or committed file | high | mitigate | gitleaks-scan.yml on push/PR; workflows carry no secret env values (only vars.* non-secrets); sops decrypt in CI uses KMS via role, never a pasted key |
| T-2-23 | Tampering | workflow file modified in a PR to exfiltrate OIDC token | medium | accept | Inherent to public-repo Actions; blast radius = readonly role (plan/read only); write paths gated by environment protection which PRs cannot satisfy — documented |
</threat_model>

<verification>
- D-08 fully encoded: path filters, plan-only infra CI, environment-gated applies
- INFR-07 behavioral proof captured (run URL + role-assumption log)
- Schema push gate: not applicable — terraform-only phase, no ORM/schema files
</verification>

<success_criteria>
- Six workflows live on main; verification run completed with the delegate-status-appropriate outcome; SUMMARY records run URL and any deferred follow-ups (delegate role, elevenlabs key)
</success_criteria>

<output>
Create `.planning/phases/02-infra-skeleton/02-07-SUMMARY.md` when done. MUST record: run URL/conclusion, role-assumption evidence, and the consolidated list of Phase 2 open follow-ups (delegate role if deferred, elevenlabs sops edit if pending) for /gsd-verify-work.
</output>
