---
phase: 11-voip-ms-telephony-local-asterisk-edge
plan: 07
type: execute
wave: 5
depends_on: [11-02, 11-05, 11-06]
files_modified:
  - apps/voice/src/klanker_voice/telephony/__main__.py
  - apps/voice/configs/telephony.toml
  - apps/voice/asterisk/docker-compose.yml
  - apps/voice/asterisk/sipp/gate-pass.xml
  - apps/voice/asterisk/sipp/fixtures/README.md
  - apps/voice/asterisk/README.md
  - apps/voice/tests/test_telephony_integration.py
autonomous: false
requirements: [D-08, D-07]
user_setup:
  - service: local-softphone
    why: "The §19-C exit criterion (real pipeline, greeting-not-clipped, converse, interrupt, clean hangup) is a manual human-run softphone proof (D-07); CI covers only the deterministic fake-media tier"
    dashboard_config:
      - task: "Install a SIP softphone (e.g. Linphone/baresip), register as the dev-softphone endpoint, and run the documented §19-C call recipe against test provider credentials"
        location: "apps/voice/asterisk/README.md → Manual §19-C softphone proof"
must_haves:
  truths:
    - "python -m klanker_voice.telephony.controller (or the telephony __main__) runs the ARI controller as its own process: loads cfg + telephony_cfg, constructs AriClient + AsteriskCallController, connects the events WS, dispatches events — no FastAPI, never touching webrtc.py/server.py (D-08)"
    - "A deterministic CI integration test drives SIP INVITE → Asterisk → fake Klanker media transport and asserts the §16/§17 lifecycle + gate behaviors without live Deepgram/Anthropic/ElevenLabs keys (D-07)"
    - "The §19-C manual softphone proof (real pipeline, greeting-not-clipped, converse, interrupt, clean hangup, through the gate) is documented in the harness README + surfaced as a human checkpoint (D-07)"
  artifacts:
    - apps/voice/src/klanker_voice/telephony/__main__.py
    - apps/voice/configs/telephony.toml
    - apps/voice/asterisk/sipp/gate-pass.xml
    - apps/voice/tests/test_telephony_integration.py
  key_links:
    - "telephony __main__ → load_config + load_telephony_config + AriClient(env creds) + AsteriskCallController → ari.run()"
    - "SIPp INVITE/200/ACK/pcap-audio/BYE scenario ↔ docker-compose Asterisk ↔ controller-under-test with a fake media transport asserting on ActiveCall/CallSession lifecycle"
  prohibitions:
    - "The CI integration test MUST NOT require live provider API keys (fake media/pipeline only, §16/D-07)"
    - "MUST NOT claim CI covers the §19-C perceptual greeting-not-clipped proof — that stays an explicitly documented manual step"
    - "The entrypoint MUST NOT import or start the FastAPI browser server (D-08)"
---

<objective>
Ship the standalone telephony entrypoint (D-08) and close the D-07 test story: a deterministic SIPp + docker-compose fake-media integration test as the CI artifact, plus a documented manual §19-C softphone proof surfaced as a human checkpoint.

Purpose: The controller runs as its own process (mirrors the eventual §15 telephony-edge deploy isolation) and reuses factories/pipeline/call_runtime in-process — never wired into the browser server. The §19-C exit criterion (perceptual greeting-not-clipped through the gate) is honestly split: CI proves the deterministic lifecycle/gate logic; a human proves the real-pipeline call.
Output: `telephony/__main__.py` entrypoint; `configs/telephony.toml` harness config (enabled=true); SIPp scenario + docker-compose SIPp service; the fake-media integration test; README manual proof + a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-voip-ms-telephony-local-asterisk-edge/11-CONTEXT.md
@.planning/phases/11-voip-ms-telephony-local-asterisk-edge/11-RESEARCH.md
@apps/voice/server.py
@apps/voice/src/klanker_voice/telephony/controller.py
@apps/voice/src/klanker_voice/telephony/ari.py
@apps/voice/asterisk/docker-compose.yml
@apps/voice/tests/test_telephony_lifecycle.py
</context>

<artifacts>
New symbols/files this plan produces:
- `apps/voice/src/klanker_voice/telephony/__main__.py` — `main()` async entrypoint + `python -m klanker_voice.telephony.controller` runnability
- `apps/voice/configs/telephony.toml` — harness config with `[telephony] enabled = true` (+ the base STT/LLM/TTS/persona tables the entrypoint needs)
- `apps/voice/asterisk/sipp/gate-pass.xml` — SIPp INVITE→200→ACK→pcap-audio→BYE scenario
- `apps/voice/asterisk/sipp/fixtures/README.md` — how to record the passphrase/DTMF pcap fixtures
- `apps/voice/tests/test_telephony_integration.py` — deterministic fake-media integration test (CI)
- README "Manual §19-C softphone proof" section
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Standalone telephony entrypoint + harness config</name>
  <read_first>
    - apps/voice/server.py lines 1-72 (module structure: load_dotenv, config loads — the analog to mirror, but telephony is a separate process with NO FastAPI)
    - apps/voice/src/klanker_voice/telephony/controller.py (AsteriskCallController constructor) + ari.py (AriClient constructor + connect/run)
    - apps/voice/src/klanker_voice/telephony/config.py (load_telephony_config) + apps/voice/pipeline.toml [telephony]
    - 11-PATTERNS.md entrypoint section + 11-RESEARCH.md R6 entrypoint shape
  </read_first>
  <action>
    Create `apps/voice/src/klanker_voice/telephony/__main__.py` (so `python -m klanker_voice.telephony` runs; also add an `if __name__ == "__main__"` runnable path in `controller.py` per D-08's `python -m klanker_voice.telephony.controller` phrasing — pick one primary and document it in the README). `main()`: `load_dotenv(override=True)`; `load_config()` + `load_knowledge_config()` + `load_quota_config()` + `load_telephony_config()`; guard `if not telephony_cfg.enabled: log + return`; read `ASTERISK_ARI_URL/USERNAME/PASSWORD` from env (raise ConfigError if user/pass unset); construct `AriClient` + `AsteriskCallController`; register the controller's StasisStart/ChannelDtmfReceived/ChannelDestroyed handlers via `ari.on(...)`; `await ari.connect()` then `await ari.run()`. No FastAPI, no HTTP server. Create `apps/voice/configs/telephony.toml`: the base STT/LLM/TTS/persona tables the pipeline needs plus a `[telephony]` table with `enabled = true` (harness config, selected via `KLANKER_PIPELINE_CONFIG=configs/telephony.toml`); secrets stay in env. Never log credentials.
  </action>
  <verify>
    <automated>cd apps/voice && KLANKER_PIPELINE_CONFIG=configs/telephony.toml uv run python -c "import klanker_voice.telephony.__main__ as m; from klanker_voice.telephony.config import load_telephony_config; assert load_telephony_config().enabled is True; print('entrypoint import + harness config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m klanker_voice.telephony` imports and reaches the enabled-guard without starting FastAPI.
    - `configs/telephony.toml` loads with `[telephony] enabled = true` and the base pipeline tables.
    - `grep -rn 'fastapi\|server import\|webrtc' apps/voice/src/klanker_voice/telephony/__main__.py` finds no FastAPI/browser-server import (D-08).
  </acceptance_criteria>
  <done>Standalone entrypoint runs the ARI controller as its own process; harness config enabled; no browser-server coupling.</done>
</task>

<task type="auto">
  <name>Task 2: SIPp scenario + docker-compose SIPp service + deterministic fake-media integration test</name>
  <read_first>
    - 11-RESEARCH.md R4 (SIPp official Dockerfile + XML scenario INVITE→200→ACK→exec play_pcap_audio→BYE; assert on the Klanker side via the fake media transport; CI-automatable scope vs manual)
    - apps/voice/tests/test_telephony_lifecycle.py (FakeRtpMediaSession + fake AriClient patterns to reuse)
    - apps/voice/asterisk/docker-compose.yml (Plan 02 — extend with a SIPp service/profile)
  </read_first>
  <action>
    Author `apps/voice/asterisk/sipp/gate-pass.xml`: a SIPp scenario that sends INVITE, waits 200 OK, sends ACK, replays a short pcap of the 4-word passphrase (or DTMF PIN) via `play_pcap_audio`, holds a few seconds, then BYE. Add `apps/voice/asterisk/sipp/fixtures/README.md` explaining how to record/generate the passphrase and DTMF pcap fixtures (the fixtures themselves may be generated locally, not necessarily committed if large — document the recipe). Extend `docker-compose.yml` with a `sipp` service (pinned SIPp image or in-repo Dockerfile target, `profiles: [integration]` so it does not run on a plain `up`). Author `apps/voice/tests/test_telephony_integration.py` as the DETERMINISTIC CI artifact: drive `SIP client → Asterisk → fake Klanker media transport` at the controller level using the fake AriClient + FakeRtpMediaSession (no live Deepgram/Anthropic/ElevenLabs, no live keys), feeding synthetic transcription frames to exercise the gate, and assert the §16/§17 + gate behaviors: unlock via fake passphrase tokens → CallSession constructed + greet fired; fail-closed timeout → goodbye + hangup, no CallSession; ChannelDestroyed → exactly-once teardown, empty registry. Mark any test that requires a running docker Asterisk/SIPp with a skip-unless-available guard so the pure-fake tier always runs in CI.
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest tests/test_telephony_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The fake-media integration test runs green with NO provider API keys set.
    - It asserts: passphrase-unlock → greet path; fail-closed → goodbye+hangup no-CallSession; ChannelDestroyed → exactly-once teardown + empty registry.
    - Any Asterisk/SIPp-dependent case is guarded to skip when docker is unavailable (CI-safe).
  </acceptance_criteria>
  <done>Deterministic fake-media integration test is the CI artifact; SIPp scenario + compose service exist for the fuller local run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: §19-C real-softphone exit-criterion proof</name>
  <action>Pause for a human to run a real SIP softphone call end-to-end through the local Asterisk edge and confirm the §19-C perceptual criteria (silent answer → gate unlock → greeting-not-clipped → converse → interrupt → clean hangup, plus fail-closed + no-leak) that CI cannot judge.</action>
  <what-built>The full local Asterisk edge: docker-compose Asterisk, the standalone telephony controller process, the socket media session, and the §24 gate — end to end. CI has proven the deterministic lifecycle/gate logic with fake media; this checkpoint is the perceptual §19-C exit-criterion proof that cannot be automated without live provider keys and a human ear.</what-built>
  <how-to-verify>
    1. `cp apps/voice/asterisk/.env.example apps/voice/asterisk/.env` and fill ARI + SIP + PIN/passphrase values; ensure provider keys are in the voice `.env` (`make -C apps/voice env`).
    2. `cd apps/voice/asterisk && docker compose up`; confirm the `core show application Stasis` module smoke check passes.
    3. `cd apps/voice && KLANKER_PIPELINE_CONFIG=configs/telephony.toml ASTERISK_ARI_*=... TELEPHONY_ACCESS_PIN=... TELEPHONY_PASSPHRASE_WORDS='w1 w2 w3 w4' uv run python -m klanker_voice.telephony.controller`.
    4. Register a SIP softphone (Linphone/baresip) as the dev-softphone endpoint and place a call to the inbound extension.
    5. Confirm: the line is SILENT on answer (no greeting/LLM/TTS); speak the 4 passphrase words in any order (or enter the DTMF PIN); the agent then GREETS (greeting NOT clipped), holds a multi-turn conversation, can be INTERRUPTED (barge-in), and HANGS UP cleanly from the softphone.
    6. Confirm the fail-closed path: place a second call, stay silent past gate_window_seconds → hear a static goodbye + clean hangup, no open call.
    7. Confirm no leaks: after each call, the controller logs an exactly-once close and leaves no bridge/registry entry (check logs).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what clipped/leaked/failed</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Document the manual §19-C proof + module run recipe in the harness README</name>
  <read_first>
    - apps/voice/asterisk/README.md (Plan 02 placeholder "Manual §19-C softphone proof" section)
  </read_first>
  <action>
    Fill in the README's "Manual §19-C softphone proof" section with the exact recipe from the checkpoint above (compose up, run `python -m klanker_voice.telephony.controller` with the harness config + env secrets, register a softphone, the silent-answer → passphrase/PIN unlock → greeting-not-clipped → converse → interrupt → clean hangup script, plus the fail-closed and no-leak checks). Explicitly state that CI's `test_telephony_integration.py` covers the deterministic lifecycle/gate logic with fake media, and that the perceptual greeting-not-clipped judgment is this documented MANUAL step (never claimed as CI-covered). Cross-link the SIPp scenario for the semi-automated local run.
  </action>
  <verify>
    <automated>grep -qi 'Manual .19-C softphone proof' apps/voice/asterisk/README.md && grep -qi 'fake media' apps/voice/asterisk/README.md && echo README_OK</automated>
  </verify>
  <acceptance_criteria>
    - README documents the full manual §19-C recipe + the CI-vs-manual split honestly.
    - The module run command and env-secret sourcing are spelled out.
  </acceptance_criteria>
  <done>Harness README documents the manual §19-C proof and the honest CI/manual boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telephony controller process ↔ env secrets | ARI creds + PIN + passphrase words are read from env at process start |
| SIPp/softphone → Asterisk → controller | The integration harness exercises the same public-facing call path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07-01 | Information disclosure | entrypoint env handling | high | mitigate | Secrets read from env only, never logged, never written to configs/telephony.toml (behavior keys only); credential regex (Plan 01) backstops TOML |
| T-11-07-02 | Elevation of privilege | process isolation | medium | mitigate | Telephony runs as its own process; no FastAPI/browser-server import (D-08) — a telephony compromise does not reach the browser signaling surface |
| T-11-07-03 | Spoofing | CI test fidelity claim | medium | mitigate | Fake-media CI tier explicitly does NOT claim the perceptual §19-C proof; the human checkpoint + README keep the boundary honest (no false assurance) |
</threat_model>

<verification>
- `cd apps/voice && uv run pytest tests/test_telephony_integration.py -q` green with no provider keys.
- `python -m klanker_voice.telephony` reaches the enabled-guard; no FastAPI import present.
- Human checkpoint approved: real softphone call — silent answer → passphrase/PIN unlock → greeting-not-clipped → converse → interrupt → clean hangup; fail-closed goodbye; no leaks.
- `git diff --stat apps/voice/server.py apps/voice/src/klanker_voice/webrtc.py` zero changes (D-08).
</verification>

<success_criteria>
- Standalone telephony entrypoint runs the controller as its own process (D-08).
- Deterministic fake-media integration test is the CI artifact (D-07); manual §19-C softphone proof documented + human-verified (ROADMAP criterion 4).
- CI-vs-manual boundary stated honestly; no false "CI covers §19-C" claim.
</success_criteria>

<output>
Create `.planning/phases/11-voip-ms-telephony-local-asterisk-edge/11-07-SUMMARY.md` when done.
</output>
