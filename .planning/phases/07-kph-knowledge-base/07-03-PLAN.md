---
phase: 07-kph-knowledge-base
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - apps/voice/knowledge/topics/defcon-run-34.md
  - apps/voice/knowledge/topics/meshtk.md
  - apps/voice/knowledge/router/topic-map.yaml
  - apps/voice/knowledge/manifest.yaml
  - apps/voice/tests/test_knowledge_router_multitopic.py
  - apps/voice/scenarios/kph_knowledge_defconrun.yaml
  - apps/voice/scenarios/kph_knowledge_meshtk.yaml
autonomous: true
requirements: [PIPE-10]
user_setup: []

must_haves:
  truths:
    - "KPH answers a directed defcon.run.34 question and a directed meshtk question correctly, each from its own deep pack. [PIPE-10]"
    - "The router discriminates km vs defcon.run.34 vs meshtk; an overlapping term (e.g. 'toolkit') below the confidence floor triggers a which-did-you-mean / fallback rather than a confident wrong pick."
    - "Switching topics mid-session keeps system[0] cache-warm (cross-topic cache warmth) — only block1 changes."
  artifacts:
    - "apps/voice/knowledge/topics/defcon-run-34.md — defcon.run.34 deep pack (facts)"
    - "apps/voice/knowledge/topics/meshtk.md — meshtk deep pack (facts)"
    - "topic-map + manifest entries for defcon.run.34 and meshtk (3-topic launch set)"
    - "per-topic eval scenarios + a multi-topic router discrimination test"
  key_links:
    - "07-03 only APPENDS to topic-map.yaml + manifest.yaml (the N-topic schema 07-01 authored) — no router.py code change; classification is data-driven."
    - "The defcon/meshtk packs FRAME their (later-indexed) raw-code chunks so KPH narrates, not reads code (Amendment 5)."
    - "Wave-2 sibling 07-02 owns retrieval.py / prompt_assembly.py / router.py / pipeline.toml — this plan touches NONE of them (zero file overlap)."
---

<objective>
The multi-topic slice: add the other two launch topics (defcon.run.34 and meshtk) as curated
deep packs with router entries, so KPH covers the full 3-topic launch set and discriminates
between them safely — including the overlap cases where a term could mean more than one topic.
Coverage is km-heavy for launch depth; defcon.run.34 (2 clips) and meshtk (1 deep clip) get
solid curated packs now and deepen later via refresh (07-04) as more audio lands.

Purpose: Turn the single-topic walking slice into the real launch topic set (PIPE-10), proving
multi-topic discrimination + cross-topic cache warmth.
Output: two deep packs + two topic-map/manifest entries + per-topic eval scenarios + a
multi-topic router discrimination test.
</objective>

<user_story>
**As a** DEF CON attendee, **I want to** ask KPH about defcon.run.34 (or meshtk) and get the
right project's answer, **so that** the concierge is trustworthy across Kurt's whole world, not
just klanker-maker.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-kph-knowledge-base/07-DESIGN-NOTES.md
@.planning/phases/07-kph-knowledge-base/07-01-SUMMARY.md

# Content to PROMOTE (already-curated digests + transcript facts):
@.planning/phases/07-kph-knowledge-base/corpus/defcon-run-34-digest.md
@.planning/phases/07-kph-knowledge-base/corpus/meshtk-digest.md
@apps/voice/knowledge/style/kurt-humor-personality.md
# defcon.run.34 clips: right.literally.the.instance / lets.mix.itup (2 defcon-ish)
# meshtk clip: (the deep meshtk narration among the 14)
# The humor deck (kurt-humor-personality.md) also carries defcon.run.34 + meshtk FACTS —
# harvest speakable facts from it into these packs.

# Schema authored by 07-01 (APPEND to these, do not rewrite):
@apps/voice/knowledge/router/topic-map.yaml
@apps/voice/knowledge/manifest.yaml
@apps/voice/scenarios/kph_knowledge_km.yaml
</context>

<artifacts>
## Artifacts this plan produces
- `apps/voice/knowledge/topics/defcon-run-34.md` — defcon.run.34 deep pack.
- `apps/voice/knowledge/topics/meshtk.md` — meshtk deep pack.
- Appended topic-map.yaml entries (defcon.run.34, meshtk: keywords, spoken names, overlap disambiguation) + manifest.yaml topic entries (with per-source descriptors for later indexing: defcon infra/terraform + apps; meshtk README + Go source; the humor deck as a facts source).
- `apps/voice/scenarios/kph_knowledge_defconrun.yaml`, `apps/voice/scenarios/kph_knowledge_meshtk.yaml`.
- `apps/voice/tests/test_knowledge_router_multitopic.py`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Author defcon.run.34 + meshtk deep packs and append topic-map + manifest entries</name>
  <files>apps/voice/knowledge/topics/defcon-run-34.md, apps/voice/knowledge/topics/meshtk.md, apps/voice/knowledge/router/topic-map.yaml, apps/voice/knowledge/manifest.yaml</files>
  <read_first>
    - .planning/phases/07-kph-knowledge-base/corpus/defcon-run-34-digest.md + corpus/meshtk-digest.md (PROMOTE these curated digests)
    - apps/voice/knowledge/style/kurt-humor-personality.md (harvest speakable defcon.run.34 + meshtk facts — the deck is a facts source per the updated Amendment 4)
    - apps/voice/knowledge/topics/klanker-maker.md (match the voice-friendly pack shape 07-01 established, incl. a do-not-say section)
    - apps/voice/knowledge/manifest.yaml + router/topic-map.yaml (the N-topic schema to APPEND to)
    - 07-DESIGN-NOTES.md Amendment 5 (defcon source paths: infra/terraform/{live,modules,providers} + apps/ as CODE; meshtk: README.md primary + cmd/internal/pkg/protos Go source)
  </read_first>
  <action>
    Author knowledge/topics/defcon-run-34.md and knowledge/topics/meshtk.md by promoting the
    corresponding corpus digests and folding in speakable facts from the two defcon clips, the
    meshtk clip, and the humor deck (kurt-humor-personality.md now doubles as a defcon.run.34 +
    meshtk FACTS source). Keep them voice-friendly (D-08), story-rich, in the same shape as the km
    pack including a do-not-say section. Frame content so that when raw-code chunks are later
    retrieved (07-02/07-04), KPH NARRATES rather than reads code (Amendment 5 mitigation).

    APPEND (do not rewrite) two topic-map.yaml entries: defcon.run.34 and meshtk, each with id,
    spoken_name (use TTS-safe forms — "DEFCON dot run, thirty-four" and "mesh T K"), a weighted
    keyword/alias list with ASR-friendly variants, and a one-line hook. Handle overlap explicitly:
    ambiguous terms (e.g. "toolkit", "run", "34") must not push a single topic over the floor on
    their own — rely on the confidence_floor + fallback 07-01 built. APPEND matching manifest.yaml
    topic entries with per-source descriptors for the later index build: defcon.run.34 →
    /Users/khundeck/working/defcon.run.34/infra/terraform/{live,modules,providers} + apps/ (kind:
    code) with a skip-on-missing note; meshtk → /Users/khundeck/working/meshtk/README.md (kind:
    docs, primary) + cmd/ internal/ pkg/ protos/ (kind: code); plus the humor deck as a facts
    source. Update tour_priority to include the 3 launch topics (D-05).
  </action>
  <verify>
    <automated>cd apps/voice && .venv/bin/python -c "import yaml; t=yaml.safe_load(open('knowledge/router/topic-map.yaml')); ids={x['id'] for x in t['topics']}; assert {'klanker-maker','defcon.run.34','meshtk'} <= ids, ids; m=yaml.safe_load(open('knowledge/manifest.yaml')); mids={x['id'] for x in m['topics']}; assert {'klanker-maker','defcon.run.34','meshtk'} <= mids, mids; print('3-topic launch set OK')"</automated>
    <automated>cd apps/voice && test -s knowledge/topics/defcon-run-34.md && test -s knowledge/topics/meshtk.md && echo "packs present"</automated>
  </verify>
  <done>defcon.run.34 + meshtk deep packs exist as voice-friendly versioned markdown with do-not-say sections; topic-map.yaml + manifest.yaml carry all three launch topics with overlap-aware keywords and per-source descriptors; tour_priority covers the launch set.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-topic router discrimination test + per-topic evals + cross-topic cache warmth proof</name>
  <files>apps/voice/tests/test_knowledge_router_multitopic.py, apps/voice/scenarios/kph_knowledge_defconrun.yaml, apps/voice/scenarios/kph_knowledge_meshtk.yaml</files>
  <read_first>
    - apps/voice/tests/test_knowledge_router.py (the classify() test surface from 07-01 to extend)
    - apps/voice/scenarios/kph_knowledge_km.yaml (per-topic scenario template)
    - apps/voice/scenarios/kph_cache_verify.yaml (cache-read assertion surface for the cross-topic warmth proof)
  </read_first>
  <behavior>
    - Test: classify() routes directed km / defcon.run.34 / meshtk utterances each to the correct topic.
    - Test: an overlapping/ambiguous utterance ("tell me about the toolkit") scores below the floor → fallback/which-did-you-mean, never a confident single pick (overlap guard).
    - Test: an ASR-garbled spoken variant of each topic name still resolves (keyword aliases).
    - Scenario kph_knowledge_defconrun + kph_knowledge_meshtk: each directed question judged factually correct from its pack.
    - Cross-topic cache warmth: after a km turn then a defcon turn, system[0] is unchanged → cache_read_input_tokens > 0 on the defcon turn (only block1 swapped).
  </behavior>
  <action>
    Add test_knowledge_router_multitopic.py exercising the three-way discrimination, the overlap
    guard (a deliberately ambiguous term stays below the floor), and ASR-variant resolution — all
    data-driven against the appended topic-map (no router.py change). Author the two per-topic
    scenarios mirroring kph_knowledge_km.yaml, judging factual coverage via
    klanker_voice.harness.judge.judge_factory with the lenient close-STT phrasing. Prove cross-topic
    cache warmth: extend/reuse the cache-verify surface so a km-then-defcon turn sequence shows
    cache_read_input_tokens > 0 on the second (topic-switched) turn — demonstrating that swapping
    block1 does not invalidate the cached block0 (Pitfall 3, the whole point of the two-block design).
  </action>
  <verify>
    <automated>cd apps/voice && .venv/bin/pytest tests/test_knowledge_router_multitopic.py -x -q</automated>
    <automated>cd apps/voice && .venv/bin/pytest tests/ -q</automated>
    <human-check>Run the defcon + meshtk scenarios and a km→defcon topic-switch cache-verify against live keys: both judged correct AND cache_read_input_tokens > 0 on the topic-switch turn. Record in the SUMMARY.</human-check>
  </verify>
  <done>The router discriminates all three launch topics and safely defers on overlap; per-topic evals are judged correct; a live topic switch keeps system[0] cache-warm (cache_read_input_tokens > 0 post-switch); full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus/humor-deck → deep packs | Curated public content (incl. the humor deck's facts) becomes spoken prompt material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Information Disclosure | defcon/meshtk packs surface a non-public identifier | medium | mitigate | Public-by-design corpus (D-02); each pack carries a do-not-say section; advisory_lint (07-01) + git-diff review (D-09). |
| T-07-09 | Correctness | router confuses meshtk vs a generic "toolkit" question | medium | mitigate | Overlap-aware keywords + confidence_floor + fallback; a dedicated overlap-guard test. |
| T-07-04 | Reputational | a promoted humor-deck fact drags edgy tone into a pack | high | mitigate | Packs carry FACTS only; tone/guardrail lives in the 07-01 style layer; the 07-05 crude-humor eval covers behavior. |
| T-07-SC | Tampering | package installs | low | accept | No new packages this plan (content + tests only). |
</threat_model>

<verification>
- `cd apps/voice && .venv/bin/pytest tests/ -q` green.
- topic-map + manifest carry all 3 launch topics; overlap guard test passes.
- Live: defcon + meshtk scenarios judged correct; cross-topic cache warmth confirmed.
</verification>

<success_criteria>
- The 3-topic launch set (km, defcon.run.34, meshtk) answers correctly and discriminates safely, with cross-topic cache warmth preserved (contributes to ROADMAP criteria 1 + 4).
</success_criteria>

<output>
Create `.planning/phases/07-kph-knowledge-base/07-03-SUMMARY.md` when done.
</output>
