---
phase: 07-kph-knowledge-base
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - apps/voice/knowledge/topics/defcon-run-34.md
  - apps/voice/knowledge/topics/meshtk.md
  - apps/voice/knowledge/router/topic-map.yaml
  - apps/voice/knowledge/manifest.yaml
  - apps/voice/scenarios/kph_knowledge_defconrun.yaml
  - apps/voice/scenarios/kph_knowledge_meshtk.yaml
  - apps/voice/tests/test_knowledge_router.py
autonomous: true
requirements: [PIPE-10]

must_haves:
  truths:
    - "A directed defcon.run.34 question routes to the defcon-run-34 deep pack; a meshtk question routes to the meshtk deep pack."
    - "The router discriminates between the three primary topics (km / defcon-run-34 / meshtk) without collision."
    - "Switching topics mid-conversation swaps system[1] only — the stable cached prefix (system[0]) stays warm, so cache_read_input_tokens > 0 survives a topic switch."
    - "Both new deep packs contain no do-not-say landmine content — enforced by the Plan-01 scrub.scan() gate wired into this plan's verify, not by prose review."
  artifacts:
    - apps/voice/knowledge/topics/defcon-run-34.md
    - apps/voice/knowledge/topics/meshtk.md
    - apps/voice/scenarios/kph_knowledge_defconrun.yaml
    - apps/voice/scenarios/kph_knowledge_meshtk.yaml
  key_links:
    - "topic-map.yaml gains `pack:` entries for defcon-run-34 and meshtk (previously hook-only)."
    - "load_topic_pack(cfg, 'defcon-run-34') and (…, 'meshtk') now resolve to real files."
  prohibitions:
    - "MUST NOT change pack.py or router.py logic (this is an additive content + scenario slice; the loader already handles N topics from Plan 01)."
    - "MUST NOT invent cross-system integrations that do not exist (tiogo/kvmlab remain standalone; defcon.run.34↔km links only where the digests assert them)."
    - "MUST NOT include any Landmines/do-not-say content from the defcon or meshtk digests."
---

## Phase Goal

**As a** visitor at the klanker-voice booth, **I want to** ask KPH about any of Kurt's
three headline systems (klanker-maker, defcon.run.34, meshtk) and get the right deep
answer, **so that** KPH can carry a real multi-topic conversation, not just one demo path.

<objective>
Extend the Plan-01 loop from one topic to the full primary set. Author the
defcon.run.34 and meshtk DEEP packs (the swappable WHAT-axis layer), promote their
topic-map entries from hook-only to full `pack:` references, and add per-topic eval
scenarios. Prove the router discriminates across all three primary topics and that a
mid-conversation topic switch keeps the stable cached prefix warm (cache survives the
swap — the whole point of the stable-prefix / swappable-suffix design).

Purpose: Turns the single-topic walking slice into the real launch topic set (the
content-rich, confirmed primary systems from the critical steers). No new code — the
Plan-01 loader and router already handle N topics; this plan feeds them content.
Output: Two deep packs + two eval scenarios + multi-topic router tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-kph-knowledge-base/07-CONTEXT.md
@.planning/phases/07-kph-knowledge-base/07-DESIGN-NOTES.md
@.planning/phases/07-kph-knowledge-base/07-RESEARCH.md
@.planning/phases/07-kph-knowledge-base/07-PATTERNS.md
@.planning/phases/07-kph-knowledge-base/corpus/defcon-run-34-digest.md
@.planning/phases/07-kph-knowledge-base/corpus/meshtk-digest.md
@.planning/phases/07-kph-knowledge-base/07-01-PLAN.md
@apps/voice/knowledge/router/topic-map.yaml
@apps/voice/scenarios/kph_knowledge_km.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author defcon.run.34 + meshtk deep packs and promote topic-map entries</name>
  <files>apps/voice/knowledge/topics/defcon-run-34.md, apps/voice/knowledge/topics/meshtk.md, apps/voice/knowledge/router/topic-map.yaml, apps/voice/knowledge/manifest.yaml</files>
  <read_first>
    - .planning/phases/07-kph-knowledge-base/corpus/defcon-run-34-digest.md (source body; EXCLUDE the "Landmines / do-not-say" section: secrets plumbing, X-Internal-Secret, documented security gaps, admin/abuse-detection posture, WAF evasion, easter-egg spoilers, registrant/PII, unreleased v1.5+ plans, AWS account structure)
    - .planning/phases/07-kph-knowledge-base/corpus/meshtk-digest.md (source body; EXCLUDE its "Landmines / do-not-say": node privkeys/pubkeys, node IDs, exact lat/long, any node-DB key material)
    - apps/voice/knowledge/topics/klanker-maker.md (the deep-pack voice-friendly format Plan 01 established — mirror its structure, tone, and length budget)
    - apps/voice/knowledge/router/topic-map.yaml (Plan-01 schema; defcon-run-34 + meshtk currently carry `hook:` only)
    - apps/voice/knowledge/manifest.yaml (Plan-01 source list to keep consistent)
  </read_first>
  <action>
    Author `knowledge/topics/defcon-run-34.md` — the defcon.run.34 DEEP pack: a
    voice-friendly, spoken-tone distillation of the defcon digest body (what it is, how the
    infra/login patterns work, what shipped v1.0-v1.3 + flash refresh, the community-project
    framing). Speak about SHIPPED features as live; describe unreleased v1.5+ only in general
    "coming soon" terms per the digest's guidance; never surface the flagged landmine
    categories (secrets/paths, internal auth headers, security gaps, abuse-detection posture,
    WAF evasion, easter-egg spoilers, real user/registrant data, AWS account structure).
    Author `knowledge/topics/meshtk.md` — the meshtk DEEP pack ("mesh T K, the meshtastic
    toolkit"): what it is, how it works, its topic map, sample Q→A distilled voice-friendly;
    exclude all node-DB key material, node IDs, and exact coordinates.
    Promote both `topic-map.yaml` entries from hook-only to full: add a `pack:` path
    (`topics/defcon-run-34.md`, `topics/meshtk.md`) alongside the existing `hook:` and
    keyword/spoken-name/priority fields. Keep the km entry unchanged. Confirm the manifest
    still lists all three sources with `public: true`.
    Match each deep pack's length budget to the km pack so the swappable suffix stays a
    focused per-turn prompt (better Haiku TTFT). The stable prefix is untouched.
    Before considering a pack done, run it through `klanker_voice.knowledge.scrub.scan()`
    (the executable do-not-say gate authored in Plan 01) — a non-empty finding list means a
    landmine leaked in and the pack must be re-authored. The gate is wired into this task's
    verify; do NOT hand-eyeball landmine exclusion.
  </action>
  <acceptance_criteria>
    - `apps/voice/knowledge/topics/defcon-run-34.md` and `topics/meshtk.md` exist and are non-empty voice-friendly markdown.
    - `cd apps/voice && python -c "from klanker_voice.config import load_config; from klanker_voice.knowledge.pack import load_topic_pack; c=load_config(); assert load_topic_pack(c,'defcon-run-34') and load_topic_pack(c,'meshtk')"` exits 0 (both deep packs now resolve).
    - `cd apps/voice && python -c "import yaml; m=yaml.safe_load(open('knowledge/router/topic-map.yaml')); packs={t['id']:t.get('pack') for t in m['topics']}; assert packs['defcon-run-34'] and packs['meshtk'] and packs['klanker-maker']"` exits 0.
    - Do-not-say gate (executable): `cd apps/voice && python -c "from klanker_voice.knowledge import scrub; assert scrub.scan(open('knowledge/topics/defcon-run-34.md').read())==[] and scrub.scan(open('knowledge/topics/meshtk.md').read())==[]"` exits 0 — both new packs scan clean through the Plan-01 `scrub.scan()` gate (no private-key blocks, no AWS-account-id-shaped strings, no `.sops`/ARN/internal-secret-header/internal-hostname markers, no node privkey/pubkey hex, no exact coordinates).
    - `cd apps/voice && pytest tests/test_knowledge_pack.py -x` still green (stable prefix unchanged, byte-identical).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/voice && python -c "from klanker_voice.config import load_config; from klanker_voice.knowledge.pack import load_topic_pack; c=load_config(); assert load_topic_pack(c,'defcon-run-34') and load_topic_pack(c,'meshtk')" && python -c "from klanker_voice.knowledge import scrub; assert scrub.scan(open('knowledge/topics/defcon-run-34.md').read())==[] and scrub.scan(open('knowledge/topics/meshtk.md').read())==[]" && pytest tests/test_knowledge_pack.py -x</automated>
  </verify>
  <done>Both primary deep packs authored (landmine-free) and wired via topic-map `pack:` entries; loader resolves all three topics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-topic router discrimination tests + per-topic eval scenarios + cross-topic cache proof</name>
  <files>apps/voice/tests/test_knowledge_router.py, apps/voice/scenarios/kph_knowledge_defconrun.yaml, apps/voice/scenarios/kph_knowledge_meshtk.yaml</files>
  <read_first>
    - apps/voice/tests/test_knowledge_router.py (Plan-01 router tests to extend)
    - apps/voice/scenarios/kph_knowledge_km.yaml (Plan-01 scenario shape to mirror)
    - apps/voice/scenarios/memory.yaml (greeting-observe + judge_factory + lenient STT phrasing)
    - apps/voice/src/klanker_voice/harness/judge.py (judge_factory, reuse verbatim)
    - .planning/phases/07-kph-knowledge-base/07-RESEARCH.md (Pitfall 1 misclassification; the "toolkit" keyword-overlap example between meshtk and generic dev-tools)
  </read_first>
  <action>
    Extend `test_knowledge_router.py` with discrimination cases: a directed defcon.run.34
    question classifies to `defcon-run-34`, a meshtk question to `meshtk`, and a km question
    to `klanker-maker` — each above the confidence floor and NOT colliding. Add the known
    keyword-overlap guard from RESEARCH (an ambiguous "toolkit"-only utterance must resolve
    via the floor to `fallback`, not silently to meshtk). Add a topic-SWITCH test: after the
    processor is on km, a defcon question flips the selected topic AND fires the ack; a
    second defcon question does NOT re-ack (same-topic follow-up).
    Author `scenarios/kph_knowledge_defconrun.yaml` and `kph_knowledge_meshtk.yaml`, each
    mirroring the km scenario: greeting-observe first turn, then a directed question judged
    by `klanker_voice.harness.judge.judge_factory` against a short expected-facts list
    (defcon: community project whose battle-tested infra/login patterns power klanker-voice's
    cloud side; meshtk: a meshtastic toolkit) with lenient "accept close speech-to-text
    renderings" phrasing.
    Note for the cross-topic cache proof: the Plan-01 `kph_cache_verify.yaml` proves cache on
    a same-topic second turn; extend the human-check to also cover a topic SWITCH (turn 1 km,
    turn 2 defcon) confirming `cache_read_input_tokens > 0` on the switched turn — proving the
    stable prefix survived the system[1] swap. Do this by adding a switch turn to the existing
    cache scenario OR documenting the switch check in this plan's verification (no new
    scenario file required if the existing one is extended — coordinate to avoid editing files
    Plan 03 owns).
  </action>
  <acceptance_criteria>
    - `cd apps/voice && pytest tests/test_knowledge_router.py -x` PASSES with the new discrimination + overlap-guard + topic-switch-ack cases.
    - `cd apps/voice && python -c "import yaml; yaml.safe_load(open('scenarios/kph_knowledge_defconrun.yaml')); yaml.safe_load(open('scenarios/kph_knowledge_meshtk.yaml'))"` exits 0 and both reference `klanker_voice.harness.judge.judge_factory`.
    - `cd apps/voice && pytest tests/` full suite green.
    - LIVE (human-check, needs key): a topic-switch run (km turn → defcon turn) shows `cache_read_input_tokens > 0` on the switched turn and the ack fires on the switch but not on a same-topic follow-up.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/voice && pytest tests/test_knowledge_router.py -x && python -c "import yaml; yaml.safe_load(open('scenarios/kph_knowledge_defconrun.yaml')); yaml.safe_load(open('scenarios/kph_knowledge_meshtk.yaml'))"</automated>
    <human-check>Run a two-turn km→defcon topic-switch against the live Anthropic API and confirm cache_read_input_tokens > 0 on the defcon turn (stable prefix stayed warm through the pack swap), and the ack fired on the switch.</human-check>
  </verify>
  <done>Router discriminates all three primary topics; per-topic eval scenarios pass; cross-topic cache warmth proven on a live switch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus digest → speakable pack | The defcon + meshtk digests carry heavier landmine sets (security gaps, WAF evasion, node keys, PII) that must never reach a pack. |
| anonymous mic user → router | Untrusted utterances may straddle topics (keyword overlap) and force a wrong-topic answer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | defcon + meshtk deep packs | high | mitigate | The Plan-01 `scrub.scan()` executable gate is wired into Task 1's verify and returns zero findings on both packs (security gaps, abuse-detection posture, WAF evasion, easter-egg spoilers, PII, AWS account structure, node keys, coordinates). Packs authored from digest bodies only, excluding every flagged landmine; git-diff review (D-09) is the final human gate. |
| T-07-03 | Correctness/Trust (public-mic) | multi-topic classify | high | mitigate | Discrimination + keyword-overlap guard tests (Task 2); ambiguous "toolkit" resolves to fallback via the floor, never a confident meshtk pick (Pitfall 1). |
| T-07-04 | Spoofing (fabrication) | cross-system claims | medium | mitigate | defcon↔km links only where the digests assert them (defcon infra/login powers the cloud side); no invented integrations; persona "never invent facts". |
| T-07-SC | Tampering (supply chain) | package installs | low | accept | No new packages this plan (content + scenarios + tests only). |
</threat_model>

<verification>
- `cd apps/voice && pytest tests/` full suite green.
- `scrub.scan()` on defcon + meshtk packs = zero landmine hits (executable gate, Task 1 verify).
- Live topic-switch cache proof: `cache_read_input_tokens > 0` on the switched turn.
- All three per-topic eval scenarios pass under the judge harness.
</verification>

<success_criteria>
- KPH answers km, defcon.run.34, and meshtk questions from their respective deep packs (criterion 2, full primary set).
- Topic switches keep the cache warm (criterion 1 survives swaps).
- No landmine content in any pack; no fabricated cross-system claims.
</success_criteria>

<output>
Create `.planning/phases/07-kph-knowledge-base/07-02-SUMMARY.md` when done.
</output>
