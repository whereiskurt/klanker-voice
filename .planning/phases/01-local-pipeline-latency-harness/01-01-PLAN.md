---
phase: 01-local-pipeline-latency-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - apps/voice/.gitignore
  - apps/voice/Makefile
  - apps/voice/scripts/bootstrap_env.sh
  - apps/voice/pyproject.toml
  - apps/voice/uv.lock
  - apps/voice/.python-version
autonomous: false
requirements: [PIPE-07]
must_haves:
  truths:
    - "Developer can produce apps/voice/.env with all three provider keys via one command (make env), sourced from SSM — no plaintext secrets ever enter the repo"
    - "pipecat-ai 1.5.x with webrtc/deepgram/anthropic/runner extras imports cleanly under Python 3.12 managed by uv"
    - "Terminal-audio prerequisites (uv, portaudio) are installed on the machine"
  artifacts:
    - path: "apps/voice/scripts/bootstrap_env.sh"
      provides: "SSM /kmv/bootstrap/* -> .env bootstrap (D-10)"
    - path: "apps/voice/Makefile"
      provides: "make env target wrapping the bootstrap script"
    - path: "apps/voice/pyproject.toml"
      provides: "uv project pinned to pipecat-ai ~=1.5.0 with correct extras"
    - path: "apps/voice/uv.lock"
      provides: "committed lockfile (supply-chain mitigation)"
  key_links:
    - from: "apps/voice/scripts/bootstrap_env.sh"
      to: "apps/voice/.env"
      via: "aws ssm get-parameter --with-decryption using the klanker-application profile, us-east-1"
      pattern: "ssm get-parameter"
    - from: "apps/voice/pyproject.toml"
      to: "pipecat-ai"
      via: "dependency pin with webrtc,deepgram,anthropic,runner extras"
      pattern: "pipecat-ai\\[webrtc,deepgram,anthropic,runner\\]~=1\\.5\\.0"
---

## Phase Goal

**As a** developer, **I want to** hold a slick, measured, interruption-safe conversation with the KlankerMaker concierge on my laptop using only three provider API keys, **so that** the core conversational value is proven before any cloud infrastructure exists.

<objective>
Stand up the toolchain and key plumbing for the Phase 1 walking skeleton: machine prerequisites (uv, portaudio), the apps/voice uv project pinned to pipecat-ai 1.5.0, and the SSM-to-.env key bootstrap per D-10. The first package installs are gated behind a blocking human legitimacy checkpoint (four PyPI packages flagged [SUS] in the research audit).

Purpose: Everything downstream (skeleton, harness, tuning) needs a working, secret-safe environment. PIPE-07 requires the whole bot to run with only three provider API keys — this plan delivers the key path and the pinned dependency tree.
Output: apps/voice scaffold with pyproject.toml + committed uv.lock, bootstrap_env.sh + Makefile env target, gitignore coverage for .env and artifacts, verified pipecat 1.5.x import.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-local-pipeline-latency-harness/01-CONTEXT.md
@.planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Machine prerequisites, repo scaffold, and SSM key bootstrap (D-10)</name>
  <read_first>
    - .planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md — "Environment Availability" table and Pitfall 5 (uv and portaudio are NOT installed; brew installs required first)
    - .planning/phases/01-local-pipeline-latency-harness/01-CONTEXT.md — decision D-10 (exact SSM parameter names and profile)
  </read_first>
  <files>.gitignore, apps/voice/.gitignore, apps/voice/Makefile, apps/voice/scripts/bootstrap_env.sh</files>
  <action>
    Install machine prerequisites: run `brew install uv portaudio` (both confirmed missing per RESEARCH Environment Availability; portaudio is required later for the pyaudio build in the dev group — Pitfall 5 says this fails at install time, not runtime).

    Create the repo scaffold per the RESEARCH "Recommended Project Structure": directories apps/voice/scripts, apps/voice/prompts, apps/voice/src/klanker_voice, apps/voice/tests, apps/voice/scenarios, apps/voice/configs, apps/voice/artifacts, and docs/ at repo root (docs/ already exists). Do NOT run uv init yet — installs are gated behind Task 2's legitimacy checkpoint.

    Write a root .gitignore (and an apps/voice/.gitignore reinforcing the same) that ignores: apps/voice/.env, apps/voice/artifacts/, .venv/, __pycache__/, *.pyc. The .env and artifacts entries are the security-relevant ones (D-09/D-10: secrets never in the repo; artifacts hold rendered audio and per-run JSON that should not bloat git — committed A/B verdict copies go under docs/tuning/ in a later plan).

    Write apps/voice/scripts/bootstrap_env.sh implementing D-10: for each of the three SSM parameters /kmv/bootstrap/deepgram_api_key, /kmv/bootstrap/anthropic_api_key, /kmv/bootstrap/elevenlabs_api_key, call `aws ssm get-parameter --with-decryption --profile klanker-application --region us-east-1 --query Parameter.Value --output text` and write the results to apps/voice/.env as DEEPGRAM_API_KEY, ANTHROPIC_API_KEY, and ELEVENLABS_API_KEY respectively (these exact env var names are what pipecat's Deepgram/Anthropic/ElevenLabs services and the runner's dotenv loading expect). Security requirements for the script: set `umask 077` before writing, chmod 600 the result, never echo a secret value to stdout or stderr (print only which parameter was fetched and a success line), fail fast with a clear message if any parameter fetch fails (no partial .env), and never run with shell xtrace enabled. Refuse to run if the CWD resolution would write .env outside apps/voice.

    Write apps/voice/Makefile with an `env` target that runs scripts/bootstrap_env.sh (D-10 asks for "make env or equivalent"). Then run `make -C apps/voice env` once to produce the real .env.

    If the AWS call fails with an authentication/SSO error, create a dynamic auth checkpoint asking the user to authenticate the klanker-application profile, then retry — do not work around it with pasted keys.
  </action>
  <verify>
    <automated>test -x apps/voice/scripts/bootstrap_env.sh && git check-ignore -q apps/voice/.env && test -f apps/voice/.env && [ "$(stat -f '%Lp' apps/voice/.env)" = "600" ] && [ "$(grep -c 'API_KEY=' apps/voice/.env)" -eq 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - `make -C apps/voice env` produces apps/voice/.env with exactly three provider key lines (DEEPGRAM_API_KEY, ANTHROPIC_API_KEY, ELEVENLABS_API_KEY), mode 600
    - apps/voice/.env and apps/voice/artifacts/ are gitignored (git check-ignore passes)
    - Script output contains no secret values (spot-check the captured stdout/stderr of the run)
    - `uv --version` and `brew list portaudio` both succeed
  </acceptance_criteria>
  <done>Developer can regenerate .env from SSM with one command; no secret material is committed or printed; machine has uv and portaudio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy gate before first uv add</name>
  <read_first>
    - .planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md — "Package Legitimacy Audit" table (four packages flagged [SUS], mitigating evidence listed)
  </read_first>
  <what-built>Nothing installed yet — this gate precedes the first package install. The research audit flagged pipecat-ai, pipecat-ai-prebuilt, kokoro-onnx, and moonshine-voice as [SUS] (PyPI download counts unavailable to the audit seam + very recent release dates). pytest and pytest-asyncio are [ASSUMED] ubiquitous. Mitigating evidence already gathered: pipecat-ai's identity was confirmed by cloning the official repo's v1.5.0 tag, and the other three are declared dependencies inside pipecat's own pyproject extras.</what-built>
  <action>PAUSE before any uv add. Present the six packages (pipecat-ai, pipecat-ai-prebuilt, kokoro-onnx, moonshine-voice, pytest, pytest-asyncio) with their [SUS]/[ASSUMED] flags and the mitigating evidence from the RESEARCH Package Legitimacy Audit, then wait for explicit user approval. This checkpoint is never auto-approved regardless of workflow mode — supply-chain gate T-1-SC.</action>
  <how-to-verify>
    1. Open https://pypi.org/project/pipecat-ai/ — confirm publisher links to github.com/pipecat-ai/pipecat and version 1.5.0 exists
    2. Open https://pypi.org/project/pipecat-ai-prebuilt/ — confirm it is the prebuilt UI package referenced by pipecat's [runner] extra
    3. Open https://pypi.org/project/kokoro-onnx/ and https://pypi.org/project/moonshine-voice/ — confirm they match github.com/thewh1teagle/kokoro-onnx and github.com/moonshine-ai/moonshine
    4. Optionally spot-check https://pypi.org/project/pytest/ and https://pypi.org/project/pytest-asyncio/
  </how-to-verify>
  <acceptance_criteria>
    - User has confirmed each flagged package's PyPI page matches its official source repository
    - No package is substituted or added beyond the audited list without re-running this gate
  </acceptance_criteria>
  <resume-signal>Type "approved" to unblock the installs, or name any package that looks wrong</resume-signal>
  <done>User explicitly approved the audited package set; Task 3 may install exactly that set and nothing else.</done>
</task>

<task type="auto">
  <name>Task 3: uv project init, pinned installs, and import smoke</name>
  <read_first>
    - .planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md — "Standard Stack" table and Installation block (exact extras: silero/elevenlabs extras are EMPTY in 1.5.0 — do not add them; [local] and [evals] go in the dev group only so Fargate images never need portaudio)
  </read_first>
  <files>apps/voice/pyproject.toml, apps/voice/uv.lock, apps/voice/.python-version</files>
  <action>
    In apps/voice run `uv init --python 3.12` (bare project named klanker-voice; package name later resolves to klanker_voice — the banned term "voiceai" must never appear in any name, per project constraints). Remove any placeholder main.py uv generates.

    Install exactly the audited set approved at the Task 2 legitimacy checkpoint (per the RESEARCH Installation block): `uv add "pipecat-ai[webrtc,deepgram,anthropic,runner]~=1.5.0"` for the main dependency group, then `uv add --group dev "pipecat-ai[local,evals]~=1.5.0" pytest pytest-asyncio`. Do not add silero or elevenlabs extras (empty in 1.5.0) and do not add any other provider SDKs — the three-key constraint (PIPE-07) forbids a fourth vendor.

    Configure pyproject.toml: add [tool.pytest.ini_options] with testpaths = ["tests"] and asyncio_mode = "auto"; set the src layout so `klanker_voice` under src/ is importable (hatchling/uv default with packages = ["src/klanker_voice"] or tool.uv package config as appropriate). Create an empty apps/voice/src/klanker_voice/__init__.py so imports resolve.

    Commit uv.lock (supply-chain mitigation T-1-SC: locked resolution + audited set).

    Smoke the install: run a python one-liner under `uv run` importing pipecat and printing pipecat.__version__, and import the specific 1.5.0 surfaces later plans rely on: pipecat.services.deepgram.stt, pipecat.services.deepgram.flux, pipecat.services.anthropic.llm, pipecat.services.elevenlabs.tts, pipecat.audio.vad.silero, pipecat.observers.user_bot_latency_observer, pipecat.transports.local.audio (dev group). If any import fails, the pinned-API assumption from RESEARCH is wrong — stop and report rather than improvising.
  </action>
  <verify>
    <automated>cd apps/voice && uv run python -c "import pipecat, pipecat.services.deepgram.flux, pipecat.services.anthropic.llm, pipecat.services.elevenlabs.tts, pipecat.audio.vad.silero, pipecat.observers.user_bot_latency_observer; print(pipecat.__version__)" | grep -E '1\.5\.'</automated>
  </verify>
  <acceptance_criteria>
    - pipecat.__version__ reports 1.5.x under uv-managed Python 3.12
    - uv.lock exists and is committed; dev group contains local+evals extras, pytest, pytest-asyncio
    - All seven listed pipecat module surfaces import without error
    - `uv run pytest tests/ -q` exits 0 (empty test dir collects zero tests without error)
  </acceptance_criteria>
  <done>The pinned 1.5.0 dependency tree is installed, locked, committed, and its Phase-1 API surfaces import cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AWS SSM -> local .env | SecureString secrets cross into a local plaintext file (gitignored, 0600) |
| PyPI -> local venv | Third-party code enters the machine at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Information Disclosure | apps/voice/.env in git history | high | mitigate | gitignore before first write + `git check-ignore` gate in Task 1 verify; SSM remains source of truth (D-10) |
| T-1-02 | Information Disclosure | bootstrap_env.sh stdout/logs | medium | mitigate | script never echoes values, umask 077, chmod 600, no xtrace |
| T-1-SC | Tampering | pip/uv installs (pipecat-ai, pipecat-ai-prebuilt, kokoro-onnx, moonshine-voice) | high | mitigate | Task 2 blocking human legitimacy checkpoint (never auto-approved) + committed uv.lock + exact audited package set only |
</threat_model>

<verification>
- `make -C apps/voice env` regenerates .env idempotently; three keys present; nothing secret in `git status` or committed files
- `cd apps/voice && uv run python -c "import pipecat; print(pipecat.__version__)"` prints 1.5.x
- Schema push gate: not applicable — no ORM/schema files in this phase (Python voice pipeline, no database)
</verification>

<success_criteria>
- PIPE-07 foundation: keys flow SSM -> .env with one command; only three provider keys exist in .env
- Pinned pipecat 1.5.0 tree installed and locked; [local]/[evals] confined to the dev group
- Legitimacy checkpoint approved by the user before any package was installed
</success_criteria>

<output>
Create `.planning/phases/01-local-pipeline-latency-harness/01-01-SUMMARY.md` when done.

## Artifacts this plan produces
- Files: .gitignore, apps/voice/.gitignore, apps/voice/Makefile, apps/voice/scripts/bootstrap_env.sh, apps/voice/pyproject.toml, apps/voice/uv.lock, apps/voice/.python-version, apps/voice/src/klanker_voice/__init__.py
- CLI entrypoints: `make -C apps/voice env`
- Config keys (env vars): DEEPGRAM_API_KEY, ANTHROPIC_API_KEY, ELEVENLABS_API_KEY (in gitignored .env only)
- Package: klanker_voice (empty package init)
</output>
