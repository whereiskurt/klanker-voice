---
phase: 01-local-pipeline-latency-harness
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - apps/voice/scripts/audition.py
  - apps/voice/pipeline.toml
  - docs/TUNING.md
autonomous: false
requirements: [PIPE-01, PIPE-06]
must_haves:
  truths:
    - "Three shortlisted ElevenLabs voices rendered the same K script side-by-side; the user picked the winner by ear (D-02)"
    - "The winning voice_id and speed live in pipeline.toml and are recorded with reasoning in docs/TUNING.md (D-02, D-03, D-12)"
    - "The user has held a final conversation with tuned K and signed off on the conversational feel: greet-first, fast-punchy delivery, natural barge-in, latency verdict visible"
  artifacts:
    - path: "apps/voice/scripts/audition.py"
      provides: "3-voice same-script audition renderer (one voice per render session)"
    - path: "apps/voice/pipeline.toml"
      provides: "final tuned config incl. chosen voice_id (Phase 4 prod artifact)"
    - path: "docs/TUNING.md"
      provides: "completed verdict record: chosen voice + final knob values (D-12)"
  key_links:
    - from: "apps/voice/scripts/audition.py"
      to: "apps/voice/artifacts/audition/"
      via: "renders one WAV set per candidate voice via the ElevenLabs API using ELEVENLABS_API_KEY"
      pattern: "audition"
    - from: "apps/voice/pipeline.toml"
      to: "docs/TUNING.md"
      via: "chosen voice_id recorded in both after the user decision"
      pattern: "voice_id"
---

## Phase Goal

**As a** developer, **I want to** hold a slick, measured, interruption-safe conversation with the KlankerMaker concierge on my laptop using only three provider API keys, **so that** the core conversational value is proven before any cloud infrastructure exists.

<objective>
Give K its voice and close the phase: render a 3-voice side-by-side audition (D-02) with the same script, let the user pick by ear, land the winner's voice_id + speed (D-03) in pipeline.toml and docs/TUNING.md (D-12), then get the user's final conversational-feel sign-off on the tuned bot.

Purpose: D-02 makes the voice an explicit user-in-the-loop deliverable, and the phase's core value — "slick" — is ultimately a human judgment. This plan ends with that judgment.
Output: audition script + rendered candidates, finalized pipeline.toml, completed TUNING.md, user sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-local-pipeline-latency-harness/01-CONTEXT.md
@.planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shortlist three voices and render the audition (D-02, D-03)</name>
  <read_first>
    - .planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md — Pitfall 8 (voice-settings changes do NOT apply mid-session — render each candidate as its own session/HTTP call, never flip settings mid-stream) and Open Question 3 (shortlist is Claude's discretion at build time; voice IDs churn too fast to pre-pin)
    - apps/voice/prompts/concierge.md — write the audition script lines in K's actual register
  </read_first>
  <files>apps/voice/scripts/audition.py</files>
  <action>
    Write apps/voice/scripts/audition.py: query the ElevenLabs voices endpoint with ELEVENLABS_API_KEY (loaded from apps/voice/.env), select a shortlist of exactly three candidates optimized for the D-03 brief — fast, punchy, high demo intelligibility, conversational rather than narration-styled (Claude's discretion per CONTEXT; record name, voice id, and a one-line rationale per candidate in an audition manifest JSON written next to the audio). Render the SAME script through each candidate using model eleven_flash_v2_5 at speed 1.1 — one voice per render call/session per Pitfall 8 (use pipecat's ElevenLabs HTTP TTS surface if convenient, otherwise a direct REST call with the existing HTTP client already in the dependency tree; no new packages). The script: K's greeting line (introduces itself as K, per D-01/D-04), one 1-2-sentence answer with a depth hook (D-05), and one playful-with-teeth line (D-07) — written in the concierge register so the user hears the persona, not placeholder text. Output WAV/MP3 files to apps/voice/artifacts/audition/ named by candidate (gitignored; the manifest travels with them). Never print or persist the API key.
  </action>
  <verify>
    <automated>cd apps/voice && uv run python scripts/audition.py && [ "$(ls artifacts/audition/*.mp3 artifacts/audition/*.wav 2>/dev/null | wc -l | tr -d ' ')" -ge 3 ] && test -s artifacts/audition/manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - Exactly three candidate voices rendered from the identical K-register script at speed 1.1 / eleven_flash_v2_5
    - manifest.json records voice ids, names, and per-candidate rationale
    - Each candidate rendered in its own session/call (no mid-stream settings flips)
  </acceptance_criteria>
  <done>Three audition takes exist locally, ready for the user's ears.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Voice audition — user picks the winner by ear (D-02)</name>
  <read_first>
    - apps/voice/artifacts/audition/manifest.json — candidate names, ids, rationale
  </read_first>
  <action>PAUSE and present the three rendered candidates (file paths + manifest rationale) to the user for a listen-and-pick decision. Do not proceed to Task 3 until a winner is named. If the user asks to re-audition, loop Task 1 exactly once with their direction, then re-present.</action>
  <decision>Which of the three auditioned ElevenLabs voices is K's voice?</decision>
  <context>D-02 locks this as a user-in-the-loop deliverable: same script, three shortlisted voices, winner chosen by ear. The pick lands in pipeline.toml and docs/TUNING.md and is inherited by every later phase. Listen to the three files in apps/voice/artifacts/audition/ (candidate names and rationale in manifest.json).</context>
  <options>
    <option id="candidate-1">
      <name>Candidate 1 (see manifest.json)</name>
      <pros>Per-candidate rationale recorded in the manifest at render time</pros>
      <cons>User's ears decide — no pre-judgment beyond the shortlist rationale</cons>
    </option>
    <option id="candidate-2">
      <name>Candidate 2 (see manifest.json)</name>
      <pros>Per-candidate rationale recorded in the manifest at render time</pros>
      <cons>User's ears decide</cons>
    </option>
    <option id="candidate-3">
      <name>Candidate 3 (see manifest.json)</name>
      <pros>Per-candidate rationale recorded in the manifest at render time</pros>
      <cons>User's ears decide</cons>
    </option>
  </options>
  <acceptance_criteria>
    - User listened to all three renders and named a winner (or requested a re-render with different candidates, which loops Task 1 once with new shortlist reasoning)
  </acceptance_criteria>
  <resume-signal>Select: candidate-1, candidate-2, or candidate-3 (or "re-audition" with any direction you want)</resume-signal>
  <done>User named a winning voice by ear; the winner's voice id is known for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Land the winner and complete docs/TUNING.md (D-12)</name>
  <read_first>
    - docs/TUNING.md — the plan-04 verdict record with the "Chosen voice" stub to complete
    - apps/voice/artifacts/audition/manifest.json — the winning candidate's voice id
  </read_first>
  <files>apps/voice/pipeline.toml, docs/TUNING.md</files>
  <action>
    Set the winning candidate's voice id into pipeline.toml under the tts section, keeping speed 1.1 unless the user's audition feedback asked for a tempo change within the 0.7-1.2 WS range (D-03). Complete the "Chosen voice" section of docs/TUNING.md: winner name + voice id, the two runners-up, the user's stated reason if given, and the final full knob-value table (stt/turn/llm/tts) so Phase 4 and Phase 5 inherit one authoritative record (D-12). Run the config test suite and one greeting-scenario eval against bot -t eval so the chosen voice is proven live in the pipeline (real WS TTS path, not just the audition render).
  </action>
  <verify>
    <automated>cd apps/voice && uv run python -c "from klanker_voice.config import load_config; c=load_config('pipeline.toml'); assert c.tts.voice_id, 'voice_id still empty'; print(c.tts.voice_id)" && uv run pytest tests/test_config.py -x -q && grep -qi 'chosen voice' ../../docs/TUNING.md && { uv run python bot.py -t eval >/tmp/klanker-bot-voice.log 2>&1 & }; BOT=$!; sleep 20; uv run pipecat eval run scenarios/greeting.yaml --bot-url ws://localhost:7860; RC=$?; kill $BOT 2>/dev/null; exit $RC</automated>
  </verify>
  <acceptance_criteria>
    - pipeline.toml tts.voice_id is the audition winner; config tests green
    - docs/TUNING.md "Chosen voice" section completed with the final knob table (D-12)
    - Greeting scenario passes with the chosen voice through the real WS TTS path
  </acceptance_criteria>
  <done>K's voice is locked in config and documented; the tuned pipeline.toml is the finished Phase-4-ready artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final conversational-feel sign-off (phase gate)</name>
  <read_first>
    - docs/TUNING.md — the recorded latency verdicts to compare against what you feel
  </read_first>
  <what-built>The fully tuned local pipeline: winning endpointing arm, chosen voice, persona v1, both run modes, harness reporting per-session numbers. Every automated gate (unit tests, five eval scenarios, harness artifacts) is green.</what-built>
  <action>PAUSE for the phase gate: ensure the bot is runnable (env fresh, final pipeline.toml in place), then walk the user through the six verification steps below and wait for their verdict on the conversational feel. On "approved", the phase closes; on specific complaints, record them verbatim in the summary for targeted re-tuning.</action>
  <how-to-verify>
    1. `make -C apps/voice env` if your .env is stale, then `cd apps/voice && uv run python bot.py -t webrtc` and open http://localhost:7860
    2. Connect and just listen: K must greet first, introducing itself as K (D-01, D-04) — no dead air
    3. Ask a few questions: answers should be fast and punchy, 1-2 sentences with a depth hook (D-03, D-05)
    4. Ask K for the long version of anything, then interrupt it mid-sentence: playback should stop essentially immediately and the next reply should be coherent with being cut off (PIPE-02)
    5. Ask something off-topic (sports, weather): K rolls with it, then steers back toward Kurt/klanker/defcon.run territory (D-06); invite a gentle roast if you want to test D-07
    6. End the session and look at the harness table it printed: voice-to-voice p50/p95 with the informational verdict marks vs 800ms/1.2s (D-13)
  </how-to-verify>
  <acceptance_criteria>
    - User confirms the conversation feels slick: greet-first, punchy persona, natural barge-in, no laggy turn-taking
    - User has seen the measured latency verdict for the final config
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the phase, or describe what feels off (specific turn-taking/voice/persona notes help target re-tuning)</resume-signal>
  <done>User signed off on the conversational feel of the tuned pipeline (or their specific objections are recorded for gap closure).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audition.py -> ElevenLabs API | API key used for voice-library reads and renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-11 | Information Disclosure | audition script/manifest leaking the ElevenLabs key | medium | mitigate | key read from env only, never printed or written to manifest; artifacts/ gitignored |
| T-1-12 | Denial of Service (spend) | audition renders + re-renders burning TTS characters | low | accept | three short renders per round at demo-scale cost; re-audition loops require explicit user request |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | no new installs this plan; uv.lock governs |
</threat_model>

<verification>
- Full unit suite + all five eval scenarios green under the final pipeline.toml
- docs/TUNING.md complete: endpointing winner, SmartTurn verdict, eager-EOT decision, chosen voice, final knobs (D-12)
- User sign-off recorded at the Task 4 checkpoint
- Schema push gate: not applicable — no ORM/schema files in this phase
</verification>

<success_criteria>
- D-02 delivered: 3-voice audition held, winner chosen by ear, voice_id in config and TUNING.md
- PIPE-01/PIPE-06 close: user-confirmed slick conversation with the tuned, persona-true, chosen-voice pipeline
- Phase 1 artifacts (pipeline.toml, concierge.md, harness JSON schema, TUNING.md) are the stable inputs Phases 4-5 inherit
</success_criteria>

<output>
Create `.planning/phases/01-local-pipeline-latency-harness/01-05-SUMMARY.md` when done.

## Artifacts this plan produces
- Files: apps/voice/scripts/audition.py
- CLI entrypoints: `uv run python scripts/audition.py`
- Config values: pipeline.toml tts.voice_id (final), tts.speed (final)
- Records: docs/TUNING.md "Chosen voice" section + final knob table
- Output paths: apps/voice/artifacts/audition/ (gitignored renders + manifest.json)
</output>
