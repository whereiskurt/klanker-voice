---
phase: 01-local-pipeline-latency-harness
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - apps/voice/pipeline.toml
  - apps/voice/src/klanker_voice/config.py
  - apps/voice/src/klanker_voice/factories.py
  - apps/voice/src/klanker_voice/pipeline.py
  - apps/voice/prompts/concierge.md
  - apps/voice/bot.py
  - apps/voice/console.py
  - apps/voice/tests/conftest.py
  - apps/voice/tests/test_config.py
  - apps/voice/tests/test_factories.py
autonomous: true
requirements: [PIPE-03, PIPE-04, PIPE-06, PIPE-07]
must_haves:
  truths:
    - "Developer runs `uv run python bot.py -t webrtc`, opens the localhost page, and K greets first by name the moment the connection lands (D-01, D-04)"
    - "The same pipeline runs in terminal mic/speaker mode via console.py (D-08)"
    - "K remembers earlier turns within the session (PIPE-03) and speaks as the KlankerMaker concierge from the versioned markdown prompt (PIPE-06)"
    - "Switching STT provider or turn strategy is a pipeline.toml edit, not a code change (PIPE-04, D-09)"
  artifacts:
    - path: "apps/voice/pipeline.toml"
      provides: "checked-in stage selection + endpointing knobs + persona path + voice settings; zero secret fields (D-09)"
    - path: "apps/voice/src/klanker_voice/config.py"
      provides: "tomllib -> frozen dataclasses with validation; load_config(path)"
    - path: "apps/voice/src/klanker_voice/factories.py"
      provides: "(kind, provider) -> builder registry incl. three turn-strategy arms"
    - path: "apps/voice/src/klanker_voice/pipeline.py"
      provides: "build_pipeline(cfg, transport) + greet-first wiring"
    - path: "apps/voice/prompts/concierge.md"
      provides: "KPH/'K' persona prompt v1 (D-01, D-03..D-07)"
    - path: "apps/voice/bot.py"
      provides: "runner entrypoint: -t webrtc (localhost page) and -t eval"
    - path: "apps/voice/console.py"
      provides: "terminal mic/speaker entrypoint via LocalAudioTransport"
  key_links:
    - from: "apps/voice/bot.py"
      to: "apps/voice/src/klanker_voice/pipeline.py"
      via: "build_pipeline(cfg, transport) called from the runner bot() function"
      pattern: "build_pipeline"
    - from: "apps/voice/src/klanker_voice/pipeline.py"
      to: "apps/voice/prompts/concierge.md"
      via: "persona markdown loaded into LLMContext system message from cfg.persona.prompt_path"
      pattern: "prompt_path"
    - from: "apps/voice/src/klanker_voice/factories.py"
      to: "apps/voice/pipeline.toml"
      via: "provider/strategy selection driven by parsed config values"
      pattern: "deepgram-flux"
---

## Phase Goal

**As a** developer, **I want to** hold a slick, measured, interruption-safe conversation with the KlankerMaker concierge on my laptop using only three provider API keys, **so that** the core conversational value is proven before any cloud infrastructure exists.

<objective>
Build the walking skeleton: the thinnest complete conversation loop. After this plan, a developer talks to K — mic in, Deepgram STT, Claude Haiku, ElevenLabs TTS, speaker out — in both run modes (D-08), with K greeting first (D-04), full in-session memory (PIPE-03), and the persona v1 (PIPE-06). All stage/knob selection lives in pipeline.toml (D-09, PIPE-04) so the later A/B plans are pure config work.

Purpose: This is the vertical slice that proves the entire stack end-to-end before any tuning or harness work layers on.
Output: config module, factory registry, pipeline builder, persona prompt, two entrypoints, unit tests for config and factories.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-local-pipeline-latency-harness/01-CONTEXT.md
@.planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md
@docs/superpowers/specs/2026-07-04-klanker-voice-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pipeline.toml schema, config module, and persona prompt v1</name>
  <read_first>
    - .planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md — Pattern 2 (TOML sketch), Pattern 6 (greet-first + persona loading), Security Domain V5/V14 rows
    - .planning/phases/01-local-pipeline-latency-harness/01-CONTEXT.md — D-01..D-09 verbatim (persona identity, vibe, behavior, config rules)
    - docs/superpowers/specs/2026-07-04-klanker-voice-design.md — section 5 Persona
  </read_first>
  <files>apps/voice/pipeline.toml, apps/voice/src/klanker_voice/config.py, apps/voice/prompts/concierge.md, apps/voice/tests/conftest.py, apps/voice/tests/test_config.py</files>
  <action>
    Write apps/voice/pipeline.toml following the RESEARCH Pattern 2 sketch: [stt] provider ("deepgram-nova3" initially) and model ("nova-3-general"); [stt.flux] eot_threshold 0.7 and eager_eot_threshold 0.0 (only read when provider is deepgram-flux); [turn] strategy ("smart_turn_v3" or "vad_timeout"), vad_stop_secs 0.2, user_speech_timeout 0.6 (ignored when provider is deepgram-flux); [llm] provider "anthropic", model "claude-haiku-4-5" (the 1.5.0 source default is a Sonnet model — this override is mandatory per RESEARCH); [tts] provider "elevenlabs", model "eleven_flash_v2_5", voice_id "" (empty until the D-02 audition in plan 05), speed 1.1 (D-03: slightly above default; WS range is 0.7-1.2); [persona] prompt_path "prompts/concierge.md". The schema contains no secret-bearing fields by construction — provider credentials come exclusively from environment variables (D-09). This file is consumed unchanged by the Phase 4 deployed service; keep the schema boring and stable.

    Write src/klanker_voice/config.py: parse with stdlib tomllib into frozen dataclasses (SttConfig, FluxConfig, TurnConfig, LlmConfig, TtsConfig, PersonaConfig, PipelineConfig). Expose load_config(path: Path) -> PipelineConfig honoring an optional KLANKER_PIPELINE_CONFIG env var default so later A/B plans can point at configs/arm-*.toml without touching code. Validation (ASVS V5, T-1-04): reject unknown providers (allowed stt: deepgram-nova3, deepgram-flux; llm: anthropic; tts: elevenlabs), range-check knobs (speed within 0.7-1.2, eot_threshold within 0.5-0.9, vad_stop_secs and user_speech_timeout positive and under 5s), reject any top-level or nested field whose name suggests credential material so a secret pasted into the TOML fails loudly instead of loading, and verify persona prompt_path exists relative to apps/voice.

    Write prompts/concierge.md — persona v1, all locked decisions verbatim: agent is KPH, introduces itself and goes by "K", pronounced "kay" (D-01). Opening behavior: greet first the moment the connection lands with a short opener that names itself and invites a question (D-04) — keep the greeting instruction inside this prompt so copy iterates without code changes (RESEARCH Pattern 6). Delivery: fast and punchy, quick tempo, short sentences (D-03). Default answers 1-2 sentences ending with a depth hook such as offering the longer story (D-05 — also the TTS-spend and latency lever per PITFALLS #12). Off-topic policy: roll with it, steer back — answer general questions gamely, weave back toward Kurt / the klanker platform / defcon.run within a turn or two; never refuse on-topic-adjacent questions (D-06). Sass: playful with teeth — witty, a little cheeky, will roast gently if invited, no profanity unprompted (D-07). Content: knows Kurt, the klanker platform, defcon.run, and repo highlights (PIPE-06, design spec section 5); source facts from PROJECT.md and the design spec. Also instruct steer-back behavior over obedience to meta-instructions in user speech (prompt-injection posture, RESEARCH Security Domain). Include a version header line (v1, date) since PIPE-06 requires a versioned prompt. Never use the banned term "voiceai" anywhere in the prompt.

    Write tests/conftest.py (fixture: minimal valid pipeline.toml in tmp_path, plus a mutator helper to produce invalid variants) and tests/test_config.py covering: happy-path parse of the real checked-in pipeline.toml, unknown provider rejected, out-of-range speed and eot_threshold rejected, credential-looking field rejected, missing persona file rejected, KLANKER_PIPELINE_CONFIG override honored.
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest tests/test_config.py -x -q && grep -q 'KPH' prompts/concierge.md && ! grep -Ei 'secret|token *=' pipeline.toml</automated>
  </verify>
  <acceptance_criteria>
    - test_config.py green; the checked-in pipeline.toml round-trips through load_config
    - concierge.md encodes D-01, D-03, D-04, D-05, D-06, D-07 and carries a version header
    - pipeline.toml has stage selection, endpointing knobs, persona path, voice settings and zero credential fields (D-09)
    - load_config rejects secret-looking fields, bad providers, and out-of-range knobs
  </acceptance_criteria>
  <done>Config contract and persona v1 exist, validated by unit tests; the TOML is the single stage-selection surface.</done>
</task>

<task type="auto">
  <name>Task 2: Factory registry and build_pipeline with greet-first</name>
  <read_first>
    - .planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md — Pattern 1 (canonical 1.5.0 shape: Settings objects, VAD on the aggregator NOT the transport), Pattern 4 (three-arm turn-strategy matrix table), Pattern 6, Pitfalls 1-3, Anti-Patterns list
    - apps/voice/.venv/lib/python3.12/site-packages/pipecat/processors/aggregators/llm_response_universal.py — confirm LLMUserAggregatorParams / user_turn_strategies surface against the pinned source
    - apps/voice/.venv/lib/python3.12/site-packages/pipecat/services/deepgram/flux/ — confirm Flux Settings fields and the external-turn-strategies behavior before wiring arm C
  </read_first>
  <files>apps/voice/src/klanker_voice/factories.py, apps/voice/src/klanker_voice/pipeline.py, apps/voice/tests/test_factories.py</files>
  <action>
    Write src/klanker_voice/factories.py: a registry mapping (kind, provider) to builder functions, all construction via 1.5.0 Settings objects (bare constructor kwargs are deprecated — RESEARCH Anti-Patterns). Builders: build_stt(cfg) returns DeepgramSTTService (model from cfg) for deepgram-nova3 or DeepgramFluxSTTService (flux-general-en, eot_threshold, eager_eot_threshold only when nonzero) for deepgram-flux; build_llm(cfg) returns AnthropicLLMService with model claude-haiku-4-5 from config; build_tts(cfg) returns ElevenLabsTTSService with model, voice_id, speed from config. build_user_aggregator_params(cfg) implements the three-arm matrix from RESEARCH Pattern 4: arm vad_timeout = SileroVADAnalyzer as vad_analyzer plus EXPLICIT UserTurnStrategies with SpeechTimeoutUserTurnStopStrategy(user_speech_timeout from cfg) — explicit because the 1.5.0 default is SmartTurn, and omitting strategies would silently measure the wrong arm (Pitfall 2); arm smart_turn_v3 = SileroVADAnalyzer plus explicit TurnAnalyzerUserTurnStopStrategy(LocalSmartTurnAnalyzerV3()) so the config is self-documenting; Flux arm = when stt provider is deepgram-flux, NEVER set user_turn_strategies and do not set vad_analyzer — Flux auto-installs ExternalUserTurnStrategies and passing our own silently re-enables local turn detection, double endpointing, double interruptions (Pitfall 3, source-verified wiring rule). Enforce this last rule in code (raise ValueError if config combines deepgram-flux with an explicit turn strategy) so misconfiguration is impossible, and cross-check field names against the installed pinned source listed in read_first — if any name greps to nothing in the installed package, stop and report (Pitfall 1 warning sign).

    Write src/klanker_voice/pipeline.py: build_pipeline(cfg, transport) that loads the persona markdown from cfg.persona.prompt_path into an LLMContext system message, creates the LLMContextAggregatorPair with the arm-appropriate LLMUserAggregatorParams from factories, assembles Pipeline([transport.input(), stt, user_agg, llm, tts, transport.output(), assistant_agg]) per RESEARCH Pattern 1, and returns the pipeline plus the context and aggregator handles (the harness plan attaches observers to the PipelineWorker, and truncation checks need the context handle). Also expose register_greet_first(transport, worker_or_task): on the transport on_client_connected event, queue an LLMRunFrame so K produces the greeting immediately (D-04, Pattern 6); confirm the exact greeting-trigger shape against the installed 1.5.0 runner/template source (RESEARCH Open Question 2 says copy the generated template handler body — 15-minute verification). Use PipelineWorker/PipelineParams names, enable_metrics=True and enable_usage_metrics=True from day one so the plan-03 observer drops in without rework. Barge-in context truncation is built into the 1.5.0 frame path (Pattern 5) — write no custom truncation bookkeeping (RESEARCH Anti-Patterns; PITFALLS #3 is verified fixed at this pin).

    Write tests/test_factories.py: arm vad_timeout produces a SpeechTimeoutUserTurnStopStrategy and no turn analyzer; arm smart_turn_v3 produces a TurnAnalyzerUserTurnStopStrategy; deepgram-flux config yields aggregator params without user_turn_strategies and without vad_analyzer; deepgram-flux plus explicit strategy raises ValueError; LLM builder passes claude-haiku-4-5 through; TTS builder passes speed 1.1 and the configured voice_id through. Construct services with dummy api_key values from env-stubbed fixtures — no network calls in unit tests.
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest tests/test_factories.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All three turn arms constructible purely from config; Flux arm provably sets no local turn strategies (unit-tested)
    - Misconfiguration (flux + explicit strategy) raises at load/build time, not at runtime
    - build_pipeline returns a 1.5.0-canonical pipeline with persona-seeded LLMContext and metrics enabled
    - No deprecated 0.x surfaces used anywhere (no transport-level VAD, no aggregation_timeout, no allow_interruptions)
  </acceptance_criteria>
  <done>One config-driven factory path builds every pipeline variant this phase needs; PIPE-04's swap mechanism is real code with tests.</done>
</task>

<task type="auto">
  <name>Task 3: Entrypoints for both run modes and live smoke (D-08)</name>
  <read_first>
    - .planning/phases/01-local-pipeline-latency-harness/01-RESEARCH.md — Code Examples (console.py LocalAudioTransport surface; bot.py runner with webrtc + eval transport_params), Alternatives table row on terminal mode (the runner has NO local-audio route — separate thin entrypoint is the intended shape)
    - apps/voice/.venv/lib/python3.12/site-packages/pipecat/runner/ — confirm RunnerArguments/create_transport/main surfaces before writing bot.py
  </read_first>
  <files>apps/voice/bot.py, apps/voice/console.py</files>
  <action>
    Write apps/voice/bot.py per the RESEARCH runner example: transport_params dict with webrtc (TransportParams, audio in/out enabled) and eval (EvalTransportParams, audio in/out enabled) factories; async bot(runner_args) that creates the transport via create_transport, calls load_config() (KLANKER_PIPELINE_CONFIG-aware), build_pipeline, registers greet-first, and runs the PipelineWorker; main() guard delegating to pipecat.runner.run.main. `uv run python bot.py -t webrtc` must serve the bundled pipecat-ai-prebuilt localhost page (the D-08 web verification surface — same SmallWebRTC transport path as prod); `-t eval` is the plan-03 harness target. The runner's dotenv loading picks up apps/voice/.env.

    Write apps/voice/console.py: thin terminal mode (the D-08 iteration surface) constructing LocalAudioTransport(LocalAudioTransportParams(audio_in_enabled=True, audio_out_enabled=True)) and reusing the exact same load_config + build_pipeline + greet-first path — zero pipeline logic duplicated. Add a module docstring noting headphones are required for barge-in testing at the terminal (echo self-interruption, RESEARCH Pitfall 6) and that pyaudio comes from the dev dependency group.

    Live smoke: start bot.py -t webrtc in the background, wait for startup, and assert the HTTP surface responds on localhost:7860; then stop it. Full conversational verification is the human-check below.
  </action>
  <verify>
    <automated>cd apps/voice && { uv run python bot.py -t webrtc >/tmp/klanker-bot-smoke.log 2>&1 & }; BOT=$!; RC=1; for i in $(seq 1 30); do if curl -sf http://localhost:7860/ -o /dev/null; then RC=0; break; fi; sleep 1; done; pkill -P $BOT 2>/dev/null; kill $BOT 2>/dev/null; exit $RC</automated>
    <human-check>
      <test>Run `make -C apps/voice env` if .env is stale, then `cd apps/voice && uv run python bot.py -t webrtc`, open http://localhost:7860, connect, and just listen. Then say "who are you?" and one follow-up that references your first question. Afterwards try `uv run python console.py` with headphones.</test>
      <expected>K speaks FIRST without you saying anything, introduces itself as K (D-01, D-04); answers are 1-2 punchy sentences with a depth hook (D-03, D-05); the follow-up answer shows it remembered the earlier turn (PIPE-03); terminal mode holds the same conversation (D-08).</expected>
      <why_human>Conversational feel, voice output, and greet-first timing need ears; no automated check hears audio.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `uv run python bot.py -t webrtc` serves the prebuilt localhost page and holds a real conversation with only the three .env keys (PIPE-07)
    - `uv run python console.py` runs the identical pipeline through laptop mic/speaker (D-08)
    - K greets first on connect, in persona, and remembers the session (D-04, PIPE-03, PIPE-06)
    - Automated smoke (process up + HTTP 200 on localhost:7860) passes
  </acceptance_criteria>
  <done>The walking skeleton talks: full mic-to-speaker loop in both run modes with persona, memory, and greet-first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/mic -> localhost bot | Untrusted user speech enters the pipeline (localhost only this phase) |
| pipeline.toml -> service construction | Config values drive code paths (checked-in, but validated anyway) |
| Bot -> vendor APIs | Keys leave the process over TLS via official SDK/WS clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-03 | Information Disclosure | logging of service Settings/constructor args | medium | mitigate | never log constructor args or dump Settings objects; keys only read from env at build time |
| T-1-04 | Tampering | pipeline.toml config injection / bad knobs | low | mitigate | config.py V5 validation: provider allowlist, range checks, credential-field rejection (Task 1) |
| T-1-05 | Elevation of Privilege | localhost web server reachable on LAN during dev | low | accept | local-only phase, no auth by design (Phase 3); dev machine on trusted network; deployed exposure is Phase 4's threat model |
| T-1-06 | Tampering (LLM) | prompt injection via user speech | low | mitigate | no tools/data access this phase; persona instructs steer-back over meta-instruction obedience (D-06, RESEARCH security domain) |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | no new installs this plan; uv.lock from plan 01 governs |
</threat_model>

<verification>
- `cd apps/voice && uv run pytest tests/ -x -q` green (config + factories)
- Live smoke: bot.py -t webrtc serves localhost:7860; human-check items harvested to end-of-phase UAT
- pipeline.toml contains no credential material; `git check-ignore -q apps/voice/.env` still passes
- Schema push gate: not applicable — no ORM/schema files in this phase
</verification>

<success_criteria>
- A developer can hold a spoken conversation with K in both run modes using only the three provider keys (PIPE-07, D-08)
- Persona v1 (D-01, D-03..D-07) live; greet-first proven (D-04); in-session memory works (PIPE-03)
- STT/turn/LLM/TTS selection is config-only (PIPE-04, D-09), with the Flux wiring rule enforced in code
</success_criteria>

<output>
Create `.planning/phases/01-local-pipeline-latency-harness/01-02-SUMMARY.md` when done.

## Artifacts this plan produces
- Files: apps/voice/pipeline.toml, apps/voice/prompts/concierge.md, apps/voice/bot.py, apps/voice/console.py, apps/voice/src/klanker_voice/config.py, apps/voice/src/klanker_voice/factories.py, apps/voice/src/klanker_voice/pipeline.py, apps/voice/tests/{conftest.py,test_config.py,test_factories.py}
- Classes: PipelineConfig, SttConfig, FluxConfig, TurnConfig, LlmConfig, TtsConfig, PersonaConfig
- Functions: load_config, build_stt, build_llm, build_tts, build_user_aggregator_params, build_pipeline, register_greet_first, bot (runner entry)
- CLI entrypoints: `uv run python bot.py -t webrtc|eval`, `uv run python console.py`
- Config keys (pipeline.toml): stt.provider, stt.model, stt.flux.eot_threshold, stt.flux.eager_eot_threshold, turn.strategy, turn.vad_stop_secs, turn.user_speech_timeout, llm.provider, llm.model, tts.provider, tts.model, tts.voice_id, tts.speed, persona.prompt_path
- Env vars consumed: KLANKER_PIPELINE_CONFIG (optional config path override)
</output>
