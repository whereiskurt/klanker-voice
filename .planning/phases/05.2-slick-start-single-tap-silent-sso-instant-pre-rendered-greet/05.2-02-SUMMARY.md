---
phase: 05.2-slick-start-single-tap-silent-sso-instant-pre-rendered-greet
plan: 02
subsystem: voice-pipeline
tags: [elevenlabs, tts, mp3, pytest, tomllib, httpx]

# Dependency graph
requires:
  - phase: 05.2-04
    provides: PersonaConfig.greet_first toggle + pipeline.toml greet_first = false (server no longer double-greets)
provides:
  - greetings.source.json — the three approved KPH greeting texts (source of truth)
  - render_greetings.py — reads tts.voice_id from pipeline.toml, calls ElevenLabs, emits greeting-N.mp3 + manifest
  - apps/voice Makefile `greetings` target (`.venv/bin/python scripts/render_greetings.py`)
  - 3 committed greeting-N.mp3 clips (mp3_44100_128, eleven_flash_v2_5, speed 1.1) rendered from voice UgBBYS2sOqTuMpoF3BR0
  - greetings.manifest.json = {voiceId, model, clips:[{text,file}]}
  - test_greeting_voice_drift.py — CI guard failing the build if manifest.voiceId or clip texts drift from pipeline.toml/source
affects: [05.2-03 (client greeting player consumes greetings.manifest.json + the mp3 clips)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Rendered-asset-as-source-of-truth: binary output (MP3s + manifest) committed to the repo so runtime needs no ElevenLabs secret; a pure-string-comparison pytest (no API key) guards it from drifting out of sync with the live config"

key-files:
  created:
    - apps/voice/client/public/greetings/greetings.source.json
    - apps/voice/scripts/render_greetings.py
    - apps/voice/client/public/greetings/greeting-1.mp3
    - apps/voice/client/public/greetings/greeting-2.mp3
    - apps/voice/client/public/greetings/greeting-3.mp3
    - apps/voice/client/public/greetings/greetings.manifest.json
    - apps/voice/tests/test_greeting_voice_drift.py
  modified:
    - apps/voice/Makefile

key-decisions:
  - "Combined Task 1 (source/script/Makefile) and Task 2's render artifacts into ONE commit (`feat(voice): render KPH greeting clips from configured voice + manifest`), per the source plan's own Task 5 Step 5 boundary — no separate 'files created, not yet rendered' commit exists in history"
  - "`.env`'s ELEVENLABS_API_KEY was already present and valid; `make -C apps/voice env` (SSM refresh) failed with an expired/unauthenticated AWS SSO profile, but the render did not need a fresh fetch since the key was already on disk from a prior session"

patterns-established:
  - "Voice-asset drift guard: any TTS voice swap in pipeline.toml must be paired with a `make -C apps/voice greetings` re-render + commit, or CI fails via test_greeting_voice_drift.py"

requirements-completed: [CLNT-01, CLNT-02]

coverage:
  - id: D1
    description: "greetings.source.json holds the 3 approved greeting texts verbatim; render_greetings.py reads tts.voice_id from pipeline.toml, calls ElevenLabs (eleven_flash_v2_5, speed 1.1, mp3_44100_128), and writes greeting-N.mp3 + greetings.manifest.json"
    requirement: "CLNT-01"
    verification:
      - kind: unit
        ref: "manual render run: `make -C apps/voice greetings` — printed 3 render lines, wrote 3 mp3s + manifest"
        status: pass
      - kind: other
        ref: "greetings.manifest.json inspected: voiceId=UgBBYS2sOqTuMpoF3BR0, model=eleven_flash_v2_5, 3 clips with texts matching greetings.source.json verbatim"
        status: pass
    human_judgment: false
  - id: D2
    description: "The 3 rendered clips are valid MP3 (mp3_44100_128) audio, not error bodies or silence"
    requirement: "CLNT-01"
    verification:
      - kind: other
        ref: "`file` command on all 3 greeting-N.mp3: 'MPEG ADTS, layer III, v1, 128 kbps, 44.1 kHz, Monaural' for each"
        status: pass
    human_judgment: true
    rationale: "File-type/bitrate inspection confirms valid MP3 structure but not perceptual audio quality (intelligibility, correct KPH voice character) — that is a listening judgment call, deferred to the phase's consolidated live-verification pass per STATE.md precedent."
  - id: D3
    description: "test_greeting_voice_drift.py fails CI if manifest.voiceId != pipeline.toml tts.voice_id or manifest clip texts != greetings.source.json — pure string comparison, no ElevenLabs key needed"
    requirement: "CLNT-02"
    verification:
      - kind: unit
        ref: "apps/voice/tests/test_greeting_voice_drift.py#test_greeting_clips_match_configured_voice"
        status: pass
      - kind: unit
        ref: "apps/voice/tests/test_greeting_voice_drift.py#test_manifest_lists_all_source_greetings"
        status: pass
      - kind: other
        ref: "manual drift-proof: temporarily set manifest.voiceId to 'WRONG_VOICE_ID', re-ran pytest -> test_greeting_clips_match_configured_voice FAILED as expected; reverted via `git checkout --`, re-ran -> both PASSED"
        status: pass
    human_judgment: false

# Metrics
duration: 20min
completed: 2026-07-06
status: complete
---

# Phase 05.2 Plan 02: Greeting source + render script + rendered clips + voice-drift CI guard Summary

**Rendered 3 KPH greeting MP3 clips from the configured ElevenLabs voice (UgBBYS2sOqTuMpoF3BR0, eleven_flash_v2_5, speed 1.1) via a new `render_greetings.py` + `make -C apps/voice greetings`, committed the clips + manifest, and added a pure-string-comparison pytest that fails CI if the clips ever drift from pipeline.toml's voice_id.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-07-07T01:07:50Z
- **Tasks:** 3 (Task 1 auto, Task 2 checkpoint:human-verify — pre-authorized, Task 3 auto/tdd)
- **Files modified:** 8 (1 modified, 7 created)

## Accomplishments
- `greetings.source.json` holds the 3 approved, written-for-the-ear KPH greeting texts verbatim (no emojis/markdown/lists; "DEFCON dot run" spelled out)
- `render_greetings.py` mirrors `audition.py`'s ElevenLabs HTTP call pattern: reads `[tts] voice_id` from `pipeline.toml`, renders each source text via `eleven_flash_v2_5` at speed `1.1` to `mp3_44100_128`, and writes `greetings.manifest.json` = `{voiceId, model, clips:[{text,file}]}`
- `apps/voice/Makefile` gained a `.PHONY: greetings` target running `.venv/bin/python scripts/render_greetings.py`, matching the repo's existing venv-python convention
- Live, billed ElevenLabs render executed (checkpoint pre-authorized by the user) — 3 `greeting-N.mp3` clips + manifest committed; manifest confirms `voiceId: UgBBYS2sOqTuMpoF3BR0`, `model: eleven_flash_v2_5`, and all 3 clip texts match the source verbatim
- `test_greeting_voice_drift.py` added (TDD): two tests assert `manifest.voiceId == pipeline.toml tts.voice_id` and `manifest clip texts == greetings.source.json`; proved the guard bites by temporarily mutating `manifest.voiceId` (test failed as expected), then reverting via `git checkout --` (both tests green again)
- Full Python suite: 170/170 pass (168 pre-existing + 2 new drift-guard tests)

## Task Commits

Each atomic commit follows the source-plan's (`docs/superpowers/plans/2026-07-06-slick-start.md`) own commit boundaries:

1. **Task 1 + Task 2 (source/script/Makefile + rendered clips/manifest)** - `58f2aa6` (feat) — `feat(voice): render KPH greeting clips from configured voice + manifest`
2. **Task 3 (voice-drift CI guard)** - `c35d49a` (test) — `test(voice): CI guard — greeting clips must match configured voice_id`

**Plan metadata:** (this commit, following this SUMMARY)

_Note: Task 1 created files but the source plan defines only ONE commit spanning Task 1 + Task 2's rendered output (its own Task 5 Step 5) — no intermediate "files created, not yet rendered" commit exists, matching the authoritative plan exactly._

## Files Created/Modified
- `apps/voice/client/public/greetings/greetings.source.json` - the 3 approved greeting texts
- `apps/voice/scripts/render_greetings.py` - ElevenLabs render script (voice_id from pipeline.toml)
- `apps/voice/Makefile` - new `greetings` target
- `apps/voice/client/public/greetings/greeting-1.mp3` - rendered clip 1 (144K)
- `apps/voice/client/public/greetings/greeting-2.mp3` - rendered clip 2 (152K)
- `apps/voice/client/public/greetings/greeting-3.mp3` - rendered clip 3 (120K)
- `apps/voice/client/public/greetings/greetings.manifest.json` - `{voiceId, model, clips}` consumed by 05.2-03's client player + the drift guard
- `apps/voice/tests/test_greeting_voice_drift.py` - CI drift guard (2 tests)

## Decisions Made
- Followed the source plan's exact commit boundary: Task 1's created-but-unrendered files were staged but NOT committed separately; everything (source.json, script, Makefile, 3 clips, manifest) landed in one `feat` commit at the render checkpoint, matching `docs/superpowers/plans/2026-07-06-slick-start.md` Task 5 Step 5 verbatim.
- `make -C apps/voice env` (SSM key refresh) failed due to an expired/unauthenticated AWS SSO profile (`klanker-application`), but this did not block the render — `apps/voice/.env` already had a valid `ELEVENLABS_API_KEY` from a prior session, sourced directly via `set -a; . ./.env; set +a` before running the script.

## Deviations from Plan

None - plan executed exactly as written. The `make env` SSO failure did not require a deviation rule since the render notes explicitly anticipated `.env` might already contain the key ("apps/voice/.env — already contains ELEVENLABS_API_KEY"), and it did.

## Issues Encountered
- `make -C apps/voice env` failed with `ParameterNotFound`/expired SSO session against `klanker-application` profile. Not fixed (out of scope — SSO refresh is an operator action, not a plan task); worked around by using the pre-existing valid `.env`. Flagging for STATE.md blockers in case a future render needs a fresh SSM fetch and the SSO session is still stale.

## User Setup Required
None - no external service configuration required for this plan. (A future voice swap will need a human to run `aws sso login --profile klanker-application` before `make -C apps/voice env` if `.env` is stale/missing, per the pre-existing Phase 4 SSO blocker pattern.)

## Next Phase Readiness
- 05.2-03 (client greeting player + deferred Live handoff) can now consume `greetings.manifest.json` + the 3 committed MP3 clips directly — no rendering dependency remains.
- CI is now protected against silent voice drift: any future `pipeline.toml` voice_id change without a `make -C apps/voice greetings` re-render will fail `test_greeting_voice_drift.py`.
- Perceptual audio quality (does the clip actually sound like a good KPH greeting) is deferred to the phase's consolidated live-verification pass, consistent with every other 05.2/05-0x plan's deferred-checkpoint pattern in STATE.md.

---
*Phase: 05.2-slick-start-single-tap-silent-sso-instant-pre-rendered-greet*
*Completed: 2026-07-06*

## Self-Check: PASSED

All 8 created/modified deliverable files found on disk; both commits (`58f2aa6`, `c35d49a`) verified present in git log.
