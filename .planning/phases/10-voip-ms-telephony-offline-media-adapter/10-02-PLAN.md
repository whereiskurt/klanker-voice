---
phase: 10-voip-ms-telephony-offline-media-adapter
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/voice/src/klanker_voice/telephony/transport.py
  - apps/voice/src/klanker_voice/telephony/__init__.py
  - apps/voice/tests/test_telephony_transport.py
autonomous: true
requirements: []          # Telephony milestone has NO REQ-IDs — coverage is driven by ROADMAP Phase 10's 6 success criteria + CONTEXT D-01..D-10, NOT fabricated REQ-IDs (confirmed phase_req_ids=null).

must_haves:
  truths:
    - "TelephonyTransport(BaseTransport) exposes `input() -> FrameProcessor`, `output() -> FrameProcessor`, `async start()`, `async stop()`, built on pipecat's BaseInputTransport / BaseOutputTransport (SmallWebRTCTransport three-class shape); the build_pipeline contract is preserved — the graph begins at transport.input() and ends at transport.output() (D-05, spec §8/§2; ROADMAP SC2)"
    - "Input path: RTP PCMU payload → sequence/jitter handling → μ-law decode → signed-16-bit PCM → stateful resample 8 kHz→pipeline rate → `InputAudioRawFrame(audio=…, num_channels=1, sample_rate=<pipeline rate>)` pushed via BaseInputTransport.push_audio_frame; the frame's sample_rate MUST equal the resampled PCM rate (NOT 8000) so Deepgram reads it correctly (D-05, spec §9; ROADMAP SC2)"
    - "Output path: `write_audio_frame(OutputAudioRawFrame)` → stateful resample pipeline rate→8 kHz → μ-law encode → 20 ms/160-sample framing → RTP packetize (seq++, ts += 160, stable SSRC, payload_type from params) → media.write_packet (D-05, spec §8; ROADMAP SC2)"
    - "Resampling happens ONCE at the 8 kHz boundary with a stateful streaming resampler — one instance per direction, fixed rate pair, created via pipecat's `create_stream_resampler`/`SOXRStreamAudioResampler` with `clear_after_secs=None` — NOT a fresh per-frame resample (no boundary artifacts / clock drift) (D-06, spec §9; ROADMAP SC2)"
    - "`async def flush_output_audio(self) -> None` on TelephonyTransport flushes queued outbound audio and is wired to the existing pipecat InterruptionFrame path (BaseOutputTransport.handle_interruptions); the application output queue is kept shallow (target 20–60 ms); NO second VAD/endpointing system is added and the Deepgram/Flux turn strategy stays authoritative (D-07, spec §10; ROADMAP SC3)"
    - "`on_client_connected` fires exactly once when the media path is ready (lets register_greet_first fire); `on_client_disconnected` fires exactly once on terminal close and maps to lifecycle.on_transport_disconnected; telephony close is terminal — no browser-style reconnect grace; `stop()` is idempotent (D-05/D-08, spec §8/§6.8; ROADMAP SC3)"
    - "A real TelephonyTransport fed by an OfflineRtpMediaSession is accepted by create_call_session(channel=\"pstn\", …) which builds the REAL build_pipeline around it offline (no SIP/Asterisk/socket); synthetic PCMU RTP driven into input() yields a correctly-typed, correctly-sample-rated InputAudioRawFrame, and an OutputAudioRawFrame driven into output() yields captured PCMU RTP — the §19-B exit criterion expressed as a hermetic offline test (D-08/D-10, spec §19-B; ROADMAP SC4)"
    - "The full pre-existing apps/voice suite stays green — this phase only ADDS telephony; WebRTC/browser behavior is byte-unchanged (spec §22 preserve-list; ROADMAP SC-regression)"
  artifacts:
    - apps/voice/src/klanker_voice/telephony/transport.py
    - apps/voice/tests/test_telephony_transport.py
    - "architecture/coupling note in 10-02-SUMMARY.md + the transport.py module docstring (mirror Phase 9 D-08)"
  key_links:
    - "TelephonyTransport.input()/output() → build_pipeline(cfg, transport) — the existing transport-neutral seam; the graph starts at input(), ends at output()"
    - "TelephonyTransport → create_call_session(channel=\"pstn\") — the Phase 9 seam the transport plugs into; the §19-B proof runs through it unchanged"
    - "TelephonyInputTransport receive loop → media.read_packet → RtpDepacketizer → ulaw_decode → input resampler → push_audio_frame(InputAudioRawFrame)"
    - "TelephonyOutputTransport.write_audio_frame → output resampler → ulaw_encode → RtpPacketizer → media.write_packet"
    - "InterruptionFrame → BaseOutputTransport.handle_interruptions → flush_output_audio → media/queue flush (the barge-in path, D-07)"
    - "on_client_disconnected → lifecycle.on_transport_disconnected (terminal, fire-once; D-08)"
  prohibitions:
    - "NO SIP, NO UDP sockets (no `import socket` / `socket.socket`), NO live network media, NO binding to an Asterisk external-media address (Phase 11/C)"
    - "NO Asterisk configs, NO ARI/Stasis controller, NO bridges/external-media channels (Phase 11/C)"
    - "NO VoIP.ms subaccount/DID, NO phone→code→tier identity, NO call-answer security gate (Phase 12/D)"
    - "NO physical payphone/ATA (Phase 13/E); NO Terraform/Terragrunt/telephony-edge/SSM/alarms (Phase 14/F)"
    - "NO new provider construction — telephony builds NO STT/LLM/TTS/VAD; factories.py stays the single source and build_pipeline is reused verbatim (spec §22.4)"
    - "NO edits to apps/voice/src/klanker_voice/{call_runtime,pipeline,factories,webrtc}.py or server.py — the shared runtime and browser path are untouched (the ONLY new value threaded into create_call_session is channel=\"pstn\")"
    - "NO second VAD/endpointing system, and do NOT undo the repo's Flux double-endpointing guard (spec §10 / D-07)"
    - "NO application echo cancellation / AEC (spec §10 — only after measuring a real problem)"
    - "NO pipeline.toml [telephony] loader this phase (D-09 deferred to Phase 11)"
    - "NEVER rename 'klanker-voice' to 'voiceai' anywhere (project naming constraint)"
---

<!-- planner-discipline-allow: socket -->
<!-- planner-discipline-allow: SIP -->
<!-- planner-discipline-allow: Asterisk -->
<!-- planner-discipline-allow: aiortc -->
<!-- planner-discipline-allow: SmallWebRTC -->

<objective>
Build the `TelephonyTransport` (spec Phase B, D-05/D-06/D-07/D-08) that lets recorded/synthetic telephone audio traverse the REAL Klanker pipeline offline. On top of Plan 01's codec + RTP + offline `RtpMediaSession`, add `telephony/transport.py`: a `TelephonyTransport(BaseTransport)` with input/output frame processors (mirroring the `SmallWebRTCTransport` three-class shape on `BaseInputTransport`/`BaseOutputTransport`), one stateful streaming resampler per direction at the 8 kHz boundary, fire-once connect/disconnect events, idempotent start/stop, and `flush_output_audio()` wired to the existing pipecat interruption path. Then prove the §19-B exit criterion as a hermetic offline test: a real TelephonyTransport + offline media, run through `create_call_session(channel="pstn")`'s real `build_pipeline`, with synthetic PCMU RTP flowing in and PCMU RTP captured out — no SIP, no Asterisk, no socket.

Purpose: this is the seam-proving milestone. Phase 9 extracted a transport-neutral runtime; this closes the loop by building the telephony transport it accepts and demonstrating 8 kHz μ-law audio through the genuine pipeline offline — de-risking Phases 11–14.

Output: `telephony/transport.py`, updated `telephony/__init__.py`, `tests/test_telephony_transport.py`, and a short architecture/coupling note (D-08). Stop after Phase B — no Asterisk/ARI/SIP/sockets/infra.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-voip-ms-telephony-offline-media-adapter/10-CONTEXT.md
@.planning/phases/10-voip-ms-telephony-offline-media-adapter/10-PATTERNS.md
@.planning/phases/10-voip-ms-telephony-offline-media-adapter/10-01-PLAN.md
@docs/superpowers/specs/2026-07-11-voipms-telephony-integration.md

@apps/voice/src/klanker_voice/call_runtime.py
@apps/voice/src/klanker_voice/pipeline.py
@apps/voice/tests/test_call_runtime.py
@apps/voice/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: telephony/transport.py — TelephonyTransport + In/Out processors + per-direction resampling + fire-once events (D-05, D-06)</name>
  <files>apps/voice/src/klanker_voice/telephony/transport.py, apps/voice/src/klanker_voice/telephony/__init__.py</files>
  <read_first>
    - .planning/phases/10-voip-ms-telephony-offline-media-adapter/10-PATTERNS.md (transport.py section: the SmallWebRTCTransport three-class shape L919–995 / In-transport L574–688 / Out-transport L811–905; the "two distinct params objects" note; Shared Pattern 2 resampler + the soxr one-instance-per-rate-pair gotcha; Shared Pattern 3 subclass contract; Shared Pattern 4 fire-once events)
    - apps/voice/src/klanker_voice/telephony/types.py (TelephonyTransportParams + RtpMediaSession — the ctor's `params` and `media`)
    - apps/voice/src/klanker_voice/telephony/media.py (ulaw_decode/ulaw_encode, PcmFramer, RtpPacketizer, RtpDepacketizer — the codec/RTP the processors compose)
    - apps/voice/src/klanker_voice/call_runtime.py (how create_call_session registers `on_client_disconnected`/`on_client_connected` and calls build_pipeline(cfg, transport) — the two event names TelephonyTransport MUST register; L191–197)
    - apps/voice/src/klanker_voice/pipeline.py (build_pipeline signature L65–73 — graph starts at transport.input(), ends at transport.output())
    - pipecat installed: `pipecat.transports.base_input.BaseInputTransport` (ctor `(params, **kwargs)`; `start(StartFrame)`/`stop(EndFrame)`/`cancel(CancelFrame)`/`cleanup()`; `push_audio_frame(InputAudioRawFrame)`; `set_transport_ready(StartFrame)`), `pipecat.transports.base_output.BaseOutputTransport` (`write_audio_frame(OutputAudioRawFrame) -> bool` is the ONE sink override), `pipecat.transports.base_transport.{BaseTransport,TransportParams}`, `pipecat.audio.utils.create_stream_resampler`, `pipecat.frames.frames.{InputAudioRawFrame,OutputAudioRawFrame,StartFrame,EndFrame}`
  </read_first>
  <action>
    Create `apps/voice/src/klanker_voice/telephony/transport.py` with the three-class structure mirroring `SmallWebRTCTransport` (D-05, PATTERNS transport analog). Import ONLY pipecat base types + `klanker_voice.telephony` (types/media) — NEVER a socket, aiortc, SmallWebRTC, or a speech-provider factory.

    1. `TelephonyInputTransport(BaseInputTransport)`:
       - `__init__(self, media, params: TelephonyTransportParams, pipecat_params: TransportParams, **kwargs)` calling `super().__init__(pipecat_params, **kwargs)`; hold a stateful INPUT resampler `create_stream_resampler(clear_after_secs=None)` (one instance, fixed 8000→pipeline-rate pair — D-06; the soxr class raises if reused with a different rate pair, PATTERNS gotcha), an `RtpDepacketizer`, and the `media`.
       - `async start(self, frame: StartFrame)`: `await super().start(frame)`; guard against re-init (idempotent); `await self.set_transport_ready(frame)` (this is where `on_client_connected` becomes fire-once ready — D-05); if `pipecat_params.audio_in_enabled` and no task yet, spawn `self.create_task(self._receive_audio())`.
       - `async def _receive_audio(self)`: loop `packet = await self.media.read_packet()`; break on None; `parse_rtp` → depacketize (dup/reorder/one-missing) → `ulaw_decode` → `resampled = await self._input_resampler.resample(pcm, 8000, <pipeline rate>)` → `await self.push_audio_frame(InputAudioRawFrame(audio=resampled, num_channels=1, sample_rate=<pipeline rate>))`. The `sample_rate` MUST be the resampled rate, NOT 8000 (spec §9 Deepgram — PATTERNS Critical Note).
       - `async stop(self, frame: EndFrame)` / `async cancel(self, frame)` / `async cleanup(self)`: call a shared idempotent `_teardown()` (cancel the receive task once, close nothing the caller owns), then `await super().stop/cancel/cleanup(...)`.

    2. `TelephonyOutputTransport(BaseOutputTransport)`:
       - `__init__(self, media, params, pipecat_params, **kwargs)` → `super().__init__(pipecat_params, **kwargs)`; hold a stateful OUTPUT resampler `create_stream_resampler(clear_after_secs=None)` (fixed pipeline-rate→8000 pair), an `RtpPacketizer(params=params, ssrc=<stable random>)`, and a shallow outbound queue/buffer (target 20–60 ms — D-07).
       - `async write_audio_frame(self, frame: OutputAudioRawFrame) -> bool`: `pcm8k = await self._output_resampler.resample(frame.audio, frame.sample_rate, 8000)` → `ulaw = ulaw_encode(pcm8k)` → frame to 160-sample chunks (`PcmFramer`/params.samples_per_packet) → for each chunk `rtp = self._packetizer.packetize(chunk)` → `await self.media.write_packet(rtp)`; return True. Resample ONCE here at the boundary (D-06); document that the down-resample lives in `write_audio_frame` (vs setting `audio_out_sample_rate=8000` on the pydantic params — pick ONE location and state it, per PATTERNS L199).
       - `async start`/`stop`/`cancel`/`cleanup`: `set_transport_ready` in start; idempotent `_teardown()` shared across stop/cancel/cleanup.

    3. `TelephonyTransport(BaseTransport)`:
       - `__init__(self, *, call_id: str, media: RtpMediaSession, params: TelephonyTransportParams, input_name=None, output_name=None)` → `super().__init__(input_name=input_name, output_name=output_name)`; store `call_id`/`media`/`params`; construct the pydantic `TransportParams(audio_in_enabled=True, audio_out_enabled=True, audio_in_sample_rate=<pipeline rate>, audio_out_sample_rate=<pipeline rate>)` (the audio-enable container the In/Out processors need — distinct from `TelephonyTransportParams`, PATTERNS Critical Note); lazy `_input`/`_output`; register EXACTLY `on_client_connected` and `on_client_disconnected` via `self._register_event_handler(...)` (the two names create_call_session wires — Shared Pattern 4). Choose the pipeline rate deliberately (the standard pipecat input rate the existing pipeline uses; confirm from build_pipeline / the SmallWebRTC transport params in server.py) and document it.
       - `def input(self) -> TelephonyInputTransport` / `def output(self) -> TelephonyOutputTransport`: lazily construct and cache, passing `media`, `params`, and the pydantic params.

    Update `telephony/__init__.py` to export `TelephonyTransport` (and the In/Out classes if useful for tests). Add a module docstring: this is the offline "klanker-voice" telephony transport (spec Phase B), a transport-specific leaf that produces a `BaseTransport` exactly as `server.py` produces a `SmallWebRTCTransport` — never branches shared code; never renamed "voiceai".

    (`flush_output_audio` + the interruption wiring is Task 2 — leave a clearly-named stub or TODO the Task 2 change replaces, but do NOT ship un-wired interruption behavior as complete.)
  </action>
  <verify>
    <automated>(cd apps/voice && uv run python -c "import inspect; from klanker_voice.telephony.transport import TelephonyTransport; from klanker_voice.telephony.media import OfflineRtpMediaSession; from klanker_voice.telephony.types import TelephonyTransportParams; t=TelephonyTransport(call_id='c1', media=OfflineRtpMediaSession([]), params=TelephonyTransportParams()); from pipecat.transports.base_input import BaseInputTransport; from pipecat.transports.base_output import BaseOutputTransport; assert isinstance(t.input(), BaseInputTransport); assert isinstance(t.output(), BaseOutputTransport); assert set(t._event_handlers) >= {'on_client_connected','on_client_disconnected'}; print('transport shape OK')")</automated>
    <automated>(cd apps/voice && grep -v '^\s*#' src/klanker_voice/telephony/transport.py | grep -c -iE 'import socket|socket\.socket|import aiortc|smallwebrtc|asterisk|build_stt|build_llm|build_tts') # expect 0 — no socket/aiortc/SmallWebRTC/provider code in the telephony transport</automated>
    <automated>(cd apps/voice && grep -c -E 'create_stream_resampler' src/klanker_voice/telephony/transport.py) # expect >=2 — one stateful resampler per direction (D-06)</automated>
  </verify>
  <acceptance_criteria>
    - `transport.py` defines `TelephonyInputTransport(BaseInputTransport)`, `TelephonyOutputTransport(BaseOutputTransport)`, and `TelephonyTransport(BaseTransport)` with the D-05 ctor `__init__(*, call_id, media, params, …)`, lazy `input()`/`output()`, and exactly the two registered event names.
    - The shape check prints `transport shape OK` (input()/output() are BaseInput/BaseOutput subclasses; both event names registered).
    - `create_stream_resampler` appears ≥2 times (one stateful resampler per direction, `clear_after_secs=None`; D-06) — no per-frame construction inside `_receive_audio`/`write_audio_frame`.
    - The input path constructs `InputAudioRawFrame` with `sample_rate` = the resampled pipeline rate (grep confirms `sample_rate=` is not the literal 8000 in the InputAudioRawFrame construction).
    - `grep -v '^\s*#' transport.py | grep -c -iE 'import socket|socket\.socket|import aiortc|smallwebrtc|asterisk|build_stt|build_llm|build_tts'` returns 0.
  </acceptance_criteria>
  <done>TelephonyTransport mirrors the SmallWebRTCTransport three-class shape on BaseInputTransport/BaseOutputTransport, resamples once per direction with a stateful streaming resampler at the 8 kHz boundary, decodes RTP→PCM→InputAudioRawFrame (correct sample_rate) inbound and OutputAudioRawFrame→PCM→μ-law→RTP outbound, registers fire-once connect/disconnect events, and contains no socket/SIP/provider code.</done>
</task>

<task type="auto">
  <name>Task 2: flush_output_audio() + interruption wiring + idempotent stop/terminal-close semantics (D-07, D-08)</name>
  <files>apps/voice/src/klanker_voice/telephony/transport.py</files>
  <read_first>
    - .planning/phases/10-voip-ms-telephony-offline-media-adapter/10-PATTERNS.md (Interruption / flush_output_audio section: BaseOutputTransport.handle_interruptions L548–575 already clears queued audio on an InterruptionFrame; the base comment about "telephony serializers that clear the playout buffer on interruptions" is exactly D-07; Shared Pattern 4 fire-once + terminal-close mapping to lifecycle.on_transport_disconnected)
    - docs/superpowers/specs/2026-07-11-voipms-telephony-integration.md (§10 turn-taking/barge-in: interruption flush, 20–60 ms queue, NO second VAD, echo left alone; §6.8/§8 terminal close, fire-once disconnect)
    - apps/voice/src/klanker_voice/call_runtime.py (lifecycle.on_transport_disconnected is what on_client_disconnected maps to — terminal for telephony, no reconnect grace; L191–197)
    - pipecat installed: `pipecat.frames.frames.InterruptionFrame` (the barge-in signal) + `pipecat.transports.base_output.BaseOutputTransport.handle_interruptions`
  </read_first>
  <action>
    Extend `transport.py` with the D-07 interruption flush and the D-08 terminal-close semantics.

    1. Add `async def flush_output_audio(self) -> None` on `TelephonyTransport` (spec §10 signature) that clears the output processor's shallow outbound queue/buffer (drop the queued-but-unsent μ-law/RTP so barge-in audio doesn't play over the caller). Delegate to a method on `TelephonyOutputTransport` (e.g. `async def flush(self)`).

    2. Wire it to the EXISTING pipecat interruption path — do NOT add a second VAD/endpointing system (spec §10 / D-07; the Deepgram/Flux turn strategy stays authoritative). Override `TelephonyOutputTransport.handle_interruptions(self, frame: InterruptionFrame)` to (a) call `await super().handle_interruptions(frame)` so the base class's built-in queued-audio reset still runs, then (b) flush the telephony outbound buffer. Keep the application output queue SHALLOW — target 20–60 ms (spec §10); document the chosen depth. Rely on the `InterruptionFrame` the pipeline already emits — register no new detector.

    3. Terminal-close / fire-once (D-08): ensure `on_client_disconnected` is emitted EXACTLY once on terminal close (guard with a fired-once flag), and that `stop()` is idempotent (calling it twice does one teardown). Telephony close is terminal — do NOT implement any browser-style reconnect grace here (that stays a WebRTC concern; `on_client_disconnected` simply maps to `lifecycle.on_transport_disconnected` in create_call_session, unchanged). `on_client_connected` is emitted exactly once when the media path is ready (from the input transport's `set_transport_ready`, Task 1).

    Do NOT add application echo cancellation / AEC (spec §10). Do NOT touch call_runtime/pipeline/factories/server/webrtc.
  </action>
  <verify>
    <automated>(cd apps/voice && uv run python -c "import inspect; from klanker_voice.telephony.transport import TelephonyTransport, TelephonyOutputTransport; assert inspect.iscoroutinefunction(TelephonyTransport.flush_output_audio); assert 'handle_interruptions' in dir(TelephonyOutputTransport); print('flush + interruption hook OK')")</automated>
    <automated>(cd apps/voice && grep -c -iE 'SileroVAD|VADAnalyzer|new .*endpoint|second vad|EchoCancel|aec' src/klanker_voice/telephony/transport.py) # expect 0 — no second VAD/endpointing system, no AEC (spec §10 / D-07)</automated>
  </verify>
  <acceptance_criteria>
    - `TelephonyTransport.flush_output_audio` is an async method; `TelephonyOutputTransport` overrides `handle_interruptions` and calls `super().handle_interruptions(...)` before flushing the telephony buffer.
    - The shape check prints `flush + interruption hook OK`.
    - `grep -iE 'SileroVAD|VADAnalyzer|new .*endpoint|EchoCancel|aec' transport.py` returns 0 (no second VAD/endpointing, no AEC).
    - `on_client_disconnected` is guarded to fire exactly once on terminal close; `stop()` is idempotent (proven by Task 3's tests).
  </acceptance_criteria>
  <done>flush_output_audio() clears the shallow outbound telephony queue on barge-in, wired through BaseOutputTransport.handle_interruptions on the existing InterruptionFrame (no new VAD, no AEC); on_client_disconnected fires exactly once on terminal close and stop() is idempotent.</done>
</task>

<task type="auto">
  <name>Task 3: test_telephony_transport.py — transport unit tests + §19-B offline pipeline-traversal proof + architecture note (D-10, D-08)</name>
  <files>apps/voice/tests/test_telephony_transport.py</files>
  <read_first>
    - apps/voice/tests/test_call_runtime.py (the EXACT rig to reuse: `_FakeAwsClient`/`fake_aws`/`reset_active_count` fixtures, `_gate_result(bypass_accounting=True)`, `_quota_config`, and `_build_call_session(make_config_file, *, transport)` — swap FakeTransport for a real TelephonyTransport + OfflineRtpMediaSession; the event-fires-once assertion pattern driving `transport._event_handlers[...]` and counting `put_metric_data`)
    - apps/voice/tests/conftest.py (`make_config_file`, `stub_provider_keys`, `_reset_scale_in_protection_state`)
    - apps/voice/src/klanker_voice/telephony/transport.py (the API under test)
    - apps/voice/src/klanker_voice/telephony/media.py (OfflineRtpMediaSession + build_rtp/ulaw_encode to synthesize the input RTP; the write-capture list to assert PCMU RTP output)
    - .planning/phases/10-voip-ms-telephony-offline-media-adapter/10-PATTERNS.md (test sections: test_telephony_transport.py exact analog; the §19-B harness via _build_call_session with channel="pstn")
  </read_first>
  <action>
    Author `apps/voice/tests/test_telephony_transport.py` proving the D-05/D-06/D-07/D-08 contract and the §19-B exit criterion — hermetic and offline (no SIP, no Asterisk, no socket, no live providers). Reuse the test_call_runtime.py fakes verbatim (import or copy `_FakeAwsClient`/`fake_aws`/`reset_active_count`, `_gate_result`, `_quota_config`); use `stub_provider_keys` + `make_config_file` + `bypass_accounting=True` so `build_pipeline`'s factories construct client objects with no network and no real AWS.

    Write these tests:
    1. `test_rtp_input_emits_correct_audio_frame` — build synthetic PCMU RTP (ulaw_encode(silence/tone) → build_rtp with seq/ts increments), feed via an OfflineRtpMediaSession into a TelephonyTransport; drive the input processor's receive path (start it, or call the receive step directly) and assert a pushed `InputAudioRawFrame` with `num_channels == 1` and `sample_rate == <pipeline rate>` (NOT 8000) and non-empty resampled audio.
    2. `test_output_frame_emits_pcmu_rtp` — drive `TelephonyOutputTransport.write_audio_frame(OutputAudioRawFrame(...))` and assert the OfflineRtpMediaSession's write-capture list received one or more RTP datagrams that `parse_rtp` accepts, with the expected payload_type (from params), a stable SSRC, and timestamp incrementing by 160.
    3. `test_interruption_flushes_output` — enqueue outbound audio, deliver an `InterruptionFrame` (or call `flush_output_audio()`), and assert the outbound telephony queue is emptied (no further RTP written for the interrupted turn).
    4. `test_stop_is_idempotent` — call `stop()` (or the transport teardown) twice and assert exactly one teardown effect.
    5. `test_disconnect_event_fires_once` — drive terminal close and assert `on_client_disconnected` handlers run exactly once (count via the fake-AWS `put_metric_data` release emit through the wired lifecycle, mirroring test_call_runtime.py), and `on_client_connected` fired exactly once when media became ready.
    6. `test_telephone_audio_traverses_real_pipeline_offline` (the §19-B exit proof) — build a real `TelephonyTransport` fed by an OfflineRtpMediaSession pre-loaded with synthetic PCMU RTP (WAV-derived or generated), then `create_call_session(transport=telephony_transport, …, channel="pstn", …)` via the reused `_build_call_session` shape; assert the returned `CallSession` wraps the REAL `build_pipeline` (worker not None, lifecycle a real SessionLifecycle) with the telephony transport at both ends of the graph, and that the transport boundary round-trips audio (input RTP → InputAudioRawFrame; OutputAudioRawFrame → captured PCMU RTP) with NO SmallWebRTC/aiortc/HTTP/socket object anywhere in the test. Add a module docstring stating this is the hermetic offline form of §19-B; the live "Deepgram transcribes → ElevenLabs responds" round-trip needs real API keys + network and is a documented Phase-11 live eval (NOT an offline gate).

    Then produce the D-08 architecture + coupling note (mirror Phase 9): a section in `10-02-SUMMARY.md` and a short paragraph in the `transport.py` module docstring describing (a) the seam — TelephonyTransport is a transport-specific leaf that create_call_session(channel="pstn") accepts unchanged, resampling once at the 8 kHz boundary; (b) the couplings — the pipeline rate the transport targets (matched to the existing pipeline input rate), the down-resample location choice (write_audio_frame vs pydantic audio_out_sample_rate), and the fact that the live provider round-trip is deferred to a Phase-11 live eval; (c) that call_runtime/pipeline/factories were reused verbatim with zero edits.

    Finally run the FULL existing suite to prove browser voice is unchanged (spec §22 preserve-list): every pre-existing test still green alongside the new telephony tests.
  </action>
  <verify>
    <automated>(cd apps/voice && uv run pytest tests/test_telephony_transport.py -q)</automated>
    <automated>(cd apps/voice && grep -c -iE 'smallwebrtc|aiortc|TestClient|import socket' tests/test_telephony_transport.py) # expect 0 — the §19-B proof uses NO WebRTC/HTTP/socket object</automated>
    <automated>(cd apps/voice && uv run pytest -q)</automated>
  </verify>
  <acceptance_criteria>
    - `apps/voice/tests/test_telephony_transport.py` exists with the six named tests and constructs a real `TelephonyTransport` + `OfflineRtpMediaSession`, run through `create_call_session(channel="pstn")`, with NO SmallWebRTC/aiortc/HTTP/socket object (grep returns 0).
    - `uv run pytest tests/test_telephony_transport.py` passes: correct input frame (right sample_rate), PCMU RTP output (payload_type/SSRC/ts+=160), interruption flush, idempotent stop, fire-once disconnect+connect, and the §19-B offline pipeline-traversal proof.
    - `uv run pytest` (full suite) exits 0 — every pre-existing lifecycle / quota / greeting / teardown / server / slot-leak test still passes unchanged (spec §22 preserve-list; dynamodb-local-gated tests may skip as before).
    - `10-02-SUMMARY.md` contains the architecture + coupling note (seam + pipeline-rate/down-resample-location couplings + the deferred-live-eval note), and the `transport.py` module docstring summarizes the seam.
  </acceptance_criteria>
  <done>test_telephony_transport.py proves the transport emits correct Pipecat frames from RTP and PCMU RTP from Pipecat frames, flushes on interruption, is idempotent on stop, fires connect/disconnect exactly once, and — the §19-B exit proof — runs synthetic telephone audio through the real build_pipeline offline via create_call_session(channel="pstn") with no SIP/Asterisk/socket; the full pre-existing suite stays green and the D-08 note is recorded.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces (every new symbol)

**New module:** `apps/voice/src/klanker_voice/telephony/transport.py`
- `TelephonyInputTransport(BaseInputTransport)` — ctor `(media, params, pipecat_params, **kwargs)`; stateful input resampler (8000→pipeline rate, `clear_after_secs=None`); `RtpDepacketizer`; `_receive_audio()` receive loop → `push_audio_frame(InputAudioRawFrame(..., sample_rate=<pipeline rate>))`; idempotent `_teardown()`.
- `TelephonyOutputTransport(BaseOutputTransport)` — ctor `(media, params, pipecat_params, **kwargs)`; stateful output resampler (pipeline rate→8000); `RtpPacketizer`; shallow outbound queue (20–60 ms); `write_audio_frame(OutputAudioRawFrame) -> bool` (resample→ulaw_encode→frame→packetize→media.write_packet); `handle_interruptions(InterruptionFrame)` override; `flush()`.
- `TelephonyTransport(BaseTransport)` — ctor `__init__(*, call_id, media: RtpMediaSession, params: TelephonyTransportParams, input_name=None, output_name=None)`; internal pydantic `TransportParams(audio_in/out_enabled=True, audio_in/out_sample_rate=<pipeline rate>)`; lazy `input()`/`output()`; registers `on_client_connected`/`on_client_disconnected` (fire-once); `async flush_output_audio() -> None`.

**Updated:** `apps/voice/src/klanker_voice/telephony/__init__.py` — adds `TelephonyTransport` (and In/Out classes) to the barrel.

**New test module:** `apps/voice/tests/test_telephony_transport.py` — `test_rtp_input_emits_correct_audio_frame`, `test_output_frame_emits_pcmu_rtp`, `test_interruption_flushes_output`, `test_stop_is_idempotent`, `test_disconnect_event_fires_once`, `test_telephone_audio_traverses_real_pipeline_offline` (§19-B).

**Doc artifact:** architecture + coupling note in `10-02-SUMMARY.md` + the `transport.py` module docstring (D-08).

**Deferred (NOT this phase):** socket-backed `RtpMediaSession`, Asterisk/ARI, DID/identity, payphone ATA, Terraform/infra (Phases 11–14); the live Deepgram→ElevenLabs round-trip (Phase-11 live eval); the `pipeline.toml [telephony]` loader (Phase 11, D-09).
</artifacts_produced>

<threat_model>
## Trust Boundaries

The transport composes Plan 01's codec/RTP (the untrusted-input surface) and plugs into the existing quota-gated runtime. It opens NO new network listener and constructs NO real provider this phase (offline).

| Boundary | Description |
|----------|-------------|
| external media (RTP bytes) → TelephonyInputTransport._receive_audio → parse_rtp/depacketizer/ulaw_decode | The one new untrusted-input path. Offline-fed via OfflineRtpMediaSession this phase; socket-fed Phase 11. Malformed input must not crash the receive loop (relies on Plan 01's parse_rtp→None guard + total decode). |
| TelephonyTransport → create_call_session / build_pipeline | Reuses the Phase 9 transport-neutral seam UNCHANGED. No new provider construction, no new spend path opened (offline builds client objects only, gated exactly as WebRTC when live). channel="pstn" is the only new value threaded in. |
| telephony package → shared runtime files | ZERO edits to call_runtime/pipeline/factories/server/webrtc — browser path invariant preserved (spec §22). |

## STRIDE Threat Register (ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | DoS | TelephonyInputTransport receive loop on malformed RTP | high | mitigate | The receive loop consumes Plan 01's `parse_rtp` (→None on malformed, T-10-01) and total `ulaw_decode`; a bad packet is skipped, never raised out of the audio task. Task 3 drives malformed/dup/reorder input and asserts the loop stays alive. |
| T-10-05 | DoS / resource leak | outbound queue growth + release on terminal close | high | mitigate | The outbound queue is kept shallow (20–60 ms, D-07) and flushed on interruption; `stop()` is idempotent and `on_client_disconnected` fires exactly once → the existing `lifecycle.on_transport_disconnected`/`release()` single-fire guard (Phase 9 T-09-02) collapses racing terminal paths. Task 3 `test_stop_is_idempotent` + `test_disconnect_event_fires_once` assert exactly-once. |
| T-10-06 | Tampering | second VAD / endpointing / AEC creeping in | medium | mitigate | Interruption reuses the pipeline's existing `InterruptionFrame` via `BaseOutputTransport.handle_interruptions`; NO new VAD/endpointing/AEC. Task 2 grep gate asserts 0 VAD/AEC symbols; the Flux double-endpointing guard is untouched (spec §10). |
| T-10-SC | Tampering (supply chain) | package installs | n/a | accept | NO new packages — reuses pinned pipecat ~=1.5.0 (`create_stream_resampler`, base transports, frames) + Plan 01's codec/RTP. No `[ASSUMED]`/`[SUS]` package, no legitimacy checkpoint. |
</threat_model>

<source_coverage_audit>
## Multi-Source Coverage Audit (phase-complete, across Plans 01–02)

**GOAL (ROADMAP Phase 10):** "Recorded telephone audio traverses the real Klanker pipeline without SIP — add a PCMU codec, an RTP parser/packetizer, and the Pipecat-compatible TelephonyTransport (input/output processors, stateful 8 kHz↔pipeline-rate resampling, interruption flush)." → COVERED: codec+RTP+offline session (Plan 01 Tasks 2–3), TelephonyTransport+resampling+interruption (Plan 02 Tasks 1–2), §19-B pipeline traversal (Plan 02 Task 3).

**Phase 10 success criteria (4):**
1. PCMU vectors; RTP seq/ts/SSRC + reorder/dup/one-missing tolerance → Plan 01 Tasks 2–3.
2. TelephonyTransport emits correct InputAudioRawFrames / PCMU RTP with a stateful boundary resampler (no drift) → Plan 02 Tasks 1 + 3.
3. Interruption flushes output (≤20–60 ms); stop() idempotent; disconnect fires once → Plan 02 Tasks 2 + 3.
4. Recorded telephone audio traverses the real pipeline offline, no Asterisk/SIP (§19-B) → Plan 02 Task 3 (`test_telephone_audio_traverses_real_pipeline_offline`). Note: the LIVE Deepgram→ElevenLabs round-trip half of SC4 needs real keys/network and is a documented Phase-11 live eval, not an offline gate (scope call).

**REQ:** No REQ-IDs (`phase_req_ids=null`) — `requirements: []` intentional, not a gap.

**RESEARCH:** None (`has_research=false`); planned from spec + CONTEXT + PATTERNS.

**CONTEXT (D-01..D-10):** D-01 (module layout) → Plans 01+02. D-02 (codec) → Plan 01 Task 2. D-03 (RTP) → Plan 01 Task 3. D-04 (offline RtpMediaSession) → Plan 01. D-05 (TelephonyTransport) → Plan 02 Task 1. D-06 (stateful boundary resampler) → Plan 02 Task 1. D-07 (interruption flush, 20–60 ms, no 2nd VAD) → Plan 02 Task 2. D-08 (lifecycle mapping + architecture note, terminal close) → Plan 02 Tasks 2–3. D-09 (TelephonyTransportParams dataclass; TOML loader DEFERRED to Phase 11 per D-09's own option) → Plan 01 Task 1 (dataclass) + documented deferral. D-10 (tests + §19-B) → Plan 01 Tasks 2–3 + Plan 02 Task 3. ALL COVERED.

**Exclusions (not gaps):** All CONTEXT Deferred Ideas (Phases 11–14: Asterisk/ARI, SIP/sockets, socket-backed session, DID/identity, payphone ATA, Terraform/alarms, AEC, the pipeline.toml loader).

No unplanned items. No PHASE SPLIT needed — two plans, two waves (Plan 02 depends on Plan 01), each ~50% context.
</source_coverage_audit>

<verification>
- `uv run pytest tests/test_telephony_transport.py -q` (from `apps/voice`) green — correct input frames, PCMU RTP output, interruption flush, idempotent stop, fire-once events, §19-B offline traversal.
- `uv run pytest -q` (full suite) exits 0 — browser/WebRTC behavior byte-unchanged (spec §22 preserve-list).
- `grep -v '^\s*#' src/klanker_voice/telephony/transport.py | grep -c -iE 'import socket|socket\.socket|import aiortc|smallwebrtc|asterisk|build_stt|build_llm|build_tts'` returns 0.
- `grep -c -E 'create_stream_resampler' src/klanker_voice/telephony/transport.py` ≥ 2 (one resampler per direction).
- No shared-runtime file changed: `git diff --name-only` shows only `telephony/transport.py`, `telephony/__init__.py`, `tests/test_telephony_transport.py` (no `call_runtime.py`/`pipeline.py`/`factories.py`/`server.py`/`webrtc.py`).
</verification>

<success_criteria>
- `TelephonyTransport` is a pipecat-compatible `BaseTransport` (input/output on BaseInput/BaseOutput) that decodes RTP→InputAudioRawFrame (correct sample_rate) and encodes OutputAudioRawFrame→PCMU RTP, resampling once per direction with a stateful streaming resampler at the 8 kHz boundary (D-05/D-06).
- `flush_output_audio()` flushes on the existing InterruptionFrame with no second VAD/AEC and a shallow (20–60 ms) queue; `stop()` idempotent; connect/disconnect fire exactly once, terminal (D-07/D-08).
- The §19-B exit criterion holds offline: synthetic telephone audio traverses the real `build_pipeline` via `create_call_session(channel="pstn")` with no SIP/Asterisk/socket (ROADMAP SC4, hermetic form).
- The full pre-existing suite passes unchanged; no shared-runtime/provider/socket/SIP code introduced; the D-08 architecture note is recorded.
</success_criteria>

<output>
Create `.planning/phases/10-voip-ms-telephony-offline-media-adapter/10-02-SUMMARY.md` when done — including the D-08 architecture + coupling note (the seam + pipeline-rate/down-resample-location couplings + the deferred live-eval note), the exact files changed, the test results, and confirmation that call_runtime/pipeline/factories/server/webrtc were reused with zero edits (spec Phase B boundary — stop before Asterisk/SIP/sockets/infra).
</output>
