---
phase: 12-voip-ms-telephony-inbound-did
plan: 05
type: execute
wave: 2
depends_on: ["12-02", "12-03"]
files_modified:
  - infra/terraform/live/site/services/auth/service.hcl
  - docs/operators/phase12-seed-data.md
autonomous: false
requirements: [D-05, SC-1, SC-2]
must_haves:
  truths:
    - "The gsi3pk-gsi3sk-index GSI is LIVE on the kmv-auth-electro DynamoDB table before any phone→code lookup is exercised"
    - "A kph-tier Tier row (effectively unlimited) and a constrained baseline caller tier exist in the live table"
    - "Kurt's phone → defcon34 mapping exists and is queryable through the byPhone GSI"
    - "defcon34 maps to kph-tier"
  artifacts:
    - docs/operators/phase12-seed-data.md
  key_links:
    - "gsi3pk-gsi3sk-index is already declared in the dynamodb module — this plan confirms/applies it on the LIVE kmv-auth-electro table (avoids the ElectroDB false-positive where builds pass without the live GSI)"
    - "kph-tier + baseline tier seeded via `kv tier define`; Kurt's map via `kv code phone` (12-03)"
---

<objective>
[BLOCKING] Make the LIVE `kmv-auth-electro` DynamoDB table ready for the §23 byPhone lookup, and seed the D-05 data. First confirm the `gsi3pk-gsi3sk-index` GSI (already declared in the shared dynamodb module) is actually present on the live table and apply it if not — ElectroDB types/builds pass WITHOUT the live GSI, so an unverified state is a false positive. Then seed the `kph-tier` Tier row (effectively unlimited), a constrained baseline caller tier, and Kurt's phone → `defcon34` mapping (with `defcon34` → `kph-tier`) via the `kv` commands from 12-03.

Purpose: The controller (12-06) and the /tel path (12-02) must query a real byPhone GSI and resolve real seed rows; this closes the schema/data gap before any lookup runs. Requires operator AWS credentials/approval — this is `autonomous: false`.
Output: verified live GSI, seeded tier + mapping rows, and a committed seed-data doc capturing the exact commands run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-voip-ms-telephony-inbound-did/12-CONTEXT.md
@.planning/phases/12-voip-ms-telephony-inbound-did/12-RESEARCH.md
</context>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- New doc: docs/operators/phase12-seed-data.md (exact `kv tier define` / `kv code phone` invocations + the GSI-verify commands)
- Live data: kph-tier Tier row, constrained baseline caller Tier row, defcon34→phone mapping (no source symbols)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Verify (and apply if needed) gsi3pk-gsi3sk-index on the live kmv-auth-electro table</name>
  <read_first>
    - infra/terraform/modules/dynamodb/v1.0.0/main.tf (the electro block ~24-52 already declares gsi3pk/gsi3sk attributes AND the gsi3pk-gsi3sk-index index — so the module is correct; the risk is only that the live table predates this and the apply has not rolled through)
    - infra/terraform/live/site/services/auth/service.hcl (how kmv-auth-electro is declared as a table_type "electro"; the task-role IAM already grants table + /index/* access)
    - .planning/phases/12-voip-ms-telephony-inbound-did/12-RESEARCH.md (Pitfall 6 — sparse GSI behavior; the schema-push false-positive note)
  </read_first>
  <action>
    Confirm the live index state, then apply only if missing. (1) Run `aws dynamodb describe-table --table-name kmv-auth-electro` and check GlobalSecondaryIndexes for `gsi3pk-gsi3sk-index` in status ACTIVE. (2) If it is already ACTIVE, record that — no terraform apply is required (the module already declares it). (3) If it is absent or CREATING, run the auth dynamodb terragrunt unit's plan/apply (the same path that provisioned the gsi2 byBypassToken index) to create it, following defcon.run.34 conventions, and wait for describe-table to report ACTIVE. Do not drop or recreate the table; a GSI is added in place. Since this touches live infrastructure it requires operator credentials/approval (autonomous:false). Capture the describe-table GSI status in docs/operators/phase12-seed-data.md. Implements the schema-push requirement + D-05 prerequisite.
  </action>
  <verify>
    <automated>aws dynamodb describe-table --table-name kmv-auth-electro --query "Table.GlobalSecondaryIndexes[?IndexName=='gsi3pk-gsi3sk-index'].IndexStatus" --output text | grep -q ACTIVE</automated>
  </verify>
  <acceptance_criteria>
    - `describe-table` reports gsi3pk-gsi3sk-index with IndexStatus ACTIVE on kmv-auth-electro
    - No table replacement occurred (the primary table ARN/creation is unchanged)
    - The GSI status at verification time is recorded in docs/operators/phase12-seed-data.md
  </acceptance_criteria>
  <done>The byPhone GSI is provably ACTIVE on the live kmv-auth-electro table before any phone lookup runs.</done>
</task>

<task type="auto">
  <name>Task 2: Seed kph-tier + baseline caller tier + Kurt's phone→defcon34 mapping</name>
  <read_first>
    - kv/internal/app/cmd/tier.go (DefineTier + `kv tier define` flags: --session-max, --period-max, --max-concurrent, --group)
    - kv/internal/app/cmd/code.go (the `kv code phone` sub-command from 12-03; `kv code create` for defcon34 if it does not already exist and its tier mapping)
    - .planning/phases/12-voip-ms-telephony-inbound-did/12-CONTEXT.md (D-05: kph-tier effectively unlimited; caller-ID grants at most a constrained baseline tier; §11 baseline limits — 1 concurrent, ~10 min, small daily cap; defcon34 → kph-tier per §23)
    - .planning/phases/12-voip-ms-telephony-inbound-did/12-RESEARCH.md (Tier Definitions & Seed Data section for representative limit values)
  </read_first>
  <action>
    Seed the live tiers + mapping using the shipped kv commands, capturing every exact invocation in docs/operators/phase12-seed-data.md:
    (1) `kv tier define kph-tier` with effectively-unlimited limits (large session-max, large period-max, small-but-sufficient max-concurrent) per D-05.
    (2) `kv tier define` a constrained baseline caller tier (1 concurrent, ~600s session-max, small daily period cap) per §11 — this is the ceiling a caller-ID mint may grant.
    (3) Ensure the `defcon34` access code exists and maps to kph-tier (create it via `kv code create defcon34 kph-tier` only if it does not already exist — check first; do not clobber an existing defcon34 mapping).
    (4) `kv code phone defcon34 --add <Kurt's E.164 number>` to write the byPhone mapping (the operator supplies the real number; do NOT hardcode a number in the committed doc — reference it as a placeholder and store the real value only in the live table).
    Then verify the round-trip: query the byPhone GSI for the mapped number (via a small `kv`/AWS query or the /tel path against a non-prod invocation) and confirm it resolves to defcon34 → kph-tier. This exercises the live GSI (hence the ordering after Task 1). autonomous:false (writes live data). Implements D-05.
  </action>
  <verify>
    <automated>aws dynamodb get-item --table-name kmv-auth-electro --key '{"pk":{"S":"tier#kph-tier"},"sk":{"S":"tier#"}}' --query "Item.tierId.S" --output text | grep -q kph-tier</automated>
  </verify>
  <acceptance_criteria>
    - `kv tier list` (or get-item) shows kph-tier with effectively-unlimited limits and the constrained baseline tier with 1 concurrent / ~600s / small daily cap
    - The defcon34 code exists and maps to kph-tier
    - A byPhone query for Kurt's number resolves to defcon34 (verified against the live table)
    - docs/operators/phase12-seed-data.md records every command run, with Kurt's actual number represented as a placeholder (never committed)
  </acceptance_criteria>
  <done>The live table holds kph-tier + baseline tier and Kurt's phone→defcon34→kph-tier mapping, verified via a real byPhone round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DynamoDB | GSI apply + seed writes with real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05-01 | Tampering | table replacement on GSI apply | high | mitigate | GSI added in place; describe-table confirms unchanged table; no destroy/recreate |
| T-12-05-02 | Information Disclosure | Kurt's phone number committed to git | medium | mitigate | real number lives only in the live table; committed doc uses a placeholder |
| T-12-05-03 | Elevation of Privilege | baseline tier granting too much | high | mitigate | baseline tier constrained (1 concurrent/~600s/small cap); kph-tier only reachable via the §24 gate (composed in 12-06) |
</threat_model>

<verification>
- gsi3pk-gsi3sk-index ACTIVE on kmv-auth-electro
- kph-tier + baseline tier + defcon34 phone map present and round-trip-resolvable
- seed-data doc committed with placeholders for the real number
</verification>

<success_criteria>
The live kmv-auth-electro table has an ACTIVE byPhone GSI and the seeded kph-tier/baseline/Kurt-mapping rows, verified by a real byPhone round-trip, before any phone lookup is exercised (D-05, SC-1, SC-2; schema-push requirement).
</success_criteria>

<output>
Create `.planning/phases/12-voip-ms-telephony-inbound-did/12-05-SUMMARY.md` when done
</output>
