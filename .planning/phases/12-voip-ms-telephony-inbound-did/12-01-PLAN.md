---
phase: 12-voip-ms-telephony-inbound-did
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kv/internal/app/cmd/voipms.go
  - kv/internal/app/cmd/voipms_test.go
  - kv/internal/app/cmd/root.go
  - docs/operators/voipms-provisioning-runbook.md
autonomous: true
requirements: [D-03, D-04, SC-1]
must_haves:
  truths:
    - "An operator can read the VoIP.ms account balance via `kv voipms balance`"
    - "An operator can route a DID to the klanker-pbx subaccount via `kv voipms route-did`"
    - "The §25.F blank-account setup order is documented as an operator runbook a human follows step-by-step"
    - "VoIP.ms API method names used by kv are verified against the live API docs before being hardcoded"
  artifacts:
    - kv/internal/app/cmd/voipms.go
    - kv/internal/app/cmd/voipms_test.go
    - docs/operators/voipms-provisioning-runbook.md
  key_links:
    - "root.go registers NewVoipmsCmd so `kv voipms ...` resolves"
    - "VoIP.ms REST base + method-name constants centralized in one place in voipms.go"
    - "runbook documents the SSM put-parameter steps for VOIPMS_* secrets consumed later by 12-07"
---

<objective>
Deliver the D-03 provisioning split: a `kv voipms` command family that automates the API-drivable VoIP.ms steps (read balance, route DID to the `klanker-pbx` subaccount, set per-call caps, optionally create the subaccount), plus a committed operator runbook documenting the portal-only security steps in the §25.F blank-account setup order.

Purpose: The public DID must be provisioned safely and repeatably. Portal-first security steps (2FA, international/premium locks, balance alerts, API IP-whitelist) are exactly the ones a human must not skip — they are documented; the repeatable API calls are scripted (D-03).
Output: `kv voipms` sub-command tree + tests, root registration, and `docs/operators/voipms-provisioning-runbook.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-voip-ms-telephony-inbound-did/12-CONTEXT.md
@.planning/phases/12-voip-ms-telephony-inbound-did/12-RESEARCH.md
@.planning/phases/12-voip-ms-telephony-inbound-did/12-PATTERNS.md
</context>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- Go: `NewVoipmsCmd`, `getVoipmsBalance`, `routeVoipmsDidToPbx`, `setVoipmsCallDuration`, and centralized VoIP.ms REST constants (base URL + method-name consts) in `kv/internal/app/cmd/voipms.go`
- CLI surface: `kv voipms`, `kv voipms balance`, `kv voipms route-did <did>`, `kv voipms set-caps [--max-duration]`, and (if included) `kv voipms create-subaccount`
- Test symbols in `kv/internal/app/cmd/voipms_test.go` (e.g. `TestVoipmsRestRequestShape`, `TestRouteDidBuildsRequest`, `TestVoipmsMethodNamesCentralized`)
- New doc: `docs/operators/voipms-provisioning-runbook.md`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: kv voipms command family over the VoIP.ms REST API</name>
  <read_first>
    - kv/internal/app/cmd/code.go (cobra command shape: NewCodeCmd, RunE, cfg.DynamoClient, flag registration, OutOrStdout printing)
    - kv/internal/app/cmd/root.go (NewRootCmd + AddCommand registration order; how Config is built and env-overridden)
    - kv/internal/app/cmd/bypass_test.go (Go test style: t.Setenv, table-driven, request/URL assertions without live network)
    - .planning/phases/12-voip-ms-telephony-inbound-did/12-RESEARCH.md (VoIP.ms REST API Surface section: base URL https://voip.ms/api/v1/rest.php, JSON status envelope, api_username+api_password auth; the method-name candidates are MEDIUM confidence)
  </read_first>
  <action>
    Create kv/internal/app/cmd/voipms.go implementing NewVoipmsCmd(cfg *Config) *cobra.Command mirroring NewCodeCmd's structure. Sub-commands: `balance` (NoArgs, prints account balance), `route-did <did>` (ExactArgs 1, routes the DID to the klanker-pbx subaccount), `set-caps` (NoArgs, `--max-duration` int default 600, sets per-call max duration). Optionally include `create-subaccount` for the klanker-pbx subaccount create step; if included it must default outbound-disabled and IP-restricted per D-03/§25.A.
    Centralize ALL VoIP.ms REST details in one block at the top of voipms.go: the base URL constant and a named Go const per API method (subaccount create, setSubAccount, setDIDRouting/setDIDInfo, getBalance, getServersInfo, per-call duration). Make requests with net/http (stdlib) — no new dependency (RESEARCH Package Legitimacy Audit: no new packages). Auth params (api_username, api_password) come from the environment (VOIPMS_API_USERNAME / VOIPMS_API_PASSWORD), sourced from SSM by the operator per D-04 — never a literal in code, never printed. Parse the JSON `{status, ...}` envelope and return a non-nil error when status is not the success value.
    VERIFY-BEFORE-HARDCODE (RESEARCH flags method names MEDIUM confidence, A1): before finalizing the method-name constants, confirm each against the live VoIP.ms API documentation (voip.ms/resources/api). Record the confirmed method names in a short comment block above the constants citing the doc. If a name cannot be confirmed, leave a clearly-marked `// UNVERIFIED:` comment on that single constant so the reviewer sees it — do not silently guess.
    Register the command in root.go: add `root.AddCommand(NewVoipmsCmd(cfg))` alongside the existing NewCodeCmd/NewTierCmd/... registrations. Implements D-03; supports D-04 (API creds from env/SSM).
  </action>
  <verify>
    <automated>cd kv && go build ./... && go test ./internal/app/cmd/ -run TestVoipms -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd kv && go build ./...` succeeds
    - `kv voipms --help` lists the balance, route-did, and set-caps sub-commands (assert via a cobra command-tree test or `go run` help capture)
    - The VoIP.ms base URL and every method name are declared as named constants in one block in voipms.go (grep: the string `rest.php` appears exactly once, in that block)
    - No `api_password`/`api_username` literal value appears in voipms.go (grep for an assignment; creds are read from os.Getenv only)
    - A test asserts the REST request URL/params shape for at least route-did and balance without making a live network call (http.Client injected or transport stubbed)
    - root.go contains a NewVoipmsCmd registration line
  </acceptance_criteria>
  <done>`kv voipms` builds, is registered, reads creds only from the environment, centralizes verified VoIP.ms method names, and has request-shape unit tests that run offline.</done>
</task>

<task type="auto">
  <name>Task 2: VoIP.ms operator provisioning runbook (§25.F order)</name>
  <read_first>
    - .planning/phases/12-voip-ms-telephony-inbound-did/12-CONTEXT.md (D-03 the §25.F setup order; domain item 1 the full provisioning list; §25.A no-outbound-ever)
    - .planning/phases/12-voip-ms-telephony-inbound-did/12-RESEARCH.md (VoIP.ms Toronto POP IPs table; registration+DID-POP-must-match note; SSM secrets to create for D-04)
    - apps/voice/asterisk/README.md (the Phase-11 §19-C manual-proof recipe style to mirror for tone/structure)
  </read_first>
  <action>
    Create docs/operators/voipms-provisioning-runbook.md documenting the portal-first steps a human performs, in the exact §25.F order: (1) enable portal 2FA + strong portal password; (2) lock international/premium destinations; (3) set balance low + auto-recharge OFF + low-balance/spend alerts; (4) enable the API with a strong api_password distinct from the portal password + whitelist the setup IP; (5) run the scripted `kv voipms` steps (create/confirm the klanker-pbx subaccount with a strong unique SIP password, outbound DISABLED, IP-restricted to the edge egress + Toronto POP); (6) order ONE DID (Toronto POP, per-minute, CNAM off); (7) route the DID to klanker-pbx via `kv voipms route-did`; (8) re-lock the API IP whitelist.
    Include a dedicated "Secrets → SSM" subsection listing every SecureString parameter to create with `aws ssm put-parameter --type SecureString` (D-04): VOIPMS_SIP_USERNAME, VOIPMS_SIP_PASSWORD, VOIPMS_API_USERNAME, VOIPMS_API_PASSWORD, VOIPMS_DID, and the telephony endpoint auth token — with their canonical parameter paths under /kmv/secrets/use1/... — and an explicit reminder that these values live ONLY in SSM, never in git/pipeline.toml/logs. Include the Toronto POP IP list (from RESEARCH) as the SG allow-list source with a documented 6-month re-verification note. State plainly that the registration POP and the DID routing POP MUST be the same Toronto POP.
    Do NOT document any outbound-calling step — outbound stays disabled everywhere, forever (§25.A).
  </action>
  <verify>
    <automated>test -f docs/operators/voipms-provisioning-runbook.md && grep -qi "2fa" docs/operators/voipms-provisioning-runbook.md && grep -qi "outbound" docs/operators/voipms-provisioning-runbook.md && grep -q "put-parameter" docs/operators/voipms-provisioning-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/operators/voipms-provisioning-runbook.md exists
    - The eight §25.F steps appear in order (2FA → international lock → balance/alerts → API+IP whitelist → kv voipms subaccount → order DID → route DID → re-lock whitelist)
    - A "Secrets → SSM" subsection names all VOIPMS_* SecureString parameters with /kmv/secrets/use1/... paths and `aws ssm put-parameter --type SecureString`
    - The runbook states registration POP == DID POP (same Toronto POP) and lists the Toronto POP IPs
    - The runbook contains no outbound-dialing provisioning step
  </acceptance_criteria>
  <done>A human operator can provision the VoIP.ms account end-to-end from a blank account by following the committed runbook, with all secrets landing in SSM and outbound disabled throughout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → VoIP.ms REST API | api_username/api_password cross here; IP-whitelisted |
| operator → SSM | secret values written as SecureString |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01-01 | Information Disclosure | VoIP.ms API creds in kv | high | mitigate | creds read only from os.Getenv (SSM-sourced); no literal in voipms.go; never printed — enforced by grep acceptance criterion |
| T-12-01-02 | Tampering | toll fraud via API misuse | high | mitigate | outbound disabled on subaccount; no outbound method wrapped; runbook §25.A states no-outbound-ever |
| T-12-01-03 | Spoofing | unauthorized API caller | medium | mitigate | API IP-whitelist step in runbook (§25.B); api_password distinct from portal |
| T-12-01-SC | Tampering | Go module installs | low | accept | no new packages — reuses stdlib net/http + existing aws-sdk-go-v2 (RESEARCH audit clean) |
</threat_model>

<verification>
- `cd kv && go build ./... && go test ./internal/app/cmd/` green
- Runbook present with §25.F ordered steps + SSM secrets subsection
- No credential literals in voipms.go
</verification>

<success_criteria>
`kv voipms` automates the API-drivable provisioning steps with verified method names and offline tests; the operator runbook documents the portal-only §25.F steps and the SSM secret creation, with outbound disabled throughout (D-03, D-04, SC-1).
</success_criteria>

<output>
Create `.planning/phases/12-voip-ms-telephony-inbound-did/12-01-SUMMARY.md` when done
</output>
