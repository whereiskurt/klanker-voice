---
phase: 12-voip-ms-telephony-inbound-did
plan: 08
type: execute
wave: 4
depends_on: ["12-07"]
files_modified:
  - docs/operators/voipms-provisioning-runbook.md
  - .planning/phases/12-voip-ms-telephony-inbound-did/12-08-SUMMARY.md
autonomous: false
requirements: [D-06, SC-3, SC-4]
must_haves:
  truths:
    - "A call from a real mobile phone to the public DID reaches the agent, holds a multi-turn conversation, and hangs up cleanly from either side"
    - "A scanner/unknown caller who fails the §24 gate burns no STT/LLM/TTS quota and gets a static goodbye + hangup"
    - "Klanker-unavailable yields a static unavailable message, never a silent open call"
    - "The cellular proof is documented in the phase SUMMARY and the provisioning runbook (mirroring the Phase-11 §19-C proof)"
  artifacts:
    - .planning/phases/12-voip-ms-telephony-inbound-did/12-08-SUMMARY.md (the documented §19-D proof)
    - docs/operators/voipms-provisioning-runbook.md (cellular-proof recipe appended)
  key_links:
    - "the manual proof runs against the deployed edge (12-07) with real VoIP.ms + a real cell phone"
    - "quota is enforced by the existing SessionLifecycle (1 concurrent, short max, small daily cap)"
---

<objective>
The §19-D exit proof (D-06): a real mobile phone calls the public DID, reaches the agent, holds a multi-turn conversation, and hangs up cleanly from either side; a scanner/gate-fail caller burns no metered quota and gets a static goodbye + hangup; Klanker-unavailable yields a static unavailable message, never a silent open call. This is inherently manual — it runs once against the deployed edge with real VoIP.ms + a real cell phone — and is documented in the phase SUMMARY and the provisioning runbook, mirroring the Phase-11 §19-C softphone proof.

Purpose: This is the phase's headline exit criterion; the code tests (12-01..12-06) prove the parts, but "reliably reaches Klanker from the cellular network" is only meaningful against the real deployed topology. `autonomous: false` — a human places the call.
Output: a documented, human-run cellular proof (SUMMARY + runbook recipe).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-voip-ms-telephony-inbound-did/12-CONTEXT.md
@.planning/phases/12-voip-ms-telephony-inbound-did/12-RESEARCH.md
</context>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- Runbook section: a "§19-D cellular proof" recipe appended to docs/operators/voipms-provisioning-runbook.md
- The documented proof result in 12-08-SUMMARY.md
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Document the §19-D cellular-proof recipe in the runbook</name>
  <read_first>
    - apps/voice/asterisk/README.md (the Phase-11 §19-C manual softphone-proof recipe — mirror its structure/tone: numbered steps, expected observations, fail-closed checks, no-leak checks, CI-vs-manual boundary)
    - docs/operators/voipms-provisioning-runbook.md (12-01 — append the cellular-proof section here)
    - .planning/phases/12-voip-ms-telephony-inbound-did/12-CONTEXT.md (the §19-D exit criterion + fail-closed requirements; §11 quota — 1 concurrent, ~10 min, small daily cap)
  </read_first>
  <action>
    Append a "§19-D cellular proof" section to docs/operators/voipms-provisioning-runbook.md documenting the exact human procedure against the deployed edge (12-07): (1) from a real mobile phone, dial the public DID; (2) confirm the silent answer-gate behavior, pass the §24 gate (DTMF PIN or 4-word passphrase), confirm the greeting is not clipped, hold a multi-turn conversation, and hang up — confirming clean teardown from the caller side; (3) repeat and hang up from the agent/quota side (let the SessionLifecycle timer expire or a cap hit) confirming clean teardown from either side; (4) fail-closed check: as an "unknown/scanner" caller, do NOT pass the gate and confirm a static goodbye + hangup with NO metered STT/LLM/TTS usage (verify via usage/logs that no session was accounted); (5) Klanker-unavailable check: with the pipeline unavailable, confirm the caller hears a static unavailable message and the line is not left silently open; (6) no-leak check: confirm the PIN/passphrase and the pre-unlock transcript never appear in logs. Include a short "what to capture" list (call timestamps, tier granted, teardown side, usage delta) for the SUMMARY. Mark clearly that this recipe is the manual exit proof, complementing the automated CI tests.
  </action>
  <verify>
    <automated>grep -qi "19-D" docs/operators/voipms-provisioning-runbook.md && grep -qi "fail-closed\|fail closed" docs/operators/voipms-provisioning-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - The runbook has a §19-D cellular-proof section with the six numbered checks (reach+multi-turn, either-side hangup, fail-closed no-quota-burn, unavailable-message, no-leak, what-to-capture)
    - It references the deployed edge (12-07) and real VoIP.ms + a real cell phone
    - It states the SessionLifecycle quota bounds (1 concurrent, short max, small daily cap)
  </acceptance_criteria>
  <done>The cellular-proof procedure is documented and repeatable by an operator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the §19-D cellular proof from a real mobile phone</name>
  <action>Place a real cellular call to the public DID against the deployed edge and record the exit-criterion observations per the verification steps below; if a live call cannot be placed in this environment, defer by human decision and log it as an outstanding human-verify item (never fabricate a passing result).</action>
  <what-built>The deployed telephony-edge (12-07) plus the §19-D cellular-proof recipe — ready for a human to place a real cellular call to the public DID and record the result.</what-built>
  <how-to-verify>
    1. From a real mobile phone, dial the public VoIP.ms DID. Confirm the call reaches the agent.
    2. Pass the §24 gate (DTMF PIN or the 4-word passphrase). Confirm the greeting plays un-clipped, hold a multi-turn conversation, then hang up from the phone — confirm clean teardown.
    3. Place a second call and let it end from the agent/quota side (timer/cap). Confirm clean teardown from that side too.
    4. Fail-closed: place a call and do NOT pass the gate (act as a scanner). Confirm a static goodbye + hangup and verify via `kv usage` / logs that NO STT/LLM/TTS session was metered.
    5. Unavailable: if feasible, simulate Klanker-unavailable and confirm the caller hears a static unavailable message with no silent open line.
    6. No-leak: confirm the PIN/passphrase and pre-unlock transcript are absent from logs.
    7. Record timestamps, tier granted, teardown side, and usage delta in 12-08-SUMMARY.md.
    (If a real call cannot be placed in this environment — e.g. no live VoIP.ms account or no cell access — DEFER this checkpoint by human decision and record it as an outstanding human-verify item in the SUMMARY, mirroring the Phase-11 §19-C deferral. Do NOT fabricate a passing result.)
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded observations, or "defer" to log it as an outstanding human-verify item.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cellular network → public DID → edge | the real end-to-end path under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-08-01 | Denial of Service | scanner burns metered APIs | high | mitigate | fail-closed check confirms no quota burn on gate-fail (SC-4) |
| T-12-08-02 | Denial of Service | silent open call on failure | high | mitigate | unavailable-message check confirms no silent open line |
| T-12-08-03 | Information Disclosure | PIN/passphrase/transcript in logs | high | mitigate | no-leak check (Phase-11 gate guarantee, verified live) |
</threat_model>

<verification>
- Runbook §19-D section present with fail-closed + no-leak checks
- The live cellular proof is either recorded as PASS in 12-08-SUMMARY.md or logged as a deferred human-verify item (not fabricated)
</verification>

<success_criteria>
A real mobile call reaches the agent for a multi-turn conversation with clean either-side hangup, scanners fail closed with zero quota burn, and unavailability yields a static message — documented in the SUMMARY + runbook (D-06, SC-3, SC-4).
</success_criteria>

<output>
Create `.planning/phases/12-voip-ms-telephony-inbound-did/12-08-SUMMARY.md` when done
</output>
