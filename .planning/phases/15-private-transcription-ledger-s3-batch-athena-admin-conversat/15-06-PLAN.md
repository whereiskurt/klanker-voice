---
phase: 15-private-transcription-ledger-s3-batch-athena-admin-conversat
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/voice/client/src/screens/ReadyToStart.tsx
  - apps/voice/client/src/screens/readyToStart.css
  - apps/voice/client/src/screens/ReadyToStart.test.tsx
autonomous: true
requirements: [LEDG-04]
must_haves:
  truths:
    - "The pre-connect screen shows a visible 'sessions may be recorded' notice — the tap gesture then constitutes informed continuation (LEDG-04)"
    - "The notice establishes the no-expectation-of-privacy posture the LOCKED privacy ruling requires, shipped in this phase"
  artifacts:
    - apps/voice/client/src/screens/ReadyToStart.tsx
    - apps/voice/client/src/screens/ReadyToStart.test.tsx
  key_links:
    - "The notice renders on ReadyToStart (the authenticated pre-connect landing) so it is seen before the mic gesture that starts a recorded session"
---

<objective>
Ship the visible "sessions may be recorded" notice on the pre-connect screen. The LOCKED privacy ruling ("there are no expectations of privacy … pair with a visible recording notice — the notice is what establishes the posture") requires the client notice in THIS phase. It renders on `ReadyToStart` (the authenticated landing with the "Let's start talking" CTA), so the tap that unlocks the mic and starts a recorded session is informed continuation.

Purpose: the privacy-posture requirement (LEDG-04) — the one user-facing piece that makes recording legitimate.
Output: a small-print recording notice on `ReadyToStart` + its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-private-transcription-ledger-s3-batch-athena-admin-conversat/15-CONTEXT.md
@.planning/phases/15-private-transcription-ledger-s3-batch-athena-admin-conversat/15-RESEARCH.md
@.planning/phases/15-private-transcription-ledger-s3-batch-athena-admin-conversat/15-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 15-06)

New/changed symbols created by THIS plan (exclude from source-drift verification):
- A new small-print notice element + CSS class (e.g. `.ready-recording-notice`) in `ReadyToStart.tsx` / `readyToStart.css`
- A new assertion in `ReadyToStart.test.tsx`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the recording notice to ReadyToStart</name>
  <files>apps/voice/client/src/screens/ReadyToStart.tsx, apps/voice/client/src/screens/readyToStart.css, apps/voice/client/src/screens/ReadyToStart.test.tsx</files>
  <read_first>
    - apps/voice/client/src/screens/ReadyToStart.tsx (the whole file — the existing `.ready-cta-sub` small-print sub-line under the CTA is the styling convention to mirror)
    - apps/voice/client/src/screens/readyToStart.css (the `.ready-cta-sub` rule — match its small-print treatment for the new notice class)
    - apps/voice/client/src/screens/ReadyToStart.test.tsx (the existing screen-test pattern to extend; note the repo gotcha — client tests need node ≥22.12, `nvm use 23`)
    - .planning/phases/15-private-transcription-ledger-s3-batch-athena-admin-conversat/15-PATTERNS.md (section "ReadyToStart.tsx (MOD — recording notice)")
  </read_first>
  <behavior>
    - Test: `ReadyToStart` renders visible text conveying that sessions may be recorded (e.g. a phrase containing "recorded"), present in the DOM alongside the existing CTA.
    - Test: the existing CTA + sub-line still render (no regression to the current screen).
  </behavior>
  <action>
    Add a small-print notice element as a sibling of the existing `.ready-cta-sub` paragraph inside the `.ready-cta-wrap` (or just below it), with a new class such as `.ready-recording-notice`. The copy states plainly that sessions may be recorded for quality/demo purposes (short, one line; final wording is Claude's discretion but MUST contain the word "recorded" so it is unambiguous and testable). Add the matching CSS rule in `readyToStart.css` mirroring the `.ready-cta-sub` small-print styling (muted color, small size) — ensure it clears the UI-SPEC contrast floor for small text. Extend `ReadyToStart.test.tsx` to assert the notice text is present and the existing CTA still renders. Run the client suite under node ≥22.12.
  </action>
  <verify>
    <automated>cd apps/voice/client && npm test -- ReadyToStart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'recorded' apps/voice/client/src/screens/ReadyToStart.tsx` is ≥1 (the notice copy)
    - The new notice has its own class in readyToStart.css (grep the class name in both the tsx and css)
    - ReadyToStart.test.tsx asserts the recording notice renders AND the existing CTA still renders (green)
    - Full client suite (`cd apps/voice/client && npm test`) green under node ≥22.12
  </acceptance_criteria>
  <done>The pre-connect screen visibly informs users that sessions may be recorded, establishing the no-expectation-of-privacy posture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → recorded session | The user must be informed BEFORE the tap that starts a recorded session; the notice is the informed-consent surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06-01 | Repudiation | user claims no notice of recording | medium | mitigate | A persistent, visible pre-connect notice renders before the mic-start gesture; the tap constitutes informed continuation (test-asserted the notice is present) |
| T-15-06-02 | Information Disclosure | notice omitted for PSTN callers | low | accept | The LOCKED requirement is the CLIENT notice only; PSTN callers get no visual notice (a telephony unlock-greeting sentence is discretionary and out of this plan's scope) — documented, accepted |
| T-15-06-SC | Tampering | npm installs | low | accept | No new package; a text + CSS change only |
</threat_model>

<verification>
- `cd apps/voice/client && npm test` — full client suite green (node ≥22.12)
- Visual (phase gate, not a checkpoint here): the notice is legible on the pre-connect screen on mobile + desktop
</verification>

<success_criteria>
- A visible "sessions may be recorded" notice renders on the pre-connect screen before the mic-start gesture
- The existing CTA/screen is unregressed; client suite green
</success_criteria>

<output>
Create `.planning/phases/15-private-transcription-ledger-s3-batch-athena-admin-conversat/15-06-SUMMARY.md` when done
</output>
