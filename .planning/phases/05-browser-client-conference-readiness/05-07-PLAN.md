---
phase: 05-browser-client-conference-readiness
plan: 07
type: execute
wave: 6
depends_on: ["05-06"]
files_modified:
  - apps/voice/client/src/styles/tokens.css
  - apps/voice/client/src/styles/responsive.css
  - apps/voice/client/src/a11y/liveRegions.ts
  - apps/voice/client/src/screens/Live.tsx
  - apps/voice/client/src/screens/Attract.tsx
  - apps/voice/client/src/App.tsx
  - apps/voice/client/src/a11y/a11y.test.ts
autonomous: false
requirements: [CLNT-01, CLNT-02, CLNT-03, CLNT-04, CLNT-05, CLNT-06, CLNT-07, CLNT-08]
must_haves:
  truths:
    - "the full stage works one-handed on an iPhone: 100dvh with safe-area insets, mic CTA in the lower-third with a >=96px hit area, all buttons >=44px, captions legible above the CTA"
    - "connection status and countdown announce via aria-live=polite; errors via assertive; state is never conveyed by color alone (text + aria carry it too)"
    - "prefers-reduced-motion swaps the shader orb for the calm fallback and disables ambient/escalation motion"
    - "the whole experience is verified live on a real iPhone AND a restricted conference-style network (UDP-blocked), end to end"
  artifacts:
    - apps/voice/client/src/styles/responsive.css
    - apps/voice/client/src/a11y/liveRegions.ts
  key_links:
    - "the real-device + hostile-network checkpoint == the phase's success-criteria 1-5 live proof (conference readiness)"
    - "aria-live regions + reduced-motion swap == the a11y baseline the UI-SPEC mandates across every surface"
---

<objective>
Harden the client for the conference floor: real mobile/iOS layout (100dvh, safe-area insets, one-handed
lower-third CTA, >=96px mic hit area, >=44px buttons), the accessibility baseline (aria-live status/countdown/
errors, focus rings, reduced-motion orb swap, contrast floors), and then **prove it live on a real iPhone
and a restricted conference-style network** — the "verified on real phones and hostile networks" requirement
that defines this phase.

Purpose: Success criteria 1-5 are only truly met when the experience holds up on the actual devices and
networks the demo will face. This plan closes the a11y/mobile gaps left implicit in earlier slices and gates
the phase on a real-device + hostile-network verification. Output: a conference-ready client.
</objective>

## Phase Goal

**As a** member of the public at a conference, **I want to** sign in at voice.klankermaker.ai, tap the
mic, and hold a slick low-latency conversation with the KlankerMaker concierge — with a live orb, captions,
a countdown, and honest failure handling — **so that** I experience the "whoa" of the demo and can cleanly
reconnect for another turn.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-browser-client-conference-readiness/05-CONTEXT.md
@.planning/phases/05-browser-client-conference-readiness/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile/iOS layout + accessibility baseline across every stage surface</name>
  <read_first>
    - 05-UI-SPEC.md §Accessibility Baseline (captions always rendered as the audio a11y affordance; contrast >=7:1 caption / >=4.5:1 CTA; reduced-motion swaps orb to fallback + disables ambient/escalation; visible focus rings on CTA/HUD toggle/retry/reconnect; H toggles HUD, Esc dismisses transient gate copy; touch targets mic >=96px, buttons >=44px, CTA one-handed lower-third; aria-live polite for connection status + countdown, assertive for errors)
    - 05-UI-SPEC.md §Spacing Scale exceptions (touch targets; safe-area: overlays offset by max(24px, env(safe-area-inset-*)); captions+CTA inside 100dvh minus safe insets for the iOS URL bar) and §Stage Layout mobile column (orb ~55vmin upper-middle; captions full-width -16px; countdown top-center avoiding the notch)
    - .planning/phases/05-browser-client-conference-readiness/05-CONTEXT.md (mobile realities; a11y noted as a hardening pass)
  </read_first>
  <files>apps/voice/client/src/styles/tokens.css, apps/voice/client/src/styles/responsive.css, apps/voice/client/src/a11y/liveRegions.ts, apps/voice/client/src/screens/Live.tsx, apps/voice/client/src/screens/Attract.tsx, apps/voice/client/src/App.tsx, apps/voice/client/src/a11y/a11y.test.ts</files>
  <action>
    Create styles/responsive.css: use 100dvh (not 100vh) for the stage, offset all overlays by
    max(24px, env(safe-area-inset-*)), place the mic CTA in the one-handed lower-third with a >=96px hit area,
    enforce >=44px on every button (retry, reconnect, HUD toggle, gate CTA, sign out), size the orb ~55vmin
    upper-middle on portrait and move the countdown to top-center (notch-safe) via media/orientation queries,
    and add the Display clamp(24px,7vw,32px) with the 16px caption floor. liveRegions.ts provides an aria-live
    announcer utility (a polite region for connection status + countdown, an assertive region for errors) and
    the surfaces (connecting/retry, countdown, mic/UDP errors) push their status text through it. Add visible
    :focus-visible focus rings to all interactive controls, bind Esc to dismiss transient gate copy, and ensure
    the prefers-reduced-motion path (already swapping the orb in 05-02) also disables the countdown escalation
    pulse and caption motion. Verify contrast tokens meet the UI-SPEC floors (final caption >=7:1, CTA label
    >=4.5:1) — adjust token values only if a floor is missed. a11y.test.ts asserts the announcer sets
    aria-live=polite/assertive correctly and that reduced-motion state is threaded to the orb + countdown.
  </action>
  <verify>
    <automated>cd apps/voice/client && npx vitest run src/a11y/a11y.test.ts && grep -q "100dvh" src/styles/responsive.css && grep -q "safe-area-inset" src/styles/responsive.css && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 100dvh + safe-area insets; mic CTA lower-third >=96px; all buttons >=44px; countdown notch-safe on mobile.
    - aria-live polite (status/countdown) + assertive (errors) via the announcer; focus-visible rings on all controls; Esc dismisses transient gate copy.
    - prefers-reduced-motion swaps the orb to the fallback and disables escalation/caption motion; contrast floors met.
  </acceptance_criteria>
  <done>The immersive stage is one-handed mobile-usable and meets the UI-SPEC accessibility baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: conference-floor verification on real iPhone + restricted network</name>
  <action>Pause for a human to run the full flow live on a real iPhone AND a restricted/UDP-blocked network, recording device/OS/network results in the SUMMARY.</action>
  <what-built>The complete, hardened voice client — deployed and served at voice.klankermaker.ai — ready for conference-floor verification on real devices and networks (success criteria 1-5).</what-built>
  <how-to-verify>
    Run the full flow on a REAL iPhone (Safari) and on a RESTRICTED conference-style network:
    1. On the iPhone over normal Wi-Fi/cellular: load voice.klankermaker.ai, confirm the attract "whoa" (alive orb), tap "Tap to talk", sign in (OIDC redirect + return), grant mic (one gesture), connect, and hold a natural conversation one-handed. Confirm orb reactivity, dual captions, the countdown pill (notch-safe), and the HUD toggle.
    2. Confirm the distinct mic-error states by denying the mic once.
    3. On a restricted/UDP-blocked network (e.g. a locked-down guest Wi-Fi): confirm the bounded "Reconnecting… (attempt n of N)" then the honest UDP-blocked wall with "Try again" — then switch to a personal hotspot and confirm it connects (the exact copy's advice works).
    4. Let a session end and confirm the clean summary + Reconnect (re-checks quota).
    5. With a no-access account, confirm the exclusive gate; flip the kill-switch and confirm the killswitch gate.
    6. Turn on iOS "Reduce Motion" and confirm the orb swaps to the calm fallback and escalation motion is gone; confirm VoiceOver announces connection status/countdown/errors.
    Record the device, OS/browser versions, and the restricted-network result in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" once verified on a real iPhone AND a restricted network, or list what failed on-device.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public device/network → deployed client | the real conference threat surface: untrusted networks, shared/kiosk devices |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-I | Information Disclosure | shared/kiosk device leftover session | medium | mitigate | In-memory-only token (05-03) means a page reload / tab close on a shared device leaves no persisted credential; Sign out clears it immediately. Verified in the on-device checkpoint. |
| T-05-07-I | Information Disclosure | UDP-blocked wall copy | low | accept | The honest wall states a network limitation only; no internal detail (D-11). |
| T-05-07-D | Denial of Service | restricted-network retry loop | low | mitigate | Bounded retry (05-06) prevents a hostile network from causing an infinite reconnect loop; the wall is terminal until manual retry. Confirmed on the restricted network in the checkpoint. |
| T-05-07-SC | Tampering | npm installs | low | accept | No new packages (CSS/a11y only). |
</threat_model>

<verification>
- `npx vitest run src/a11y/a11y.test.ts` green; `npm run build` + `npx tsc --noEmit` clean.
- Structural: 100dvh + safe-area-inset present in responsive.css.
- Human-verify (BLOCKING): full flow on a real iPhone + a restricted/UDP-blocked network, with reduced-motion and VoiceOver checks; results recorded in the SUMMARY.
</verification>

<success_criteria>
- One-handed mobile/iOS layout with safe-area + touch-target compliance.
- UI-SPEC accessibility baseline (aria-live, focus, reduced-motion, contrast).
- Live-verified end-to-end on a real iPhone AND a restricted conference-style network (all 5 phase success criteria).
</success_criteria>

## Artifacts This Plan Produces

- New: `src/styles/responsive.css`, `src/a11y/liveRegions.ts` (+ `a11y.test.ts`)
- Modified: `src/styles/tokens.css` (contrast/clamp), `src/screens/{Live,Attract}.tsx`, `src/App.tsx` (announcer + reduced-motion threading + Esc)
- Verification artifact: real-device + hostile-network results recorded in 05-07-SUMMARY.md

<output>
Create `.planning/phases/05-browser-client-conference-readiness/05-07-SUMMARY.md` when done.
</output>
