---
phase: quick-260716-1g0
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/voice/src/klanker_voice/telephony/config.py
  - apps/voice/configs/telephony.toml
  - apps/voice/tests/test_telephony_config.py
  - apps/voice/src/klanker_voice/telephony/controller.py
  - apps/voice/src/klanker_voice/telephony/gate.py
  - apps/voice/tests/test_telephony_controller.py
autonomous: true
requirements:
  - CTF-OTP-REV2
must_haves:
  truths:
    - "A caller inside the §24 gate who enters the armed DTMF code 990011 hears the current OTP (digit-spaced, twice) and the line hangs up."
    - "The real TELEPHONY_ACCESS_PIN still unlocks the concierge unchanged; a wrong/no entry still fails closed."
    - "The announcement path never calls quota.start_gate/greet_now and always reaches exactly one _close_active_call (no silently-open PSTN line)."
    - "The DTMF trigger code and the OTP code/url/bearer never appear in TOML or logs."
  artifacts:
    - "apps/voice/src/klanker_voice/telephony/config.py (AnnouncementEntry.code_env_var; did optional)"
    - "apps/voice/configs/telephony.toml (code-keyed [[telephony.announcement]])"
    - "apps/voice/src/klanker_voice/telephony/controller.py (_announcements_by_code, _gate_announcement, DTMF suffix match)"
    - "apps/voice/src/klanker_voice/telephony/gate.py (GateProcessor.cancel_for_takeover)"
  key_links:
    - "on_channel_dtmf_received -> raw trailing buffer -> endswith(code) -> gate.cancel_for_takeover + _gate_announcement"
    - "_gate_announcement mirrors _gate_fail_closed -> _fetch_ctf_otp -> speak_goodbye -> grace -> _close_active_call"
    - "code value resolved from os.environ[entry.code_env_var] at controller __init__, never from TOML"
---

<objective>
Implement Revision 2 of the CTF phone-OTP feature: trigger the OTP readout by a DTMF
access code (990011) gated INSIDE the existing §24 answer-gate, replacing the dead
Revision-1 DID-keyed pre-gate branch (the dialed DID is not visible at the edge — VoIP.ms
routes every DID to one sub-account, so `on_stasis_start` always sees
`did=557010_klanker-pbx`).

Authoritative design: the "Revision 2 (2026-07-16) — DTMF-code trigger, not DID" section at
the END of `docs/superpowers/specs/2026-07-15-ctf-phone-otp-announcement-did-design.md`.

Purpose: Make the CTF announcement actually fire on a live call, without weakening the
§24 gate's redaction boundary (D-05e), fail-closed idempotency, or the PIN/passphrase
unlock — all of which are DELICATE.

Output: Config surface keyed by an env-var NAME (not a DID, not a secret), a controller
DTMF trigger that dispatches to a new `_gate_announcement` (mirroring `_gate_fail_closed`),
a minimal additive gate method to resolve the gate on takeover, the dead Rev-1 path removed,
and hermetic tests. Auth `/ctf/otp` is LIVE and OUT OF SCOPE.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/superpowers/specs/2026-07-15-ctf-phone-otp-announcement-did-design.md
@apps/voice/src/klanker_voice/telephony/config.py
@apps/voice/src/klanker_voice/telephony/controller.py
@apps/voice/src/klanker_voice/telephony/gate.py
@apps/voice/configs/telephony.toml
@apps/voice/tests/test_telephony_config.py
@apps/voice/tests/test_telephony_controller.py
@apps/voice/tests/test_telephony_lifecycle.py
</context>

<interface_context>
Key seams (already present, reuse verbatim):

- `AnnouncementEntry` (config.py) frozen dataclass. Today: `did, otp_url, otp_env_var="", line_template=""`.
- `_parse_announcements(raw)` (config.py) validates each `[[telephony.announcement]]` table.
- `_CREDENTIAL_FIELD_RE` (config.py) rejects any TOML key containing `key/secret/token/password/credential/bearer/auth/pin/passphrase/words`. CONFIRMED: `code_env_var` splits to `code`/`env`/`var` — none match, so it passes the gate cleanly. Do NOT name the field with any of those tokens.
- `_fetch_ctf_otp(url, headers) -> str | None` (controller.py, module-level) — already present, keep unchanged; monkeypatchable in tests.
- `_build_announcement_line(template, code) -> str` (controller.py, module-level) — already present, keep unchanged.
- `ANNOUNCEMENT_PLAYBACK_GRACE_SECONDS = 12.0` (controller.py) — already present; reuse for the announcement grace (the spoken OTP line is long, ~25 words; `goodbye_grace_seconds`=5s is too short for it).
- `_gate_fail_closed(active_call, reason)` (controller.py ~line 1006) — TEMPLATE for `_gate_announcement`: `speak_goodbye(worker, copy)` -> `asyncio.sleep(grace)` -> `_close_active_call(active_call, reason)`. NOTE: it deliberately issues NO explicit `hangup` — `_close_active_call` -> `call_session.close()` -> `lifecycle.release()` cascades into the composed `on_released` hook wired in `_finish_stasis_start_gated`, which hangs up the SIP channel exactly once.
- `on_channel_dtmf_received` (controller.py ~line 1028) — today: `gate_mode` guard, then `accumulate_dtmf(active_call.dtmf_buffer, digit, self._pin)` -> `gate.unlock("dtmf")`.
- `accumulate_dtmf(buffer, digit, pin)` (gate.py) — windows buffer to `len(pin)`; keep UNCHANGED (its own gate-level tests must stay green).
- `GateProcessor` (gate.py): `_resolved`, `_unlocked`, `_timer_task`, `unlock(method)`. `_gate_fail_closed`/`_gate_unlock` are only ever reached AFTER the gate is already `_resolved` — that invariant is what makes single-teardown safe.
- Test rig (test_telephony_lifecycle.py, imported by test_telephony_controller.py): `_build_controller(make_config_file, *, telephony_cfg, access_pin, ...)`, `_gated_cfg(**overrides)`, `_stasis_event(*, exten=..., caller_number=...)`, `FakeAriClient` with `.count(name, arg=...)`, `stub_call_session_run` (autouse — fakes `CallSession.run` so no real pipeline connects), `_FakeTtsSynthSpy`.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert AnnouncementEntry + telephony.toml from DID-keyed to DTMF-code-keyed</name>
  <files>apps/voice/src/klanker_voice/telephony/config.py, apps/voice/configs/telephony.toml, apps/voice/tests/test_telephony_config.py</files>
  <behavior>
    - Absent [[telephony.announcement]] table still parses to `announcements == ()` (byte-unchanged default).
    - A well-formed code-keyed entry parses: `code_env_var == "CTF_ANNOUNCEMENT_CODE"`, `otp_url` set, `otp_env_var == "CTF_OTP_AUTH_TOKEN"`, `line_template` contains `{code}`; `did` defaults to `""` when omitted and is informational when present.
    - An entry MISSING `code_env_var` (or empty) is REJECTED with a ConfigError mentioning `code_env_var`.
    - An entry missing `otp_url` is still rejected (`otp_url`); a `line_template` without `{code}` is still rejected (`line_template`).
    - `otp_env_var` remains optional (defaults `""`).
    - A `did` omitted no longer raises (the old "did must be non-empty" test is replaced): the entry parses with `did == ""`.
    - The shipped apps/voice/configs/telephony.toml round-trips: exactly one announcement entry, `code_env_var == "CTF_ANNOUNCEMENT_CODE"`, `otp_env_var == "CTF_OTP_AUTH_TOKEN"`, `{code}` in `line_template`.
    - A credential-looking key inside an announcement table is still refused by the shared gate.
  </behavior>
  <action>
    In config.py `AnnouncementEntry`: add field `code_env_var: str = ""` (holds only the NAME of the env var carrying the DTMF trigger code; the value comes from SSM at runtime). Reorder so `did: str = ""` becomes optional/informational (default empty) — it is NO LONGER a matcher (all DIDs arrive as the sub-account, so the dialed number is invisible at the edge). Keep `otp_url` (required, no default), `otp_env_var: str = ""`, `line_template: str = ""`. Update the docstring to describe the DTMF-code trigger (per the spec Revision 2 section) and to state the credential-gate reasoning for the `code_env_var` name (splits to code/env/var, none credential tokens, mirrors `otp_env_var`).

    In `_parse_announcements`: REMOVE the `did` non-empty validation; read `did = str(item.get("did", "")).strip()` with no error when empty. ADD required validation: `code_env_var = str(item.get("code_env_var", "")).strip()`; if empty raise ConfigError mentioning `code_env_var` ("telephony.announcement[{i}].code_env_var must be a non-empty string"). Keep the existing `otp_url` and `line_template`/`{code}` checks unchanged. Construct `AnnouncementEntry(did=did, otp_url=otp_url, otp_env_var=otp_env_var, line_template=line_template, code_env_var=code_env_var)`.

    In apps/voice/configs/telephony.toml: convert the single `[[telephony.announcement]]` entry — remove the active `did = "7254043234"` matcher line (replace with a commented informational note that the DID is NOT visible at the edge, all DIDs arrive as the sub-account `557010_klanker-pbx`), add `code_env_var = "CTF_ANNOUNCEMENT_CODE"  # NAME only; value 990011 lives in SSM (/kmv/secrets/use1/ctf/announcement_code), never TOML (D-09)`. Keep `otp_url`, `otp_env_var`, `line_template` exactly as they are. Update the block's header comment to describe the DTMF-code trigger inside the §24 gate (not the dead DID branch).

    In test_telephony_config.py: update `VALID_ANNOUNCEMENT_TOML` to add `code_env_var = "CTF_ANNOUNCEMENT_CODE"` and drop the `did` line (or keep it commented/optional in a separate test). REPLACE `test_announcement_entry_missing_did_rejected` with `test_announcement_entry_missing_code_env_var_rejected` (expect ConfigError matching `code_env_var`). Add `test_announcement_did_optional_defaults_empty` proving an entry with no `did` parses to `entry.did == ""`. Update `test_well_formed_announcement_entry_parses` to assert `entry.code_env_var == "CTF_ANNOUNCEMENT_CODE"`. Update `test_real_checked_in_telephony_toml_has_announcement_entry` to assert `entry.code_env_var == "CTF_ANNOUNCEMENT_CODE"` (drop the `entry.did == "7254043234"` assertion). Add a credential-gate parametrized case (or extend the existing one) confirming a bad key inside the announcement table is refused.
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest tests/test_telephony_config.py -q</automated>
  </verify>
  <done>test_telephony_config.py passes; AnnouncementEntry carries `code_env_var` (required at load), `did` optional; the shipped telephony.toml is code-keyed with the trigger value only in SSM.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DTMF-code trigger + _gate_announcement; remove dead Rev-1 DID path; gate takeover</name>
  <files>apps/voice/src/klanker_voice/telephony/controller.py, apps/voice/src/klanker_voice/telephony/gate.py, apps/voice/tests/test_telephony_controller.py</files>
  <behavior>
    - A DTMF sequence ending in the armed announcement code (990011) invokes `_gate_announcement` (spy) and NEVER calls `quota.start_gate` (spy raises if hit) and NEVER calls `greet_now`.
    - Fat-fingered noise before the code still matches (suffix semantics): e.g. "12990011" triggers.
    - The real PIN (e.g. "4242") still unlocks the concierge exactly as before — every existing PIN/passphrase/fail-closed test in test_telephony_lifecycle.py stays green, unmodified.
    - When gate_mode excludes DTMF ("passphrase"), the announcement code is ignored (no dispatch).
    - _gate_announcement, OTP-fetch returns None: tears down via exactly one `_close_active_call`, WITHOUT calling `speak_goodbye` with a line and WITHOUT `build_tts`.
    - _gate_announcement, success: `speak_goodbye` called with the digit-spaced twice-substituted line, then exactly one `_close_active_call`; no quota.start_gate, no greet_now, and no racing fail-closed goodbye even if the gate window would otherwise expire (the gate is resolved on takeover).
    - An entry whose `code_env_var` is unset arms NO trigger — the gate behaves normally (no dispatch).
    - GateProcessor.cancel_for_takeover: resolves the gate WITHOUT unlocking (no `_unlocked`, no on_unlock, no on_fail_closed afterward); idempotent; keeps redaction (still swallows transcription frames).
  </behavior>
  <action>
    gate.py — add `GateProcessor.cancel_for_takeover(self, reason: str) -> None`: if `self._resolved` return; set `self._resolved = True`; cancel `self._timer_task` if present; log only `{reason, call_id}` (never transcript/PIN/words). Do NOT set `_unlocked` (redaction boundary stays closed — transcription frames keep being swallowed while the announcement plays and tears down). This gives `_gate_announcement` the same "gate already resolved before teardown" invariant that `_gate_fail_closed` relies on, so no fail-closed timer can race a second goodbye. Do NOT touch `unlock`, `_fire_fail_closed`, `process_frame`, or `accumulate_dtmf`.

    controller.py — imports: `_announcements_by_code` uses `os.environ` (already imported) and `AnnouncementEntry` (already imported).

    __init__: REPLACE the `_announcements_by_did` dict with `_announcements_by_code: dict[str, AnnouncementEntry]`. Add an injectable kwarg `announcement_codes: dict[str, AnnouncementEntry] | None = None` (mirrors the `access_pin`/`passphrase_words` test seam). Resolve: if `announcement_codes is not None`, use `dict(announcement_codes)`; else build `{code: entry for entry in telephony_cfg.announcements if (code := os.environ.get(entry.code_env_var, "").strip())}` — an entry whose env var is unset/empty is skipped (no trigger armed). Update the field comment to describe the DTMF-code trigger.

    on_stasis_start: REMOVE the Rev-1 pre-gate announcement dispatch block (the `announcement_entry = self._announcements_by_did.get(did)` lookup and its `_run_announcement(...)`/`return`, currently ~lines 506-522). The gated/ungated branch that follows becomes the only path again.

    REMOVE the now-dead `_run_announcement` method (currently ~lines 548-622). Keep `_fetch_ctf_otp` and `_build_announcement_line` (reused by `_gate_announcement`). Keep `ANNOUNCEMENT_PLAYBACK_GRACE_SECONDS` and `CTF_OTP_FETCH_TIMEOUT_SECONDS`.

    ActiveCall: add a raw trailing DTMF buffer field `dtmf_raw: str = ""` (bounded, additive — the existing `dtmf_buffer` PIN field stays untouched). Add a module constant `DTMF_RAW_MAX_DIGITS = 32`.

    on_channel_dtmf_received: keep the `gate_mode not in ("dtmf","either")` guard and the `active_call is None or active_call.gate is None or not digit` guard byte-for-byte. Append the digit to the raw buffer: `active_call.dtmf_raw = (active_call.dtmf_raw + digit)[-DTMF_RAW_MAX_DIGITS:]`. FIRST do the existing PIN path unchanged (`accumulate_dtmf(active_call.dtmf_buffer, digit, self._pin)`; on match `await active_call.gate.unlock("dtmf")`; then `return`) — PIN keeps strict priority and byte-identical behavior. Only if the PIN did NOT match, iterate armed codes: `for code, entry in self._announcements_by_code.items(): if code and active_call.dtmf_raw.endswith(code): await self._gate_announcement(active_call, entry); return`. (Adding `return` after the PIN unlock is behaviorally equivalent to the old fall-through — nothing followed it before.)

    Add `_gate_announcement(self, active_call: ActiveCall, entry: AnnouncementEntry) -> None` MIRRORING `_gate_fail_closed`: first, if `active_call.gate is not None`, call `active_call.gate.cancel_for_takeover("announcement")` to stop the fail-closed timer (single teardown). Read `bearer = os.environ.get(entry.otp_env_var, "") if entry.otp_env_var else ""`; `headers = {"Authorization": f"Bearer {bearer}"} if bearer else {}`; `code = await _fetch_ctf_otp(entry.otp_url, headers)`. On `not code`: log by category only (channel id, NEVER code/url/bearer) and `await self._close_active_call(active_call, "announcement otp fetch failed")`, return. On success: `line = _build_announcement_line(entry.line_template, code)`; `await speak_goodbye(active_call.call_session.worker, line)`; `await asyncio.sleep(ANNOUNCEMENT_PLAYBACK_GRACE_SECONDS)`; `await self._close_active_call(active_call, "announcement complete")`. NO `quota.start_gate`, NO `greet_now`, NO lifecycle promotion. Issue NO explicit `hangup` (mirror `_gate_fail_closed`: the composed `on_released` hook hangs up the SIP channel exactly once via `_close_active_call`). Logging: only channel id.

    test_telephony_controller.py — reuse the hermetic rig. Update `_announcement_entry(**overrides)` to include `code_env_var="CTF_ANNOUNCEMENT_CODE"`. Replace the Rev-1 DID tests (`test_announcement_did_dispatches_before_gate_no_quota_no_pipeline`, `test_non_announcement_did_still_enters_gated_path`, `test_announcement_otp_fetch_failure_tears_down_no_tts`, `test_announcement_success_speaks_line_then_hangs_up_after_playback`) with Rev-2 DTMF-trigger tests, building the controller with `announcement_codes={"990011": _announcement_entry()}`, `access_pin="4242"`, `_gated_cfg()`:
      (a) `test_announcement_code_dispatches_no_quota_no_greet`: spy `_gate_announcement`; spy `quota.start_gate` (raise if called) and `greet_now`; drive DTMF "990011" via `on_channel_dtmf_received`; assert `_gate_announcement` called once with the entry, start_gate/greet never called.
      (b) `test_pin_still_unlocks_concierge`: with the same armed code, drive "4242"; assert `gate.unlocked is True` and `greet_now` fired once (PIN unchanged).
      (c) `test_fat_fingered_prefix_before_code_still_matches`: drive "12990011"; assert dispatch.
      (d) `test_announcement_otp_fetch_none_tears_down_no_speak`: monkeypatch `_fetch_ctf_otp` -> None and `speak_goodbye` (spy raising if called with a line), monkeypatch `build_tts` to raise; drive "990011"; assert exactly one `_close_active_call` teardown (hangup chan-1 once, destroy_bridge once, session closed, `controller.calls == {}`) and speak_goodbye never called.
      (e) `test_announcement_success_speaks_digitspaced_line_then_closes`: monkeypatch `_fetch_ctf_otp` -> "123456", spy `speak_goodbye`, shrink `ANNOUNCEMENT_PLAYBACK_GRACE_SECONDS` to 0.05; drive "990011"; assert speak_goodbye called once with the digit-spaced twice-substituted line, then `_close_active_call` teardown (hangup chan-1 once, `controller.calls == {}`), and no `quota.start_gate`/`greet_now`.
      (f) `test_gate_mode_passphrase_ignores_announcement_code`: `_gated_cfg(gate_mode="passphrase")`; drive "990011"; assert `_gate_announcement` never dispatched.
    Keep `test_build_announcement_line_spaces_digits_and_substitutes_both_occurrences` unchanged.
    Add a gate-level test (in test_telephony_controller.py or an existing gate test file) for `cancel_for_takeover`: it flips `_resolved` True, leaves `_unlocked` False, and a subsequent gate-window expiry does NOT fire `on_fail_closed`.
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest tests/test_telephony_controller.py tests/test_telephony_lifecycle.py -q</automated>
  </verify>
  <done>DTMF code 990011 (suffix-matched) dispatches to _gate_announcement with no quota/greet; PIN unlock and all existing gate tests stay green; OTP-fetch None tears down without speaking; success speaks the digit-spaced line then a single _close_active_call; the dead Rev-1 DID path is gone; gate.cancel_for_takeover resolves without unlocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller keypad -> controller (DTMF) | Caller-controlled digits cross into the controller's DTMF handler; must not bypass or weaken the §24 gate for non-announcement callers. |
| controller -> auth /ctf/otp | Outbound fetch of a live OTP; internal-only endpoint, optional bearer from env/SSM. |
| SSM -> telephony-edge env | The trigger code value (990011) and the OTP bearer live in SSM, injected as env vars; never in TOML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-OTP2-01 | Information Disclosure | logs / TOML | high | mitigate | The DTMF trigger code, the OTP code, otp_url, and the bearer are NEVER logged (channel id only) and NEVER stored in TOML — only the env-var NAME `code_env_var` is in TOML; verified regex-clean against `_CREDENTIAL_FIELD_RE`. |
| T-OTP2-02 | Elevation of Privilege | on_channel_dtmf_received | high | mitigate | Announcement dispatch requires an armed code AND `gate_mode in {dtmf,either}` AND an active gated call; the real PIN keeps strict priority and byte-identical unlock; a normal caller's gate posture is unchanged. |
| T-OTP2-03 | Denial of Service | _gate_announcement teardown | high | mitigate | Mirrors `_gate_fail_closed`: `gate.cancel_for_takeover` stops the fail-closed timer so no racing second goodbye; always reaches exactly one idempotent `_close_active_call` (no silently-open PSTN line); bounded `ANNOUNCEMENT_PLAYBACK_GRACE_SECONDS` so a stuck synth cannot hang the line. |
| T-OTP2-04 | Tampering | OTP fetch failure | medium | mitigate | `_fetch_ctf_otp` fails closed uniformly (None) on any non-200/timeout/malformed body -> teardown with no spoken line, no oracle. |
| T-OTP2-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages installed; nothing to audit. |
</threat_model>

<verification>
Regression sanity (all three telephony rigs green together):

```
cd apps/voice && uv run pytest tests/test_telephony_lifecycle.py tests/test_telephony_controller.py tests/test_telephony_config.py -q
```

Redaction/no-secret spot checks:
- No `990011` literal anywhere in `apps/voice/configs/telephony.toml` or `apps/voice/src` (grep).
- `_gate_announcement` and `on_channel_dtmf_received` log only the channel id — never code/url/bearer/PIN.
- `cancel_for_takeover` never sets `_unlocked` (redaction stays closed during announcement teardown).
</verification>

<success_criteria>
- `code_env_var` is required at config load; `did` is optional/informational; the shipped telephony.toml is code-keyed with the trigger value only in SSM.
- DTMF 990011 (suffix-matched, gate_mode permitting) dispatches to `_gate_announcement`; the real PIN still unlocks the concierge; wrong/no entry still fails closed.
- `_gate_announcement` never calls `quota.start_gate`/`greet_now`, speaks the digit-spaced OTP line on success (or tears down silently on fetch failure), and always reaches exactly one `_close_active_call`.
- The dead Rev-1 DID branch (`_announcements_by_did`, the `on_stasis_start` dispatch, `_run_announcement`) is removed.
- All three telephony test files pass.
</success_criteria>

<deferred>
Ops/deploy follow-ups — NOT implemented here; the orchestrator provisions after the code lands:
- New SSM SecureString `/kmv/secrets/use1/ctf/announcement_code` = `990011` (add `announcement_code` to the `ctf` secret definition in site.hcl).
- telephony-edge task-def gains env var `CTF_ANNOUNCEMENT_CODE` (auth does not need it). Apply the secrets unit, then redeploy telephony-edge.
</deferred>

<output>
Create `.planning/quick/260716-1g0-ctf-phone-otp-revision-2-dtmf-code-trigg/260716-1g0-SUMMARY.md` when done.
</output>
