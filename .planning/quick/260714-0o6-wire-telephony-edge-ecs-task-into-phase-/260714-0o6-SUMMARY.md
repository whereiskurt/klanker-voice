---
quick_id: 260714-0o6
title: Wire telephony-edge into the Phase-15 transcription ledger
status: complete
date: 2026-07-14
commit: 49e5366
---

# Summary — Quick Task 260714-0o6

Fixed a live data-loss bug: PSTN phone-call transcripts were silently dropped
because the telephony-edge ECS task had **no** Phase-15 ledger wiring, so the
in-container ledger writer's `_bucket()` was `""` and every S3 flush failed
boto3 parameter validation.

## Changes (commit `49e5366`)

| File | Change |
|------|--------|
| `infra/.../region/us-east-1/ecs-task/terragrunt.hcl` | Added `task.name == "telephony-edge"` branch injecting `KMV_LEDGER_BUCKET` (+ updated comment explaining the dropped-transcripts cause) |
| `infra/.../services/telephony-edge/service.hcl` | Added `LedgerPutOnly` (`s3:PutObject` on `kmv-ledger-*/ledger/*`) to the task role |
| `infra/.../services/telephony-edge/service.hcl` | Added `KMV_LEDGER_SALT` secret (shared SSM SecureString) |
| `infra/.../services/telephony-edge/service.hcl` | Added parity `NOTE` in env block (mirrors voice) |

All mirror the existing `voice/service.hcl` ledger wiring.

## Verification

- `terragrunt hcl format --check` → exit 0 (no reformatting needed) on both files.
- Brace/bracket/paren balance clean.
- Pattern-for-pattern match with the wired `voice` service.

## Deploy / follow-up (NOT done here — infra-source only)

1. Merge to `main` (or operator terragrunt apply) → `deploy.yml` applies
   `ecs-task` + `ecs-service`, landing the env + secret + IAM on telephony-edge.
2. Place a test PSTN call; confirm a fresh object under
   `s3://kmv-ledger-use1-adba57e4419be01f/ledger/dt=<today>/` with `caller_id`
   set, `email` null, and no `ledger flush failed` in `/ecs/telephony-edge`.
3. Does not recover calls dropped before deploy.
