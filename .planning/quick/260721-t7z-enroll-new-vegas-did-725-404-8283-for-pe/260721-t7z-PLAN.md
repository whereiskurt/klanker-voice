---
phase: quick-260721-t7z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/voice/configs/telephony.toml
autonomous: true
requirements: [QUICK-260721-t7z]
user_setup: []

must_haves:
  truths:
    - "Dialing the new Vegas DID 725-404-8283 replies via SMS from that same DID"
    - "The concierge resolves DID 7254048283 from its KVD8283 caller-ID name prefix"
    - "telephony-edge runs a new task definition carrying the updated config"
  artifacts:
    - "apps/voice/configs/telephony.toml with KVD8283 -> 7254048283 mapping and 7254048283 in sms_reply_dids"
  key_links:
    - "cid_prefix_dids tag KVD8283 -> sms_reply_dids entry 7254048283 (both must agree for per-DID SMS)"
    - "merge to main -> build-telephony-edge.yml (path filter apps/voice/**) -> ECS telephony-edge-use1 steady state"
---

<objective>
Enroll the new Las Vegas DID 725-404-8283 (bare digits 7254048283) into the
telephony per-DID SMS-reply mechanism so a caller who dials it gets a text
back FROM that same number, matching the existing behavior of the two prior
Vegas DIDs (7254043234, 7254043283). Then ship the config change via the
automatic telephony-edge deploy.

The DID is already ordered, routed to sub-account 557010_klanker-pbx,
callerid_prefix "KVD8283" set in VoIP.ms (readback-verified), and SMS-enabled
(sms_enabled=1). The ONLY remaining work is the two config lines plus deploy.

Purpose: give the new Vegas DID full per-DID concierge + SMS-reply parity.
Output: updated telephony.toml, merged PR, redeployed telephony-edge service.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/voice/configs/telephony.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enroll KVD8283 -> 7254048283 in telephony.toml</name>
  <files>apps/voice/configs/telephony.toml</files>
  <action>
    Make exactly three edits, all under the [telephony] section of
    apps/voice/configs/telephony.toml. First, in the [telephony.cid_prefix_dids]
    table, add a third mapping line `"KVD8283" = "7254048283"` immediately after
    the existing `"KVD3283" = "7254043283"` line — this ties the VoIP.ms caller-ID
    name prefix already set on the DID to its bare-digit number so the controller
    resolves the dialed DID from ${CALLERID(name)}. Second, append the bare-digit
    string `"7254048283"` to the `sms_reply_dids` array so it reads
    `["7254043234", "7254043283", "7254048283"]` — this enrolls the DID for the
    per-DID reply-from-itself path. Third, update the adjacent narrative comment
    above sms_reply_dids that currently states both Las Vegas DIDs are enrolled so
    it truthfully names three enrolled Vegas DIDs (7254043234, 7254043283,
    7254048283). Do NOT modify `otp_only_dids` — 8283 is a normal concierge DID,
    not OTP-only — and do NOT touch `sms_dids` (stays empty), the announcement
    block, or any secret-bearing env-var references.
  </action>
  <verify>
    <automated>cd apps/voice && grep -q '"KVD8283" = "7254048283"' configs/telephony.toml && grep -q '"7254043234", "7254043283", "7254048283"' configs/telephony.toml && ! grep -q '7254048283' <(sed -n '/otp_only_dids/p' configs/telephony.toml)</automated>
  </verify>
  <done>
    configs/telephony.toml contains the KVD8283 -> 7254048283 cid_prefix mapping
    and 7254048283 as the third element of sms_reply_dids; otp_only_dids is
    unchanged (still only the two prior DIDs); the enrolled-DIDs comment names
    three Vegas DIDs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify config parses, commit, and deploy telephony-edge</name>
  <files>apps/voice/configs/telephony.toml</files>
  <action>
    Confirm the edited TOML still parses under the config schema by running the
    telephony config + SMS parsing tests from apps/voice (uv-managed). Then commit
    the change on the existing branch chore/enroll-did-8283, push it, open a PR
    with `gh pr create`, and after required checks pass merge it to main. Merging
    triggers .github/workflows/build-telephony-edge.yml (path filter apps/voice/**)
    which rebuilds the image and deploys automatically. Watch the "Build:
    Telephony Edge" workflow run to success with `gh run watch`, then confirm the
    ECS service telephony-edge-use1 on cluster app-use1-kmv reaches steady state
    on a new task definition revision (aws ecs describe-services). Note in the
    SUMMARY the deployed image tag / task-def revision. Per the known
    telephony-edge deploy-revert behavior, verify the running task carries the
    NEW telephony-edge image (not a stale revision reverted by a concurrent
    voice/auth deploy) before declaring success.
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest tests/test_telephony_config.py tests/test_telephony_sms.py -q</automated>
    <human-check>gh run watch shows "Build: Telephony Edge" succeeded AND `aws ecs describe-services --cluster app-use1-kmv --services telephony-edge-use1` shows the service at steady state on a new task definition revision carrying the updated config.</human-check>
  </verify>
  <done>
    Telephony config/SMS parsing tests pass locally; PR merged to main; the
    "Build: Telephony Edge" workflow completed successfully; ECS service
    telephony-edge-use1 is at steady state on a new task definition running the
    telephony-edge image with the enrolled 8283 config.
  </done>
</task>

</tasks>

<verification>
- grep confirms KVD8283 mapping and 7254048283 present in sms_reply_dids, absent from otp_only_dids
- telephony config + SMS pytest suites pass (schema still valid)
- telephony-edge-use1 redeployed to steady state on a new task definition
- Live confirmation (operator): dialing 725-404-8283 answers as concierge and, on the SMS path, texts back from 725-404-8283
</verification>

<success_criteria>
Dialing 725-404-8283 is handled as a normal concierge DID and replies via SMS
from itself, at parity with 7254043234 and 7254043283, with the change live on
telephony-edge.
</success_criteria>

<output>
Create `.planning/quick/260721-t7z-enroll-new-vegas-did-725-404-8283-for-pe/260721-t7z-SUMMARY.md` when done
</output>
