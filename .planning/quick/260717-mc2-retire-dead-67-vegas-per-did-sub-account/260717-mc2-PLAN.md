---
phase: 260717-mc2-retire-dead-67-vegas-per-did-sub-account
plan: 01
type: execute
wave: 1
autonomous: true
files_modified:
  - apps/voice/asterisk/pjsip.conf
  - apps/voice/configs/telephony.toml
  - apps/voice/src/klanker_voice/telephony/config.py
  - apps/voice/src/klanker_voice/telephony/controller.py
  - infra/terraform/live/site/services/telephony-edge/service.hcl
  - apps/voice/tests/test_asterisk_configs.py
  - apps/voice/tests/test_telephony_config.py
  - apps/voice/tests/test_telephony_lifecycle.py
---

<objective>
Retire the DEAD #67 Option-A per-DID VoIP.ms sub-account scaffolding. Approach C (CALLERID name
prefix, shipped 260717-buf/#71) replaced it entirely; the sub-account map + registrations are
INERT (no DID routes to a per-DID sub-account). Remove the code/config/infra-source now; the
operational teardown (terragrunt apply, 4 SSM params, 2 VoIP.ms sub-accounts, entrypoint no-op
unsets) is sequenced by the orchestrator AFTER deploy.
</objective>

<safety>
- KEEP the entrypoint.sh vegas `unset` lines this pass — harmless no-ops that still scrub the
  vegas SIP creds from the controller env IF the old task-def is momentarily injecting them
  during the deploy/terragrunt-apply window (D-09). Dropped in a follow-up after the task-def
  stops injecting.
- KEEP the live klanker-pbx trunk (voipms-registration / voipms-auth / voipms-endpoint /
  voipms-identify / voipms-aor) and the base VOIPMS_SIP_USERNAME/PASSWORD secret + unsets.
- Observe-only for routing: no DID is routed to a vegas sub-account, so removing the
  registration legs cannot break inbound.
</safety>

<tasks>
<task type="auto"><name>Task 1: remove vegas pjsip registration/auth blocks</name>
<files>apps/voice/asterisk/pjsip.conf</files>
<action>Delete the four sections `[voipms-auth-vegas3234]`, `[voipms-registration-vegas3234]`,
`[voipms-auth-vegas3283]`, `[voipms-registration-vegas3283]` and the per-DID-sub-account comment
block above them (the paragraph starting "Per-DID sub-account registrations..."). Keep everything
for the klanker-pbx trunk + the shared voipms-endpoint/identify/aor untouched.</action>
<verify><automated>cd apps/voice && uv run pytest tests/test_asterisk_configs.py -q</automated></verify>
<done>No vegas sections in pjsip.conf; klanker-pbx trunk intact; config lint green (after Task 5 drops the vegas registration test).</done>
</task>

<task type="auto"><name>Task 2: remove subaccount_did_map from telephony config</name>
<files>apps/voice/src/klanker_voice/telephony/config.py</files>
<action>Remove the `subaccount_did_map` field (+ its comment), the `_parse_subaccount_dids`
function, and its call/assignment in `load_telephony_config`. Leave `cid_prefix_did_map` (Approach
C) untouched.</action>
<verify><automated>cd apps/voice && uv run pytest tests/test_telephony_config.py -q</automated></verify>
<done>TelephonyConfig has no subaccount_did_map; loader clean; config tests green (after Task 5).</done>
</task>

<task type="auto"><name>Task 3: drop subaccount term from controller resolution + toml table</name>
<files>apps/voice/src/klanker_voice/telephony/controller.py, apps/voice/configs/telephony.toml</files>
<action>controller.py on_stasis_start: remove the `self._telephony_cfg.subaccount_did_map.get(did, "")`
term so resolution is `_dialed_did_from_cidname(cidname, cfg.cid_prefix_did_map) or _dialed_did_from_sip_to(sip_to)`;
update the resolution comment to drop the sub-account-map mention. telephony.toml: remove the
`[telephony.subaccount_dids]` table + its comment block. Keep cid_prefix_dids + the announcement.</action>
<verify><automated>cd apps/voice && uv run pytest tests/test_telephony_controller.py tests/test_telephony_sms.py -q</automated></verify>
<done>Resolution = cidname-prefix OR To:; no subaccount_dids in toml.</done>
</task>

<task type="auto"><name>Task 4: remove vegas secret injections from service.hcl</name>
<files>infra/terraform/live/site/services/telephony-edge/service.hcl</files>
<action>Delete the four container `secrets` entries VOIPMS_SIP_USERNAME_VEGAS3234 /
_PASSWORD_VEGAS3234 / _USERNAME_VEGAS3283 / _PASSWORD_VEGAS3283 (valueFrom the sip_*_vegas* SSM
params) + the per-DID comment above them. Keep the base VOIPMS_SIP_USERNAME/PASSWORD (klanker-pbx)
and every non-vegas secret. (Applied via terragrunt by the orchestrator after merge.)</action>
<verify><automated>grep -c VEGAS infra/terraform/live/site/services/telephony-edge/service.hcl</automated></verify>
<done>Zero VEGAS references in service.hcl; base trunk secret intact.</done>
</task>

<task type="auto"><name>Task 5: drop the now-dead vegas tests</name>
<files>apps/voice/tests/test_asterisk_configs.py, apps/voice/tests/test_telephony_config.py, apps/voice/tests/test_telephony_lifecycle.py</files>
<action>Remove: test_asterisk_configs `test_per_did_subaccount_registrations_exist_and_target_toronto`;
test_telephony_config `test_subaccount_dids_absent_defaults_empty`, `test_subaccount_dids_parses_and_normalizes`,
`test_subaccount_dids_non_table_rejected`, `test_shipped_telephony_toml_maps_both_vegas_subaccounts`;
test_telephony_lifecycle the subaccount-map resolution test (`_telephony_cfg(subaccount_did_map=...)`).
Keep every cid_prefix / Approach-C test.</action>
<verify><automated>cd apps/voice && uv run pytest tests/ -q -k "telephony or asterisk"</automated></verify>
<done>Full telephony+asterisk suite green with the subaccount mechanism fully removed.</done>
</task>
</tasks>

<verification>
cd apps/voice && uv run pytest tests/ -q -k "telephony or asterisk"  → all pass; `git grep -in vegas -- apps infra` shows only historical comments/transcripts, no live scaffolding.
</verification>

<orchestrator_followup>
After merge+deploy of this code: (1) terragrunt apply the telephony-edge service unit (task-def
drops the 4 vegas secret injections); (2) verify new task healthy + registration yes; (3) delete
the 4 SSM params `/kmv/secrets/use1/voipms/sip_{username,password}_vegas{3234,3283}`; (4) delete
the 2 VoIP.ms sub-accounts vegas3234/vegas3283 (delSubAccount); (5) tiny follow-up commit removing
the now-no-op entrypoint.sh vegas unsets.
</orchestrator_followup>
