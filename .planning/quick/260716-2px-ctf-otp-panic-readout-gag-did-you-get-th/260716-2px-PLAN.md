---
phase: quick-260716-2px
plan: 01
type: execute
wave: 1
autonomous: true
files_modified:
  - apps/voice/src/klanker_voice/telephony/controller.py
  - apps/voice/configs/telephony.toml
  - apps/voice/tests/test_telephony_controller.py
must_haves:
  truths:
    - "The announcement is still ONE single TTS utterance via speak_goodbye (no multi-utterance sequencing)."
    - "Script order: slow read x2 (unchanged) -> 'Did you get that? <break> ... No?' -> ~3 accelerating passes of the digits (shrinking break tags, periods dropped, space-separated) -> abrupt (no break) 'BYYYYYEEEE!'."
    - "Digits stay SPACE-separated at every speed so ElevenLabs never reads 123456 as one number."
    - "_gate_announcement teardown grace is widened to cover the longer gag so nothing is cut off mid-playback."
    - "The §24 gate, cancel_for_takeover, PIN path, _fetch_ctf_otp, no-quota/no-greet, and single _close_active_call teardown are byte-identical."
  artifacts:
    - apps/voice/src/klanker_voice/telephony/controller.py
    - apps/voice/configs/telephony.toml
---

<objective>
Add an escalating "panic readout" gag to the CTF phone-OTP announcement. After
the slow OTP read (twice, unchanged), the agent asks "Did you get that? ... No?"
then reads the 6 digits in ~3 accelerating passes and cuts dead into a drawn-out
"BYYYYYEEEE!". All TTS in one utterance — supersedes the (skipped) drum idea.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: panic-readout gag builder + widened grace + tests</name>
  <files>apps/voice/src/klanker_voice/telephony/controller.py, apps/voice/configs/telephony.toml, apps/voice/tests/test_telephony_controller.py</files>
  <behavior>
    - `_build_announcement_script(template, code)` returns: the template with each {code} substituted by the SLOW paced read (existing behavior: `f"{d}."` joined by `<break time="{ANNOUNCEMENT_DIGIT_PAUSE_SECONDS}s" />`), followed by the gag tail.
    - Gag tail = `" "` + ANNOUNCEMENT_DIDYOUGET_COPY + ` <break time="0.5s" /> ... ` + ANNOUNCEMENT_NO_COPY + accelerating passes + ` ` + ANNOUNCEMENT_BYE_COPY (NO break tag immediately before the BYE — abrupt).
    - Accelerating passes: for each pause p in ANNOUNCEMENT_ACCEL_PAUSES, the digits joined by a separator that shrinks — p>0 -> `f' <break time="{p}s" /> '.join(code)` (no periods); p==0 -> `" ".join(code)` (single space, fastest, still digit-separated). Passes joined by a single space.
    - Digits are ALWAYS space-or-break separated (never concatenated) so "123456" is never read as a number.
    - Both {code} occurrences in the template are substituted (slow read appears twice).
    - _gate_announcement teardown grace = ANNOUNCEMENT_PLAYBACK_GRACE_SECONDS + 2*len(code)*ANNOUNCEMENT_DIGIT_PAUSE_SECONDS + ANNOUNCEMENT_GAG_TAIL_SECONDS.
  </behavior>
  <action>
    controller.py:
    - Add tunable module constants near ANNOUNCEMENT_DIGIT_PAUSE_SECONDS:
      `ANNOUNCEMENT_ACCEL_PAUSES = (0.3, 0.15, 0.0)` (shrinking inter-digit pauses, last=run-fast),
      `ANNOUNCEMENT_DIDYOUGET_COPY = "Did you get that?"`,
      `ANNOUNCEMENT_NO_COPY = "No?"`,
      `ANNOUNCEMENT_BYE_COPY = "BYYYYYEEEE!"`,
      `ANNOUNCEMENT_GAG_TAIL_SECONDS = 8.0` (grace budget for the "did you get that" + accel + bye).
    - Add a private helper `_pace_digits_slow(code)` factoring the EXISTING slow join (`f' <break time="{ANNOUNCEMENT_DIGIT_PAUSE_SECONDS}s" /> '.join(f"{d}." for d in code)`), and a `_build_accel_tail(code)` helper. RENAME `_build_announcement_line` -> `_build_announcement_script(template, code)` that returns `template.replace("{code}", _pace_digits_slow(code)) + " " + _build_accel_tail(code)`. Keep all three pure/module-level/unit-testable.
    - `_build_accel_tail(code)`: builds the gag tail string exactly per <behavior> using the constants. Ensure NO `<break>` immediately precedes ANNOUNCEMENT_BYE_COPY (abrupt cut).
    - Update `_gate_announcement`: replace the call to `_build_announcement_line` with `_build_announcement_script`, and extend the grace formula with `+ ANNOUNCEMENT_GAG_TAIL_SECONDS`. Everything else in `_gate_announcement` stays byte-identical (cancel_for_takeover, _fetch_ctf_otp, speak_goodbye seam, single _close_active_call, logging discipline — never log the code).

    apps/voice/configs/telephony.toml: change `line_template` to `"Hey! Let me get you that O T P. {code}. That's {code}."` (DROP the trailing "Buh bye." — the bye now comes from the gag tail). Leave otp_url/otp_env_var/code_env_var and the SSM-only comments unchanged.

    tests (test_telephony_controller.py): rename the builder test to e.g. `test_build_announcement_script_slow_read_twice_then_panic_gag`; assert:
      * the slow paced read (digit "." + `<break time="1.0s" />`) appears TWICE (both {code} occurrences),
      * the tail contains ANNOUNCEMENT_DIDYOUGET_COPY, ANNOUNCEMENT_NO_COPY, and ANNOUNCEMENT_BYE_COPY,
      * the accelerating passes are present with shrinking break tags (assert `<break time="0.3s" />` and `<break time="0.15s" />` both appear in the tail; and the fastest pass has the digits single-space separated),
      * digits are never concatenated (e.g. "123456" as a substring does NOT appear),
      * NO `<break ... />` occurs immediately before ANNOUNCEMENT_BYE_COPY (abrupt) — e.g. assert the substring `f'/> {ANNOUNCEMENT_BYE_COPY}'` is NOT present and the char before BYE is a space not a `>`.
    Update `test_announcement_success_speaks_digitspaced_line_then_closes` to expect `_build_announcement_script(entry.line_template, "123456")` (derive from the builder; do NOT hardcode a brittle literal). Keep the DTMF-trigger/no-quota/no-greet/teardown assertions unchanged. Fix the import (`_build_announcement_line` -> `_build_announcement_script`) at the top of the test file.
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest tests/test_telephony_controller.py tests/test_telephony_lifecycle.py -q</automated>
  </verify>
  <done>The announcement speaks slow read x2 -> "Did you get that? ... No?" -> ~3 accelerating digit passes -> abrupt "BYYYYYEEEE!" as one utterance; digits never concatenated; grace widened; gate/PIN/teardown byte-identical; tests green.</done>
</task>

</tasks>

<verification>
- cd apps/voice && uv run pytest tests/test_telephony_controller.py tests/test_telephony_lifecycle.py tests/test_telephony_config.py -q  (all green)
</verification>

<success_criteria>
- One TTS utterance: slow x2 -> gag -> abrupt BYE; digits space-separated at all speeds.
- Teardown grace covers the full ~25-30s script.
- No change to the §24 gate, PIN path, cancel_for_takeover, OTP fetch, or teardown.
- Deploy is a follow-up (orchestrator merges + redeploys telephony-edge).
</success_criteria>

<output>
Create .planning/quick/260716-2px-ctf-otp-panic-readout-gag-did-you-get-th/260716-2px-SUMMARY.md when done.
</output>
