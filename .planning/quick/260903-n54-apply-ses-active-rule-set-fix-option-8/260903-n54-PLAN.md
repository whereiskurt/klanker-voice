---
phase: 260903-n54-apply-ses-active-rule-set-fix-option-8
plan: 01
type: execute
wave: 1
depends_on: [260903-mqd]
files_modified:
  - infra/terraform/modules/email/v1.0.0/ses.tf
  - docs/operators/ses-active-rule-set-fix.md
autonomous: false
requirements: [QUICK-260903-n54]

must_haves:
  truths:
    - "aws_ses_active_receipt_rule_set.main declares the literal \"sandbox-email-shared\" instead of following aws_ses_receipt_rule_set.main.rule_set_name."
    - "The plan for the email unit shows ZERO destroys. Any destroy of this resource type deactivates inbound mail account-wide and must abort the apply."
    - "After the apply, describe-active-receipt-rule-set still reports sandbox-email-shared holding all three rules."
    - "A second plan run after the apply is clean — that is what proves it sticks rather than merely surviving one apply."
    - "The doc records what was actually applied and why §8 was chosen over its own §5 recommendation."
  artifacts:
    - infra/terraform/modules/email/v1.0.0/ses.tf
    - docs/operators/ses-active-rule-set-fix.md
  key_links:
    - "Live value is ALREADY sandbox-email-shared (hand-applied 2026-09-03). The provider's Read populates rule_set_name from DescribeActiveReceiptRuleSet, so refresh should reconcile state to the live value and the literal config should match it → no-op plan. VERIFY THIS IN THE PLAN, do not assume."
    - "If the plan instead shows destroy+create (rule_set_name being ForceNew with no in-place update path), STOP — destroy calls SetActiveReceiptRuleSet with no name, leaving a window with NO active rule set and dropping mail for both systems. Fall back to the §5 removed-block variant."
    - "Only one email unit exists (live/site/region/us-east-1/email); the state address is unprefixed because terragrunt.hcl:65-67 makes the module the root."
    - "CI apply path: terragrunt-apply.yml, modules=email → resolves to region/us-east-1/email (workflow lines ~113-130). Human-gated by the required reviewer on the terraform-apply environment (D-08)."
    - "Editing modules/email/v1.0.0 in place rather than cutting v1.1.0: all 13 modules are v1.0.0, none has been bumped, and the us-east-1 email unit is the only consumer."
---

# Quick Task 260903-n54: Apply the SES fix (option §8)

Make Terraform assert the shared rule set instead of stealing the account-wide pointer.
Operator chose §8 (assert) over the doc's §5 (forget) specifically because it self-heals:
every future apply re-asserts the correct value rather than merely declining to fight.

## Task 1: Point the activation resource at the shared set

**Files:** `infra/terraform/modules/email/v1.0.0/ses.tf`

**Action:** Replace `rule_set_name = aws_ses_receipt_rule_set.main.rule_set_name` with the
literal `"sandbox-email-shared"` on `aws_ses_active_receipt_rule_set.main`. Add a comment block
explaining the one-active-set-per-account constraint, the shared-account situation, why the
literal is deliberate, and that this does NOT fix the inert-kmv-email problem (doc §9).

**Verify:** `terragrunt plan` in `live/site/region/us-east-1/email` shows **0 to destroy**.

**Done:** Config asserts the shared set; plan proves the apply is safe.

## Task 2: Plan, land, apply, verify

**Action:**
1. `terragrunt plan` locally — read it in full. Abort on any destroy.
2. PR to `main`, merge.
3. Dispatch `terragrunt-apply.yml` with `modules=email`, approve the environment gate, watch it.
4. `aws ses describe-active-receipt-rule-set` — must still be `sandbox-email-shared` with
   `km-operator-inbound`, `km-sandbox-catchall`, `kmv-auth-inbound`.
5. Re-run `terragrunt plan` — must be clean. This is the "it sticks" gate.

**Done:** The account pointer is Terraform-asserted, and a fresh plan agrees with reality.

## Task 3: Record what was applied

**Files:** `docs/operators/ses-active-rule-set-fix.md`

**Action:** Convert the doc from "here is what to do" to "here is what was done": mark §8 as the
applied option with the date and commit, note why it was chosen over §5, and keep §9 intact as
the still-open follow-up.

**Done:** The doc describes the live state, not a pending decision.
