---
id: 260903-n54
title: Apply the SES fix (option §8)
status: complete
date: 2026-09-11
---

# Quick Task 260903-n54 — Summary

Planned 2026-09-03, executed 2026-09-11 as a side-quest during the hibernate/wake rollout
rather than through this plan's own GSD run. The plan's intent was met in full; the route
differed.

## What was done

`infra/terraform/modules/email/v1.0.0/ses.tf` — `aws_ses_active_receipt_rule_set.main` now
declares the literal `"sandbox-email-shared"` instead of following
`aws_ses_receipt_rule_set.main.rule_set_name`. Shipped in **PR #99**, commit `d9de59c`,
merged to `main` as `01ec05d`.

## How the must-haves were satisfied

| Must-have | Outcome |
|---|---|
| Declares the literal, not the reference | Done — `ses.tf:26-28` |
| Email unit plan shows **zero destroys** | Exceeded: plans **No changes** (run 34598166153) |
| Live set still reports `sandbox-email-shared` with all three rules | Untouched — no apply was run, see below |
| A **second** plan run after is clean | Confirmed on `main` after merge (run 34599033335) |
| Doc records what was applied and why §8 over §5 | Done — new RESOLVED section at the top of `docs/operators/ses-active-rule-set-fix.md` |

## The deviation worth knowing

**No apply was performed, and none was needed.** The plan assumed an apply of the `email`
unit. But the live pointer had already been hand-restored to `sandbox-email-shared` on
2026-09-03, so landing the code simply made Terraform's desired state match reality. The
`email` unit went straight from `1 to change` to `No changes`.

That is a strictly better outcome than the plan anticipated: it removes the hazard without
ever applying the unit whose apply *is* the hazard. The plan's key_link correctly predicted
this ("refresh should reconcile state to the live value and the literal config should match it
→ no-op plan. VERIFY THIS IN THE PLAN, do not assume") — and it was verified in the plan, twice,
not assumed.

The plan's abort condition (destroy+create, meaning `rule_set_name` is ForceNew) did not
arise — there was no diff at all to force one.

## Correction on the record

`d9de59c`'s commit message and PR #99's body both describe the change as "§5". **It is §8.**
§5 is the `removed`-block variant that makes Terraform forget the resource; §8 is the assert
variant that was actually implemented and that this plan specified. The code is right; the
citation was wrong, and the doc now says so explicitly.

## Still open

§9 of the doc — our own receipt rules still attach to the now-inert `kmv-email` set, so a new
receive/forward rule would apply cleanly and receive nothing. Out of scope here, as this plan
stated.
