---
phase: quick-260722-ri1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/voice/src/klanker_voice/telephony/controller.py
  - apps/voice/configs/telephony.toml
  - apps/voice/tests/test_telephony_controller.py
  - apps/voice/tests/test_telephony_sms.py
autonomous: true
requirements:
  - QUICK-260722-ri1-otp-gag-v6

must_haves:
  truths:
    - "A caller who dials the announcement code hears the operator-approved v6 gag: new opener, comma-paced then space-paced code read, wheels-come-off derangement jumble, six-group pre-reveal pause, then the punchline."
    - "The same OTP code always produces the identical spoken script (deterministic jumble, no call-path randomness)."
    - "Every jumble digit is drawn from the code's own digit multiset (never a foreign digit)."
    - "A caller who could NOT be texted never hears 'SMS on the way' (no false promise)."
    - "No angle-bracket markup ever appears in the assembled script (TTS reads markup aloud)."
    - "The full apps/voice pytest suite passes."
  artifacts:
    - apps/voice/src/klanker_voice/telephony/controller.py
    - apps/voice/configs/telephony.toml
    - apps/voice/tests/test_telephony_controller.py
    - apps/voice/tests/test_telephony_sms.py
  key_links:
    - "telephony.toml line_template {code}/{code_fast} placeholders <-> _build_announcement_script substitution"
    - "seeded random.Random(code) <-> deterministic jumble <-> reproducible tests"
    - "sms_eligible flag <-> SMS-vs-fallback closing copy"
    - "new tail length <-> ANNOUNCEMENT_GAG_TAIL_SECONDS teardown grace"
---

<objective>
Rework the CTF OTP announcement gag script (spoken when a caller dials the armed
announcement code) to the operator-approved v6 audition take: new opener, a
comma-paced then space-paced code read, a deterministic "wheels-come-off" digit
derangement jumble derived from the caller's own OTP, a long six-group pre-reveal
pause, and the "SMS on the way" punchline (with an honest non-SMS fallback).

Purpose: The prior 6-fast-pass "accelerating rattle" gag is replaced by the
operator-auditioned v6 shape. This is copy/assembly only — gate mechanics, DTMF
matching, SMS bodies, the send flow, and the OTP fetch are untouched.

Output: Reworked announcement constants + `_pace_digits_slow` / `_build_accel_tail`
/ `_build_announcement_script` in controller.py, updated `line_template` in
telephony.toml, and updated/added tests. Full apps/voice suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/voice/src/klanker_voice/telephony/controller.py
@apps/voice/configs/telephony.toml
@apps/voice/tests/test_telephony_controller.py
@apps/voice/tests/test_telephony_sms.py

# Reference — the exact approved v6 utterance for code 830429:
#   Hey! Let me get that one time password for you. Ready? . ... 8, 3, 0, 4, 2, 9.
#   That's 8 3 0 4 2 9. Did you get that? ... 8, 3, 0, 2 9 4... wait.
#   8 3 4 0 9 2 0 9 8 2 4 3 4 2 0 3 9 8 8 0. [six ellipsis groups]
#   Just kidding. SMS on the way. Hack the planet.
# (Segment B/C/D shuffle values above were hand-picked in audition; the seeded
#  shuffle produces its OWN values — tests assert STRUCTURE + determinism, not
#  the hand-picked digits.)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rework the announcement constants and assembly functions (controller.py + telephony.toml)</name>
  <files>apps/voice/src/klanker_voice/telephony/controller.py, apps/voice/configs/telephony.toml</files>
  <behavior>
    For a fixed code (use 830429 as the reference the executor reasons against),
    _build_announcement_script(template, code) must, in order, produce:
    - Opener: template with {code} substituted comma-paced ("reasonable pace")
      and {code_fast} substituted space-paced (the faster re-read).
    - Tail: "Did you get that?" + " ... " then
      Segment A = first three code digits comma-paced with a trailing comma-space,
      Segment B = last three code digits shuffled space-paced (MUST differ from
        true order; deterministic rotate-by-one fix-up if a shuffle equals it),
        followed by "... wait. ",
      Segment C = all six code digits shuffled space-paced (MUST differ from the
        true code),
      Segment D = a 14-digit space-paced runaway, each digit drawn from the code's
        OWN digits only (never a foreign digit),
    - Pre-reveal pause of six ellipsis groups, then the closing.
    - Closing: sms-eligible -> "Just kidding. SMS on the way. Hack the planet."
               non-sms fallback -> "Just kidding. " + the unchanged bye copy.
    - Determinism: seed random.Random(code) (or equivalent code-derived seed) so
      the same code yields byte-identical output every time; NO `random`-module
      global randomness on the call path.
    - No `<` or `>` anywhere in the output.
  </behavior>
  <action>
Rework ONLY the announcement copy/assembly region of controller.py (roughly lines 199-240 and the functions _pace_digits_slow / _build_accel_tail / _build_announcement_script near lines 366-423) plus the telephony.toml line_template. Do NOT touch SMS bodies, _send_sms_sequence, _fetch_ctf_otp, gate mechanics, or DTMF matching.

CONSTANTS (preserve each doc-comment's markup-free / 7-bit-ASCII warnings; update the wording so the comments stay honest about the new values):
- Re-value ANNOUNCEMENT_SLOW_DIGIT_SEP from ". ... " to ", " (comma pace — the "reasonable pace" read). It feeds _pace_digits_slow, which now serves the {code} placeholder.
- Add a clearly-named fast separator constant (e.g. ANNOUNCEMENT_FAST_DIGIT_SEP = " ") and a _pace_digits_fast(code) helper that space-joins digits, serving {code_fast} and the jumble segments.
- Reduce ANNOUNCEMENT_SLOW_DIGIT_SECONDS from 0.9 to ~0.4 (comma pace is far faster than the old very-slow read); update its doc comment to say it now sizes the two paced reads.
- Remove the now-dead ANNOUNCEMENT_ACCEL_SEPS (the 6-pass form) and ANNOUNCEMENT_NO_COPY ("No?"). Keep ANNOUNCEMENT_DIDYOUGET_COPY = "Did you get that?" and ANNOUNCEMENT_BYE_COPY = "Good luck! Hack the planet!" unchanged.
- Change ANNOUNCEMENT_SMS_PUNCHLINE_COPY to "Just kidding. SMS on the way. Hack the planet." (keep the pure-7-bit-ASCII doc note).
- Re-value ANNOUNCEMENT_PUNCHLINE_PAUSE to six ellipsis groups: ". ... ... ... ... ... ... " (leading period, six "... " groups, trailing space — operator-tuned, do not shorten). Its role changes: it now sits BEFORE the whole reveal line and applies to BOTH the sms and non-sms closings.
- Recompute ANNOUNCEMENT_GAG_TAIL_SECONDS honestly for the new tail (~26 space-paced spoken digits + the six-group pause + the short reveal; the v6 clip ran ~18s total with the tail ~half of that). Set a generous value (grace only bounds teardown; larger is safe) with a comment justifying the arithmetic — e.g. ~15-18s.

FUNCTIONS:
- _pace_digits_slow(code): now comma-paces via ANNOUNCEMENT_SLOW_DIGIT_SEP (unchanged body, new separator value); refresh its doc comment/example to the comma form.
- Add _pace_digits_fast(code): space-paces via ANNOUNCEMENT_FAST_DIGIT_SEP.
- _build_accel_tail(code, closing): rewrite to the v6 shape. Seed rng = random.Random(code). Build Segment A (first three digits comma-paced with trailing comma-space), Segment B (rng shuffle of the last three digits, space-paced, differ-from-true with a deterministic rotate-by-one fix-up, then "... wait. "), Segment C (rng shuffle of all six digits, space-paced, differ-from-true-code), Segment D (14 digits each rng.choice from the code's own digits, space-paced). Assemble as: ANNOUNCEMENT_DIDYOUGET_COPY + " ... " + segA + segB + segC + " " + segD + closing. The `closing` argument already carries the pre-reveal pause prepended (see below), so the pause lands between the last runaway digit and the reveal. Keep the doc comment markup-free-warning; update it to describe the four segments and the determinism seed.
- _build_announcement_script(template, code, sms_eligible=False): substitute BOTH placeholders — template.replace("{code}", _pace_digits_slow(code)).replace("{code_fast}", _pace_digits_fast(code)) (replace {code} first; {code_fast} is not matched by the {code} literal so order is safe). Build closing = ANNOUNCEMENT_PUNCHLINE_PAUSE + (ANNOUNCEMENT_SMS_PUNCHLINE_COPY if sms_eligible else "Just kidding. " + ANNOUNCEMENT_BYE_COPY). Both branches now get the pause (the non-sms fallback must NEVER say "SMS on the way" — it uses the bye copy instead). Return substituted_template + " " + _build_accel_tail(code, closing). Update the doc comment to the v6 structure.

telephony.toml: change the [[telephony.announcement]] line_template (currently `"Hey! Let me get you that O T P. {code}. That's {code}."`) to:
  line_template = "Hey! Let me get that one time password for you. Ready? . ... {code}. That's {code_fast}."
The config loader only validates that {code} is present (config.py line 331) — {code} is still present, so no loader change is needed.

Leave the grace-math call site (controller.py ~1447-1451: PLAYBACK_GRACE + 2*len(code)*SLOW_DIGIT_SECONDS + GAG_TAIL) structurally as-is; it now reflects two paced reads via the reduced SLOW_DIGIT_SECONDS plus the recomputed GAG_TAIL.
  </action>
  <verify>
    <automated>cd apps/voice && uv run python -c "from klanker_voice.telephony.controller import _build_announcement_script as b; t='Hey! Let me get that one time password for you. Ready? . ... {code}. That\'s {code_fast}.'; s=b(t,'830429'); s2=b(t,'830429'); assert s==s2, 'not deterministic'; assert '<' not in s and '>' not in s, 'markup leaked'; assert s.startswith('Hey! Let me get that one time password for you. Ready? . ... 8, 3, 0, 4, 2, 9. That\'s 8 3 0 4 2 9.'), 'opener wrong'; assert 'Did you get that? ...' in s; assert '8, 3, 0, ' in s; assert 'wait.' in s; assert s.rstrip().endswith('SMS on the way. Hack the planet.'), 'punchline wrong'; assert set(c for c in s if c.isdigit()) <= set('830429'), 'foreign digit'; nf=b(t,'830429',False); assert 'SMS' not in nf, 'fallback promises SMS'; print('OK')"</automated>
  </verify>
  <done>controller.py + telephony.toml reworked; the smoke check prints OK (deterministic, no markup, correct opener/punchline, code-only digits, non-SMS fallback contains no "SMS").</done>
</task>

<task type="auto">
  <name>Task 2: Update and extend the announcement tests, full suite green</name>
  <files>apps/voice/tests/test_telephony_controller.py, apps/voice/tests/test_telephony_sms.py</files>
  <action>
Update every test that asserts the OLD script shape, then add the new-structure tests. Do NOT weaken unrelated SMS-flow assertions.

test_telephony_controller.py:
- Rewrite test_build_announcement_script_slow_read_twice_then_panic_gag (~line 314) to the v6 structure. Drop the removed-symbol imports (ANNOUNCEMENT_NO_COPY) and the dead assertions ("1, 2, 3, 4, 5, 6" / "1 2 3 4 5 6" accel passes, count-of-paced==2, NO_COPY present). Assert instead: no "<break" / "/>" / "<" / ">"; ANNOUNCEMENT_DIDYOUGET_COPY present; the comma-paced opener form present; the reveal punchline at the end; the full code never appears as a bare concatenated number.
- Leave the line ~550 test (goodbye_calls == [_build_announcement_script(entry.line_template, "123456")]) unchanged — it compares against the same function and stays self-consistent.

test_telephony_sms.py:
- test_script_eligible_swaps_in_check_your_phone_punchline (~165): replace the `"Check your phone" in eligible` assertion with `"SMS on the way" in eligible` (and rename the test to match); keep the ANNOUNCEMENT_SMS_PUNCHLINE_COPY-in-eligible / not-in-legacy and no-markup / no-bare-code assertions.
- test_script_eligible_pauses_before_punchline (~176): the pre-reveal pause now applies to BOTH paths, so the `ANNOUNCEMENT_PUNCHLINE_PAUSE not in legacy` assertion is no longer true — update it: assert the pause is present in BOTH eligible and legacy output, and that eligible closes with ANNOUNCEMENT_PUNCHLINE_PAUSE + ANNOUNCEMENT_SMS_PUNCHLINE_COPY while legacy closes with the pause + "Just kidding. " + ANNOUNCEMENT_BYE_COPY.
- Leave test_script_ineligible_is_byte_identical_to_legacy (~156) — still valid (BYE_COPY present, SMS_PUNCHLINE absent).
- Verify the hook-level tests (~311-396, ~600-611) still hold: sms-eligible goodbye contains ANNOUNCEMENT_SMS_PUNCHLINE_COPY; ineligible/no-relay goodbye contains ANNOUNCEMENT_BYE_COPY and NOT ANNOUNCEMENT_SMS_PUNCHLINE_COPY. These should pass unchanged; only adjust if a symbol was removed.

ADD (place near the existing _build_announcement_script branch tests in test_telephony_sms.py):
- (a) Structure + determinism for a fixed code (e.g. "830429"): call _build_announcement_script twice and assert identical output; assert it starts with the comma/space opener form and ends with the reveal punchline; assert Segment B (the shuffled last-three) differs from the true last-three order; assert every digit character in the assembled string is a member of the code's digit multiset. Do NOT assert the hand-picked audition shuffle values — assert structure/invariants only.
- (b) Non-SMS fallback never contains "SMS": assert "SMS" not in _build_announcement_script(template, code, False).
- (c) Markup regression guard: assert "<" not in line and ">" not in line for the assembled script.

Any test that imports a removed symbol (ANNOUNCEMENT_NO_COPY, ANNOUNCEMENT_ACCEL_SEPS) must drop that import.
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest -q</automated>
  </verify>
  <done>Full apps/voice pytest suite passes; announcement tests assert the v6 structure, determinism, code-only digits, non-SMS-fallback-has-no-"SMS", and no-markup guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assembled script -> ElevenLabs streaming TTS | Any `<`/`>` markup in the string is read ALOUD ("borked" readout); the string must stay markup-free. |
| OTP code -> spoken jumble | The jumble is derived from the live OTP; it must never leak the code as a bare number, but every jumble digit is intentionally from the code (the "almost the code" gag). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-RI1-01 | Tampering | assembled announcement script | medium | mitigate | No-markup guard test ("<"/">" absent) + all pacing is plain punctuation, no angle-bracket break tags. |
| T-RI1-02 | Information disclosure | non-SMS fallback closing | low | mitigate | Fallback path uses ANNOUNCEMENT_BYE_COPY and is tested to never contain "SMS" — a caller who was not texted is never told an SMS is coming. |
| T-RI1-03 | Repudiation | jumble determinism | low | mitigate | Seeded random.Random(code); no call-path global randomness, so behavior is reproducible and testable. |
</threat_model>

<verification>
- `cd apps/voice && uv run pytest -q` passes (full suite).
- The Task 1 smoke check prints OK.
- Manual read-through of the assembled string for code 830429 matches the approved v6 shape (opener, comma/space reads, Did-you-get-that, A/B/C/D jumble, six-group pause, punchline).
</verification>

<success_criteria>
- Announcement gag emits the operator-approved v6 script; same code -> identical output.
- Every jumble digit is from the code's own digit multiset; the full code never appears concatenated.
- Non-SMS callers never hear "SMS on the way".
- No angle-bracket markup in the spoken line.
- SMS bodies, send flow, OTP fetch, gate + DTMF mechanics untouched.
- Full apps/voice test suite green.
</success_criteria>

<output>
Create `.planning/quick/260722-ri1-rework-the-ctf-otp-announcement-gag-scri/260722-ri1-SUMMARY.md` when done.
</output>
