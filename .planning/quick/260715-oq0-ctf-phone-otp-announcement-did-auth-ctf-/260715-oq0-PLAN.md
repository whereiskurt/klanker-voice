---
phase: quick-260715-oq0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/auth/webapp/src/lib/ctf-totp.ts
  - apps/auth/webapp/src/app/ctf/otp/route.ts
  - apps/auth/webapp/src/lib/__tests__/ctf-totp.test.ts
  - apps/auth/webapp/src/app/ctf/__tests__/ctf-otp-route.test.ts
  - apps/voice/src/klanker_voice/telephony/config.py
  - apps/voice/configs/telephony.toml
  - apps/voice/src/klanker_voice/telephony/controller.py
  - apps/voice/tests/test_telephony_config.py
  - apps/voice/tests/test_telephony_controller.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "GET /use1/ctf/otp returns { code, digits:6, period:120, expiresIn } for the current 120s TOTP step computed from CTF_OTP_SECRET; cache-control: no-store."
    - "Every /ctf/otp failure mode (missing secret, bad/absent bearer when CTF_OTP_AUTH_TOKEN set, internal error) returns an IDENTICAL uniform 404 — no distinct 401/403, no oracle."
    - "The /ctf/otp issuer emits exactly ONE code (the current step) — never a skew range; ±1 skew is a verifier-only (out-of-scope meshtk) concern."
    - "A call to DID 7254043234 routes to the announcement branch BEFORE the §24 gate — no mint, no quota.start_gate, no STT/LLM/conversation pipeline — hears the OTP line spoken (digits spaced, twice), then hangs up."
    - "Absent [[telephony.announcement]] table -> empty announcements tuple -> telephony behavior byte-unchanged."
    - "No secret ever lives in TOML: the announcement entry carries only the NAME of the bearer env var; the token value is read from env at call time."
  artifacts:
    - apps/auth/webapp/src/app/ctf/otp/route.ts
    - apps/auth/webapp/src/lib/ctf-totp.ts
    - apps/voice/src/klanker_voice/telephony/config.py
    - apps/voice/src/klanker_voice/telephony/controller.py
    - apps/voice/configs/telephony.toml
  key_links:
    - "controller.on_stasis_start: after `did = _normalize_token(dialplan.get('exten'))` and transport construction, a DID-match on the announcements tuple dispatches to _run_announcement(...) and returns, ahead of the require_gate branch at line ~440."
    - "_run_announcement fetches otp_url (optional Bearer from os.environ[<env-var-name>]), builds the line via a pure line-builder, synthesizes with factories.build_tts over the existing TelephonyTransport/RTP media, then tears down via _teardown_gate_resources (no ActiveCall registered)."
    - "Shared TOTP contract (issuer side): HMAC-SHA1, 6 digits, 120s period, T0=0, base32 CTF_OTP_SECRET — must match meshtk's independent verifier."
---

<objective>
Implement the CTF phone-OTP announcement DID per the APPROVED spec at
docs/superpowers/specs/2026-07-15-ctf-phone-otp-announcement-did-design.md (authoritative —
read it in full first; do not re-derive the design). Two in-repo pieces:

1. **auth app** — a new internal-only `GET /ctf/otp` route that returns the current 120s TOTP
   computed from `CTF_OTP_SECRET`, with the exact no-oracle uniform-404 posture of the existing
   `/tel` route and an optional shared bearer via the NEW env var `CTF_OTP_AUTH_TOKEN`.
2. **telephony (voice app)** — a new announcement-DID branch in `controller.on_stasis_start`
   that, for a configured DID, answers, fetches the OTP, speaks it (digits spaced, twice), and
   hangs up — skipping the §24 gate, mint, quota, STT, LLM, and conversation entirely.

meshtk verify/award is EXPLICITLY OUT OF SCOPE — this repo only defines the shared TOTP contract
and the issuer that emits ONE current-step code.

Purpose: a "proof-of-phone-call" CTF mechanic — possession of the code requires actually placing
the call to an internal-only line.
Output: `/ctf/otp` route + TOTP helper; `announcements` config surface + `_run_announcement`
branch; the concrete DID entry for 7254043234; tests for both pieces; ops follow-up notes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/superpowers/specs/2026-07-15-ctf-phone-otp-announcement-did-design.md
@apps/auth/webapp/src/app/tel/[e164]/route.ts
@apps/auth/webapp/src/app/tel/__tests__/tel-route.test.ts
@apps/voice/src/klanker_voice/telephony/config.py
@apps/voice/src/klanker_voice/telephony/controller.py
@apps/voice/src/klanker_voice/telephony/pickup_cue.py
@apps/voice/configs/telephony.toml

Reference (do not re-read fully unless needed): apps/voice/src/klanker_voice/factories.py
(build_tts), apps/voice/src/klanker_voice/pipeline.py (speak_goodbye / TTSSpeakFrame / worker
seam), apps/voice/tests/test_telephony_controller.py (monkeypatch rig).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: auth GET /ctf/otp route + local HMAC-SHA1 TOTP helper</name>
  <files>apps/auth/webapp/src/lib/ctf-totp.ts, apps/auth/webapp/src/app/ctf/otp/route.ts, apps/auth/webapp/src/lib/__tests__/ctf-totp.test.ts, apps/auth/webapp/src/app/ctf/__tests__/ctf-otp-route.test.ts</files>
  <behavior>
    - ctf-totp.ts: `computeTotp(secretBase32, { period, digits, now })` returns { code, expiresIn }.
      HMAC-SHA1 (RFC 6238, T0=0), dynamic truncation, zero-padded to `digits`. Defaults period=120,
      digits=6. `now` (ms) injectable for a fixed clock. expiresIn = period - floor(now/1000) % period,
      always within [1, period].
    - RFC 6238 SHA1 vector: base32 secret "GEZDGNBVGY3TQOJQGEZDGNBVGY3TQOJQ" (ASCII "12345678901234567890"),
      now=59000ms, period=30, digits=8 -> "94287082" (proves HMAC-SHA1 + truncation correctness).
    - Route: a known CTF_OTP_SECRET + fixed clock returns the same code computeTotp produces; digits=6,
      period=120; expiresIn within [1, 120].
    - Route no-oracle: missing CTF_OTP_SECRET, absent/wrong bearer (when CTF_OTP_AUTH_TOKEN set), and any
      internal error ALL return an identical minimal 404 (never 401/403); correct bearer returns 200.
  </behavior>
  <action>
    TOTP dependency decision (surface + recommend): the auth webapp has NO otplib/otpauth dependency
    (only `jose` for JWT). RECOMMENDATION — do NOT add a dependency; implement a tiny local TOTP helper
    with Node's built-in `crypto` (HMAC-SHA1 is zero-dep). Create `apps/auth/webapp/src/lib/ctf-totp.ts`:
    a base32 decoder (RFC 4648, no padding assumptions) for the secret, then standard RFC 6238 TOTP —
    T0=0, counter = floor(unixSeconds / period), 8-byte big-endian counter, HMAC-SHA1(key, counter),
    dynamic-truncation offset from the last nibble, 31-bit int mod 10^digits, left-padded to `digits`.
    Export `computeTotp(secretBase32: string, opts?: { period?: number; digits?: number; now?: number })
    -> { code: string; expiresIn: number }`. TOTP params are FIXED CONSTANTS (period=120, digits=6),
    never request input. The issuer emits ONLY the current-step code — do NOT emit a range; ±1 skew lives
    solely in the out-of-scope meshtk verifier.

    Create `apps/auth/webapp/src/app/ctf/otp/route.ts`, mirroring tel/[e164]/route.ts's structure:
    a single `notFound()` helper returning a minimal 404 with cache-control no-store; one try/catch whose
    catch returns notFound() (uniform failure, no leak). Order inside try: (1) optional shared-bearer —
    read `process.env.CTF_OTP_AUTH_TOKEN` (a NEW env var, NOT reusing TELEPHONY_ENDPOINT_AUTH_TOKEN); if
    set and the Authorization header != `Bearer <token>`, return the SAME notFound(); (2) read
    `process.env.CTF_OTP_SECRET` (SSM base32, env only — NEVER TOML); if absent/empty, notFound();
    (3) `const { code, expiresIn } = computeTotp(secret)` with fixed period=120/digits=6; (4) return
    `NextResponse.json({ code, digits: 6, period: 120, expiresIn }, { status: 200 })` with header
    `cache-control: no-store`. It is internet-locked at deploy time exactly like /tel and inherits the
    next.config.ts basePath — served at `/use1/ctf/otp`. Do NOT log the code, the secret, or any
    caller-distinguishing value (match the tel route's log discipline; a success-only tier-less no-op is
    fine, but simplest is no log at all).

    Tests: put helper tests in `apps/auth/webapp/src/lib/__tests__/ctf-totp.test.ts` (RFC vector +
    expiresIn bounds) and route tests in `apps/auth/webapp/src/app/ctf/__tests__/ctf-otp-route.test.ts`,
    following tel-route.test.ts's shape (makeRequest header stub; set/clear process.env in
    beforeAll/afterEach; dynamic `import("../otp/route")` after env is set). Assert: known secret + a fixed
    injected clock yields the expected 6-digit code and expiresIn in [1,120]; bearer enforced when
    CTF_OTP_AUTH_TOKEN is set (missing/wrong bearer == same 404 as a missing-secret 404); correct bearer
    -> 200. For the fixed-clock route assertion, either inject the clock via a computeTotp seam the route
    exposes for tests, or assert the route's `code` equals `computeTotp(secret).code` computed in the same
    tick (compare code equality rather than a hardcoded value to avoid clock flakiness).
  </action>
  <verify>
    <automated>cd apps/auth/webapp && npx vitest run src/lib/__tests__/ctf-totp.test.ts src/app/ctf/__tests__/ctf-otp-route.test.ts</automated>
  </verify>
  <done>Helper passes the RFC 6238 SHA1 vector; /ctf/otp returns the correct current-step TOTP JSON with cache-control no-store on success and an identical uniform 404 on every failure (missing secret, bad/absent bearer, internal error).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: telephony config — announcements field + loader/validation + DID 7254043234 TOML entry</name>
  <files>apps/voice/src/klanker_voice/telephony/config.py, apps/voice/configs/telephony.toml, apps/voice/tests/test_telephony_config.py</files>
  <behavior>
    - A [[telephony.announcement]] array-of-tables parses into TelephonyConfig.announcements: a tuple of
      frozen AnnouncementEntry(did, otp_url, otp_env_var, line_template).
    - Absent table -> announcements == () and every other field unchanged (byte-identical default).
    - Malformed entry (missing/empty did, missing otp_url, or a line_template without a "{code}"
      placeholder) raises ConfigError.
    - A credential-looking key inside the announcement table is still refused by the existing shared gate.
  </behavior>
  <action>
    Add a frozen `AnnouncementEntry` dataclass to config.py with fields `did: str`, `otp_url: str`,
    `otp_env_var: str`, `line_template: str`. Add `announcements: tuple[AnnouncementEntry, ...] = ()` to
    `TelephonyConfig`. In `load_telephony_config`, after the existing gate_mode validation, read
    `table.get("announcement")` (TOML `[[telephony.announcement]]` -> a list of dicts; absent -> None ->
    empty tuple). Validate each entry in the existing ConfigError style: `did` present and non-empty
    (str), `otp_url` present (str), `line_template` present AND contains the substring "{code}" (else
    ConfigError naming the field), `otp_env_var` optional str (default ""). Build the frozen tuple and
    pass it to the TelephonyConfig(...) constructor. Reuse `_load_toml_data`'s shared credential gate
    unchanged — do NOT weaken it.

    CRITICAL naming deviation from the spec (surface + apply): the spec proposes the TOML key
    `otp_auth_env_var`, but config.py's `_CREDENTIAL_FIELD_RE` rejects ANY key containing `_auth_`
    (pattern `(?:^|_)auth(?:_|$)`), so `otp_auth_env_var` would be refused by the credential gate before
    parsing. Use the key **`otp_env_var`** instead (clean of every credential token, exactly mirroring the
    working precedent `tel_mint_env_var`). The VALUE it holds is still `CTF_OTP_AUTH_TOKEN` (the env var
    name). Document this one-line rationale next to the field. `did`, `otp_url`, `line_template`, and
    `announcement` are all clean of the regex.

    Add the concrete entry to `apps/voice/configs/telephony.toml` (the file the standalone telephony edge
    process loads via KLANKER_PIPELINE_CONFIG — NOT pipeline.toml, whose telephony is disabled). Append,
    after the [telephony] block, an array-of-tables:
      [[telephony.announcement]]
      did = "7254043234"                     # Las Vegas DID; matches the normalized dialplan exten
      otp_url = "https://auth.klankermaker.ai/use1/ctf/otp"
      otp_env_var = "CTF_OTP_AUTH_TOKEN"     # NAME only; token VALUE from env/SSM at call time (D-09)
      line_template = "Hey! Let me get you that O T P. {code}. That's {code}. Buh bye."
    Include a comment noting the CTF_OTP_AUTH_TOKEN value is env-only (never in TOML), consistent with the
    file's existing secrets note.

    Tests (test_telephony_config.py, existing patterns): (a) a config WITHOUT [[telephony.announcement]]
    yields `announcements == ()` and unchanged defaults; (b) a well-formed entry parses into a
    one-element tuple with the expected did/otp_url/otp_env_var/line_template; (c) an entry whose
    line_template omits "{code}" raises ConfigError; (d) a missing/empty `did` raises ConfigError.
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest tests/test_telephony_config.py -x -q</automated>
  </verify>
  <done>TelephonyConfig gains a frozen `announcements` tuple parsed from [[telephony.announcement]] with ConfigError on malformed entries and () when absent; configs/telephony.toml carries the concrete 7254043234 entry using the credential-safe `otp_env_var` key; no secret value in TOML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: telephony controller — _run_announcement branch + line-builder + tests + ops notes</name>
  <files>apps/voice/src/klanker_voice/telephony/controller.py, apps/voice/tests/test_telephony_controller.py</files>
  <behavior>
    - A StasisStart whose normalized DID matches an announcement entry dispatches to _run_announcement and
      returns BEFORE the require_gate branch: neither _finish_stasis_start_gated nor
      _finish_stasis_start_ungated runs, and quota.start_gate is never called.
    - The line-builder digit-spaces the code ("123456" -> "1 2 3 4 5 6") and substitutes it into the
      template for EVERY "{code}" occurrence (the template speaks it twice).
    - On OTP-fetch failure (mocked to return None) the call hangs up / tears down and no TTS line is spoken.
    - On success (fetch + TTS mocked) the built line is synthesized and the channel is hung up after
      playback completes.
    - A non-announcement DID is completely unaffected (existing gated/ungated paths unchanged).
  </behavior>
  <action>
    Add a module-level pure helper `_build_announcement_line(template: str, code: str) -> str` that returns
    `template.replace("{code}", " ".join(code))` (digit-spacing via space-join over the code string;
    str.replace substitutes both occurrences). Keep it a standalone function so it is unit-testable without
    a call.

    Add a module-level async `_fetch_ctf_otp(url: str, headers: dict[str, str]) -> str | None` mirroring the
    existing `_fetch_tel_token`: aiohttp GET with a short ClientTimeout, return the JSON body's `code`
    (a non-empty str) on 200, else None; swallow every exception and return None (log by CATEGORY only —
    never the code, never the endpoint URL). Make it a module function so tests monkeypatch it exactly like
    `_fetch_tel_token`.

    In `on_stasis_start`, AFTER `did = _normalize_token(dialplan.get("exten"))` and after the
    transport/identity are constructed (right before the `if not self._telephony_cfg.require_gate:` branch,
    ~line 440), add: look up the announcement entry whose `.did == did` in
    `self._telephony_cfg.announcements` (exact-string match on the normalized exten — the TOML did
    "7254043234" matches the raw 10-digit exten, NOT the +E.164 form). If matched, `await
    self._run_announcement(...)` passing what it needs (sip_channel_id, entry, transport, media, bridge_id,
    external_media_channel_id) and `return` — skipping gate, mint, quota, STT, LLM, and conversation
    entirely. Build the DID->entry lookup once (e.g. a dict comprehension in `__init__` from
    `telephony_cfg.announcements`) or scan the tuple inline; either is fine for the expected small N.

    Implement `_run_announcement`: the channel is already answered upstream (on_stasis_start calls
    `self._ari.answer` before media setup) and the media/bridge/transport already exist. Read the optional
    bearer from `os.environ.get(entry.otp_env_var, "")` (name from config, value from env at call time —
    never a literal, mirroring `_mint_tier_from_caller_id`); build headers `{"Authorization": f"Bearer
    {bearer}"}` only when set. `code = await _fetch_ctf_otp(entry.otp_url, headers)`; if None (any
    non-200 / network / malformed-body failure) log by category only and tear down immediately (v1 has NO
    spoken error line). On success, build the line via `_build_announcement_line(entry.line_template, code)`
    and synthesize it over the EXISTING RTP path, reusing the greeting/goodbye playback machinery: build a
    minimal TTS-only worker (factories.build_tts(self._cfg) wired through a small pipeline over the existing
    `transport`, via pipeline.build_pipeline/build_worker — inspect pipeline.py/factories.py and reuse the
    smallest seam), queue a `TTSSpeakFrame(text=line, append_to_context=False)` (the same frame
    pipeline.speak_goodbye uses), await playback completion (a bot-stopped-speaking transport event or the
    worker draining — confirm the exact completion signal against the installed pipecat and use a bounded
    fallback timeout so a stuck synth can never hang the line), then hang up. Because NO ActiveCall is
    registered for an announcement call, tear down with the existing `_teardown_gate_resources(bridge_id,
    external_media_channel_id, media, sip_channel_id)` (hangs up SIP + external-media, destroys the bridge,
    closes the socket) — reuse it for BOTH the failure and post-playback paths so no PSTN line is ever left
    silently open. No STT, no LLM, no gate, no quota — public and one-directional. Logging discipline (§13):
    never log the code, the otp_url, or the bearer.

    Ops / secrets follow-ups (DOCUMENT ONLY — do NOT implement infra in this quick task; note in the
    SUMMARY): `CTF_OTP_SECRET` (SSM SecureString, base32) and `CTF_OTP_AUTH_TOKEN` (SSM SecureString) need
    task-definition wiring — the secret + token for the auth service, and the token ALSO for the
    voice/telephony-edge service (the controller reads CTF_OTP_AUTH_TOKEN by name at call time). Flag these
    as deferred deploy steps.

    Tests (test_telephony_controller.py, reusing the existing hermetic rig — _build_controller / _gated_cfg
    / _stasis_event / FakeAriClient / stub_call_session_run — and monkeypatching `_fetch_ctf_otp`, plus
    `factories.build_tts` or the TTS/worker seam so no real ElevenLabs/network call happens): (1) a
    _gated_cfg extended with an announcement entry for the test DID, on a StasisStart for that DID, invokes
    _run_announcement (spy) and does NOT enter _finish_stasis_start_gated — assert quota.start_gate is never
    called (spy raising AssertionError if hit); (2) a non-announcement DID still enters the gated path;
    (3) `_build_announcement_line("A {code}. That's {code}.", "123456")` == "A 1 2 3 4 5 6. That's 1 2 3 4
    5 6."; (4) OTP fetch returning None tears down (hangup observed on the fake ARI) with no TTS line;
    (5) on success, hangup follows playback (assert the fake ARI recorded the SIP-channel hangup after the
    TTS synth spy fired).
  </action>
  <verify>
    <automated>cd apps/voice && uv run pytest tests/test_telephony_controller.py -x -q</automated>
  </verify>
  <done>on_stasis_start dispatches a matched announcement DID to _run_announcement (before the §24 gate, quota never called), the line-builder spaces digits and substitutes both {code} occurrences, OTP-fetch failure and post-playback both tear the call down, and non-announcement DIDs are unchanged. Ops wiring for CTF_OTP_SECRET / CTF_OTP_AUTH_TOKEN noted as deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public PSTN caller -> telephony edge | Anyone who dials 7254043234 hears the current OTP; that IS the intended CTF proof (possession requires placing the call). |
| telephony edge -> auth /ctf/otp | Internal-only server-to-server HTTP; the OTP code crosses here. |
| random internet client -> auth /ctf/otp | Untrusted; must NOT be able to scrape codes — internal-only network lock + optional shared bearer + uniform 404. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-OTP-01 | Information Disclosure | GET /ctf/otp | high | mitigate | Internal-only deploy-time network lock (inherited from /tel) + optional CTF_OTP_AUTH_TOKEN bearer; possession of a code requires an actual call to the internal edge. |
| T-OTP-02 | Information Disclosure | /ctf/otp failure modes | medium | mitigate | Uniform 404 on every failure (missing secret, bad/absent bearer, internal error) — no distinct 401/403, no enumeration oracle; no logging of code/secret. |
| T-OTP-03 | Tampering / Spoofing | shared TOTP secret | high | mitigate | CTF_OTP_SECRET is SSM SecureString (base32), env-only, never in TOML; issuer emits only the current step (no skew range) — skew is verifier-only in meshtk. |
| T-OTP-04 | Information Disclosure | _run_announcement logging | medium | mitigate | Log by category only — never the code, otp_url, or bearer; TTS line spoken to the caller is the only egress of the code, which is by design. |
| T-OTP-05 | Denial of Service | _fetch_ctf_otp + TTS synth | low | mitigate | Short aiohttp ClientTimeout on the OTP fetch and a bounded fallback timeout on playback so a slow/stuck upstream can never hang the PSTN line; teardown always reached. |
| T-OTP-06 | Elevation of Privilege | announcement DID bypasses §24 gate | medium | accept | The gate bypass is scoped ONLY to DIDs explicitly listed in [[telephony.announcement]]; no other DID's gate posture changes. Accepted per spec — the announcement line is intentionally public and one-directional (no STT/LLM/quota reachable). |
</threat_model>

<verification>
- auth: `cd apps/auth/webapp && npx vitest run src/lib/__tests__/ctf-totp.test.ts src/app/ctf/__tests__/ctf-otp-route.test.ts`
- telephony: `cd apps/voice && uv run pytest tests/test_telephony_config.py tests/test_telephony_controller.py -x -q`
- Full telephony regression sanity: `cd apps/voice && uv run pytest tests/test_telephony_lifecycle.py -q` (announcement branch must not regress the gated/ungated paths).
</verification>

<success_criteria>
- `/use1/ctf/otp` returns the correct current-step 120s TOTP JSON on success with cache-control no-store, and an identical uniform 404 on every failure — proven by tests.
- The issuer emits exactly one code (no skew range).
- DID 7254043234 routes to _run_announcement before the §24 gate; quota.start_gate is never called for it; the OTP line is spoken (digits spaced, twice) and the call hangs up; non-announcement DIDs are byte-unchanged.
- No secret in TOML: the announcement entry carries only the bearer env-var NAME (`otp_env_var` = "CTF_OTP_AUTH_TOKEN"); absent table -> empty announcements tuple.
- Ops follow-ups (CTF_OTP_SECRET + CTF_OTP_AUTH_TOKEN SSM SecureString + task-def wiring for auth, token also for telephony-edge) captured in the SUMMARY as deferred deploy steps.
</success_criteria>

<output>
Create `.planning/quick/260715-oq0-ctf-phone-otp-announcement-did-auth-ctf-/260715-oq0-SUMMARY.md` when done.
</output>
