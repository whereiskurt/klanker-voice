---
phase: 260718-9el
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/voice/src/klanker_voice/telephony/controller.py
  - apps/voice/tests/test_telephony_sms.py
autonomous: true
requirements: [CTF-SMS-SPLIT]
must_haves:
  truths:
    - "An sms-eligible CTF announcement sends TWO SMS in order: the OTP URL first, then a static 'Hack the planet!' second."
    - "Both sends share the same dst, send_dids, and relay headers; the pair rides one fire-and-forget task parked on active_call.sms_task."
    - "A failed first send does NOT prevent the second send; no exception escapes the task."
    - "Both bodies are pure 7-bit GSM-7 ASCII with no leftover braces."
  artifacts:
    - "apps/voice/src/klanker_voice/telephony/controller.py — ANNOUNCEMENT_SMS_SECOND_BODY constant + _send_sms_sequence helper"
    - "apps/voice/tests/test_telephony_sms.py — updated capture-list tests asserting two ordered sends"
  key_links:
    - "_gate_announcement sms_eligible branch → _send_sms_sequence → _send_sms_via_relay (once per body, in order)"
---

<objective>
Split the single CTF OTP SMS into TWO sequential messages: message 1 is the OTP redemption URL, message 2 is a static "Hack the planet!" placeholder. Both ride ONE fire-and-forget task so a slow/failing send never hangs the PSTN line and a failed first send never blocks the second.

Purpose: The operator wants the URL and the closing beat as distinct texts, with message 2 reserved as a future chained CTF clue.
Output: New module constant, a never-raise sequence helper, an updated announcement branch, and updated/added tests — full telephony regression green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md

# Implementation targets
@apps/voice/src/klanker_voice/telephony/controller.py
@apps/voice/tests/test_telephony_sms.py
@apps/voice/tests/test_telephony_controller.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add second-body constant + _send_sms_sequence helper, wire the sms_eligible branch to send both bodies in order</name>
  <files>apps/voice/src/klanker_voice/telephony/controller.py</files>
  <behavior>
    - ANNOUNCEMENT_SMS_BODY_TEMPLATE formatted with a code is exactly "Here: https://q.defcon.run/c?v=&lt;code&gt;" and contains the code.
    - ANNOUNCEMENT_SMS_SECOND_BODY equals "Hack the planet!" exactly, is pure 7-bit ASCII, has no braces, and contains no {code} substitution.
    - _send_sms_sequence awaits _send_sms_via_relay once per body IN THE GIVEN ORDER; when the first send returns False (or its underlying call fails), the second send still fires.
    - No exception escapes _send_sms_sequence (it inherits _send_sms_via_relay's never-raise contract; wrap defensively so even an unexpected raise cannot escape the parked task).
  </behavior>
  <action>
Edit apps/voice/src/klanker_voice/telephony/controller.py:

1. Change ANNOUNCEMENT_SMS_BODY_TEMPLATE (~L278-280) from the current "CTF flag redemption: https://q.defcon.run/c?v={code}" to "Here: https://q.defcon.run/c?v={code}". Keep the existing docstring block above it (the GSM-7 / UCS-2-drop warning at ~L264-277) intact — it still applies.

2. Immediately after that constant, add a new module constant ANNOUNCEMENT_SMS_SECOND_BODY = "Hack the planet!". Give it a docstring that states: (a) it is a STATIC second SMS with NO {code} substitution; (b) it is a placeholder the operator may later replace with a chained next-step clue for the CTF player; (c) it MUST stay pure 7-bit GSM-7 ASCII for the same UCS-2-silent-drop reason documented on ANNOUNCEMENT_SMS_BODY_TEMPLATE (reference that existing warning by name rather than restating the em-dash example verbatim).

3. Add a module-level async helper _send_sms_sequence(url, headers, dst, bodies, send_dids) placed directly after _send_sms_via_relay (~after L571). It iterates bodies IN ORDER and awaits _send_sms_via_relay(url, headers, dst, body, send_dids) once per body. It never raises: _send_sms_via_relay already never raises, and the loop must not short-circuit on a False return — a failed first send MUST NOT prevent the second. Wrap the body of the loop defensively so an unexpected exception cannot escape the parked task. Do NOT return a value the callers depend on (fire-and-forget). Do NOT change _send_sms_via_relay's signature or contract. Preserve the never-log discipline: emit NO new log line carrying the URL/body/dst/dids/bearer (the URL contains the live OTP).

4. In _gate_announcement's sms_eligible branch (~L1390-1398): keep the eligibility gate unchanged (send_dids AND relay_url AND dst). Build the URL body via ANNOUNCEMENT_SMS_BODY_TEMPLATE.format(code=code) exactly as today, then build an ordered bodies sequence of [url_body, ANNOUNCEMENT_SMS_SECOND_BODY]. Replace the single asyncio.create_task(_send_sms_via_relay(...)) with a single asyncio.create_task(_send_sms_sequence(entry.sms_relay_url, relay_headers, dst, bodies, send_dids)) parked on active_call.sms_task exactly as before — still fire-and-forget, still never awaited on teardown. Update the adjacent explanatory comment to note that TWO messages are now sent sequentially in that one task (URL first, then the static beat), without leaking any secret content.

Do NOT touch ANNOUNCEMENT_SMS_PUNCHLINE_COPY (the SPOKEN payoff) — the spoken script path is unchanged.
  </action>
  <verify>
    <automated>cd apps/voice &amp;&amp; uv run python -c "from klanker_voice.telephony import controller as c; assert c.ANNOUNCEMENT_SMS_BODY_TEMPLATE.format(code='X')=='Here: https://q.defcon.run/c?v=X'; assert c.ANNOUNCEMENT_SMS_SECOND_BODY=='Hack the planet!'; assert all(ord(ch)&lt;128 for ch in c.ANNOUNCEMENT_SMS_SECOND_BODY) and '{' not in c.ANNOUNCEMENT_SMS_SECOND_BODY; assert hasattr(c,'_send_sms_sequence'); print('ok')"</automated>
  </verify>
  <done>ANNOUNCEMENT_SMS_BODY_TEMPLATE emits the "Here: ..." URL; ANNOUNCEMENT_SMS_SECOND_BODY is the ASCII "Hack the planet!"; _send_sms_sequence exists and sends bodies in order without short-circuiting or raising; the sms_eligible branch parks one _send_sms_sequence task on active_call.sms_task.</done>
</task>

<task type="auto">
  <name>Task 2: Update SMS tests to expect two ordered sends and cover _send_sms_sequence</name>
  <files>apps/voice/tests/test_telephony_sms.py</files>
  <action>
Edit apps/voice/tests/test_telephony_sms.py:

1. Import _send_sms_sequence and ANNOUNCEMENT_SMS_SECOND_BODY (and ANNOUNCEMENT_SMS_BODY_TEMPLATE if not already) from klanker_voice.telephony.controller in the existing import block (~L22-32).

2. GSM-7 regression (test_sms_body_is_gsm7_ascii_safe ~L130-140): keep the existing message-1 assertions. Add assertions (in-place, or as a sibling test test_sms_second_body_is_gsm7_ascii_safe) that ANNOUNCEMENT_SMS_SECOND_BODY has no chars with ord >= 128 and contains no "{" or "}".

3. _gate_announcement-driving tests that capture sends (the _fake_relay capture-list tests): they already append each call to a `sent` list, so update the ASSERTIONS to expect TWO entries. Specifically:
   - test_hook_eligible_posts_one_relay_call_and_speaks_punchline (~L251-283): rename intent to two-send; assert len(sent) == 2. Send #1 message == "Here: https://q.defcon.run/c?v=483920" (contains the OTP "483920"); send #2 message == "Hack the planet!". Assert BOTH sends carry the same to ("5197101515") and same dids (("6134805878",)) and same url (RELAY_URL). Keep the goodbye/punchline assertions unchanged.
   - test_hook_per_did_enrolled_texts_from_dialed_did (~L490-517): assert len(sent) == 2 and BOTH sends' dids == ("7254043283",) and to == "5197101515"; assert send[0].message starts the URL and send[1].message == "Hack the planet!".
   - test_hook_per_did_unresolved_falls_back_to_pool (~L549-577): assert len(sent) == 2 and BOTH sends' dids == ("6134805878",).
   - Ineligible/no-relay/unenrolled tests (_fail_relay: ~L286-334, ~L520-546) MUST still assert the relay is NEVER called — leave their _fail_relay guards intact (a two-send change must not fire ANY send when ineligible).

4. Failure-path never-raise (test_hook_failing_relay_never_breaks_teardown ~L336-357, _boom_relay): keep it green — teardown still completes, controller.calls == {}. Because _send_sms_sequence wraps _send_sms_via_relay, a raising relay must not break teardown.

5. Log-discipline test (test_hook_log_discipline_no_secret_leak ~L360-392): still green — assert the OTP never appears in logs. No change needed beyond confirming it passes with the two-send path.

6. Add a focused unit test for _send_sms_sequence (near the _send_sms_via_relay unit tests ~L179-216): monkeypatch/replace _send_sms_via_relay with a capture stub recording (message) per call, drive _send_sms_sequence(RELAY_URL, {}, "5197101515", ["m1", "m2"], DIDS), and assert the captured messages == ["m1", "m2"] (ORDER preserved). Add a second case where the stub returns False on the first call (or raises on the first call) and assert BOTH messages were still attempted in order — proving a first-send failure does not stop the second and nothing escapes.
  </action>
  <verify>
    <automated>cd apps/voice &amp;&amp; uv run pytest tests/test_telephony_sms.py tests/test_telephony_controller.py -q</automated>
  </verify>
  <done>All SMS + controller tests green; the eligible/per-DID hook tests assert exactly two ordered sends (URL then "Hack the planet!") with matching dst/dids; the new _send_sms_sequence test proves order preservation and first-failure-does-not-block-second; ineligible tests still prove zero sends.</done>
</task>

</tasks>

<verification>
Run the targeted suites then the full telephony regression:

```
cd apps/voice && uv run pytest tests/test_telephony_sms.py tests/test_telephony_controller.py -q
cd apps/voice && uv run pytest tests/test_telephony_*.py -q
```

All green. Manually confirm no new log statement in controller.py emits the URL, body, dst, dids, or bearer.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telephony-edge → auth /ctf/sms relay | The built SMS (URL contains the live OTP) crosses to the auth relay; bearer-authenticated. |
| telephony-edge → logs | The OTP/URL/dst/dids/bearer must never cross into any log sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260718-01 | Information Disclosure | new _send_sms_sequence + updated announcement comment | high | mitigate | Add NO new log line carrying URL/body/dst/dids/bearer; the sequence helper logs nothing itself (inherits _send_sms_via_relay's generic-marker-only discipline). Verify via test_hook_log_discipline_no_secret_leak. |
| T-260718-02 | Denial of Service | _send_sms_sequence parked on active_call.sms_task | medium | mitigate | Two sequential awaits ride ONE fire-and-forget task, never awaited on teardown; each _send_sms_via_relay keeps its 12s timeout. A slow/failing send cannot hang the PSTN line. Verify via test_hook_failing_relay_never_breaks_teardown. |
| T-260718-03 | Tampering | GSM-7 encoding of both bodies | low | accept | Both bodies are basic 7-bit GSM-7 ("Here: ...URL" and "Hack the planet!"); a UCS-2-forcing char would be silently dropped by VoIP.ms. Guarded by the ASCII-safety tests. No package installs in this task. |
</threat_model>

<success_criteria>
- ANNOUNCEMENT_SMS_BODY_TEMPLATE emits "Here: https://q.defcon.run/c?v={code}".
- ANNOUNCEMENT_SMS_SECOND_BODY == "Hack the planet!" (static, ASCII, no braces, documented as a future-clue placeholder).
- _send_sms_sequence sends bodies in order, never short-circuits on failure, never raises.
- The sms_eligible branch parks exactly one _send_sms_sequence task on active_call.sms_task (fire-and-forget, never awaited on teardown), gating unchanged.
- `uv run pytest tests/test_telephony_sms.py tests/test_telephony_controller.py -q` and `tests/test_telephony_*.py -q` are all green.
- No new log line leaks the URL/OTP/dst/dids/bearer.
</success_criteria>

<output>
Create `.planning/quick/260718-9el-ctf-otp-sms-split-into-two-sequential-me/260718-9el-SUMMARY.md` when done.
</output>
