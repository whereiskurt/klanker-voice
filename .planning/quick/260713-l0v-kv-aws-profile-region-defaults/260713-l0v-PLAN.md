---
phase: quick-260713-l0v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kv/internal/app/cmd/root.go
  - kv/internal/app/cmd/telephony.go
  - kv/internal/app/cmd/voipms.go
  - kv/internal/app/cmd/root_test.go
  - kv/internal/app/cmd/voipms_test.go
autonomous: true
requirements: [KV-OPS-DEFAULTS]
must_haves:
  truths:
    - "After `aws sso login`, `kv telephony list` / `kv voipms balance` work with zero hand-exported AWS_PROFILE/AWS_REGION/VOIPMS_* env vars"
    - "AWS profile defaults to klanker-application, but AWS_PROFILE or AWS_ACCESS_KEY_ID (CI) still win, and --profile \"\" forces pure-ambient creds"
    - "Region defaults to us-east-1 when AWS_REGION is unset; --region still overrides"
    - "VoIP.ms creds resolve from env first, else from SSM; failure never leaks api_password or the SSM param values"
  artifacts:
    - "kv/internal/app/cmd/root.go — Config.Profile, --profile flag, resolveAWSProfile, shared loadAWS helper"
    - "kv/internal/app/cmd/voipms.go — env→SSM creds resolver reusing the ssmGetParameterAPI seam"
  key_links:
    - "DynamoClient + SSMClient both route through Config.loadAWS (single AWS-config construction path)"
    - "resolveVoipmsCreds is invoked at all three call sites (voipms balance/route-did/create-subaccount RunEs + telephony list RunE) via the in-scope cfg"
---

<objective>
Make the `kv` operator CLI "just work" after `aws sso login`: default the AWS profile to `klanker-application`, default the region to `us-east-1`, and let it fetch VoIP.ms API creds from SSM when env vars are absent — all still overridable via flags/env.

Purpose: A laptop operator should never hand-export AWS creds/region or VoIP.ms creds again, while CI (static AWS creds) and local dynamodb-local tests keep working byte-for-byte.
Output: Centralized AWS-config loading with do-what-I-mean profile/region defaults + an env→SSM VoIP.ms creds fallback, fully unit-tested without live AWS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@kv/internal/app/cmd/root.go
@kv/internal/app/cmd/telephony.go
@kv/internal/app/cmd/voipms.go
@kv/internal/app/cmd/voipms_test.go
@kv/internal/app/cmd/telephony_test.go

# Verified facts (do not re-research):
# - Operator SSO profile present in ~/.aws/config as [profile klanker-application] (sso_session=Developer, acct 052251888500).
# - VoIP.ms SSM params (docs/operators/voipms-provisioning-runbook.md lines 168-169):
#     username -> /kmv/secrets/use1/voipms/api_username
#     password -> /kmv/secrets/use1/voipms/api_password
# - ssmGetParameterAPI interface seam + fakeSSMGetParameterClient already exist in telephony.go / telephony_test.go.
# - cfg (*Config) is already in closure scope at every VoIP.ms creds call site.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize AWS config loading in root.go with profile + region defaults</name>
  <files>kv/internal/app/cmd/root.go, kv/internal/app/cmd/root_test.go</files>
  <behavior>
    resolveAWSProfile(awsProfileEnv, awsAccessKeyEnv string) string — pure, table-tested 3-way precedence:
    - awsProfileEnv="prod", accessKey="" -> "prod"            (AWS_PROFILE wins)
    - awsProfileEnv="prod", accessKey="AKIA..." -> "prod"     (AWS_PROFILE still wins even with static creds)
    - awsProfileEnv="",     accessKey="AKIA..." -> ""         (explicit static/CI creds -> NO profile)
    - awsProfileEnv="",     accessKey="" -> "klanker-application" (operator SSO default)
    resolveAWSRegion(awsRegionEnv string) string — returns awsRegionEnv if set, else "us-east-1".
  </behavior>
  <action>
Add a `Profile string` field to the `Config` struct.

Add two pure package-level helpers:
- `resolveAWSProfile(awsProfileEnv, awsAccessKeyEnv string) string` implementing the do-what-I-mean precedence in the behavior block: if AWS_PROFILE set -> return it; else if AWS_ACCESS_KEY_ID set -> return "" (ambient static/CI creds win, profile suppressed); else -> return "klanker-application".
- `resolveAWSRegion(awsRegionEnv string) string` -> returns the arg if non-empty else "us-east-1".

Add ONE shared loader method on Config: `func (c *Config) loadAWS(ctx context.Context) (aws.Config, error)` (import `"github.com/aws/aws-sdk-go-v2/aws"`). It builds `[]func(*awsconfig.LoadOptions) error`: always `awsconfig.WithRegion(resolveAWSRegion(c.Region))`, and WHEN `c.Profile != ""` also `awsconfig.WithSharedConfigProfile(c.Profile)`. Then `awsconfig.LoadDefaultConfig(ctx, opts...)`, wrapping any error as "load aws config: %w".

Refactor `DynamoClient` to call `c.loadAWS(ctx)` instead of its own inline LoadDefaultConfig. MUST preserve the existing `EndpointURL` override for dynamodb-local: keep the `dynamodb.NewFromConfig(cfg, func(o *dynamodb.Options){ if c.EndpointURL != "" { o.BaseEndpoint = &c.EndpointURL } })` closure exactly as-is. (Region defaulting to us-east-1 is inert for local-endpoint tests since the endpoint override wins.)

In `NewRootCmd`, add a `--profile` persistent flag: `root.PersistentFlags().StringVar(&cfg.Profile, "profile", resolveAWSProfile(os.Getenv("AWS_PROFILE"), os.Getenv("AWS_ACCESS_KEY_ID")), "AWS shared-config profile (default klanker-application; empty forces ambient creds; AWS_PROFILE/AWS_ACCESS_KEY_ID honored)")`. Change the existing `--region` flag default from `os.Getenv("AWS_REGION")` to `resolveAWSRegion(os.Getenv("AWS_REGION"))` and update its usage string to mention the us-east-1 default. Leave all other flags untouched.

Add `root_test.go` (package cmd) with `TestResolveAWSProfile` (table test covering all four precedence rows above) and `TestResolveAWSRegion` (set -> passthrough; empty -> us-east-1). No live AWS.

Do NOT add viper. Do NOT add lipgloss. Do NOT change any other command's output or flags.
  </action>
  <verify>
    <automated>cd kv && go build ./... && go test ./internal/app/cmd/ -run 'TestResolveAWSProfile|TestResolveAWSRegion' -v</automated>
  </verify>
  <done>Config has Profile; --profile flag exists; region defaults to us-east-1; resolveAWSProfile/resolveAWSRegion are table-tested and green; DynamoClient routes through loadAWS with its EndpointURL override intact; full `go build ./...` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route SSMClient through the shared loader and add an env→SSM VoIP.ms creds resolver</name>
  <files>kv/internal/app/cmd/telephony.go, kv/internal/app/cmd/voipms.go, kv/internal/app/cmd/voipms_test.go</files>
  <behavior>
    voipmsCredsFromSSM(ctx, api ssmGetParameterAPI) (voipmsCreds, error) — reads the two params via GetParameter WithDecryption(true); returns the creds on success; returns a leak-free error on any failure.
    resolveVoipmsCreds(ctx, ssmFactory func(context.Context)(ssmGetParameterAPI,error)) (voipmsCreds, error):
    - env set (VOIPMS_API_USERNAME + VOIPMS_API_PASSWORD) -> returns env creds, ssmFactory NEVER called.
    - env unset, SSM returns both params -> returns SSM creds.
    - env unset, SSM errors -> returns an error that names NEITHER the api_password value NOR the fetched param values.
  </behavior>
  <action>
In `telephony.go`: refactor `(c *Config) SSMClient` to build its config via `c.loadAWS(ctx)` (Task 1's shared helper) instead of its own inline `awsconfig.LoadDefaultConfig(WithRegion(...))`. Keep returning `ssm.NewFromConfig(cfg)`. Region default stays us-east-1 (loadAWS already yields that), so no behavior regression for the redact-by-default secrets flow. If this removes the last direct use of `awsconfig` in telephony.go, drop the now-unused import.

In `voipms.go`:
- Add two SSM-path constants. CRITICAL NAMING CONSTRAINT: `voipms_test.go`'s `TestVoipmsMethodNamesCentralized` credential-literal regex `(?i)(api[_]?username|api[_]?password)\s*[:=]=?\s*"` will fail the build if a constant identifier ends with `...APIUsername`/`...APIPassword` immediately before `=`. Name them WITHOUT `api` adjoining username/password, e.g. `voipmsUsernameSSMPath = "/kmv/secrets/use1/voipms/api_username"` and `voipmsPasswordSSMPath = "/kmv/secrets/use1/voipms/api_password"` (the path VALUE containing api_username is fine — it is followed by a closing quote, not `=`).
- Add `voipmsCredsFromSSM(ctx context.Context, api ssmGetParameterAPI) (voipmsCreds, error)` that calls `api.GetParameter` with `WithDecryption: aws.Bool(true)` for each of the two path constants and assembles a `voipmsCreds`. On any GetParameter error return a leak-free error: use `shortSSMErrorNote(err)` for the cause and never interpolate the parameter values; e.g. "read VoIP.ms credentials from SSM: %s". Requires importing the ssm + aws packages already used by telephony.go (same package, so `ssmGetParameterAPI` and `shortSSMErrorNote` are directly reachable).
- Add `resolveVoipmsCreds(ctx context.Context, ssmFactory func(context.Context) (ssmGetParameterAPI, error)) (voipmsCreds, error)`: first try `voipmsCredsFromEnv()` and return immediately on success (env wins, for override/testing); on env miss, call `ssmFactory(ctx)` then `voipmsCredsFromSSM`. If the factory or SSM read fails, return ONE clear operator-facing error that leaks nothing and states both options, e.g.: "could not resolve VoIP.ms API credentials: set VOIPMS_API_USERNAME/VOIPMS_API_PASSWORD, or ensure your AWS profile can read /kmv/secrets/use1/voipms/* (%s)" with `%s` = the leak-free cause.
- Add a thin Config method `func (c *Config) resolveVoipmsCreds(ctx context.Context) (voipmsCreds, error)` that delegates to the package function passing an ssmFactory closure `func(ctx) (ssmGetParameterAPI, error) { return c.SSMClient(ctx) }` (`*ssm.Client` satisfies `ssmGetParameterAPI`).
- Update the head-of-file credentials comment (currently "read ONLY from the environment ... No literal api_username/api_password value is ever assigned") to state the new env-first-then-SSM behavior. Keep the phrasing free of any `identifier = "..."` credential-literal shape so the regex stays satisfied; keep `voipmsCredsFromEnv` and its two `os.Getenv(...)` reads intact (that test also asserts both os.Getenv lines exist).
- Rewire the three call sites to the new Config method (cfg is already in closure scope at each):
  - `voipms.go` `balance`, `route-did`, `create-subaccount` RunEs: replace `creds, err := voipmsCredsFromEnv()` with `creds, err := cfg.resolveVoipmsCreds(c.Context())`.
  - `telephony.go` list RunE (~line 406): replace `if creds, err := voipmsCredsFromEnv(); err != nil {` with `if creds, err := cfg.resolveVoipmsCreds(c.Context()); err != nil {` — its graceful-degradation branch (readInboundDIDs with credsOK=false) is unchanged; "creds present" now means env-OR-SSM.

Do NOT regress `do()`'s `*url.Error` unwrap (leave that function untouched). Never print the fetched creds.

In `voipms_test.go` add three tests using the existing `fakeSSMGetParameterClient` seam (in telephony_test.go, same package):
- `TestResolveVoipmsCreds_EnvWins`: set both VOIPMS_API_* via `t.Setenv`; pass an ssmFactory that fails the test if invoked; assert env creds returned and factory never called.
- `TestResolveVoipmsCreds_SSMFallbackSuccess`: clear the env vars; ssmFactory returns a `fakeSSMGetParameterClient` whose `values` map has both path constants; assert returned creds match the canned values.
- `TestResolveVoipmsCreds_SSMFallbackError`: clear the env vars; ssmFactory returns a fake whose `errs` map returns an error for a path; assert an error is returned AND `err.Error()` contains neither the canned api_password value nor a raw param value (leak guard).
  </action>
  <verify>
    <automated>cd kv && go build ./... && go test ./...</automated>
  </verify>
  <done>SSMClient routes through loadAWS; VoIP.ms creds resolve env-first then SSM at all three call sites; the three resolver tests + the pre-existing `TestVoipmsMethodNamesCentralized`/`TestVoipmsCredsFromEnv` all pass; full `go test ./...` green in kv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kv process → SSM | Decrypted VoIP.ms api_password crosses into process memory; must never reach logs/stdout/error strings |
| operator laptop → CI | The klanker-application default must not hijack CI runners that carry static AWS creds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-l0v-01 | Information Disclosure | voipmsCredsFromSSM / resolveVoipmsCreds error path | high | mitigate | Error strings use `shortSSMErrorNote(err)` only and never interpolate the api_password or fetched param VALUES; `do()`'s existing `*url.Error` unwrap (which strips the password-bearing query URL) left untouched; resolver-error leak-guard asserted in TestResolveVoipmsCreds_SSMFallbackError |
| T-l0v-02 | Elevation of Privilege / Tampering | resolveAWSProfile default | medium | mitigate | AWS_ACCESS_KEY_ID precedence returns empty profile so CI/static-cred runners are unaffected; `--profile ""` escape hatch forces ambient creds; all four precedence rows table-tested in TestResolveAWSProfile |
</threat_model>

<verification>
- `cd kv && go build ./...` clean (no unused imports after the telephony.go awsconfig refactor).
- `cd kv && go test ./...` green across all three packages — includes the new profile/region/creds-resolver tests AND the pre-existing invariant tests (`TestVoipmsMethodNamesCentralized`, `TestVoipmsCredsFromEnv`, telephony redaction tests).
- Manual sanity (operator, not automated): after `aws sso login`, `kv telephony list` reads DIDs + SSM secrets with no VOIPMS_*/AWS_PROFILE/AWS_REGION exported.
</verification>

<success_criteria>
- Config gains Profile; `--profile` persistent flag added; region defaults to us-east-1; both defaults overridable via flag/env.
- DynamoClient and SSMClient both construct AWS config through the single `Config.loadAWS` helper (no duplicated LoadDefaultConfig), DynamoDB EndpointURL override preserved for dynamodb-local.
- VoIP.ms creds resolve env-first then SSM at all three call sites; no api_password/param-value ever appears in any error or output.
- No viper, no lipgloss, no changes to the voice service / terraform / docs (a one-line comment update in voipms.go excepted). Full kv test suite green.
</success_criteria>

<output>
Create `.planning/quick/260713-l0v-kv-aws-profile-region-defaults/260713-l0v-SUMMARY.md` when done
</output>
