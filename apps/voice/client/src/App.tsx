// klanker-voice — App shell.
//
// Renders the full-bleed 100dvh immersive stage background (D-05). The
// attract landing screen (orb + CTA) is wired in here by Task 3.
export default function App() {
  return <div className="stage" />;
}
