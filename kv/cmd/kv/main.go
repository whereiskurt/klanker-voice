// Command kv is the klanker-voice operator CLI for managing access codes and
// tiers (sibling to klanker-maker's km).
package main

import "github.com/whereiskurt/klanker-voice/kv/internal/app/cmd"

func main() {
	cmd.Execute()
}
